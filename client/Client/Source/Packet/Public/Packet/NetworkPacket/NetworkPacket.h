// ============================================================================
// myungdal tech-demo project
// 
// Author: 안명달 (Myungdal Ahn)
// Email: mooondal@gmail.com
// GitHub: https://github.com/myungdal/tech_demo.git
// ============================================================================

#pragma once

#include "Packet/BasePacket/Packet.h"
#include "Packet/NetworkPacket/PacketHeader.h"


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// NetworkPacket
// 네트워크 전송을 위한 패킷 클래스입니다.
// Packet을 상속받아 PacketHeader를 추가합니다.
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#pragma pack(push, 1)
class PACKET_API NetworkPacket : public Packet
{
	friend class NetworkPacketWriter;

protected:
	// [예외] 자동 생성 패킷 클래스(NetworkPacketAutoGenerated 등)에서 직접 접근하므로 protected
	PacketHeader mPacketHeader;		// 패킷 헤더 (라우팅 정보 포함)

public:
	NetworkPacket()
	{
		mPacketSize = static_cast<PacketSize>(sizeof(NetworkPacket));
	}

	// 패킷 헤더 접근자
	PacketHeader& GetHeader() { return mPacketHeader; }
	const PacketHeader& ConstGetHeader() const { return mPacketHeader; }
	void SetHeader(const PacketHeader& header) { mPacketHeader = header; }
};
#pragma pack(pop)

