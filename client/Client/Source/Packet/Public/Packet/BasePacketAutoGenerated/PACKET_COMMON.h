// ============================================================================
// myungdal tech-demo project
// 
// Author: 안명달 (Myungdal Ahn)
// Email: mooondal@gmail.com
// GitHub: https://github.com/myungdal/tech_demo.git
// ============================================================================

#pragma once

#include "Packet/BasePacketAutoGenerated/PACKET_TYPE.h"
#include "Packet/NetworkPacket/NetworkPacket.h"
#include "Packet/NetworkPacket/NetworkPacketWriter.h"
#include "Packet/BasePacket/PacketWriter.h"
#include "Packet/PacketDataList/PacketDataList.h"
#include "Packet/PacketFuncs/PacketValidator.h"
#include "Packet/PacketFuncs/PacketDispatcher.h"

#pragma pack(push, 1)

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class LOG_ACHIEVEMENT;

class LOG_ACHIEVEMENT_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::LOG_ACHIEVEMENT;

public:
   // (7)
   explicit LOG_ACHIEVEMENT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::LOG_ACHIEVEMENT, buf)
   {
   }

   // (9)
   explicit LOG_ACHIEVEMENT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       LogId log_id,
       UserId user_id,
       AchievementSid achievement_sid,
       AchievementStepSid achievement_step_sid,
       time_t date_created,
       time_t date_log
   )
       :
       PacketWriter(trait, PacketTypes::LOG_ACHIEVEMENT, buf)
   {
       WriteValue(id);

       WriteValue(log_id);

       WriteValue(user_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step_sid);

       WriteValue(date_created);

       WriteValue(date_log);


   }

   // (8)
   explicit LOG_ACHIEVEMENT_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       LogId log_id,
       UserId user_id,
       AchievementSid achievement_sid,
       AchievementStepSid achievement_step_sid,
       time_t date_created,
       time_t date_log
   )
   {
       WriteValue(id);

       WriteValue(log_id);

       WriteValue(user_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step_sid);

       WriteValue(date_created);

       WriteValue(date_log);


   }

   LOG_ACHIEVEMENT& GetPacket() const noexcept { return *reinterpret_cast<LOG_ACHIEVEMENT*>(GetPacketBufPtr()); }
   LOG_ACHIEVEMENT* GetPacketPtr() const noexcept { return reinterpret_cast<LOG_ACHIEVEMENT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class LOG_ACHIEVEMENT : public Packet
{
   public:
      using Writer = LOG_ACHIEVEMENT_WRITER;


private:
   Uuid m_c_id;

   LogId m_c_log_id;

   UserId m_c_user_id;

   AchievementSid m_c_achievement_sid;

   AchievementStepSid m_c_achievement_step_sid;

   time_t m_c_date_created;

   time_t m_c_date_log;

public:
   LOG_ACHIEVEMENT()
   {
       memset(this, 0, sizeof(LOG_ACHIEVEMENT));
       mPacketSize = static_cast<PacketSize>(sizeof(LOG_ACHIEVEMENT));
   }
   LOG_ACHIEVEMENT(const LOG_ACHIEVEMENT&) = delete;
   LOG_ACHIEVEMENT(LOG_ACHIEVEMENT &&) = delete;
   LOG_ACHIEVEMENT& operator = (const LOG_ACHIEVEMENT&) = delete;
   LOG_ACHIEVEMENT& operator = (LOG_ACHIEVEMENT &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   LogId Get_c_log_id() const noexcept { return m_c_log_id; }
   LogId& Ref_c_log_id() noexcept { return m_c_log_id; }
   const LogId& ConstRef_c_log_id() const noexcept { return m_c_log_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   AchievementSid Get_c_achievement_sid() const noexcept { return m_c_achievement_sid; }
   AchievementSid& Ref_c_achievement_sid() noexcept { return m_c_achievement_sid; }
   const AchievementSid& ConstRef_c_achievement_sid() const noexcept { return m_c_achievement_sid; }

   AchievementStepSid Get_c_achievement_step_sid() const noexcept { return m_c_achievement_step_sid; }
   AchievementStepSid& Ref_c_achievement_step_sid() noexcept { return m_c_achievement_step_sid; }
   const AchievementStepSid& ConstRef_c_achievement_step_sid() const noexcept { return m_c_achievement_step_sid; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_log() const noexcept { return m_c_date_log; }
   time_t& Ref_c_date_log() noexcept { return m_c_date_log; }
   const time_t& ConstRef_c_date_log() const noexcept { return m_c_date_log; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(LOG_ACHIEVEMENT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(LOG_ACHIEVEMENT) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(LOG_ACHIEVEMENT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(LOG_ACHIEVEMENT))
           {
               size += sizeof(LOG_ACHIEVEMENT);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(LogId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(AchievementSid);
       }

       {
           thisSize += sizeof(AchievementStepSid);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, LOG_ACHIEVEMENT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_log_id: {}", tTabStr, t->Get_c_log_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_achievement_sid: {}", tTabStr, t->Get_c_achievement_sid());
       result += std::format(L"\n{}c_achievement_step_sid: {}", tTabStr, t->Get_c_achievement_step_sid());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_log: {}", tTabStr, t->Get_c_date_log());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class INSERT_LOG_ACHIEVEMENT;

class INSERT_LOG_ACHIEVEMENT_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::INSERT_LOG_ACHIEVEMENT;

public:
   // (7)
   explicit INSERT_LOG_ACHIEVEMENT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::INSERT_LOG_ACHIEVEMENT, buf)
   {
   }

   // (9)
   explicit INSERT_LOG_ACHIEVEMENT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       LogId log_id,
       UserId user_id,
       AchievementSid achievement_sid,
       AchievementStepSid achievement_step_sid,
       time_t date_created,
       time_t date_log
   )
       :
       NetworkPacketWriter(trait, PacketTypes::INSERT_LOG_ACHIEVEMENT, buf)
   {
       WriteValue(id);

       WriteValue(log_id);

       WriteValue(user_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step_sid);

       WriteValue(date_created);

       WriteValue(date_log);


   }

   // (8)
   explicit INSERT_LOG_ACHIEVEMENT_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       LogId log_id,
       UserId user_id,
       AchievementSid achievement_sid,
       AchievementStepSid achievement_step_sid,
       time_t date_created,
       time_t date_log
   )
   {
       WriteValue(id);

       WriteValue(log_id);

       WriteValue(user_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step_sid);

       WriteValue(date_created);

       WriteValue(date_log);


   }

   INSERT_LOG_ACHIEVEMENT& GetPacket() const noexcept { return *reinterpret_cast<INSERT_LOG_ACHIEVEMENT*>(GetPacketBufPtr()); }
   INSERT_LOG_ACHIEVEMENT* GetPacketPtr() const noexcept { return reinterpret_cast<INSERT_LOG_ACHIEVEMENT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class INSERT_LOG_ACHIEVEMENT : public NetworkPacket
{
   public:
      using Writer = INSERT_LOG_ACHIEVEMENT_WRITER;


private:
   Uuid m_c_id;

   LogId m_c_log_id;

   UserId m_c_user_id;

   AchievementSid m_c_achievement_sid;

   AchievementStepSid m_c_achievement_step_sid;

   time_t m_c_date_created;

   time_t m_c_date_log;

public:
   INSERT_LOG_ACHIEVEMENT()
   {
       memset(this, 0, sizeof(INSERT_LOG_ACHIEVEMENT));
       mPacketSize = static_cast<PacketSize>(sizeof(INSERT_LOG_ACHIEVEMENT));
   }
   INSERT_LOG_ACHIEVEMENT(const INSERT_LOG_ACHIEVEMENT&) = delete;
   INSERT_LOG_ACHIEVEMENT(INSERT_LOG_ACHIEVEMENT &&) = delete;
   INSERT_LOG_ACHIEVEMENT& operator = (const INSERT_LOG_ACHIEVEMENT&) = delete;
   INSERT_LOG_ACHIEVEMENT& operator = (INSERT_LOG_ACHIEVEMENT &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   LogId Get_c_log_id() const noexcept { return m_c_log_id; }
   LogId& Ref_c_log_id() noexcept { return m_c_log_id; }
   const LogId& ConstRef_c_log_id() const noexcept { return m_c_log_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   AchievementSid Get_c_achievement_sid() const noexcept { return m_c_achievement_sid; }
   AchievementSid& Ref_c_achievement_sid() noexcept { return m_c_achievement_sid; }
   const AchievementSid& ConstRef_c_achievement_sid() const noexcept { return m_c_achievement_sid; }

   AchievementStepSid Get_c_achievement_step_sid() const noexcept { return m_c_achievement_step_sid; }
   AchievementStepSid& Ref_c_achievement_step_sid() noexcept { return m_c_achievement_step_sid; }
   const AchievementStepSid& ConstRef_c_achievement_step_sid() const noexcept { return m_c_achievement_step_sid; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_log() const noexcept { return m_c_date_log; }
   time_t& Ref_c_date_log() noexcept { return m_c_date_log; }
   const time_t& ConstRef_c_date_log() const noexcept { return m_c_date_log; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(INSERT_LOG_ACHIEVEMENT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(INSERT_LOG_ACHIEVEMENT) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(INSERT_LOG_ACHIEVEMENT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(INSERT_LOG_ACHIEVEMENT))
           {
               size += sizeof(INSERT_LOG_ACHIEVEMENT);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(LogId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(AchievementSid);
       }

       {
           thisSize += sizeof(AchievementStepSid);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, INSERT_LOG_ACHIEVEMENT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_log_id: {}", tTabStr, t->Get_c_log_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_achievement_sid: {}", tTabStr, t->Get_c_achievement_sid());
       result += std::format(L"\n{}c_achievement_step_sid: {}", tTabStr, t->Get_c_achievement_step_sid());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_log: {}", tTabStr, t->Get_c_date_log());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class LOG_ACHIEVEMENT_ALL;

class LOG_ACHIEVEMENT_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::LOG_ACHIEVEMENT_ALL;

public:
   // (7)
   explicit LOG_ACHIEVEMENT_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::LOG_ACHIEVEMENT_ALL, buf)
   {
   }

   // (9)
   explicit LOG_ACHIEVEMENT_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       LogId log_id,
       UserId user_id,
       AchievementSid achievement_sid,
       AchievementStepSid achievement_step_sid,
       time_t date_created,
       time_t date_log
   )
       :
       PacketWriter(trait, PacketTypes::LOG_ACHIEVEMENT_ALL, buf)
   {
       WriteValue(id);

       WriteValue(log_id);

       WriteValue(user_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step_sid);

       WriteValue(date_created);

       WriteValue(date_log);


   }

   // (8)
   explicit LOG_ACHIEVEMENT_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       LogId log_id,
       UserId user_id,
       AchievementSid achievement_sid,
       AchievementStepSid achievement_step_sid,
       time_t date_created,
       time_t date_log
   )
   {
       WriteValue(id);

       WriteValue(log_id);

       WriteValue(user_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step_sid);

       WriteValue(date_created);

       WriteValue(date_log);


   }

   LOG_ACHIEVEMENT_ALL& GetPacket() const noexcept { return *reinterpret_cast<LOG_ACHIEVEMENT_ALL*>(GetPacketBufPtr()); }
   LOG_ACHIEVEMENT_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<LOG_ACHIEVEMENT_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class LOG_ACHIEVEMENT_ALL : public Packet
{
   public:
      using Writer = LOG_ACHIEVEMENT_ALL_WRITER;


private:
   Uuid m_c_id;

   LogId m_c_log_id;

   UserId m_c_user_id;

   AchievementSid m_c_achievement_sid;

   AchievementStepSid m_c_achievement_step_sid;

   time_t m_c_date_created;

   time_t m_c_date_log;

public:
   LOG_ACHIEVEMENT_ALL()
   {
       memset(this, 0, sizeof(LOG_ACHIEVEMENT_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(LOG_ACHIEVEMENT_ALL));
   }
   LOG_ACHIEVEMENT_ALL(const LOG_ACHIEVEMENT_ALL&) = delete;
   LOG_ACHIEVEMENT_ALL(LOG_ACHIEVEMENT_ALL &&) = delete;
   LOG_ACHIEVEMENT_ALL& operator = (const LOG_ACHIEVEMENT_ALL&) = delete;
   LOG_ACHIEVEMENT_ALL& operator = (LOG_ACHIEVEMENT_ALL &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   LogId Get_c_log_id() const noexcept { return m_c_log_id; }
   LogId& Ref_c_log_id() noexcept { return m_c_log_id; }
   const LogId& ConstRef_c_log_id() const noexcept { return m_c_log_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   AchievementSid Get_c_achievement_sid() const noexcept { return m_c_achievement_sid; }
   AchievementSid& Ref_c_achievement_sid() noexcept { return m_c_achievement_sid; }
   const AchievementSid& ConstRef_c_achievement_sid() const noexcept { return m_c_achievement_sid; }

   AchievementStepSid Get_c_achievement_step_sid() const noexcept { return m_c_achievement_step_sid; }
   AchievementStepSid& Ref_c_achievement_step_sid() noexcept { return m_c_achievement_step_sid; }
   const AchievementStepSid& ConstRef_c_achievement_step_sid() const noexcept { return m_c_achievement_step_sid; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_log() const noexcept { return m_c_date_log; }
   time_t& Ref_c_date_log() noexcept { return m_c_date_log; }
   const time_t& ConstRef_c_date_log() const noexcept { return m_c_date_log; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(LOG_ACHIEVEMENT_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(LOG_ACHIEVEMENT_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(LOG_ACHIEVEMENT_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(LOG_ACHIEVEMENT_ALL))
           {
               size += sizeof(LOG_ACHIEVEMENT_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(LogId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(AchievementSid);
       }

       {
           thisSize += sizeof(AchievementStepSid);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, LOG_ACHIEVEMENT_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_log_id: {}", tTabStr, t->Get_c_log_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_achievement_sid: {}", tTabStr, t->Get_c_achievement_sid());
       result += std::format(L"\n{}c_achievement_step_sid: {}", tTabStr, t->Get_c_achievement_step_sid());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_log: {}", tTabStr, t->Get_c_date_log());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_ACHIEVEMENT;

class STATIC_ACHIEVEMENT_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_ACHIEVEMENT;

public:
   // (7)
   explicit STATIC_ACHIEVEMENT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_ACHIEVEMENT, buf)
   {
   }

   // (9)
   explicit STATIC_ACHIEVEMENT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AchievementSid achievement_sid,
       const wchar_t* name,
       const wchar_t* condition_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_ACHIEVEMENT, buf)
   {
       WriteValue(achievement_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

   }

   // (8)
   explicit STATIC_ACHIEVEMENT_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       AchievementSid achievement_sid,
       const wchar_t* name,
       const wchar_t* condition_script
   )
   {
       WriteValue(achievement_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

   }

   STATIC_ACHIEVEMENT& GetPacket() const noexcept { return *reinterpret_cast<STATIC_ACHIEVEMENT*>(GetPacketBufPtr()); }
   STATIC_ACHIEVEMENT* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_ACHIEVEMENT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_ACHIEVEMENT : public Packet
{
   public:
      using Writer = STATIC_ACHIEVEMENT_WRITER;


private:
   AchievementSid m_c_achievement_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

public:
   STATIC_ACHIEVEMENT()
   {
       memset(this, 0, sizeof(STATIC_ACHIEVEMENT));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_ACHIEVEMENT));
   }
   STATIC_ACHIEVEMENT(const STATIC_ACHIEVEMENT&) = delete;
   STATIC_ACHIEVEMENT(STATIC_ACHIEVEMENT &&) = delete;
   STATIC_ACHIEVEMENT& operator = (const STATIC_ACHIEVEMENT&) = delete;
   STATIC_ACHIEVEMENT& operator = (STATIC_ACHIEVEMENT &&) = delete;

   AchievementSid Get_c_achievement_sid() const noexcept { return m_c_achievement_sid; }
   AchievementSid& Ref_c_achievement_sid() noexcept { return m_c_achievement_sid; }
   const AchievementSid& ConstRef_c_achievement_sid() const noexcept { return m_c_achievement_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_ACHIEVEMENT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_ACHIEVEMENT) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_ACHIEVEMENT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_ACHIEVEMENT))
           {
               size += sizeof(STATIC_ACHIEVEMENT);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(AchievementSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_ACHIEVEMENT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_achievement_sid: {}", tTabStr, t->Get_c_achievement_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_ACHIEVEMENT_ALL;

class STATIC_ACHIEVEMENT_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_ACHIEVEMENT_ALL;

public:
   // (7)
   explicit STATIC_ACHIEVEMENT_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_ACHIEVEMENT_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_ACHIEVEMENT_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AchievementSid achievement_sid,
       const wchar_t* name,
       const wchar_t* condition_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_ACHIEVEMENT_ALL, buf)
   {
       WriteValue(achievement_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

   }

   // (8)
   explicit STATIC_ACHIEVEMENT_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       AchievementSid achievement_sid,
       const wchar_t* name,
       const wchar_t* condition_script
   )
   {
       WriteValue(achievement_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

   }

   STATIC_ACHIEVEMENT_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_ACHIEVEMENT_ALL*>(GetPacketBufPtr()); }
   STATIC_ACHIEVEMENT_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_ACHIEVEMENT_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_ACHIEVEMENT_ALL : public Packet
{
   public:
      using Writer = STATIC_ACHIEVEMENT_ALL_WRITER;


private:
   AchievementSid m_c_achievement_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

public:
   STATIC_ACHIEVEMENT_ALL()
   {
       memset(this, 0, sizeof(STATIC_ACHIEVEMENT_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_ACHIEVEMENT_ALL));
   }
   STATIC_ACHIEVEMENT_ALL(const STATIC_ACHIEVEMENT_ALL&) = delete;
   STATIC_ACHIEVEMENT_ALL(STATIC_ACHIEVEMENT_ALL &&) = delete;
   STATIC_ACHIEVEMENT_ALL& operator = (const STATIC_ACHIEVEMENT_ALL&) = delete;
   STATIC_ACHIEVEMENT_ALL& operator = (STATIC_ACHIEVEMENT_ALL &&) = delete;

   AchievementSid Get_c_achievement_sid() const noexcept { return m_c_achievement_sid; }
   AchievementSid& Ref_c_achievement_sid() noexcept { return m_c_achievement_sid; }
   const AchievementSid& ConstRef_c_achievement_sid() const noexcept { return m_c_achievement_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_ACHIEVEMENT_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_ACHIEVEMENT_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_ACHIEVEMENT_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_ACHIEVEMENT_ALL))
           {
               size += sizeof(STATIC_ACHIEVEMENT_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(AchievementSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_ACHIEVEMENT_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_achievement_sid: {}", tTabStr, t->Get_c_achievement_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_ACHIEVEMENT_STEP;

class STATIC_ACHIEVEMENT_STEP_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_ACHIEVEMENT_STEP;

public:
   // (7)
   explicit STATIC_ACHIEVEMENT_STEP_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_ACHIEVEMENT_STEP, buf)
   {
   }

   // (9)
   explicit STATIC_ACHIEVEMENT_STEP_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       AchievementSid achievement_sid,
       AchievementStep achievement_step,
       const wchar_t* name,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_ACHIEVEMENT_STEP, buf)
   {
       WriteValue(id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_ACHIEVEMENT_STEP_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       AchievementSid achievement_sid,
       AchievementStep achievement_step,
       const wchar_t* name,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
   {
       WriteValue(id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_ACHIEVEMENT_STEP& GetPacket() const noexcept { return *reinterpret_cast<STATIC_ACHIEVEMENT_STEP*>(GetPacketBufPtr()); }
   STATIC_ACHIEVEMENT_STEP* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_ACHIEVEMENT_STEP*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_ACHIEVEMENT_STEP : public Packet
{
   public:
      using Writer = STATIC_ACHIEVEMENT_STEP_WRITER;


private:
   int64_t m_c_id;

   AchievementSid m_c_achievement_sid;

   AchievementStep m_c_achievement_step;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_ACHIEVEMENT_STEP()
   {
       memset(this, 0, sizeof(STATIC_ACHIEVEMENT_STEP));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_ACHIEVEMENT_STEP));
   }
   STATIC_ACHIEVEMENT_STEP(const STATIC_ACHIEVEMENT_STEP&) = delete;
   STATIC_ACHIEVEMENT_STEP(STATIC_ACHIEVEMENT_STEP &&) = delete;
   STATIC_ACHIEVEMENT_STEP& operator = (const STATIC_ACHIEVEMENT_STEP&) = delete;
   STATIC_ACHIEVEMENT_STEP& operator = (STATIC_ACHIEVEMENT_STEP &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   AchievementSid Get_c_achievement_sid() const noexcept { return m_c_achievement_sid; }
   AchievementSid& Ref_c_achievement_sid() noexcept { return m_c_achievement_sid; }
   const AchievementSid& ConstRef_c_achievement_sid() const noexcept { return m_c_achievement_sid; }

   AchievementStep Get_c_achievement_step() const noexcept { return m_c_achievement_step; }
   AchievementStep& Ref_c_achievement_step() noexcept { return m_c_achievement_step; }
   const AchievementStep& ConstRef_c_achievement_step() const noexcept { return m_c_achievement_step; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_ACHIEVEMENT_STEP) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_ACHIEVEMENT_STEP) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_ACHIEVEMENT_STEP); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_ACHIEVEMENT_STEP))
           {
               size += sizeof(STATIC_ACHIEVEMENT_STEP);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(AchievementSid);
       }

       {
           thisSize += sizeof(AchievementStep);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_ACHIEVEMENT_STEP>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_achievement_sid: {}", tTabStr, t->Get_c_achievement_sid());
       result += std::format(L"\n{}c_achievement_step: {}", tTabStr, t->Get_c_achievement_step());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_ACHIEVEMENT_STEP_ALL;

class STATIC_ACHIEVEMENT_STEP_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_ACHIEVEMENT_STEP_ALL;

public:
   // (7)
   explicit STATIC_ACHIEVEMENT_STEP_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_ACHIEVEMENT_STEP_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_ACHIEVEMENT_STEP_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       AchievementSid achievement_sid,
       AchievementStep achievement_step,
       const wchar_t* name,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_ACHIEVEMENT_STEP_ALL, buf)
   {
       WriteValue(id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_ACHIEVEMENT_STEP_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       AchievementSid achievement_sid,
       AchievementStep achievement_step,
       const wchar_t* name,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
   {
       WriteValue(id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_ACHIEVEMENT_STEP_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_ACHIEVEMENT_STEP_ALL*>(GetPacketBufPtr()); }
   STATIC_ACHIEVEMENT_STEP_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_ACHIEVEMENT_STEP_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_ACHIEVEMENT_STEP_ALL : public Packet
{
   public:
      using Writer = STATIC_ACHIEVEMENT_STEP_ALL_WRITER;


private:
   int64_t m_c_id;

   AchievementSid m_c_achievement_sid;

   AchievementStep m_c_achievement_step;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_ACHIEVEMENT_STEP_ALL()
   {
       memset(this, 0, sizeof(STATIC_ACHIEVEMENT_STEP_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_ACHIEVEMENT_STEP_ALL));
   }
   STATIC_ACHIEVEMENT_STEP_ALL(const STATIC_ACHIEVEMENT_STEP_ALL&) = delete;
   STATIC_ACHIEVEMENT_STEP_ALL(STATIC_ACHIEVEMENT_STEP_ALL &&) = delete;
   STATIC_ACHIEVEMENT_STEP_ALL& operator = (const STATIC_ACHIEVEMENT_STEP_ALL&) = delete;
   STATIC_ACHIEVEMENT_STEP_ALL& operator = (STATIC_ACHIEVEMENT_STEP_ALL &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   AchievementSid Get_c_achievement_sid() const noexcept { return m_c_achievement_sid; }
   AchievementSid& Ref_c_achievement_sid() noexcept { return m_c_achievement_sid; }
   const AchievementSid& ConstRef_c_achievement_sid() const noexcept { return m_c_achievement_sid; }

   AchievementStep Get_c_achievement_step() const noexcept { return m_c_achievement_step; }
   AchievementStep& Ref_c_achievement_step() noexcept { return m_c_achievement_step; }
   const AchievementStep& ConstRef_c_achievement_step() const noexcept { return m_c_achievement_step; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_ACHIEVEMENT_STEP_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_ACHIEVEMENT_STEP_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_ACHIEVEMENT_STEP_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_ACHIEVEMENT_STEP_ALL))
           {
               size += sizeof(STATIC_ACHIEVEMENT_STEP_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(AchievementSid);
       }

       {
           thisSize += sizeof(AchievementStep);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_ACHIEVEMENT_STEP_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_achievement_sid: {}", tTabStr, t->Get_c_achievement_sid());
       result += std::format(L"\n{}c_achievement_step: {}", tTabStr, t->Get_c_achievement_step());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_BUILDING;

class STATIC_BUILDING_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_BUILDING;

public:
   // (7)
   explicit STATIC_BUILDING_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_BUILDING, buf)
   {
   }

   // (9)
   explicit STATIC_BUILDING_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ItemSid building_item_sid,
       BuildingType building_type
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_BUILDING, buf)
   {
       WriteValue(building_item_sid);

       WriteValue(building_type);


   }

   // (8)
   explicit STATIC_BUILDING_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ItemSid building_item_sid,
       BuildingType building_type
   )
   {
       WriteValue(building_item_sid);

       WriteValue(building_type);


   }

   STATIC_BUILDING& GetPacket() const noexcept { return *reinterpret_cast<STATIC_BUILDING*>(GetPacketBufPtr()); }
   STATIC_BUILDING* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_BUILDING*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_BUILDING : public Packet
{
   public:
      using Writer = STATIC_BUILDING_WRITER;


private:
   ItemSid m_c_building_item_sid;

   BuildingType m_c_building_type;

public:
   STATIC_BUILDING()
   {
       memset(this, 0, sizeof(STATIC_BUILDING));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_BUILDING));
   }
   STATIC_BUILDING(const STATIC_BUILDING&) = delete;
   STATIC_BUILDING(STATIC_BUILDING &&) = delete;
   STATIC_BUILDING& operator = (const STATIC_BUILDING&) = delete;
   STATIC_BUILDING& operator = (STATIC_BUILDING &&) = delete;

   ItemSid Get_c_building_item_sid() const noexcept { return m_c_building_item_sid; }
   ItemSid& Ref_c_building_item_sid() noexcept { return m_c_building_item_sid; }
   const ItemSid& ConstRef_c_building_item_sid() const noexcept { return m_c_building_item_sid; }

   BuildingType Get_c_building_type() const noexcept { return m_c_building_type; }
   BuildingType& Ref_c_building_type() noexcept { return m_c_building_type; }
   const BuildingType& ConstRef_c_building_type() const noexcept { return m_c_building_type; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_BUILDING) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_BUILDING) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_BUILDING); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_BUILDING))
           {
               size += sizeof(STATIC_BUILDING);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(BuildingType);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_BUILDING>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_building_item_sid: {}", tTabStr, t->Get_c_building_item_sid());
       result += std::format(L"\n{}c_building_type: {}", tTabStr, t->Get_c_building_type());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_BUILDING_ALL;

class STATIC_BUILDING_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_BUILDING_ALL;

public:
   // (7)
   explicit STATIC_BUILDING_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_BUILDING_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_BUILDING_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ItemSid building_item_sid,
       BuildingType building_type
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_BUILDING_ALL, buf)
   {
       WriteValue(building_item_sid);

       WriteValue(building_type);


   }

   // (8)
   explicit STATIC_BUILDING_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ItemSid building_item_sid,
       BuildingType building_type
   )
   {
       WriteValue(building_item_sid);

       WriteValue(building_type);


   }

   STATIC_BUILDING_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_BUILDING_ALL*>(GetPacketBufPtr()); }
   STATIC_BUILDING_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_BUILDING_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_BUILDING_ALL : public Packet
{
   public:
      using Writer = STATIC_BUILDING_ALL_WRITER;


private:
   ItemSid m_c_building_item_sid;

   BuildingType m_c_building_type;

public:
   STATIC_BUILDING_ALL()
   {
       memset(this, 0, sizeof(STATIC_BUILDING_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_BUILDING_ALL));
   }
   STATIC_BUILDING_ALL(const STATIC_BUILDING_ALL&) = delete;
   STATIC_BUILDING_ALL(STATIC_BUILDING_ALL &&) = delete;
   STATIC_BUILDING_ALL& operator = (const STATIC_BUILDING_ALL&) = delete;
   STATIC_BUILDING_ALL& operator = (STATIC_BUILDING_ALL &&) = delete;

   ItemSid Get_c_building_item_sid() const noexcept { return m_c_building_item_sid; }
   ItemSid& Ref_c_building_item_sid() noexcept { return m_c_building_item_sid; }
   const ItemSid& ConstRef_c_building_item_sid() const noexcept { return m_c_building_item_sid; }

   BuildingType Get_c_building_type() const noexcept { return m_c_building_type; }
   BuildingType& Ref_c_building_type() noexcept { return m_c_building_type; }
   const BuildingType& ConstRef_c_building_type() const noexcept { return m_c_building_type; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_BUILDING_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_BUILDING_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_BUILDING_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_BUILDING_ALL))
           {
               size += sizeof(STATIC_BUILDING_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(BuildingType);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_BUILDING_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_building_item_sid: {}", tTabStr, t->Get_c_building_item_sid());
       result += std::format(L"\n{}c_building_type: {}", tTabStr, t->Get_c_building_type());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_CHARACTER;

class STATIC_CHARACTER_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_CHARACTER;

public:
   // (7)
   explicit STATIC_CHARACTER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_CHARACTER, buf)
   {
   }

   // (9)
   explicit STATIC_CHARACTER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ItemSid character_item_sid,
       const wchar_t* name,
       CharacterType character_type,
       CharacterColor character_color,
       TacticsType tactics_type,
       GridMoverType grid_mover_type,
       int64_t physics_size,
       Scalar skill_distance_best_min,
       Scalar skill_distance_best_max,
       Scalar hp_threshold_flee,
       Scalar hp_threshold_combat,
       Scalar hp_threshold_last_stand
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_CHARACTER, buf)
   {
       WriteValue(character_item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(character_type);

       WriteValue(character_color);

       WriteValue(tactics_type);

       WriteValue(grid_mover_type);

       WriteValue(physics_size);

       WriteValue(skill_distance_best_min);

       WriteValue(skill_distance_best_max);

       WriteValue(hp_threshold_flee);

       WriteValue(hp_threshold_combat);

       WriteValue(hp_threshold_last_stand);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   // (8)
   explicit STATIC_CHARACTER_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ItemSid character_item_sid,
       const wchar_t* name,
       CharacterType character_type,
       CharacterColor character_color,
       TacticsType tactics_type,
       GridMoverType grid_mover_type,
       int64_t physics_size,
       Scalar skill_distance_best_min,
       Scalar skill_distance_best_max,
       Scalar hp_threshold_flee,
       Scalar hp_threshold_combat,
       Scalar hp_threshold_last_stand
   )
   {
       WriteValue(character_item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(character_type);

       WriteValue(character_color);

       WriteValue(tactics_type);

       WriteValue(grid_mover_type);

       WriteValue(physics_size);

       WriteValue(skill_distance_best_min);

       WriteValue(skill_distance_best_max);

       WriteValue(hp_threshold_flee);

       WriteValue(hp_threshold_combat);

       WriteValue(hp_threshold_last_stand);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   STATIC_CHARACTER& GetPacket() const noexcept { return *reinterpret_cast<STATIC_CHARACTER*>(GetPacketBufPtr()); }
   STATIC_CHARACTER* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_CHARACTER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_CHARACTER : public Packet
{
   public:
      using Writer = STATIC_CHARACTER_WRITER;


private:
   ItemSid m_c_character_item_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   CharacterType m_c_character_type;

   CharacterColor m_c_character_color;

   TacticsType m_c_tactics_type;

   GridMoverType m_c_grid_mover_type;

   int64_t m_c_physics_size;

   Scalar m_c_skill_distance_best_min;

   Scalar m_c_skill_distance_best_max;

   Scalar m_c_hp_threshold_flee;

   Scalar m_c_hp_threshold_combat;

   Scalar m_c_hp_threshold_last_stand;

public:
   STATIC_CHARACTER()
   {
       memset(this, 0, sizeof(STATIC_CHARACTER));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_CHARACTER));
   }
   STATIC_CHARACTER(const STATIC_CHARACTER&) = delete;
   STATIC_CHARACTER(STATIC_CHARACTER &&) = delete;
   STATIC_CHARACTER& operator = (const STATIC_CHARACTER&) = delete;
   STATIC_CHARACTER& operator = (STATIC_CHARACTER &&) = delete;

   ItemSid Get_c_character_item_sid() const noexcept { return m_c_character_item_sid; }
   ItemSid& Ref_c_character_item_sid() noexcept { return m_c_character_item_sid; }
   const ItemSid& ConstRef_c_character_item_sid() const noexcept { return m_c_character_item_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   CharacterType Get_c_character_type() const noexcept { return m_c_character_type; }
   CharacterType& Ref_c_character_type() noexcept { return m_c_character_type; }
   const CharacterType& ConstRef_c_character_type() const noexcept { return m_c_character_type; }

   CharacterColor Get_c_character_color() const noexcept { return m_c_character_color; }
   CharacterColor& Ref_c_character_color() noexcept { return m_c_character_color; }
   const CharacterColor& ConstRef_c_character_color() const noexcept { return m_c_character_color; }

   TacticsType Get_c_tactics_type() const noexcept { return m_c_tactics_type; }
   TacticsType& Ref_c_tactics_type() noexcept { return m_c_tactics_type; }
   const TacticsType& ConstRef_c_tactics_type() const noexcept { return m_c_tactics_type; }

   GridMoverType Get_c_grid_mover_type() const noexcept { return m_c_grid_mover_type; }
   GridMoverType& Ref_c_grid_mover_type() noexcept { return m_c_grid_mover_type; }
   const GridMoverType& ConstRef_c_grid_mover_type() const noexcept { return m_c_grid_mover_type; }

   int64_t Get_c_physics_size() const noexcept { return m_c_physics_size; }
   int64_t& Ref_c_physics_size() noexcept { return m_c_physics_size; }
   const int64_t& ConstRef_c_physics_size() const noexcept { return m_c_physics_size; }

   Scalar Get_c_skill_distance_best_min() const noexcept { return m_c_skill_distance_best_min; }
   Scalar& Ref_c_skill_distance_best_min() noexcept { return m_c_skill_distance_best_min; }
   const Scalar& ConstRef_c_skill_distance_best_min() const noexcept { return m_c_skill_distance_best_min; }

   Scalar Get_c_skill_distance_best_max() const noexcept { return m_c_skill_distance_best_max; }
   Scalar& Ref_c_skill_distance_best_max() noexcept { return m_c_skill_distance_best_max; }
   const Scalar& ConstRef_c_skill_distance_best_max() const noexcept { return m_c_skill_distance_best_max; }

   Scalar Get_c_hp_threshold_flee() const noexcept { return m_c_hp_threshold_flee; }
   Scalar& Ref_c_hp_threshold_flee() noexcept { return m_c_hp_threshold_flee; }
   const Scalar& ConstRef_c_hp_threshold_flee() const noexcept { return m_c_hp_threshold_flee; }

   Scalar Get_c_hp_threshold_combat() const noexcept { return m_c_hp_threshold_combat; }
   Scalar& Ref_c_hp_threshold_combat() noexcept { return m_c_hp_threshold_combat; }
   const Scalar& ConstRef_c_hp_threshold_combat() const noexcept { return m_c_hp_threshold_combat; }

   Scalar Get_c_hp_threshold_last_stand() const noexcept { return m_c_hp_threshold_last_stand; }
   Scalar& Ref_c_hp_threshold_last_stand() noexcept { return m_c_hp_threshold_last_stand; }
   const Scalar& ConstRef_c_hp_threshold_last_stand() const noexcept { return m_c_hp_threshold_last_stand; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_CHARACTER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_CHARACTER) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_CHARACTER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_CHARACTER))
           {
               size += sizeof(STATIC_CHARACTER);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ItemSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(CharacterType);
       }

       {
           thisSize += sizeof(CharacterColor);
       }

       {
           thisSize += sizeof(TacticsType);
       }

       {
           thisSize += sizeof(GridMoverType);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_CHARACTER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_character_item_sid: {}", tTabStr, t->Get_c_character_item_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_character_type: {}", tTabStr, t->Get_c_character_type());
       result += std::format(L"\n{}c_character_color: {}", tTabStr, t->Get_c_character_color());
       result += std::format(L"\n{}c_tactics_type: {}", tTabStr, t->Get_c_tactics_type());
       result += std::format(L"\n{}c_grid_mover_type: {}", tTabStr, t->Get_c_grid_mover_type());
       result += std::format(L"\n{}c_physics_size: {}", tTabStr, t->Get_c_physics_size());
       result += std::format(L"\n{}c_skill_distance_best_min: {}", tTabStr, t->Get_c_skill_distance_best_min());
       result += std::format(L"\n{}c_skill_distance_best_max: {}", tTabStr, t->Get_c_skill_distance_best_max());
       result += std::format(L"\n{}c_hp_threshold_flee: {}", tTabStr, t->Get_c_hp_threshold_flee());
       result += std::format(L"\n{}c_hp_threshold_combat: {}", tTabStr, t->Get_c_hp_threshold_combat());
       result += std::format(L"\n{}c_hp_threshold_last_stand: {}", tTabStr, t->Get_c_hp_threshold_last_stand());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_CHARACTER_ALL;

class STATIC_CHARACTER_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_CHARACTER_ALL;

public:
   // (7)
   explicit STATIC_CHARACTER_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_CHARACTER_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_CHARACTER_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ItemSid character_item_sid,
       const wchar_t* name,
       CharacterType character_type,
       CharacterColor character_color,
       TacticsType tactics_type,
       GridMoverType grid_mover_type,
       int64_t physics_size,
       Scalar skill_distance_best_min,
       Scalar skill_distance_best_max,
       Scalar hp_threshold_flee,
       Scalar hp_threshold_combat,
       Scalar hp_threshold_last_stand
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_CHARACTER_ALL, buf)
   {
       WriteValue(character_item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(character_type);

       WriteValue(character_color);

       WriteValue(tactics_type);

       WriteValue(grid_mover_type);

       WriteValue(physics_size);

       WriteValue(skill_distance_best_min);

       WriteValue(skill_distance_best_max);

       WriteValue(hp_threshold_flee);

       WriteValue(hp_threshold_combat);

       WriteValue(hp_threshold_last_stand);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   // (8)
   explicit STATIC_CHARACTER_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ItemSid character_item_sid,
       const wchar_t* name,
       CharacterType character_type,
       CharacterColor character_color,
       TacticsType tactics_type,
       GridMoverType grid_mover_type,
       int64_t physics_size,
       Scalar skill_distance_best_min,
       Scalar skill_distance_best_max,
       Scalar hp_threshold_flee,
       Scalar hp_threshold_combat,
       Scalar hp_threshold_last_stand
   )
   {
       WriteValue(character_item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(character_type);

       WriteValue(character_color);

       WriteValue(tactics_type);

       WriteValue(grid_mover_type);

       WriteValue(physics_size);

       WriteValue(skill_distance_best_min);

       WriteValue(skill_distance_best_max);

       WriteValue(hp_threshold_flee);

       WriteValue(hp_threshold_combat);

       WriteValue(hp_threshold_last_stand);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   STATIC_CHARACTER_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_CHARACTER_ALL*>(GetPacketBufPtr()); }
   STATIC_CHARACTER_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_CHARACTER_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_CHARACTER_ALL : public Packet
{
   public:
      using Writer = STATIC_CHARACTER_ALL_WRITER;


private:
   ItemSid m_c_character_item_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   CharacterType m_c_character_type;

   CharacterColor m_c_character_color;

   TacticsType m_c_tactics_type;

   GridMoverType m_c_grid_mover_type;

   int64_t m_c_physics_size;

   Scalar m_c_skill_distance_best_min;

   Scalar m_c_skill_distance_best_max;

   Scalar m_c_hp_threshold_flee;

   Scalar m_c_hp_threshold_combat;

   Scalar m_c_hp_threshold_last_stand;

public:
   STATIC_CHARACTER_ALL()
   {
       memset(this, 0, sizeof(STATIC_CHARACTER_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_CHARACTER_ALL));
   }
   STATIC_CHARACTER_ALL(const STATIC_CHARACTER_ALL&) = delete;
   STATIC_CHARACTER_ALL(STATIC_CHARACTER_ALL &&) = delete;
   STATIC_CHARACTER_ALL& operator = (const STATIC_CHARACTER_ALL&) = delete;
   STATIC_CHARACTER_ALL& operator = (STATIC_CHARACTER_ALL &&) = delete;

   ItemSid Get_c_character_item_sid() const noexcept { return m_c_character_item_sid; }
   ItemSid& Ref_c_character_item_sid() noexcept { return m_c_character_item_sid; }
   const ItemSid& ConstRef_c_character_item_sid() const noexcept { return m_c_character_item_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   CharacterType Get_c_character_type() const noexcept { return m_c_character_type; }
   CharacterType& Ref_c_character_type() noexcept { return m_c_character_type; }
   const CharacterType& ConstRef_c_character_type() const noexcept { return m_c_character_type; }

   CharacterColor Get_c_character_color() const noexcept { return m_c_character_color; }
   CharacterColor& Ref_c_character_color() noexcept { return m_c_character_color; }
   const CharacterColor& ConstRef_c_character_color() const noexcept { return m_c_character_color; }

   TacticsType Get_c_tactics_type() const noexcept { return m_c_tactics_type; }
   TacticsType& Ref_c_tactics_type() noexcept { return m_c_tactics_type; }
   const TacticsType& ConstRef_c_tactics_type() const noexcept { return m_c_tactics_type; }

   GridMoverType Get_c_grid_mover_type() const noexcept { return m_c_grid_mover_type; }
   GridMoverType& Ref_c_grid_mover_type() noexcept { return m_c_grid_mover_type; }
   const GridMoverType& ConstRef_c_grid_mover_type() const noexcept { return m_c_grid_mover_type; }

   int64_t Get_c_physics_size() const noexcept { return m_c_physics_size; }
   int64_t& Ref_c_physics_size() noexcept { return m_c_physics_size; }
   const int64_t& ConstRef_c_physics_size() const noexcept { return m_c_physics_size; }

   Scalar Get_c_skill_distance_best_min() const noexcept { return m_c_skill_distance_best_min; }
   Scalar& Ref_c_skill_distance_best_min() noexcept { return m_c_skill_distance_best_min; }
   const Scalar& ConstRef_c_skill_distance_best_min() const noexcept { return m_c_skill_distance_best_min; }

   Scalar Get_c_skill_distance_best_max() const noexcept { return m_c_skill_distance_best_max; }
   Scalar& Ref_c_skill_distance_best_max() noexcept { return m_c_skill_distance_best_max; }
   const Scalar& ConstRef_c_skill_distance_best_max() const noexcept { return m_c_skill_distance_best_max; }

   Scalar Get_c_hp_threshold_flee() const noexcept { return m_c_hp_threshold_flee; }
   Scalar& Ref_c_hp_threshold_flee() noexcept { return m_c_hp_threshold_flee; }
   const Scalar& ConstRef_c_hp_threshold_flee() const noexcept { return m_c_hp_threshold_flee; }

   Scalar Get_c_hp_threshold_combat() const noexcept { return m_c_hp_threshold_combat; }
   Scalar& Ref_c_hp_threshold_combat() noexcept { return m_c_hp_threshold_combat; }
   const Scalar& ConstRef_c_hp_threshold_combat() const noexcept { return m_c_hp_threshold_combat; }

   Scalar Get_c_hp_threshold_last_stand() const noexcept { return m_c_hp_threshold_last_stand; }
   Scalar& Ref_c_hp_threshold_last_stand() noexcept { return m_c_hp_threshold_last_stand; }
   const Scalar& ConstRef_c_hp_threshold_last_stand() const noexcept { return m_c_hp_threshold_last_stand; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_CHARACTER_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_CHARACTER_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_CHARACTER_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_CHARACTER_ALL))
           {
               size += sizeof(STATIC_CHARACTER_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ItemSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(CharacterType);
       }

       {
           thisSize += sizeof(CharacterColor);
       }

       {
           thisSize += sizeof(TacticsType);
       }

       {
           thisSize += sizeof(GridMoverType);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_CHARACTER_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_character_item_sid: {}", tTabStr, t->Get_c_character_item_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_character_type: {}", tTabStr, t->Get_c_character_type());
       result += std::format(L"\n{}c_character_color: {}", tTabStr, t->Get_c_character_color());
       result += std::format(L"\n{}c_tactics_type: {}", tTabStr, t->Get_c_tactics_type());
       result += std::format(L"\n{}c_grid_mover_type: {}", tTabStr, t->Get_c_grid_mover_type());
       result += std::format(L"\n{}c_physics_size: {}", tTabStr, t->Get_c_physics_size());
       result += std::format(L"\n{}c_skill_distance_best_min: {}", tTabStr, t->Get_c_skill_distance_best_min());
       result += std::format(L"\n{}c_skill_distance_best_max: {}", tTabStr, t->Get_c_skill_distance_best_max());
       result += std::format(L"\n{}c_hp_threshold_flee: {}", tTabStr, t->Get_c_hp_threshold_flee());
       result += std::format(L"\n{}c_hp_threshold_combat: {}", tTabStr, t->Get_c_hp_threshold_combat());
       result += std::format(L"\n{}c_hp_threshold_last_stand: {}", tTabStr, t->Get_c_hp_threshold_last_stand());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_CHARACTER_SKILL;

class STATIC_CHARACTER_SKILL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_CHARACTER_SKILL;

public:
   // (7)
   explicit STATIC_CHARACTER_SKILL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_CHARACTER_SKILL, buf)
   {
   }

   // (9)
   explicit STATIC_CHARACTER_SKILL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       ItemSid character_item_sid,
       const wchar_t* name,
       ComponentType skill_component_type,
       StatValue skill_cost_sp,
       SkillSpan skill_cooltime,
       Scalar skill_distance_min,
       Scalar skill_distance_max,
       SkillSpan skill_motion_duration,
       SkillPower skill_power
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_CHARACTER_SKILL, buf)
   {
       WriteValue(id);

       WriteValue(character_item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(skill_component_type);

       WriteValue(skill_cost_sp);

       WriteValue(skill_cooltime);

       WriteValue(skill_distance_min);

       WriteValue(skill_distance_max);

       WriteValue(skill_motion_duration);

       WriteValue(skill_power);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   // (8)
   explicit STATIC_CHARACTER_SKILL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       ItemSid character_item_sid,
       const wchar_t* name,
       ComponentType skill_component_type,
       StatValue skill_cost_sp,
       SkillSpan skill_cooltime,
       Scalar skill_distance_min,
       Scalar skill_distance_max,
       SkillSpan skill_motion_duration,
       SkillPower skill_power
   )
   {
       WriteValue(id);

       WriteValue(character_item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(skill_component_type);

       WriteValue(skill_cost_sp);

       WriteValue(skill_cooltime);

       WriteValue(skill_distance_min);

       WriteValue(skill_distance_max);

       WriteValue(skill_motion_duration);

       WriteValue(skill_power);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   STATIC_CHARACTER_SKILL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_CHARACTER_SKILL*>(GetPacketBufPtr()); }
   STATIC_CHARACTER_SKILL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_CHARACTER_SKILL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_CHARACTER_SKILL : public Packet
{
   public:
      using Writer = STATIC_CHARACTER_SKILL_WRITER;


private:
   int64_t m_c_id;

   ItemSid m_c_character_item_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   ComponentType m_c_skill_component_type;

   StatValue m_c_skill_cost_sp;

   SkillSpan m_c_skill_cooltime;

   Scalar m_c_skill_distance_min;

   Scalar m_c_skill_distance_max;

   SkillSpan m_c_skill_motion_duration;

   SkillPower m_c_skill_power;

public:
   STATIC_CHARACTER_SKILL()
   {
       memset(this, 0, sizeof(STATIC_CHARACTER_SKILL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_CHARACTER_SKILL));
   }
   STATIC_CHARACTER_SKILL(const STATIC_CHARACTER_SKILL&) = delete;
   STATIC_CHARACTER_SKILL(STATIC_CHARACTER_SKILL &&) = delete;
   STATIC_CHARACTER_SKILL& operator = (const STATIC_CHARACTER_SKILL&) = delete;
   STATIC_CHARACTER_SKILL& operator = (STATIC_CHARACTER_SKILL &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   ItemSid Get_c_character_item_sid() const noexcept { return m_c_character_item_sid; }
   ItemSid& Ref_c_character_item_sid() noexcept { return m_c_character_item_sid; }
   const ItemSid& ConstRef_c_character_item_sid() const noexcept { return m_c_character_item_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   ComponentType Get_c_skill_component_type() const noexcept { return m_c_skill_component_type; }
   ComponentType& Ref_c_skill_component_type() noexcept { return m_c_skill_component_type; }
   const ComponentType& ConstRef_c_skill_component_type() const noexcept { return m_c_skill_component_type; }

   StatValue Get_c_skill_cost_sp() const noexcept { return m_c_skill_cost_sp; }
   StatValue& Ref_c_skill_cost_sp() noexcept { return m_c_skill_cost_sp; }
   const StatValue& ConstRef_c_skill_cost_sp() const noexcept { return m_c_skill_cost_sp; }

   SkillSpan Get_c_skill_cooltime() const noexcept { return m_c_skill_cooltime; }
   SkillSpan& Ref_c_skill_cooltime() noexcept { return m_c_skill_cooltime; }
   const SkillSpan& ConstRef_c_skill_cooltime() const noexcept { return m_c_skill_cooltime; }

   Scalar Get_c_skill_distance_min() const noexcept { return m_c_skill_distance_min; }
   Scalar& Ref_c_skill_distance_min() noexcept { return m_c_skill_distance_min; }
   const Scalar& ConstRef_c_skill_distance_min() const noexcept { return m_c_skill_distance_min; }

   Scalar Get_c_skill_distance_max() const noexcept { return m_c_skill_distance_max; }
   Scalar& Ref_c_skill_distance_max() noexcept { return m_c_skill_distance_max; }
   const Scalar& ConstRef_c_skill_distance_max() const noexcept { return m_c_skill_distance_max; }

   SkillSpan Get_c_skill_motion_duration() const noexcept { return m_c_skill_motion_duration; }
   SkillSpan& Ref_c_skill_motion_duration() noexcept { return m_c_skill_motion_duration; }
   const SkillSpan& ConstRef_c_skill_motion_duration() const noexcept { return m_c_skill_motion_duration; }

   SkillPower Get_c_skill_power() const noexcept { return m_c_skill_power; }
   SkillPower& Ref_c_skill_power() noexcept { return m_c_skill_power; }
   const SkillPower& ConstRef_c_skill_power() const noexcept { return m_c_skill_power; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_CHARACTER_SKILL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_CHARACTER_SKILL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_CHARACTER_SKILL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_CHARACTER_SKILL))
           {
               size += sizeof(STATIC_CHARACTER_SKILL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(ComponentType);
       }

       {
           thisSize += sizeof(StatValue);
       }

       {
           thisSize += sizeof(SkillSpan);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(SkillSpan);
       }

       {
           thisSize += sizeof(SkillPower);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_CHARACTER_SKILL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_character_item_sid: {}", tTabStr, t->Get_c_character_item_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_skill_component_type: {}", tTabStr, t->Get_c_skill_component_type());
       result += std::format(L"\n{}c_skill_cost_sp: {}", tTabStr, t->Get_c_skill_cost_sp());
       result += std::format(L"\n{}c_skill_cooltime: {}", tTabStr, t->Get_c_skill_cooltime());
       result += std::format(L"\n{}c_skill_distance_min: {}", tTabStr, t->Get_c_skill_distance_min());
       result += std::format(L"\n{}c_skill_distance_max: {}", tTabStr, t->Get_c_skill_distance_max());
       result += std::format(L"\n{}c_skill_motion_duration: {}", tTabStr, t->Get_c_skill_motion_duration());
       result += std::format(L"\n{}c_skill_power: {}", tTabStr, t->Get_c_skill_power());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_CHARACTER_SKILL_ALL;

class STATIC_CHARACTER_SKILL_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_CHARACTER_SKILL_ALL;

public:
   // (7)
   explicit STATIC_CHARACTER_SKILL_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_CHARACTER_SKILL_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_CHARACTER_SKILL_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       ItemSid character_item_sid,
       const wchar_t* name,
       ComponentType skill_component_type,
       StatValue skill_cost_sp,
       SkillSpan skill_cooltime,
       Scalar skill_distance_min,
       Scalar skill_distance_max,
       SkillSpan skill_motion_duration,
       SkillPower skill_power
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_CHARACTER_SKILL_ALL, buf)
   {
       WriteValue(id);

       WriteValue(character_item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(skill_component_type);

       WriteValue(skill_cost_sp);

       WriteValue(skill_cooltime);

       WriteValue(skill_distance_min);

       WriteValue(skill_distance_max);

       WriteValue(skill_motion_duration);

       WriteValue(skill_power);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   // (8)
   explicit STATIC_CHARACTER_SKILL_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       ItemSid character_item_sid,
       const wchar_t* name,
       ComponentType skill_component_type,
       StatValue skill_cost_sp,
       SkillSpan skill_cooltime,
       Scalar skill_distance_min,
       Scalar skill_distance_max,
       SkillSpan skill_motion_duration,
       SkillPower skill_power
   )
   {
       WriteValue(id);

       WriteValue(character_item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(skill_component_type);

       WriteValue(skill_cost_sp);

       WriteValue(skill_cooltime);

       WriteValue(skill_distance_min);

       WriteValue(skill_distance_max);

       WriteValue(skill_motion_duration);

       WriteValue(skill_power);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   STATIC_CHARACTER_SKILL_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_CHARACTER_SKILL_ALL*>(GetPacketBufPtr()); }
   STATIC_CHARACTER_SKILL_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_CHARACTER_SKILL_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_CHARACTER_SKILL_ALL : public Packet
{
   public:
      using Writer = STATIC_CHARACTER_SKILL_ALL_WRITER;


private:
   int64_t m_c_id;

   ItemSid m_c_character_item_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   ComponentType m_c_skill_component_type;

   StatValue m_c_skill_cost_sp;

   SkillSpan m_c_skill_cooltime;

   Scalar m_c_skill_distance_min;

   Scalar m_c_skill_distance_max;

   SkillSpan m_c_skill_motion_duration;

   SkillPower m_c_skill_power;

public:
   STATIC_CHARACTER_SKILL_ALL()
   {
       memset(this, 0, sizeof(STATIC_CHARACTER_SKILL_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_CHARACTER_SKILL_ALL));
   }
   STATIC_CHARACTER_SKILL_ALL(const STATIC_CHARACTER_SKILL_ALL&) = delete;
   STATIC_CHARACTER_SKILL_ALL(STATIC_CHARACTER_SKILL_ALL &&) = delete;
   STATIC_CHARACTER_SKILL_ALL& operator = (const STATIC_CHARACTER_SKILL_ALL&) = delete;
   STATIC_CHARACTER_SKILL_ALL& operator = (STATIC_CHARACTER_SKILL_ALL &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   ItemSid Get_c_character_item_sid() const noexcept { return m_c_character_item_sid; }
   ItemSid& Ref_c_character_item_sid() noexcept { return m_c_character_item_sid; }
   const ItemSid& ConstRef_c_character_item_sid() const noexcept { return m_c_character_item_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   ComponentType Get_c_skill_component_type() const noexcept { return m_c_skill_component_type; }
   ComponentType& Ref_c_skill_component_type() noexcept { return m_c_skill_component_type; }
   const ComponentType& ConstRef_c_skill_component_type() const noexcept { return m_c_skill_component_type; }

   StatValue Get_c_skill_cost_sp() const noexcept { return m_c_skill_cost_sp; }
   StatValue& Ref_c_skill_cost_sp() noexcept { return m_c_skill_cost_sp; }
   const StatValue& ConstRef_c_skill_cost_sp() const noexcept { return m_c_skill_cost_sp; }

   SkillSpan Get_c_skill_cooltime() const noexcept { return m_c_skill_cooltime; }
   SkillSpan& Ref_c_skill_cooltime() noexcept { return m_c_skill_cooltime; }
   const SkillSpan& ConstRef_c_skill_cooltime() const noexcept { return m_c_skill_cooltime; }

   Scalar Get_c_skill_distance_min() const noexcept { return m_c_skill_distance_min; }
   Scalar& Ref_c_skill_distance_min() noexcept { return m_c_skill_distance_min; }
   const Scalar& ConstRef_c_skill_distance_min() const noexcept { return m_c_skill_distance_min; }

   Scalar Get_c_skill_distance_max() const noexcept { return m_c_skill_distance_max; }
   Scalar& Ref_c_skill_distance_max() noexcept { return m_c_skill_distance_max; }
   const Scalar& ConstRef_c_skill_distance_max() const noexcept { return m_c_skill_distance_max; }

   SkillSpan Get_c_skill_motion_duration() const noexcept { return m_c_skill_motion_duration; }
   SkillSpan& Ref_c_skill_motion_duration() noexcept { return m_c_skill_motion_duration; }
   const SkillSpan& ConstRef_c_skill_motion_duration() const noexcept { return m_c_skill_motion_duration; }

   SkillPower Get_c_skill_power() const noexcept { return m_c_skill_power; }
   SkillPower& Ref_c_skill_power() noexcept { return m_c_skill_power; }
   const SkillPower& ConstRef_c_skill_power() const noexcept { return m_c_skill_power; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_CHARACTER_SKILL_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_CHARACTER_SKILL_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_CHARACTER_SKILL_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_CHARACTER_SKILL_ALL))
           {
               size += sizeof(STATIC_CHARACTER_SKILL_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(ComponentType);
       }

       {
           thisSize += sizeof(StatValue);
       }

       {
           thisSize += sizeof(SkillSpan);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(SkillSpan);
       }

       {
           thisSize += sizeof(SkillPower);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_CHARACTER_SKILL_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_character_item_sid: {}", tTabStr, t->Get_c_character_item_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_skill_component_type: {}", tTabStr, t->Get_c_skill_component_type());
       result += std::format(L"\n{}c_skill_cost_sp: {}", tTabStr, t->Get_c_skill_cost_sp());
       result += std::format(L"\n{}c_skill_cooltime: {}", tTabStr, t->Get_c_skill_cooltime());
       result += std::format(L"\n{}c_skill_distance_min: {}", tTabStr, t->Get_c_skill_distance_min());
       result += std::format(L"\n{}c_skill_distance_max: {}", tTabStr, t->Get_c_skill_distance_max());
       result += std::format(L"\n{}c_skill_motion_duration: {}", tTabStr, t->Get_c_skill_motion_duration());
       result += std::format(L"\n{}c_skill_power: {}", tTabStr, t->Get_c_skill_power());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_GAME;

class STATIC_GAME_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_GAME;

public:
   // (7)
   explicit STATIC_GAME_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_GAME, buf)
   {
   }

   // (9)
   explicit STATIC_GAME_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       GameSid game_sid,
       const wchar_t* name,
       GameType game_type,
       MapSid map_sid
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_GAME, buf)
   {
       WriteValue(game_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(game_type);

       WriteValue(map_sid);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   // (8)
   explicit STATIC_GAME_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       GameSid game_sid,
       const wchar_t* name,
       GameType game_type,
       MapSid map_sid
   )
   {
       WriteValue(game_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(game_type);

       WriteValue(map_sid);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   STATIC_GAME& GetPacket() const noexcept { return *reinterpret_cast<STATIC_GAME*>(GetPacketBufPtr()); }
   STATIC_GAME* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_GAME*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_GAME : public Packet
{
   public:
      using Writer = STATIC_GAME_WRITER;


private:
   GameSid m_c_game_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   GameType m_c_game_type;

   MapSid m_c_map_sid;

public:
   STATIC_GAME()
   {
       memset(this, 0, sizeof(STATIC_GAME));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_GAME));
   }
   STATIC_GAME(const STATIC_GAME&) = delete;
   STATIC_GAME(STATIC_GAME &&) = delete;
   STATIC_GAME& operator = (const STATIC_GAME&) = delete;
   STATIC_GAME& operator = (STATIC_GAME &&) = delete;

   GameSid Get_c_game_sid() const noexcept { return m_c_game_sid; }
   GameSid& Ref_c_game_sid() noexcept { return m_c_game_sid; }
   const GameSid& ConstRef_c_game_sid() const noexcept { return m_c_game_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   GameType Get_c_game_type() const noexcept { return m_c_game_type; }
   GameType& Ref_c_game_type() noexcept { return m_c_game_type; }
   const GameType& ConstRef_c_game_type() const noexcept { return m_c_game_type; }

   MapSid Get_c_map_sid() const noexcept { return m_c_map_sid; }
   MapSid& Ref_c_map_sid() noexcept { return m_c_map_sid; }
   const MapSid& ConstRef_c_map_sid() const noexcept { return m_c_map_sid; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_GAME) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_GAME) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_GAME); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_GAME))
           {
               size += sizeof(STATIC_GAME);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(GameSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(GameType);
       }

       {
           thisSize += sizeof(MapSid);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_GAME>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_game_sid: {}", tTabStr, t->Get_c_game_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_game_type: {}", tTabStr, t->Get_c_game_type());
       result += std::format(L"\n{}c_map_sid: {}", tTabStr, t->Get_c_map_sid());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_GAME_ALL;

class STATIC_GAME_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_GAME_ALL;

public:
   // (7)
   explicit STATIC_GAME_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_GAME_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_GAME_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       GameSid game_sid,
       const wchar_t* name,
       GameType game_type,
       MapSid map_sid
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_GAME_ALL, buf)
   {
       WriteValue(game_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(game_type);

       WriteValue(map_sid);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   // (8)
   explicit STATIC_GAME_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       GameSid game_sid,
       const wchar_t* name,
       GameType game_type,
       MapSid map_sid
   )
   {
       WriteValue(game_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(game_type);

       WriteValue(map_sid);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   STATIC_GAME_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_GAME_ALL*>(GetPacketBufPtr()); }
   STATIC_GAME_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_GAME_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_GAME_ALL : public Packet
{
   public:
      using Writer = STATIC_GAME_ALL_WRITER;


private:
   GameSid m_c_game_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   GameType m_c_game_type;

   MapSid m_c_map_sid;

public:
   STATIC_GAME_ALL()
   {
       memset(this, 0, sizeof(STATIC_GAME_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_GAME_ALL));
   }
   STATIC_GAME_ALL(const STATIC_GAME_ALL&) = delete;
   STATIC_GAME_ALL(STATIC_GAME_ALL &&) = delete;
   STATIC_GAME_ALL& operator = (const STATIC_GAME_ALL&) = delete;
   STATIC_GAME_ALL& operator = (STATIC_GAME_ALL &&) = delete;

   GameSid Get_c_game_sid() const noexcept { return m_c_game_sid; }
   GameSid& Ref_c_game_sid() noexcept { return m_c_game_sid; }
   const GameSid& ConstRef_c_game_sid() const noexcept { return m_c_game_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   GameType Get_c_game_type() const noexcept { return m_c_game_type; }
   GameType& Ref_c_game_type() noexcept { return m_c_game_type; }
   const GameType& ConstRef_c_game_type() const noexcept { return m_c_game_type; }

   MapSid Get_c_map_sid() const noexcept { return m_c_map_sid; }
   MapSid& Ref_c_map_sid() noexcept { return m_c_map_sid; }
   const MapSid& ConstRef_c_map_sid() const noexcept { return m_c_map_sid; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_GAME_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_GAME_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_GAME_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_GAME_ALL))
           {
               size += sizeof(STATIC_GAME_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(GameSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(GameType);
       }

       {
           thisSize += sizeof(MapSid);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_GAME_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_game_sid: {}", tTabStr, t->Get_c_game_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_game_type: {}", tTabStr, t->Get_c_game_type());
       result += std::format(L"\n{}c_map_sid: {}", tTabStr, t->Get_c_map_sid());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_ITEM;

class STATIC_ITEM_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_ITEM;

public:
   // (7)
   explicit STATIC_ITEM_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_ITEM, buf)
   {
   }

   // (9)
   explicit STATIC_ITEM_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ItemSid item_sid,
       const wchar_t* name,
       ItemType item_type,
       ItemGrade item_grade,
       ItemQuantity max_quantity,
       int64_t item_tag,
       const wchar_t* use_condition_script,
       const wchar_t* use_action_script,
       int64_t uasset_icon_img
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_ITEM, buf)
   {
       WriteValue(item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(item_type);

       WriteValue(item_grade);

       WriteValue(max_quantity);

       WriteValue(item_tag);

       PacketSize& use_condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& use_condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& use_action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& use_action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(uasset_icon_img);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       use_condition_scriptOffset = GetPacketSize();
       use_condition_scriptLength = (PacketSize)wcsnlen_s(use_condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == use_condition_scriptLength) WriteString(L"", 0);
       else WriteString(use_condition_script, use_condition_scriptLength); 

       use_action_scriptOffset = GetPacketSize();
       use_action_scriptLength = (PacketSize)wcsnlen_s(use_action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == use_action_scriptLength) WriteString(L"", 0);
       else WriteString(use_action_script, use_action_scriptLength); 

   }

   // (8)
   explicit STATIC_ITEM_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ItemSid item_sid,
       const wchar_t* name,
       ItemType item_type,
       ItemGrade item_grade,
       ItemQuantity max_quantity,
       int64_t item_tag,
       const wchar_t* use_condition_script,
       const wchar_t* use_action_script,
       int64_t uasset_icon_img
   )
   {
       WriteValue(item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(item_type);

       WriteValue(item_grade);

       WriteValue(max_quantity);

       WriteValue(item_tag);

       PacketSize& use_condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& use_condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& use_action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& use_action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(uasset_icon_img);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       use_condition_scriptOffset = GetPacketSize();
       use_condition_scriptLength = (PacketSize)wcsnlen_s(use_condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == use_condition_scriptLength) WriteString(L"", 0);
       else WriteString(use_condition_script, use_condition_scriptLength); 

       use_action_scriptOffset = GetPacketSize();
       use_action_scriptLength = (PacketSize)wcsnlen_s(use_action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == use_action_scriptLength) WriteString(L"", 0);
       else WriteString(use_action_script, use_action_scriptLength); 

   }

   STATIC_ITEM& GetPacket() const noexcept { return *reinterpret_cast<STATIC_ITEM*>(GetPacketBufPtr()); }
   STATIC_ITEM* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_ITEM*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_ITEM : public Packet
{
   public:
      using Writer = STATIC_ITEM_WRITER;


private:
   ItemSid m_c_item_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   ItemType m_c_item_type;

   ItemGrade m_c_item_grade;

   ItemQuantity m_c_max_quantity;

   int64_t m_c_item_tag;

   PacketSize m_c_use_condition_script_offset = 0;
   PacketSize m_c_use_condition_script_length = 0;

   PacketSize m_c_use_action_script_offset = 0;
   PacketSize m_c_use_action_script_length = 0;

   int64_t m_c_uasset_icon_img;

public:
   STATIC_ITEM()
   {
       memset(this, 0, sizeof(STATIC_ITEM));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_ITEM));
   }
   STATIC_ITEM(const STATIC_ITEM&) = delete;
   STATIC_ITEM(STATIC_ITEM &&) = delete;
   STATIC_ITEM& operator = (const STATIC_ITEM&) = delete;
   STATIC_ITEM& operator = (STATIC_ITEM &&) = delete;

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   ItemType Get_c_item_type() const noexcept { return m_c_item_type; }
   ItemType& Ref_c_item_type() noexcept { return m_c_item_type; }
   const ItemType& ConstRef_c_item_type() const noexcept { return m_c_item_type; }

   ItemGrade Get_c_item_grade() const noexcept { return m_c_item_grade; }
   ItemGrade& Ref_c_item_grade() noexcept { return m_c_item_grade; }
   const ItemGrade& ConstRef_c_item_grade() const noexcept { return m_c_item_grade; }

   ItemQuantity Get_c_max_quantity() const noexcept { return m_c_max_quantity; }
   ItemQuantity& Ref_c_max_quantity() noexcept { return m_c_max_quantity; }
   const ItemQuantity& ConstRef_c_max_quantity() const noexcept { return m_c_max_quantity; }

   int64_t Get_c_item_tag() const noexcept { return m_c_item_tag; }
   int64_t& Ref_c_item_tag() noexcept { return m_c_item_tag; }
   const int64_t& ConstRef_c_item_tag() const noexcept { return m_c_item_tag; }

   const wchar_t* Get_c_use_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_use_condition_script_offset) + m_c_use_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_use_condition_script_offset);
   }

   wchar_t* Ref_c_use_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_use_condition_script_offset) + m_c_use_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_use_condition_script_offset);
   }

   const wchar_t* Get_c_use_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_use_action_script_offset) + m_c_use_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_use_action_script_offset);
   }

   wchar_t* Ref_c_use_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_use_action_script_offset) + m_c_use_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_use_action_script_offset);
   }

   int64_t Get_c_uasset_icon_img() const noexcept { return m_c_uasset_icon_img; }
   int64_t& Ref_c_uasset_icon_img() noexcept { return m_c_uasset_icon_img; }
   const int64_t& ConstRef_c_uasset_icon_img() const noexcept { return m_c_uasset_icon_img; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_ITEM) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_ITEM) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_ITEM); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_ITEM))
           {
               size += sizeof(STATIC_ITEM);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ItemSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(ItemType);
       }

       {
           thisSize += sizeof(ItemGrade);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           if (GetPacketSize() < m_c_use_condition_script_offset)
               return false;

           if (0 < m_c_use_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_use_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_use_action_script_offset)
               return false;

           if (0 < m_c_use_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_use_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(int64_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_ITEM>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_item_type: {}", tTabStr, t->Get_c_item_type());
       result += std::format(L"\n{}c_item_grade: {}", tTabStr, t->Get_c_item_grade());
       result += std::format(L"\n{}c_max_quantity: {}", tTabStr, t->Get_c_max_quantity());
       result += std::format(L"\n{}c_item_tag: {}", tTabStr, t->Get_c_item_tag());
       result += std::format(L"\n{}c_use_condition_script: {}", tTabStr, t->Get_c_use_condition_script());
       result += std::format(L"\n{}c_use_action_script: {}", tTabStr, t->Get_c_use_action_script());
       result += std::format(L"\n{}c_uasset_icon_img: {}", tTabStr, t->Get_c_uasset_icon_img());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_ITEM_ALL;

class STATIC_ITEM_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_ITEM_ALL;

public:
   // (7)
   explicit STATIC_ITEM_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_ITEM_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_ITEM_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ItemSid item_sid,
       const wchar_t* name,
       ItemType item_type,
       ItemGrade item_grade,
       ItemQuantity max_quantity,
       int64_t item_tag,
       const wchar_t* use_condition_script,
       const wchar_t* use_action_script,
       int64_t uasset_icon_img
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_ITEM_ALL, buf)
   {
       WriteValue(item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(item_type);

       WriteValue(item_grade);

       WriteValue(max_quantity);

       WriteValue(item_tag);

       PacketSize& use_condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& use_condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& use_action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& use_action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(uasset_icon_img);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       use_condition_scriptOffset = GetPacketSize();
       use_condition_scriptLength = (PacketSize)wcsnlen_s(use_condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == use_condition_scriptLength) WriteString(L"", 0);
       else WriteString(use_condition_script, use_condition_scriptLength); 

       use_action_scriptOffset = GetPacketSize();
       use_action_scriptLength = (PacketSize)wcsnlen_s(use_action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == use_action_scriptLength) WriteString(L"", 0);
       else WriteString(use_action_script, use_action_scriptLength); 

   }

   // (8)
   explicit STATIC_ITEM_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ItemSid item_sid,
       const wchar_t* name,
       ItemType item_type,
       ItemGrade item_grade,
       ItemQuantity max_quantity,
       int64_t item_tag,
       const wchar_t* use_condition_script,
       const wchar_t* use_action_script,
       int64_t uasset_icon_img
   )
   {
       WriteValue(item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(item_type);

       WriteValue(item_grade);

       WriteValue(max_quantity);

       WriteValue(item_tag);

       PacketSize& use_condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& use_condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& use_action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& use_action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(uasset_icon_img);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       use_condition_scriptOffset = GetPacketSize();
       use_condition_scriptLength = (PacketSize)wcsnlen_s(use_condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == use_condition_scriptLength) WriteString(L"", 0);
       else WriteString(use_condition_script, use_condition_scriptLength); 

       use_action_scriptOffset = GetPacketSize();
       use_action_scriptLength = (PacketSize)wcsnlen_s(use_action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == use_action_scriptLength) WriteString(L"", 0);
       else WriteString(use_action_script, use_action_scriptLength); 

   }

   STATIC_ITEM_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_ITEM_ALL*>(GetPacketBufPtr()); }
   STATIC_ITEM_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_ITEM_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_ITEM_ALL : public Packet
{
   public:
      using Writer = STATIC_ITEM_ALL_WRITER;


private:
   ItemSid m_c_item_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   ItemType m_c_item_type;

   ItemGrade m_c_item_grade;

   ItemQuantity m_c_max_quantity;

   int64_t m_c_item_tag;

   PacketSize m_c_use_condition_script_offset = 0;
   PacketSize m_c_use_condition_script_length = 0;

   PacketSize m_c_use_action_script_offset = 0;
   PacketSize m_c_use_action_script_length = 0;

   int64_t m_c_uasset_icon_img;

public:
   STATIC_ITEM_ALL()
   {
       memset(this, 0, sizeof(STATIC_ITEM_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_ITEM_ALL));
   }
   STATIC_ITEM_ALL(const STATIC_ITEM_ALL&) = delete;
   STATIC_ITEM_ALL(STATIC_ITEM_ALL &&) = delete;
   STATIC_ITEM_ALL& operator = (const STATIC_ITEM_ALL&) = delete;
   STATIC_ITEM_ALL& operator = (STATIC_ITEM_ALL &&) = delete;

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   ItemType Get_c_item_type() const noexcept { return m_c_item_type; }
   ItemType& Ref_c_item_type() noexcept { return m_c_item_type; }
   const ItemType& ConstRef_c_item_type() const noexcept { return m_c_item_type; }

   ItemGrade Get_c_item_grade() const noexcept { return m_c_item_grade; }
   ItemGrade& Ref_c_item_grade() noexcept { return m_c_item_grade; }
   const ItemGrade& ConstRef_c_item_grade() const noexcept { return m_c_item_grade; }

   ItemQuantity Get_c_max_quantity() const noexcept { return m_c_max_quantity; }
   ItemQuantity& Ref_c_max_quantity() noexcept { return m_c_max_quantity; }
   const ItemQuantity& ConstRef_c_max_quantity() const noexcept { return m_c_max_quantity; }

   int64_t Get_c_item_tag() const noexcept { return m_c_item_tag; }
   int64_t& Ref_c_item_tag() noexcept { return m_c_item_tag; }
   const int64_t& ConstRef_c_item_tag() const noexcept { return m_c_item_tag; }

   const wchar_t* Get_c_use_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_use_condition_script_offset) + m_c_use_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_use_condition_script_offset);
   }

   wchar_t* Ref_c_use_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_use_condition_script_offset) + m_c_use_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_use_condition_script_offset);
   }

   const wchar_t* Get_c_use_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_use_action_script_offset) + m_c_use_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_use_action_script_offset);
   }

   wchar_t* Ref_c_use_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_use_action_script_offset) + m_c_use_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_use_action_script_offset);
   }

   int64_t Get_c_uasset_icon_img() const noexcept { return m_c_uasset_icon_img; }
   int64_t& Ref_c_uasset_icon_img() noexcept { return m_c_uasset_icon_img; }
   const int64_t& ConstRef_c_uasset_icon_img() const noexcept { return m_c_uasset_icon_img; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_ITEM_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_ITEM_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_ITEM_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_ITEM_ALL))
           {
               size += sizeof(STATIC_ITEM_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ItemSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(ItemType);
       }

       {
           thisSize += sizeof(ItemGrade);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           if (GetPacketSize() < m_c_use_condition_script_offset)
               return false;

           if (0 < m_c_use_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_use_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_use_action_script_offset)
               return false;

           if (0 < m_c_use_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_use_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(int64_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_ITEM_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_item_type: {}", tTabStr, t->Get_c_item_type());
       result += std::format(L"\n{}c_item_grade: {}", tTabStr, t->Get_c_item_grade());
       result += std::format(L"\n{}c_max_quantity: {}", tTabStr, t->Get_c_max_quantity());
       result += std::format(L"\n{}c_item_tag: {}", tTabStr, t->Get_c_item_tag());
       result += std::format(L"\n{}c_use_condition_script: {}", tTabStr, t->Get_c_use_condition_script());
       result += std::format(L"\n{}c_use_action_script: {}", tTabStr, t->Get_c_use_action_script());
       result += std::format(L"\n{}c_uasset_icon_img: {}", tTabStr, t->Get_c_uasset_icon_img());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_ITEM_STAT;

class STATIC_ITEM_STAT_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_ITEM_STAT;

public:
   // (7)
   explicit STATIC_ITEM_STAT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_ITEM_STAT, buf)
   {
   }

   // (9)
   explicit STATIC_ITEM_STAT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       ItemSid item_sid,
       const wchar_t* name,
       StatType stat_type,
       StatValue stat_value
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_ITEM_STAT, buf)
   {
       WriteValue(id);

       WriteValue(item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(stat_type);

       WriteValue(stat_value);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   // (8)
   explicit STATIC_ITEM_STAT_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       ItemSid item_sid,
       const wchar_t* name,
       StatType stat_type,
       StatValue stat_value
   )
   {
       WriteValue(id);

       WriteValue(item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(stat_type);

       WriteValue(stat_value);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   STATIC_ITEM_STAT& GetPacket() const noexcept { return *reinterpret_cast<STATIC_ITEM_STAT*>(GetPacketBufPtr()); }
   STATIC_ITEM_STAT* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_ITEM_STAT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_ITEM_STAT : public Packet
{
   public:
      using Writer = STATIC_ITEM_STAT_WRITER;


private:
   int64_t m_c_id;

   ItemSid m_c_item_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   StatType m_c_stat_type;

   StatValue m_c_stat_value;

public:
   STATIC_ITEM_STAT()
   {
       memset(this, 0, sizeof(STATIC_ITEM_STAT));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_ITEM_STAT));
   }
   STATIC_ITEM_STAT(const STATIC_ITEM_STAT&) = delete;
   STATIC_ITEM_STAT(STATIC_ITEM_STAT &&) = delete;
   STATIC_ITEM_STAT& operator = (const STATIC_ITEM_STAT&) = delete;
   STATIC_ITEM_STAT& operator = (STATIC_ITEM_STAT &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   StatType Get_c_stat_type() const noexcept { return m_c_stat_type; }
   StatType& Ref_c_stat_type() noexcept { return m_c_stat_type; }
   const StatType& ConstRef_c_stat_type() const noexcept { return m_c_stat_type; }

   StatValue Get_c_stat_value() const noexcept { return m_c_stat_value; }
   StatValue& Ref_c_stat_value() noexcept { return m_c_stat_value; }
   const StatValue& ConstRef_c_stat_value() const noexcept { return m_c_stat_value; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_ITEM_STAT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_ITEM_STAT) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_ITEM_STAT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_ITEM_STAT))
           {
               size += sizeof(STATIC_ITEM_STAT);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(StatType);
       }

       {
           thisSize += sizeof(StatValue);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_ITEM_STAT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_stat_type: {}", tTabStr, t->Get_c_stat_type());
       result += std::format(L"\n{}c_stat_value: {}", tTabStr, t->Get_c_stat_value());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_ITEM_STAT_ALL;

class STATIC_ITEM_STAT_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_ITEM_STAT_ALL;

public:
   // (7)
   explicit STATIC_ITEM_STAT_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_ITEM_STAT_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_ITEM_STAT_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       ItemSid item_sid,
       const wchar_t* name,
       StatType stat_type,
       StatValue stat_value
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_ITEM_STAT_ALL, buf)
   {
       WriteValue(id);

       WriteValue(item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(stat_type);

       WriteValue(stat_value);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   // (8)
   explicit STATIC_ITEM_STAT_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       ItemSid item_sid,
       const wchar_t* name,
       StatType stat_type,
       StatValue stat_value
   )
   {
       WriteValue(id);

       WriteValue(item_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(stat_type);

       WriteValue(stat_value);


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   STATIC_ITEM_STAT_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_ITEM_STAT_ALL*>(GetPacketBufPtr()); }
   STATIC_ITEM_STAT_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_ITEM_STAT_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_ITEM_STAT_ALL : public Packet
{
   public:
      using Writer = STATIC_ITEM_STAT_ALL_WRITER;


private:
   int64_t m_c_id;

   ItemSid m_c_item_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   StatType m_c_stat_type;

   StatValue m_c_stat_value;

public:
   STATIC_ITEM_STAT_ALL()
   {
       memset(this, 0, sizeof(STATIC_ITEM_STAT_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_ITEM_STAT_ALL));
   }
   STATIC_ITEM_STAT_ALL(const STATIC_ITEM_STAT_ALL&) = delete;
   STATIC_ITEM_STAT_ALL(STATIC_ITEM_STAT_ALL &&) = delete;
   STATIC_ITEM_STAT_ALL& operator = (const STATIC_ITEM_STAT_ALL&) = delete;
   STATIC_ITEM_STAT_ALL& operator = (STATIC_ITEM_STAT_ALL &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   StatType Get_c_stat_type() const noexcept { return m_c_stat_type; }
   StatType& Ref_c_stat_type() noexcept { return m_c_stat_type; }
   const StatType& ConstRef_c_stat_type() const noexcept { return m_c_stat_type; }

   StatValue Get_c_stat_value() const noexcept { return m_c_stat_value; }
   StatValue& Ref_c_stat_value() noexcept { return m_c_stat_value; }
   const StatValue& ConstRef_c_stat_value() const noexcept { return m_c_stat_value; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_ITEM_STAT_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_ITEM_STAT_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_ITEM_STAT_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_ITEM_STAT_ALL))
           {
               size += sizeof(STATIC_ITEM_STAT_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(StatType);
       }

       {
           thisSize += sizeof(StatValue);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_ITEM_STAT_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_stat_type: {}", tTabStr, t->Get_c_stat_type());
       result += std::format(L"\n{}c_stat_value: {}", tTabStr, t->Get_c_stat_value());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_MAIL;

class STATIC_MAIL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_MAIL;

public:
   // (7)
   explicit STATIC_MAIL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_MAIL, buf)
   {
   }

   // (9)
   explicit STATIC_MAIL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       MailSid mail_sid,
       MailType mail_type,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_MAIL, buf)
   {
       WriteValue(mail_sid);

       WriteValue(mail_type);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_MAIL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       MailSid mail_sid,
       MailType mail_type,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* action_script
   )
   {
       WriteValue(mail_sid);

       WriteValue(mail_type);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_MAIL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_MAIL*>(GetPacketBufPtr()); }
   STATIC_MAIL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_MAIL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_MAIL : public Packet
{
   public:
      using Writer = STATIC_MAIL_WRITER;


private:
   MailSid m_c_mail_sid;

   MailType m_c_mail_type;

   PacketSize m_c_title_offset = 0;
   PacketSize m_c_title_length = 0;

   PacketSize m_c_msg_offset = 0;
   PacketSize m_c_msg_length = 0;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_MAIL()
   {
       memset(this, 0, sizeof(STATIC_MAIL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_MAIL));
   }
   STATIC_MAIL(const STATIC_MAIL&) = delete;
   STATIC_MAIL(STATIC_MAIL &&) = delete;
   STATIC_MAIL& operator = (const STATIC_MAIL&) = delete;
   STATIC_MAIL& operator = (STATIC_MAIL &&) = delete;

   MailSid Get_c_mail_sid() const noexcept { return m_c_mail_sid; }
   MailSid& Ref_c_mail_sid() noexcept { return m_c_mail_sid; }
   const MailSid& ConstRef_c_mail_sid() const noexcept { return m_c_mail_sid; }

   MailType Get_c_mail_type() const noexcept { return m_c_mail_type; }
   MailType& Ref_c_mail_type() noexcept { return m_c_mail_type; }
   const MailType& ConstRef_c_mail_type() const noexcept { return m_c_mail_type; }

   const wchar_t* Get_c_title() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   wchar_t* Ref_c_title()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   const wchar_t* Get_c_msg() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   wchar_t* Ref_c_msg()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_MAIL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_MAIL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_MAIL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_MAIL))
           {
               size += sizeof(STATIC_MAIL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(MailSid);
       }

       {
           thisSize += sizeof(MailType);
       }

       {
           if (GetPacketSize() < m_c_title_offset)
               return false;

           if (0 < m_c_title_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_title_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_offset)
               return false;

           if (0 < m_c_msg_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_MAIL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_mail_sid: {}", tTabStr, t->Get_c_mail_sid());
       result += std::format(L"\n{}c_mail_type: {}", tTabStr, t->Get_c_mail_type());
       result += std::format(L"\n{}c_title: {}", tTabStr, t->Get_c_title());
       result += std::format(L"\n{}c_msg: {}", tTabStr, t->Get_c_msg());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_MAIL_ALL;

class STATIC_MAIL_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_MAIL_ALL;

public:
   // (7)
   explicit STATIC_MAIL_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_MAIL_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_MAIL_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       MailSid mail_sid,
       MailType mail_type,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_MAIL_ALL, buf)
   {
       WriteValue(mail_sid);

       WriteValue(mail_type);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_MAIL_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       MailSid mail_sid,
       MailType mail_type,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* action_script
   )
   {
       WriteValue(mail_sid);

       WriteValue(mail_type);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_MAIL_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_MAIL_ALL*>(GetPacketBufPtr()); }
   STATIC_MAIL_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_MAIL_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_MAIL_ALL : public Packet
{
   public:
      using Writer = STATIC_MAIL_ALL_WRITER;


private:
   MailSid m_c_mail_sid;

   MailType m_c_mail_type;

   PacketSize m_c_title_offset = 0;
   PacketSize m_c_title_length = 0;

   PacketSize m_c_msg_offset = 0;
   PacketSize m_c_msg_length = 0;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_MAIL_ALL()
   {
       memset(this, 0, sizeof(STATIC_MAIL_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_MAIL_ALL));
   }
   STATIC_MAIL_ALL(const STATIC_MAIL_ALL&) = delete;
   STATIC_MAIL_ALL(STATIC_MAIL_ALL &&) = delete;
   STATIC_MAIL_ALL& operator = (const STATIC_MAIL_ALL&) = delete;
   STATIC_MAIL_ALL& operator = (STATIC_MAIL_ALL &&) = delete;

   MailSid Get_c_mail_sid() const noexcept { return m_c_mail_sid; }
   MailSid& Ref_c_mail_sid() noexcept { return m_c_mail_sid; }
   const MailSid& ConstRef_c_mail_sid() const noexcept { return m_c_mail_sid; }

   MailType Get_c_mail_type() const noexcept { return m_c_mail_type; }
   MailType& Ref_c_mail_type() noexcept { return m_c_mail_type; }
   const MailType& ConstRef_c_mail_type() const noexcept { return m_c_mail_type; }

   const wchar_t* Get_c_title() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   wchar_t* Ref_c_title()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   const wchar_t* Get_c_msg() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   wchar_t* Ref_c_msg()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_MAIL_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_MAIL_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_MAIL_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_MAIL_ALL))
           {
               size += sizeof(STATIC_MAIL_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(MailSid);
       }

       {
           thisSize += sizeof(MailType);
       }

       {
           if (GetPacketSize() < m_c_title_offset)
               return false;

           if (0 < m_c_title_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_title_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_offset)
               return false;

           if (0 < m_c_msg_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_MAIL_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_mail_sid: {}", tTabStr, t->Get_c_mail_sid());
       result += std::format(L"\n{}c_mail_type: {}", tTabStr, t->Get_c_mail_type());
       result += std::format(L"\n{}c_title: {}", tTabStr, t->Get_c_title());
       result += std::format(L"\n{}c_msg: {}", tTabStr, t->Get_c_msg());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_MAP;

class STATIC_MAP_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_MAP;

public:
   // (7)
   explicit STATIC_MAP_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_MAP, buf)
   {
   }

   // (9)
   explicit STATIC_MAP_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       MapSid map_sid,
       const wchar_t* name,
       int64_t tile_theme,
       int64_t grid_col_count_factor,
       int64_t grid_row_count_factor,
       int64_t level_division0,
       int64_t level_division1,
       int64_t level_division2,
       int64_t level_division3,
       int64_t level_division4,
       int64_t level_division5,
       int64_t level_division6,
       int64_t fractal_scale_x0,
       int64_t fractal_scale_x1,
       int64_t fractal_scale_x2,
       int64_t fractal_scale_x3,
       int64_t fractal_scale_y0,
       int64_t fractal_scale_y1,
       int64_t fractal_scale_y2,
       int64_t fractal_scale_y3,
       int64_t fractal_scale_w0,
       int64_t fractal_scale_w1,
       int64_t fractal_scale_w2,
       int64_t fractal_scale_w3,
       int64_t random_seed,
       const wchar_t* initial_system_group_name,
       const wchar_t* initial_system_group_fsm_state_name
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_MAP, buf)
   {
       WriteValue(map_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(tile_theme);

       WriteValue(grid_col_count_factor);

       WriteValue(grid_row_count_factor);

       WriteValue(level_division0);

       WriteValue(level_division1);

       WriteValue(level_division2);

       WriteValue(level_division3);

       WriteValue(level_division4);

       WriteValue(level_division5);

       WriteValue(level_division6);

       WriteValue(fractal_scale_x0);

       WriteValue(fractal_scale_x1);

       WriteValue(fractal_scale_x2);

       WriteValue(fractal_scale_x3);

       WriteValue(fractal_scale_y0);

       WriteValue(fractal_scale_y1);

       WriteValue(fractal_scale_y2);

       WriteValue(fractal_scale_y3);

       WriteValue(fractal_scale_w0);

       WriteValue(fractal_scale_w1);

       WriteValue(fractal_scale_w2);

       WriteValue(fractal_scale_w3);

       WriteValue(random_seed);

       PacketSize& initial_system_group_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& initial_system_group_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& initial_system_group_fsm_state_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& initial_system_group_fsm_state_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       initial_system_group_nameOffset = GetPacketSize();
       initial_system_group_nameLength = (PacketSize)wcsnlen_s(initial_system_group_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == initial_system_group_nameLength) WriteString(L"", 0);
       else WriteString(initial_system_group_name, initial_system_group_nameLength); 

       initial_system_group_fsm_state_nameOffset = GetPacketSize();
       initial_system_group_fsm_state_nameLength = (PacketSize)wcsnlen_s(initial_system_group_fsm_state_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == initial_system_group_fsm_state_nameLength) WriteString(L"", 0);
       else WriteString(initial_system_group_fsm_state_name, initial_system_group_fsm_state_nameLength); 

   }

   // (8)
   explicit STATIC_MAP_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       MapSid map_sid,
       const wchar_t* name,
       int64_t tile_theme,
       int64_t grid_col_count_factor,
       int64_t grid_row_count_factor,
       int64_t level_division0,
       int64_t level_division1,
       int64_t level_division2,
       int64_t level_division3,
       int64_t level_division4,
       int64_t level_division5,
       int64_t level_division6,
       int64_t fractal_scale_x0,
       int64_t fractal_scale_x1,
       int64_t fractal_scale_x2,
       int64_t fractal_scale_x3,
       int64_t fractal_scale_y0,
       int64_t fractal_scale_y1,
       int64_t fractal_scale_y2,
       int64_t fractal_scale_y3,
       int64_t fractal_scale_w0,
       int64_t fractal_scale_w1,
       int64_t fractal_scale_w2,
       int64_t fractal_scale_w3,
       int64_t random_seed,
       const wchar_t* initial_system_group_name,
       const wchar_t* initial_system_group_fsm_state_name
   )
   {
       WriteValue(map_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(tile_theme);

       WriteValue(grid_col_count_factor);

       WriteValue(grid_row_count_factor);

       WriteValue(level_division0);

       WriteValue(level_division1);

       WriteValue(level_division2);

       WriteValue(level_division3);

       WriteValue(level_division4);

       WriteValue(level_division5);

       WriteValue(level_division6);

       WriteValue(fractal_scale_x0);

       WriteValue(fractal_scale_x1);

       WriteValue(fractal_scale_x2);

       WriteValue(fractal_scale_x3);

       WriteValue(fractal_scale_y0);

       WriteValue(fractal_scale_y1);

       WriteValue(fractal_scale_y2);

       WriteValue(fractal_scale_y3);

       WriteValue(fractal_scale_w0);

       WriteValue(fractal_scale_w1);

       WriteValue(fractal_scale_w2);

       WriteValue(fractal_scale_w3);

       WriteValue(random_seed);

       PacketSize& initial_system_group_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& initial_system_group_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& initial_system_group_fsm_state_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& initial_system_group_fsm_state_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       initial_system_group_nameOffset = GetPacketSize();
       initial_system_group_nameLength = (PacketSize)wcsnlen_s(initial_system_group_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == initial_system_group_nameLength) WriteString(L"", 0);
       else WriteString(initial_system_group_name, initial_system_group_nameLength); 

       initial_system_group_fsm_state_nameOffset = GetPacketSize();
       initial_system_group_fsm_state_nameLength = (PacketSize)wcsnlen_s(initial_system_group_fsm_state_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == initial_system_group_fsm_state_nameLength) WriteString(L"", 0);
       else WriteString(initial_system_group_fsm_state_name, initial_system_group_fsm_state_nameLength); 

   }

   STATIC_MAP& GetPacket() const noexcept { return *reinterpret_cast<STATIC_MAP*>(GetPacketBufPtr()); }
   STATIC_MAP* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_MAP*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_MAP : public Packet
{
   public:
      using Writer = STATIC_MAP_WRITER;


private:
   MapSid m_c_map_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   int64_t m_c_tile_theme;

   int64_t m_c_grid_col_count_factor;

   int64_t m_c_grid_row_count_factor;

   int64_t m_c_level_division0;

   int64_t m_c_level_division1;

   int64_t m_c_level_division2;

   int64_t m_c_level_division3;

   int64_t m_c_level_division4;

   int64_t m_c_level_division5;

   int64_t m_c_level_division6;

   int64_t m_c_fractal_scale_x0;

   int64_t m_c_fractal_scale_x1;

   int64_t m_c_fractal_scale_x2;

   int64_t m_c_fractal_scale_x3;

   int64_t m_c_fractal_scale_y0;

   int64_t m_c_fractal_scale_y1;

   int64_t m_c_fractal_scale_y2;

   int64_t m_c_fractal_scale_y3;

   int64_t m_c_fractal_scale_w0;

   int64_t m_c_fractal_scale_w1;

   int64_t m_c_fractal_scale_w2;

   int64_t m_c_fractal_scale_w3;

   int64_t m_c_random_seed;

   PacketSize m_c_initial_system_group_name_offset = 0;
   PacketSize m_c_initial_system_group_name_length = 0;

   PacketSize m_c_initial_system_group_fsm_state_name_offset = 0;
   PacketSize m_c_initial_system_group_fsm_state_name_length = 0;

public:
   STATIC_MAP()
   {
       memset(this, 0, sizeof(STATIC_MAP));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_MAP));
   }
   STATIC_MAP(const STATIC_MAP&) = delete;
   STATIC_MAP(STATIC_MAP &&) = delete;
   STATIC_MAP& operator = (const STATIC_MAP&) = delete;
   STATIC_MAP& operator = (STATIC_MAP &&) = delete;

   MapSid Get_c_map_sid() const noexcept { return m_c_map_sid; }
   MapSid& Ref_c_map_sid() noexcept { return m_c_map_sid; }
   const MapSid& ConstRef_c_map_sid() const noexcept { return m_c_map_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   int64_t Get_c_tile_theme() const noexcept { return m_c_tile_theme; }
   int64_t& Ref_c_tile_theme() noexcept { return m_c_tile_theme; }
   const int64_t& ConstRef_c_tile_theme() const noexcept { return m_c_tile_theme; }

   int64_t Get_c_grid_col_count_factor() const noexcept { return m_c_grid_col_count_factor; }
   int64_t& Ref_c_grid_col_count_factor() noexcept { return m_c_grid_col_count_factor; }
   const int64_t& ConstRef_c_grid_col_count_factor() const noexcept { return m_c_grid_col_count_factor; }

   int64_t Get_c_grid_row_count_factor() const noexcept { return m_c_grid_row_count_factor; }
   int64_t& Ref_c_grid_row_count_factor() noexcept { return m_c_grid_row_count_factor; }
   const int64_t& ConstRef_c_grid_row_count_factor() const noexcept { return m_c_grid_row_count_factor; }

   int64_t Get_c_level_division0() const noexcept { return m_c_level_division0; }
   int64_t& Ref_c_level_division0() noexcept { return m_c_level_division0; }
   const int64_t& ConstRef_c_level_division0() const noexcept { return m_c_level_division0; }

   int64_t Get_c_level_division1() const noexcept { return m_c_level_division1; }
   int64_t& Ref_c_level_division1() noexcept { return m_c_level_division1; }
   const int64_t& ConstRef_c_level_division1() const noexcept { return m_c_level_division1; }

   int64_t Get_c_level_division2() const noexcept { return m_c_level_division2; }
   int64_t& Ref_c_level_division2() noexcept { return m_c_level_division2; }
   const int64_t& ConstRef_c_level_division2() const noexcept { return m_c_level_division2; }

   int64_t Get_c_level_division3() const noexcept { return m_c_level_division3; }
   int64_t& Ref_c_level_division3() noexcept { return m_c_level_division3; }
   const int64_t& ConstRef_c_level_division3() const noexcept { return m_c_level_division3; }

   int64_t Get_c_level_division4() const noexcept { return m_c_level_division4; }
   int64_t& Ref_c_level_division4() noexcept { return m_c_level_division4; }
   const int64_t& ConstRef_c_level_division4() const noexcept { return m_c_level_division4; }

   int64_t Get_c_level_division5() const noexcept { return m_c_level_division5; }
   int64_t& Ref_c_level_division5() noexcept { return m_c_level_division5; }
   const int64_t& ConstRef_c_level_division5() const noexcept { return m_c_level_division5; }

   int64_t Get_c_level_division6() const noexcept { return m_c_level_division6; }
   int64_t& Ref_c_level_division6() noexcept { return m_c_level_division6; }
   const int64_t& ConstRef_c_level_division6() const noexcept { return m_c_level_division6; }

   int64_t Get_c_fractal_scale_x0() const noexcept { return m_c_fractal_scale_x0; }
   int64_t& Ref_c_fractal_scale_x0() noexcept { return m_c_fractal_scale_x0; }
   const int64_t& ConstRef_c_fractal_scale_x0() const noexcept { return m_c_fractal_scale_x0; }

   int64_t Get_c_fractal_scale_x1() const noexcept { return m_c_fractal_scale_x1; }
   int64_t& Ref_c_fractal_scale_x1() noexcept { return m_c_fractal_scale_x1; }
   const int64_t& ConstRef_c_fractal_scale_x1() const noexcept { return m_c_fractal_scale_x1; }

   int64_t Get_c_fractal_scale_x2() const noexcept { return m_c_fractal_scale_x2; }
   int64_t& Ref_c_fractal_scale_x2() noexcept { return m_c_fractal_scale_x2; }
   const int64_t& ConstRef_c_fractal_scale_x2() const noexcept { return m_c_fractal_scale_x2; }

   int64_t Get_c_fractal_scale_x3() const noexcept { return m_c_fractal_scale_x3; }
   int64_t& Ref_c_fractal_scale_x3() noexcept { return m_c_fractal_scale_x3; }
   const int64_t& ConstRef_c_fractal_scale_x3() const noexcept { return m_c_fractal_scale_x3; }

   int64_t Get_c_fractal_scale_y0() const noexcept { return m_c_fractal_scale_y0; }
   int64_t& Ref_c_fractal_scale_y0() noexcept { return m_c_fractal_scale_y0; }
   const int64_t& ConstRef_c_fractal_scale_y0() const noexcept { return m_c_fractal_scale_y0; }

   int64_t Get_c_fractal_scale_y1() const noexcept { return m_c_fractal_scale_y1; }
   int64_t& Ref_c_fractal_scale_y1() noexcept { return m_c_fractal_scale_y1; }
   const int64_t& ConstRef_c_fractal_scale_y1() const noexcept { return m_c_fractal_scale_y1; }

   int64_t Get_c_fractal_scale_y2() const noexcept { return m_c_fractal_scale_y2; }
   int64_t& Ref_c_fractal_scale_y2() noexcept { return m_c_fractal_scale_y2; }
   const int64_t& ConstRef_c_fractal_scale_y2() const noexcept { return m_c_fractal_scale_y2; }

   int64_t Get_c_fractal_scale_y3() const noexcept { return m_c_fractal_scale_y3; }
   int64_t& Ref_c_fractal_scale_y3() noexcept { return m_c_fractal_scale_y3; }
   const int64_t& ConstRef_c_fractal_scale_y3() const noexcept { return m_c_fractal_scale_y3; }

   int64_t Get_c_fractal_scale_w0() const noexcept { return m_c_fractal_scale_w0; }
   int64_t& Ref_c_fractal_scale_w0() noexcept { return m_c_fractal_scale_w0; }
   const int64_t& ConstRef_c_fractal_scale_w0() const noexcept { return m_c_fractal_scale_w0; }

   int64_t Get_c_fractal_scale_w1() const noexcept { return m_c_fractal_scale_w1; }
   int64_t& Ref_c_fractal_scale_w1() noexcept { return m_c_fractal_scale_w1; }
   const int64_t& ConstRef_c_fractal_scale_w1() const noexcept { return m_c_fractal_scale_w1; }

   int64_t Get_c_fractal_scale_w2() const noexcept { return m_c_fractal_scale_w2; }
   int64_t& Ref_c_fractal_scale_w2() noexcept { return m_c_fractal_scale_w2; }
   const int64_t& ConstRef_c_fractal_scale_w2() const noexcept { return m_c_fractal_scale_w2; }

   int64_t Get_c_fractal_scale_w3() const noexcept { return m_c_fractal_scale_w3; }
   int64_t& Ref_c_fractal_scale_w3() noexcept { return m_c_fractal_scale_w3; }
   const int64_t& ConstRef_c_fractal_scale_w3() const noexcept { return m_c_fractal_scale_w3; }

   int64_t Get_c_random_seed() const noexcept { return m_c_random_seed; }
   int64_t& Ref_c_random_seed() noexcept { return m_c_random_seed; }
   const int64_t& ConstRef_c_random_seed() const noexcept { return m_c_random_seed; }

   const wchar_t* Get_c_initial_system_group_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_initial_system_group_name_offset) + m_c_initial_system_group_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_initial_system_group_name_offset);
   }

   wchar_t* Ref_c_initial_system_group_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_initial_system_group_name_offset) + m_c_initial_system_group_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_initial_system_group_name_offset);
   }

   const wchar_t* Get_c_initial_system_group_fsm_state_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_initial_system_group_fsm_state_name_offset) + m_c_initial_system_group_fsm_state_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_initial_system_group_fsm_state_name_offset);
   }

   wchar_t* Ref_c_initial_system_group_fsm_state_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_initial_system_group_fsm_state_name_offset) + m_c_initial_system_group_fsm_state_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_initial_system_group_fsm_state_name_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_MAP) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_MAP) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_MAP); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_MAP))
           {
               size += sizeof(STATIC_MAP);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(MapSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           if (GetPacketSize() < m_c_initial_system_group_name_offset)
               return false;

           if (0 < m_c_initial_system_group_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_initial_system_group_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_initial_system_group_fsm_state_name_offset)
               return false;

           if (0 < m_c_initial_system_group_fsm_state_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_initial_system_group_fsm_state_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_MAP>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_map_sid: {}", tTabStr, t->Get_c_map_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_tile_theme: {}", tTabStr, t->Get_c_tile_theme());
       result += std::format(L"\n{}c_grid_col_count_factor: {}", tTabStr, t->Get_c_grid_col_count_factor());
       result += std::format(L"\n{}c_grid_row_count_factor: {}", tTabStr, t->Get_c_grid_row_count_factor());
       result += std::format(L"\n{}c_level_division0: {}", tTabStr, t->Get_c_level_division0());
       result += std::format(L"\n{}c_level_division1: {}", tTabStr, t->Get_c_level_division1());
       result += std::format(L"\n{}c_level_division2: {}", tTabStr, t->Get_c_level_division2());
       result += std::format(L"\n{}c_level_division3: {}", tTabStr, t->Get_c_level_division3());
       result += std::format(L"\n{}c_level_division4: {}", tTabStr, t->Get_c_level_division4());
       result += std::format(L"\n{}c_level_division5: {}", tTabStr, t->Get_c_level_division5());
       result += std::format(L"\n{}c_level_division6: {}", tTabStr, t->Get_c_level_division6());
       result += std::format(L"\n{}c_fractal_scale_x0: {}", tTabStr, t->Get_c_fractal_scale_x0());
       result += std::format(L"\n{}c_fractal_scale_x1: {}", tTabStr, t->Get_c_fractal_scale_x1());
       result += std::format(L"\n{}c_fractal_scale_x2: {}", tTabStr, t->Get_c_fractal_scale_x2());
       result += std::format(L"\n{}c_fractal_scale_x3: {}", tTabStr, t->Get_c_fractal_scale_x3());
       result += std::format(L"\n{}c_fractal_scale_y0: {}", tTabStr, t->Get_c_fractal_scale_y0());
       result += std::format(L"\n{}c_fractal_scale_y1: {}", tTabStr, t->Get_c_fractal_scale_y1());
       result += std::format(L"\n{}c_fractal_scale_y2: {}", tTabStr, t->Get_c_fractal_scale_y2());
       result += std::format(L"\n{}c_fractal_scale_y3: {}", tTabStr, t->Get_c_fractal_scale_y3());
       result += std::format(L"\n{}c_fractal_scale_w0: {}", tTabStr, t->Get_c_fractal_scale_w0());
       result += std::format(L"\n{}c_fractal_scale_w1: {}", tTabStr, t->Get_c_fractal_scale_w1());
       result += std::format(L"\n{}c_fractal_scale_w2: {}", tTabStr, t->Get_c_fractal_scale_w2());
       result += std::format(L"\n{}c_fractal_scale_w3: {}", tTabStr, t->Get_c_fractal_scale_w3());
       result += std::format(L"\n{}c_random_seed: {}", tTabStr, t->Get_c_random_seed());
       result += std::format(L"\n{}c_initial_system_group_name: {}", tTabStr, t->Get_c_initial_system_group_name());
       result += std::format(L"\n{}c_initial_system_group_fsm_state_name: {}", tTabStr, t->Get_c_initial_system_group_fsm_state_name());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_MAP_ALL;

class STATIC_MAP_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_MAP_ALL;

public:
   // (7)
   explicit STATIC_MAP_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_MAP_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_MAP_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       MapSid map_sid,
       const wchar_t* name,
       int64_t tile_theme,
       int64_t grid_col_count_factor,
       int64_t grid_row_count_factor,
       int64_t level_division0,
       int64_t level_division1,
       int64_t level_division2,
       int64_t level_division3,
       int64_t level_division4,
       int64_t level_division5,
       int64_t level_division6,
       int64_t fractal_scale_x0,
       int64_t fractal_scale_x1,
       int64_t fractal_scale_x2,
       int64_t fractal_scale_x3,
       int64_t fractal_scale_y0,
       int64_t fractal_scale_y1,
       int64_t fractal_scale_y2,
       int64_t fractal_scale_y3,
       int64_t fractal_scale_w0,
       int64_t fractal_scale_w1,
       int64_t fractal_scale_w2,
       int64_t fractal_scale_w3,
       int64_t random_seed,
       const wchar_t* initial_system_group_name,
       const wchar_t* initial_system_group_fsm_state_name
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_MAP_ALL, buf)
   {
       WriteValue(map_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(tile_theme);

       WriteValue(grid_col_count_factor);

       WriteValue(grid_row_count_factor);

       WriteValue(level_division0);

       WriteValue(level_division1);

       WriteValue(level_division2);

       WriteValue(level_division3);

       WriteValue(level_division4);

       WriteValue(level_division5);

       WriteValue(level_division6);

       WriteValue(fractal_scale_x0);

       WriteValue(fractal_scale_x1);

       WriteValue(fractal_scale_x2);

       WriteValue(fractal_scale_x3);

       WriteValue(fractal_scale_y0);

       WriteValue(fractal_scale_y1);

       WriteValue(fractal_scale_y2);

       WriteValue(fractal_scale_y3);

       WriteValue(fractal_scale_w0);

       WriteValue(fractal_scale_w1);

       WriteValue(fractal_scale_w2);

       WriteValue(fractal_scale_w3);

       WriteValue(random_seed);

       PacketSize& initial_system_group_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& initial_system_group_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& initial_system_group_fsm_state_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& initial_system_group_fsm_state_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       initial_system_group_nameOffset = GetPacketSize();
       initial_system_group_nameLength = (PacketSize)wcsnlen_s(initial_system_group_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == initial_system_group_nameLength) WriteString(L"", 0);
       else WriteString(initial_system_group_name, initial_system_group_nameLength); 

       initial_system_group_fsm_state_nameOffset = GetPacketSize();
       initial_system_group_fsm_state_nameLength = (PacketSize)wcsnlen_s(initial_system_group_fsm_state_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == initial_system_group_fsm_state_nameLength) WriteString(L"", 0);
       else WriteString(initial_system_group_fsm_state_name, initial_system_group_fsm_state_nameLength); 

   }

   // (8)
   explicit STATIC_MAP_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       MapSid map_sid,
       const wchar_t* name,
       int64_t tile_theme,
       int64_t grid_col_count_factor,
       int64_t grid_row_count_factor,
       int64_t level_division0,
       int64_t level_division1,
       int64_t level_division2,
       int64_t level_division3,
       int64_t level_division4,
       int64_t level_division5,
       int64_t level_division6,
       int64_t fractal_scale_x0,
       int64_t fractal_scale_x1,
       int64_t fractal_scale_x2,
       int64_t fractal_scale_x3,
       int64_t fractal_scale_y0,
       int64_t fractal_scale_y1,
       int64_t fractal_scale_y2,
       int64_t fractal_scale_y3,
       int64_t fractal_scale_w0,
       int64_t fractal_scale_w1,
       int64_t fractal_scale_w2,
       int64_t fractal_scale_w3,
       int64_t random_seed,
       const wchar_t* initial_system_group_name,
       const wchar_t* initial_system_group_fsm_state_name
   )
   {
       WriteValue(map_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(tile_theme);

       WriteValue(grid_col_count_factor);

       WriteValue(grid_row_count_factor);

       WriteValue(level_division0);

       WriteValue(level_division1);

       WriteValue(level_division2);

       WriteValue(level_division3);

       WriteValue(level_division4);

       WriteValue(level_division5);

       WriteValue(level_division6);

       WriteValue(fractal_scale_x0);

       WriteValue(fractal_scale_x1);

       WriteValue(fractal_scale_x2);

       WriteValue(fractal_scale_x3);

       WriteValue(fractal_scale_y0);

       WriteValue(fractal_scale_y1);

       WriteValue(fractal_scale_y2);

       WriteValue(fractal_scale_y3);

       WriteValue(fractal_scale_w0);

       WriteValue(fractal_scale_w1);

       WriteValue(fractal_scale_w2);

       WriteValue(fractal_scale_w3);

       WriteValue(random_seed);

       PacketSize& initial_system_group_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& initial_system_group_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& initial_system_group_fsm_state_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& initial_system_group_fsm_state_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       initial_system_group_nameOffset = GetPacketSize();
       initial_system_group_nameLength = (PacketSize)wcsnlen_s(initial_system_group_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == initial_system_group_nameLength) WriteString(L"", 0);
       else WriteString(initial_system_group_name, initial_system_group_nameLength); 

       initial_system_group_fsm_state_nameOffset = GetPacketSize();
       initial_system_group_fsm_state_nameLength = (PacketSize)wcsnlen_s(initial_system_group_fsm_state_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == initial_system_group_fsm_state_nameLength) WriteString(L"", 0);
       else WriteString(initial_system_group_fsm_state_name, initial_system_group_fsm_state_nameLength); 

   }

   STATIC_MAP_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_MAP_ALL*>(GetPacketBufPtr()); }
   STATIC_MAP_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_MAP_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_MAP_ALL : public Packet
{
   public:
      using Writer = STATIC_MAP_ALL_WRITER;


private:
   MapSid m_c_map_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   int64_t m_c_tile_theme;

   int64_t m_c_grid_col_count_factor;

   int64_t m_c_grid_row_count_factor;

   int64_t m_c_level_division0;

   int64_t m_c_level_division1;

   int64_t m_c_level_division2;

   int64_t m_c_level_division3;

   int64_t m_c_level_division4;

   int64_t m_c_level_division5;

   int64_t m_c_level_division6;

   int64_t m_c_fractal_scale_x0;

   int64_t m_c_fractal_scale_x1;

   int64_t m_c_fractal_scale_x2;

   int64_t m_c_fractal_scale_x3;

   int64_t m_c_fractal_scale_y0;

   int64_t m_c_fractal_scale_y1;

   int64_t m_c_fractal_scale_y2;

   int64_t m_c_fractal_scale_y3;

   int64_t m_c_fractal_scale_w0;

   int64_t m_c_fractal_scale_w1;

   int64_t m_c_fractal_scale_w2;

   int64_t m_c_fractal_scale_w3;

   int64_t m_c_random_seed;

   PacketSize m_c_initial_system_group_name_offset = 0;
   PacketSize m_c_initial_system_group_name_length = 0;

   PacketSize m_c_initial_system_group_fsm_state_name_offset = 0;
   PacketSize m_c_initial_system_group_fsm_state_name_length = 0;

public:
   STATIC_MAP_ALL()
   {
       memset(this, 0, sizeof(STATIC_MAP_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_MAP_ALL));
   }
   STATIC_MAP_ALL(const STATIC_MAP_ALL&) = delete;
   STATIC_MAP_ALL(STATIC_MAP_ALL &&) = delete;
   STATIC_MAP_ALL& operator = (const STATIC_MAP_ALL&) = delete;
   STATIC_MAP_ALL& operator = (STATIC_MAP_ALL &&) = delete;

   MapSid Get_c_map_sid() const noexcept { return m_c_map_sid; }
   MapSid& Ref_c_map_sid() noexcept { return m_c_map_sid; }
   const MapSid& ConstRef_c_map_sid() const noexcept { return m_c_map_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   int64_t Get_c_tile_theme() const noexcept { return m_c_tile_theme; }
   int64_t& Ref_c_tile_theme() noexcept { return m_c_tile_theme; }
   const int64_t& ConstRef_c_tile_theme() const noexcept { return m_c_tile_theme; }

   int64_t Get_c_grid_col_count_factor() const noexcept { return m_c_grid_col_count_factor; }
   int64_t& Ref_c_grid_col_count_factor() noexcept { return m_c_grid_col_count_factor; }
   const int64_t& ConstRef_c_grid_col_count_factor() const noexcept { return m_c_grid_col_count_factor; }

   int64_t Get_c_grid_row_count_factor() const noexcept { return m_c_grid_row_count_factor; }
   int64_t& Ref_c_grid_row_count_factor() noexcept { return m_c_grid_row_count_factor; }
   const int64_t& ConstRef_c_grid_row_count_factor() const noexcept { return m_c_grid_row_count_factor; }

   int64_t Get_c_level_division0() const noexcept { return m_c_level_division0; }
   int64_t& Ref_c_level_division0() noexcept { return m_c_level_division0; }
   const int64_t& ConstRef_c_level_division0() const noexcept { return m_c_level_division0; }

   int64_t Get_c_level_division1() const noexcept { return m_c_level_division1; }
   int64_t& Ref_c_level_division1() noexcept { return m_c_level_division1; }
   const int64_t& ConstRef_c_level_division1() const noexcept { return m_c_level_division1; }

   int64_t Get_c_level_division2() const noexcept { return m_c_level_division2; }
   int64_t& Ref_c_level_division2() noexcept { return m_c_level_division2; }
   const int64_t& ConstRef_c_level_division2() const noexcept { return m_c_level_division2; }

   int64_t Get_c_level_division3() const noexcept { return m_c_level_division3; }
   int64_t& Ref_c_level_division3() noexcept { return m_c_level_division3; }
   const int64_t& ConstRef_c_level_division3() const noexcept { return m_c_level_division3; }

   int64_t Get_c_level_division4() const noexcept { return m_c_level_division4; }
   int64_t& Ref_c_level_division4() noexcept { return m_c_level_division4; }
   const int64_t& ConstRef_c_level_division4() const noexcept { return m_c_level_division4; }

   int64_t Get_c_level_division5() const noexcept { return m_c_level_division5; }
   int64_t& Ref_c_level_division5() noexcept { return m_c_level_division5; }
   const int64_t& ConstRef_c_level_division5() const noexcept { return m_c_level_division5; }

   int64_t Get_c_level_division6() const noexcept { return m_c_level_division6; }
   int64_t& Ref_c_level_division6() noexcept { return m_c_level_division6; }
   const int64_t& ConstRef_c_level_division6() const noexcept { return m_c_level_division6; }

   int64_t Get_c_fractal_scale_x0() const noexcept { return m_c_fractal_scale_x0; }
   int64_t& Ref_c_fractal_scale_x0() noexcept { return m_c_fractal_scale_x0; }
   const int64_t& ConstRef_c_fractal_scale_x0() const noexcept { return m_c_fractal_scale_x0; }

   int64_t Get_c_fractal_scale_x1() const noexcept { return m_c_fractal_scale_x1; }
   int64_t& Ref_c_fractal_scale_x1() noexcept { return m_c_fractal_scale_x1; }
   const int64_t& ConstRef_c_fractal_scale_x1() const noexcept { return m_c_fractal_scale_x1; }

   int64_t Get_c_fractal_scale_x2() const noexcept { return m_c_fractal_scale_x2; }
   int64_t& Ref_c_fractal_scale_x2() noexcept { return m_c_fractal_scale_x2; }
   const int64_t& ConstRef_c_fractal_scale_x2() const noexcept { return m_c_fractal_scale_x2; }

   int64_t Get_c_fractal_scale_x3() const noexcept { return m_c_fractal_scale_x3; }
   int64_t& Ref_c_fractal_scale_x3() noexcept { return m_c_fractal_scale_x3; }
   const int64_t& ConstRef_c_fractal_scale_x3() const noexcept { return m_c_fractal_scale_x3; }

   int64_t Get_c_fractal_scale_y0() const noexcept { return m_c_fractal_scale_y0; }
   int64_t& Ref_c_fractal_scale_y0() noexcept { return m_c_fractal_scale_y0; }
   const int64_t& ConstRef_c_fractal_scale_y0() const noexcept { return m_c_fractal_scale_y0; }

   int64_t Get_c_fractal_scale_y1() const noexcept { return m_c_fractal_scale_y1; }
   int64_t& Ref_c_fractal_scale_y1() noexcept { return m_c_fractal_scale_y1; }
   const int64_t& ConstRef_c_fractal_scale_y1() const noexcept { return m_c_fractal_scale_y1; }

   int64_t Get_c_fractal_scale_y2() const noexcept { return m_c_fractal_scale_y2; }
   int64_t& Ref_c_fractal_scale_y2() noexcept { return m_c_fractal_scale_y2; }
   const int64_t& ConstRef_c_fractal_scale_y2() const noexcept { return m_c_fractal_scale_y2; }

   int64_t Get_c_fractal_scale_y3() const noexcept { return m_c_fractal_scale_y3; }
   int64_t& Ref_c_fractal_scale_y3() noexcept { return m_c_fractal_scale_y3; }
   const int64_t& ConstRef_c_fractal_scale_y3() const noexcept { return m_c_fractal_scale_y3; }

   int64_t Get_c_fractal_scale_w0() const noexcept { return m_c_fractal_scale_w0; }
   int64_t& Ref_c_fractal_scale_w0() noexcept { return m_c_fractal_scale_w0; }
   const int64_t& ConstRef_c_fractal_scale_w0() const noexcept { return m_c_fractal_scale_w0; }

   int64_t Get_c_fractal_scale_w1() const noexcept { return m_c_fractal_scale_w1; }
   int64_t& Ref_c_fractal_scale_w1() noexcept { return m_c_fractal_scale_w1; }
   const int64_t& ConstRef_c_fractal_scale_w1() const noexcept { return m_c_fractal_scale_w1; }

   int64_t Get_c_fractal_scale_w2() const noexcept { return m_c_fractal_scale_w2; }
   int64_t& Ref_c_fractal_scale_w2() noexcept { return m_c_fractal_scale_w2; }
   const int64_t& ConstRef_c_fractal_scale_w2() const noexcept { return m_c_fractal_scale_w2; }

   int64_t Get_c_fractal_scale_w3() const noexcept { return m_c_fractal_scale_w3; }
   int64_t& Ref_c_fractal_scale_w3() noexcept { return m_c_fractal_scale_w3; }
   const int64_t& ConstRef_c_fractal_scale_w3() const noexcept { return m_c_fractal_scale_w3; }

   int64_t Get_c_random_seed() const noexcept { return m_c_random_seed; }
   int64_t& Ref_c_random_seed() noexcept { return m_c_random_seed; }
   const int64_t& ConstRef_c_random_seed() const noexcept { return m_c_random_seed; }

   const wchar_t* Get_c_initial_system_group_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_initial_system_group_name_offset) + m_c_initial_system_group_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_initial_system_group_name_offset);
   }

   wchar_t* Ref_c_initial_system_group_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_initial_system_group_name_offset) + m_c_initial_system_group_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_initial_system_group_name_offset);
   }

   const wchar_t* Get_c_initial_system_group_fsm_state_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_initial_system_group_fsm_state_name_offset) + m_c_initial_system_group_fsm_state_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_initial_system_group_fsm_state_name_offset);
   }

   wchar_t* Ref_c_initial_system_group_fsm_state_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_initial_system_group_fsm_state_name_offset) + m_c_initial_system_group_fsm_state_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_initial_system_group_fsm_state_name_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_MAP_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_MAP_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_MAP_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_MAP_ALL))
           {
               size += sizeof(STATIC_MAP_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(MapSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           if (GetPacketSize() < m_c_initial_system_group_name_offset)
               return false;

           if (0 < m_c_initial_system_group_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_initial_system_group_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_initial_system_group_fsm_state_name_offset)
               return false;

           if (0 < m_c_initial_system_group_fsm_state_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_initial_system_group_fsm_state_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_MAP_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_map_sid: {}", tTabStr, t->Get_c_map_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_tile_theme: {}", tTabStr, t->Get_c_tile_theme());
       result += std::format(L"\n{}c_grid_col_count_factor: {}", tTabStr, t->Get_c_grid_col_count_factor());
       result += std::format(L"\n{}c_grid_row_count_factor: {}", tTabStr, t->Get_c_grid_row_count_factor());
       result += std::format(L"\n{}c_level_division0: {}", tTabStr, t->Get_c_level_division0());
       result += std::format(L"\n{}c_level_division1: {}", tTabStr, t->Get_c_level_division1());
       result += std::format(L"\n{}c_level_division2: {}", tTabStr, t->Get_c_level_division2());
       result += std::format(L"\n{}c_level_division3: {}", tTabStr, t->Get_c_level_division3());
       result += std::format(L"\n{}c_level_division4: {}", tTabStr, t->Get_c_level_division4());
       result += std::format(L"\n{}c_level_division5: {}", tTabStr, t->Get_c_level_division5());
       result += std::format(L"\n{}c_level_division6: {}", tTabStr, t->Get_c_level_division6());
       result += std::format(L"\n{}c_fractal_scale_x0: {}", tTabStr, t->Get_c_fractal_scale_x0());
       result += std::format(L"\n{}c_fractal_scale_x1: {}", tTabStr, t->Get_c_fractal_scale_x1());
       result += std::format(L"\n{}c_fractal_scale_x2: {}", tTabStr, t->Get_c_fractal_scale_x2());
       result += std::format(L"\n{}c_fractal_scale_x3: {}", tTabStr, t->Get_c_fractal_scale_x3());
       result += std::format(L"\n{}c_fractal_scale_y0: {}", tTabStr, t->Get_c_fractal_scale_y0());
       result += std::format(L"\n{}c_fractal_scale_y1: {}", tTabStr, t->Get_c_fractal_scale_y1());
       result += std::format(L"\n{}c_fractal_scale_y2: {}", tTabStr, t->Get_c_fractal_scale_y2());
       result += std::format(L"\n{}c_fractal_scale_y3: {}", tTabStr, t->Get_c_fractal_scale_y3());
       result += std::format(L"\n{}c_fractal_scale_w0: {}", tTabStr, t->Get_c_fractal_scale_w0());
       result += std::format(L"\n{}c_fractal_scale_w1: {}", tTabStr, t->Get_c_fractal_scale_w1());
       result += std::format(L"\n{}c_fractal_scale_w2: {}", tTabStr, t->Get_c_fractal_scale_w2());
       result += std::format(L"\n{}c_fractal_scale_w3: {}", tTabStr, t->Get_c_fractal_scale_w3());
       result += std::format(L"\n{}c_random_seed: {}", tTabStr, t->Get_c_random_seed());
       result += std::format(L"\n{}c_initial_system_group_name: {}", tTabStr, t->Get_c_initial_system_group_name());
       result += std::format(L"\n{}c_initial_system_group_fsm_state_name: {}", tTabStr, t->Get_c_initial_system_group_fsm_state_name());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_MISSION;

class STATIC_MISSION_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_MISSION;

public:
   // (7)
   explicit STATIC_MISSION_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_MISSION, buf)
   {
   }

   // (9)
   explicit STATIC_MISSION_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       MissionSid mission_sid,
       const wchar_t* name,
       const wchar_t* condition_script,
       Ratio ratio,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_MISSION, buf)
   {
       WriteValue(mission_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(ratio);

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_MISSION_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       MissionSid mission_sid,
       const wchar_t* name,
       const wchar_t* condition_script,
       Ratio ratio,
       const wchar_t* action_script
   )
   {
       WriteValue(mission_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(ratio);

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_MISSION& GetPacket() const noexcept { return *reinterpret_cast<STATIC_MISSION*>(GetPacketBufPtr()); }
   STATIC_MISSION* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_MISSION*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_MISSION : public Packet
{
   public:
      using Writer = STATIC_MISSION_WRITER;


private:
   MissionSid m_c_mission_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   Ratio m_c_ratio;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_MISSION()
   {
       memset(this, 0, sizeof(STATIC_MISSION));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_MISSION));
   }
   STATIC_MISSION(const STATIC_MISSION&) = delete;
   STATIC_MISSION(STATIC_MISSION &&) = delete;
   STATIC_MISSION& operator = (const STATIC_MISSION&) = delete;
   STATIC_MISSION& operator = (STATIC_MISSION &&) = delete;

   MissionSid Get_c_mission_sid() const noexcept { return m_c_mission_sid; }
   MissionSid& Ref_c_mission_sid() noexcept { return m_c_mission_sid; }
   const MissionSid& ConstRef_c_mission_sid() const noexcept { return m_c_mission_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   Ratio Get_c_ratio() const noexcept { return m_c_ratio; }
   Ratio& Ref_c_ratio() noexcept { return m_c_ratio; }
   const Ratio& ConstRef_c_ratio() const noexcept { return m_c_ratio; }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_MISSION) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_MISSION) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_MISSION); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_MISSION))
           {
               size += sizeof(STATIC_MISSION);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(MissionSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(Ratio);
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_MISSION>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_mission_sid: {}", tTabStr, t->Get_c_mission_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_ratio: {}", tTabStr, t->Get_c_ratio());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_MISSION_ALL;

class STATIC_MISSION_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_MISSION_ALL;

public:
   // (7)
   explicit STATIC_MISSION_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_MISSION_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_MISSION_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       MissionSid mission_sid,
       const wchar_t* name,
       const wchar_t* condition_script,
       Ratio ratio,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_MISSION_ALL, buf)
   {
       WriteValue(mission_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(ratio);

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_MISSION_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       MissionSid mission_sid,
       const wchar_t* name,
       const wchar_t* condition_script,
       Ratio ratio,
       const wchar_t* action_script
   )
   {
       WriteValue(mission_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(ratio);

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_MISSION_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_MISSION_ALL*>(GetPacketBufPtr()); }
   STATIC_MISSION_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_MISSION_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_MISSION_ALL : public Packet
{
   public:
      using Writer = STATIC_MISSION_ALL_WRITER;


private:
   MissionSid m_c_mission_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   Ratio m_c_ratio;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_MISSION_ALL()
   {
       memset(this, 0, sizeof(STATIC_MISSION_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_MISSION_ALL));
   }
   STATIC_MISSION_ALL(const STATIC_MISSION_ALL&) = delete;
   STATIC_MISSION_ALL(STATIC_MISSION_ALL &&) = delete;
   STATIC_MISSION_ALL& operator = (const STATIC_MISSION_ALL&) = delete;
   STATIC_MISSION_ALL& operator = (STATIC_MISSION_ALL &&) = delete;

   MissionSid Get_c_mission_sid() const noexcept { return m_c_mission_sid; }
   MissionSid& Ref_c_mission_sid() noexcept { return m_c_mission_sid; }
   const MissionSid& ConstRef_c_mission_sid() const noexcept { return m_c_mission_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   Ratio Get_c_ratio() const noexcept { return m_c_ratio; }
   Ratio& Ref_c_ratio() noexcept { return m_c_ratio; }
   const Ratio& ConstRef_c_ratio() const noexcept { return m_c_ratio; }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_MISSION_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_MISSION_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_MISSION_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_MISSION_ALL))
           {
               size += sizeof(STATIC_MISSION_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(MissionSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(Ratio);
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_MISSION_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_mission_sid: {}", tTabStr, t->Get_c_mission_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_ratio: {}", tTabStr, t->Get_c_ratio());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_PRODUCT;

class STATIC_PRODUCT_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_PRODUCT;

public:
   // (7)
   explicit STATIC_PRODUCT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_PRODUCT, buf)
   {
   }

   // (9)
   explicit STATIC_PRODUCT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ProductSid product_sid,
       const wchar_t* name,
       const wchar_t* product_id_google,
       const wchar_t* product_id_apple,
       int64_t price_google,
       int64_t price_apple,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_PRODUCT, buf)
   {
       WriteValue(product_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& product_id_googleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_id_googleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& product_id_appleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_id_appleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(price_google);

       WriteValue(price_apple);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       product_id_googleOffset = GetPacketSize();
       product_id_googleLength = (PacketSize)wcsnlen_s(product_id_google, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_id_googleLength) WriteString(L"", 0);
       else WriteString(product_id_google, product_id_googleLength); 

       product_id_appleOffset = GetPacketSize();
       product_id_appleLength = (PacketSize)wcsnlen_s(product_id_apple, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_id_appleLength) WriteString(L"", 0);
       else WriteString(product_id_apple, product_id_appleLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_PRODUCT_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ProductSid product_sid,
       const wchar_t* name,
       const wchar_t* product_id_google,
       const wchar_t* product_id_apple,
       int64_t price_google,
       int64_t price_apple,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
   {
       WriteValue(product_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& product_id_googleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_id_googleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& product_id_appleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_id_appleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(price_google);

       WriteValue(price_apple);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       product_id_googleOffset = GetPacketSize();
       product_id_googleLength = (PacketSize)wcsnlen_s(product_id_google, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_id_googleLength) WriteString(L"", 0);
       else WriteString(product_id_google, product_id_googleLength); 

       product_id_appleOffset = GetPacketSize();
       product_id_appleLength = (PacketSize)wcsnlen_s(product_id_apple, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_id_appleLength) WriteString(L"", 0);
       else WriteString(product_id_apple, product_id_appleLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_PRODUCT& GetPacket() const noexcept { return *reinterpret_cast<STATIC_PRODUCT*>(GetPacketBufPtr()); }
   STATIC_PRODUCT* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_PRODUCT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_PRODUCT : public Packet
{
   public:
      using Writer = STATIC_PRODUCT_WRITER;


private:
   ProductSid m_c_product_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   PacketSize m_c_product_id_google_offset = 0;
   PacketSize m_c_product_id_google_length = 0;

   PacketSize m_c_product_id_apple_offset = 0;
   PacketSize m_c_product_id_apple_length = 0;

   int64_t m_c_price_google;

   int64_t m_c_price_apple;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_PRODUCT()
   {
       memset(this, 0, sizeof(STATIC_PRODUCT));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_PRODUCT));
   }
   STATIC_PRODUCT(const STATIC_PRODUCT&) = delete;
   STATIC_PRODUCT(STATIC_PRODUCT &&) = delete;
   STATIC_PRODUCT& operator = (const STATIC_PRODUCT&) = delete;
   STATIC_PRODUCT& operator = (STATIC_PRODUCT &&) = delete;

   ProductSid Get_c_product_sid() const noexcept { return m_c_product_sid; }
   ProductSid& Ref_c_product_sid() noexcept { return m_c_product_sid; }
   const ProductSid& ConstRef_c_product_sid() const noexcept { return m_c_product_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   const wchar_t* Get_c_product_id_google() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_product_id_google_offset) + m_c_product_id_google_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_product_id_google_offset);
   }

   wchar_t* Ref_c_product_id_google()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_product_id_google_offset) + m_c_product_id_google_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_product_id_google_offset);
   }

   const wchar_t* Get_c_product_id_apple() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_product_id_apple_offset) + m_c_product_id_apple_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_product_id_apple_offset);
   }

   wchar_t* Ref_c_product_id_apple()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_product_id_apple_offset) + m_c_product_id_apple_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_product_id_apple_offset);
   }

   int64_t Get_c_price_google() const noexcept { return m_c_price_google; }
   int64_t& Ref_c_price_google() noexcept { return m_c_price_google; }
   const int64_t& ConstRef_c_price_google() const noexcept { return m_c_price_google; }

   int64_t Get_c_price_apple() const noexcept { return m_c_price_apple; }
   int64_t& Ref_c_price_apple() noexcept { return m_c_price_apple; }
   const int64_t& ConstRef_c_price_apple() const noexcept { return m_c_price_apple; }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_PRODUCT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_PRODUCT) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_PRODUCT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_PRODUCT))
           {
               size += sizeof(STATIC_PRODUCT);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ProductSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_product_id_google_offset)
               return false;

           if (0 < m_c_product_id_google_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_product_id_google_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_product_id_apple_offset)
               return false;

           if (0 < m_c_product_id_apple_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_product_id_apple_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_PRODUCT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_product_sid: {}", tTabStr, t->Get_c_product_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_product_id_google: {}", tTabStr, t->Get_c_product_id_google());
       result += std::format(L"\n{}c_product_id_apple: {}", tTabStr, t->Get_c_product_id_apple());
       result += std::format(L"\n{}c_price_google: {}", tTabStr, t->Get_c_price_google());
       result += std::format(L"\n{}c_price_apple: {}", tTabStr, t->Get_c_price_apple());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_PRODUCT_ALL;

class STATIC_PRODUCT_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_PRODUCT_ALL;

public:
   // (7)
   explicit STATIC_PRODUCT_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_PRODUCT_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_PRODUCT_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ProductSid product_sid,
       const wchar_t* name,
       const wchar_t* product_id_google,
       const wchar_t* product_id_apple,
       int64_t price_google,
       int64_t price_apple,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_PRODUCT_ALL, buf)
   {
       WriteValue(product_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& product_id_googleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_id_googleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& product_id_appleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_id_appleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(price_google);

       WriteValue(price_apple);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       product_id_googleOffset = GetPacketSize();
       product_id_googleLength = (PacketSize)wcsnlen_s(product_id_google, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_id_googleLength) WriteString(L"", 0);
       else WriteString(product_id_google, product_id_googleLength); 

       product_id_appleOffset = GetPacketSize();
       product_id_appleLength = (PacketSize)wcsnlen_s(product_id_apple, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_id_appleLength) WriteString(L"", 0);
       else WriteString(product_id_apple, product_id_appleLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_PRODUCT_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ProductSid product_sid,
       const wchar_t* name,
       const wchar_t* product_id_google,
       const wchar_t* product_id_apple,
       int64_t price_google,
       int64_t price_apple,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
   {
       WriteValue(product_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& product_id_googleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_id_googleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& product_id_appleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_id_appleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(price_google);

       WriteValue(price_apple);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       product_id_googleOffset = GetPacketSize();
       product_id_googleLength = (PacketSize)wcsnlen_s(product_id_google, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_id_googleLength) WriteString(L"", 0);
       else WriteString(product_id_google, product_id_googleLength); 

       product_id_appleOffset = GetPacketSize();
       product_id_appleLength = (PacketSize)wcsnlen_s(product_id_apple, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_id_appleLength) WriteString(L"", 0);
       else WriteString(product_id_apple, product_id_appleLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_PRODUCT_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_PRODUCT_ALL*>(GetPacketBufPtr()); }
   STATIC_PRODUCT_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_PRODUCT_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_PRODUCT_ALL : public Packet
{
   public:
      using Writer = STATIC_PRODUCT_ALL_WRITER;


private:
   ProductSid m_c_product_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   PacketSize m_c_product_id_google_offset = 0;
   PacketSize m_c_product_id_google_length = 0;

   PacketSize m_c_product_id_apple_offset = 0;
   PacketSize m_c_product_id_apple_length = 0;

   int64_t m_c_price_google;

   int64_t m_c_price_apple;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_PRODUCT_ALL()
   {
       memset(this, 0, sizeof(STATIC_PRODUCT_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_PRODUCT_ALL));
   }
   STATIC_PRODUCT_ALL(const STATIC_PRODUCT_ALL&) = delete;
   STATIC_PRODUCT_ALL(STATIC_PRODUCT_ALL &&) = delete;
   STATIC_PRODUCT_ALL& operator = (const STATIC_PRODUCT_ALL&) = delete;
   STATIC_PRODUCT_ALL& operator = (STATIC_PRODUCT_ALL &&) = delete;

   ProductSid Get_c_product_sid() const noexcept { return m_c_product_sid; }
   ProductSid& Ref_c_product_sid() noexcept { return m_c_product_sid; }
   const ProductSid& ConstRef_c_product_sid() const noexcept { return m_c_product_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   const wchar_t* Get_c_product_id_google() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_product_id_google_offset) + m_c_product_id_google_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_product_id_google_offset);
   }

   wchar_t* Ref_c_product_id_google()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_product_id_google_offset) + m_c_product_id_google_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_product_id_google_offset);
   }

   const wchar_t* Get_c_product_id_apple() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_product_id_apple_offset) + m_c_product_id_apple_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_product_id_apple_offset);
   }

   wchar_t* Ref_c_product_id_apple()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_product_id_apple_offset) + m_c_product_id_apple_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_product_id_apple_offset);
   }

   int64_t Get_c_price_google() const noexcept { return m_c_price_google; }
   int64_t& Ref_c_price_google() noexcept { return m_c_price_google; }
   const int64_t& ConstRef_c_price_google() const noexcept { return m_c_price_google; }

   int64_t Get_c_price_apple() const noexcept { return m_c_price_apple; }
   int64_t& Ref_c_price_apple() noexcept { return m_c_price_apple; }
   const int64_t& ConstRef_c_price_apple() const noexcept { return m_c_price_apple; }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_PRODUCT_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_PRODUCT_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_PRODUCT_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_PRODUCT_ALL))
           {
               size += sizeof(STATIC_PRODUCT_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ProductSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_product_id_google_offset)
               return false;

           if (0 < m_c_product_id_google_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_product_id_google_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_product_id_apple_offset)
               return false;

           if (0 < m_c_product_id_apple_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_product_id_apple_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_PRODUCT_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_product_sid: {}", tTabStr, t->Get_c_product_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_product_id_google: {}", tTabStr, t->Get_c_product_id_google());
       result += std::format(L"\n{}c_product_id_apple: {}", tTabStr, t->Get_c_product_id_apple());
       result += std::format(L"\n{}c_price_google: {}", tTabStr, t->Get_c_price_google());
       result += std::format(L"\n{}c_price_apple: {}", tTabStr, t->Get_c_price_apple());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_QUEST;

class STATIC_QUEST_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_QUEST;

public:
   // (7)
   explicit STATIC_QUEST_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_QUEST, buf)
   {
   }

   // (9)
   explicit STATIC_QUEST_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       QuestSid quest_sid,
       const wchar_t* name,
       QuestChapter quest_chapter,
       QuestIndex quest_index,
       const wchar_t* condition_script,
       const wchar_t* action_script,
       const wchar_t* complete_condition_script,
       const wchar_t* complete_action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_QUEST, buf)
   {
       WriteValue(quest_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(quest_chapter);

       WriteValue(quest_index);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& complete_condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& complete_condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& complete_action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& complete_action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

       complete_condition_scriptOffset = GetPacketSize();
       complete_condition_scriptLength = (PacketSize)wcsnlen_s(complete_condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == complete_condition_scriptLength) WriteString(L"", 0);
       else WriteString(complete_condition_script, complete_condition_scriptLength); 

       complete_action_scriptOffset = GetPacketSize();
       complete_action_scriptLength = (PacketSize)wcsnlen_s(complete_action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == complete_action_scriptLength) WriteString(L"", 0);
       else WriteString(complete_action_script, complete_action_scriptLength); 

   }

   // (8)
   explicit STATIC_QUEST_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       QuestSid quest_sid,
       const wchar_t* name,
       QuestChapter quest_chapter,
       QuestIndex quest_index,
       const wchar_t* condition_script,
       const wchar_t* action_script,
       const wchar_t* complete_condition_script,
       const wchar_t* complete_action_script
   )
   {
       WriteValue(quest_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(quest_chapter);

       WriteValue(quest_index);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& complete_condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& complete_condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& complete_action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& complete_action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

       complete_condition_scriptOffset = GetPacketSize();
       complete_condition_scriptLength = (PacketSize)wcsnlen_s(complete_condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == complete_condition_scriptLength) WriteString(L"", 0);
       else WriteString(complete_condition_script, complete_condition_scriptLength); 

       complete_action_scriptOffset = GetPacketSize();
       complete_action_scriptLength = (PacketSize)wcsnlen_s(complete_action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == complete_action_scriptLength) WriteString(L"", 0);
       else WriteString(complete_action_script, complete_action_scriptLength); 

   }

   STATIC_QUEST& GetPacket() const noexcept { return *reinterpret_cast<STATIC_QUEST*>(GetPacketBufPtr()); }
   STATIC_QUEST* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_QUEST*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_QUEST : public Packet
{
   public:
      using Writer = STATIC_QUEST_WRITER;


private:
   QuestSid m_c_quest_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   QuestChapter m_c_quest_chapter;

   QuestIndex m_c_quest_index;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

   PacketSize m_c_complete_condition_script_offset = 0;
   PacketSize m_c_complete_condition_script_length = 0;

   PacketSize m_c_complete_action_script_offset = 0;
   PacketSize m_c_complete_action_script_length = 0;

public:
   STATIC_QUEST()
   {
       memset(this, 0, sizeof(STATIC_QUEST));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_QUEST));
   }
   STATIC_QUEST(const STATIC_QUEST&) = delete;
   STATIC_QUEST(STATIC_QUEST &&) = delete;
   STATIC_QUEST& operator = (const STATIC_QUEST&) = delete;
   STATIC_QUEST& operator = (STATIC_QUEST &&) = delete;

   QuestSid Get_c_quest_sid() const noexcept { return m_c_quest_sid; }
   QuestSid& Ref_c_quest_sid() noexcept { return m_c_quest_sid; }
   const QuestSid& ConstRef_c_quest_sid() const noexcept { return m_c_quest_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   QuestChapter Get_c_quest_chapter() const noexcept { return m_c_quest_chapter; }
   QuestChapter& Ref_c_quest_chapter() noexcept { return m_c_quest_chapter; }
   const QuestChapter& ConstRef_c_quest_chapter() const noexcept { return m_c_quest_chapter; }

   QuestIndex Get_c_quest_index() const noexcept { return m_c_quest_index; }
   QuestIndex& Ref_c_quest_index() noexcept { return m_c_quest_index; }
   const QuestIndex& ConstRef_c_quest_index() const noexcept { return m_c_quest_index; }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   const wchar_t* Get_c_complete_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_complete_condition_script_offset) + m_c_complete_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_complete_condition_script_offset);
   }

   wchar_t* Ref_c_complete_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_complete_condition_script_offset) + m_c_complete_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_complete_condition_script_offset);
   }

   const wchar_t* Get_c_complete_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_complete_action_script_offset) + m_c_complete_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_complete_action_script_offset);
   }

   wchar_t* Ref_c_complete_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_complete_action_script_offset) + m_c_complete_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_complete_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_QUEST) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_QUEST) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_QUEST); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_QUEST))
           {
               size += sizeof(STATIC_QUEST);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(QuestSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(QuestChapter);
       }

       {
           thisSize += sizeof(QuestIndex);
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_complete_condition_script_offset)
               return false;

           if (0 < m_c_complete_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_complete_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_complete_action_script_offset)
               return false;

           if (0 < m_c_complete_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_complete_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_QUEST>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_quest_sid: {}", tTabStr, t->Get_c_quest_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_quest_chapter: {}", tTabStr, t->Get_c_quest_chapter());
       result += std::format(L"\n{}c_quest_index: {}", tTabStr, t->Get_c_quest_index());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       result += std::format(L"\n{}c_complete_condition_script: {}", tTabStr, t->Get_c_complete_condition_script());
       result += std::format(L"\n{}c_complete_action_script: {}", tTabStr, t->Get_c_complete_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_QUEST_ALL;

class STATIC_QUEST_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_QUEST_ALL;

public:
   // (7)
   explicit STATIC_QUEST_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_QUEST_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_QUEST_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       QuestSid quest_sid,
       const wchar_t* name,
       QuestChapter quest_chapter,
       QuestIndex quest_index,
       const wchar_t* condition_script,
       const wchar_t* action_script,
       const wchar_t* complete_condition_script,
       const wchar_t* complete_action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_QUEST_ALL, buf)
   {
       WriteValue(quest_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(quest_chapter);

       WriteValue(quest_index);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& complete_condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& complete_condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& complete_action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& complete_action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

       complete_condition_scriptOffset = GetPacketSize();
       complete_condition_scriptLength = (PacketSize)wcsnlen_s(complete_condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == complete_condition_scriptLength) WriteString(L"", 0);
       else WriteString(complete_condition_script, complete_condition_scriptLength); 

       complete_action_scriptOffset = GetPacketSize();
       complete_action_scriptLength = (PacketSize)wcsnlen_s(complete_action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == complete_action_scriptLength) WriteString(L"", 0);
       else WriteString(complete_action_script, complete_action_scriptLength); 

   }

   // (8)
   explicit STATIC_QUEST_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       QuestSid quest_sid,
       const wchar_t* name,
       QuestChapter quest_chapter,
       QuestIndex quest_index,
       const wchar_t* condition_script,
       const wchar_t* action_script,
       const wchar_t* complete_condition_script,
       const wchar_t* complete_action_script
   )
   {
       WriteValue(quest_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(quest_chapter);

       WriteValue(quest_index);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& complete_condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& complete_condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& complete_action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& complete_action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

       complete_condition_scriptOffset = GetPacketSize();
       complete_condition_scriptLength = (PacketSize)wcsnlen_s(complete_condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == complete_condition_scriptLength) WriteString(L"", 0);
       else WriteString(complete_condition_script, complete_condition_scriptLength); 

       complete_action_scriptOffset = GetPacketSize();
       complete_action_scriptLength = (PacketSize)wcsnlen_s(complete_action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == complete_action_scriptLength) WriteString(L"", 0);
       else WriteString(complete_action_script, complete_action_scriptLength); 

   }

   STATIC_QUEST_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_QUEST_ALL*>(GetPacketBufPtr()); }
   STATIC_QUEST_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_QUEST_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_QUEST_ALL : public Packet
{
   public:
      using Writer = STATIC_QUEST_ALL_WRITER;


private:
   QuestSid m_c_quest_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   QuestChapter m_c_quest_chapter;

   QuestIndex m_c_quest_index;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

   PacketSize m_c_complete_condition_script_offset = 0;
   PacketSize m_c_complete_condition_script_length = 0;

   PacketSize m_c_complete_action_script_offset = 0;
   PacketSize m_c_complete_action_script_length = 0;

public:
   STATIC_QUEST_ALL()
   {
       memset(this, 0, sizeof(STATIC_QUEST_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_QUEST_ALL));
   }
   STATIC_QUEST_ALL(const STATIC_QUEST_ALL&) = delete;
   STATIC_QUEST_ALL(STATIC_QUEST_ALL &&) = delete;
   STATIC_QUEST_ALL& operator = (const STATIC_QUEST_ALL&) = delete;
   STATIC_QUEST_ALL& operator = (STATIC_QUEST_ALL &&) = delete;

   QuestSid Get_c_quest_sid() const noexcept { return m_c_quest_sid; }
   QuestSid& Ref_c_quest_sid() noexcept { return m_c_quest_sid; }
   const QuestSid& ConstRef_c_quest_sid() const noexcept { return m_c_quest_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   QuestChapter Get_c_quest_chapter() const noexcept { return m_c_quest_chapter; }
   QuestChapter& Ref_c_quest_chapter() noexcept { return m_c_quest_chapter; }
   const QuestChapter& ConstRef_c_quest_chapter() const noexcept { return m_c_quest_chapter; }

   QuestIndex Get_c_quest_index() const noexcept { return m_c_quest_index; }
   QuestIndex& Ref_c_quest_index() noexcept { return m_c_quest_index; }
   const QuestIndex& ConstRef_c_quest_index() const noexcept { return m_c_quest_index; }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   const wchar_t* Get_c_complete_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_complete_condition_script_offset) + m_c_complete_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_complete_condition_script_offset);
   }

   wchar_t* Ref_c_complete_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_complete_condition_script_offset) + m_c_complete_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_complete_condition_script_offset);
   }

   const wchar_t* Get_c_complete_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_complete_action_script_offset) + m_c_complete_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_complete_action_script_offset);
   }

   wchar_t* Ref_c_complete_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_complete_action_script_offset) + m_c_complete_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_complete_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_QUEST_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_QUEST_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_QUEST_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_QUEST_ALL))
           {
               size += sizeof(STATIC_QUEST_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(QuestSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(QuestChapter);
       }

       {
           thisSize += sizeof(QuestIndex);
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_complete_condition_script_offset)
               return false;

           if (0 < m_c_complete_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_complete_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_complete_action_script_offset)
               return false;

           if (0 < m_c_complete_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_complete_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_QUEST_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_quest_sid: {}", tTabStr, t->Get_c_quest_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_quest_chapter: {}", tTabStr, t->Get_c_quest_chapter());
       result += std::format(L"\n{}c_quest_index: {}", tTabStr, t->Get_c_quest_index());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       result += std::format(L"\n{}c_complete_condition_script: {}", tTabStr, t->Get_c_complete_condition_script());
       result += std::format(L"\n{}c_complete_action_script: {}", tTabStr, t->Get_c_complete_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_REWARD_GROUP;

class STATIC_REWARD_GROUP_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_REWARD_GROUP;

public:
   // (7)
   explicit STATIC_REWARD_GROUP_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD_GROUP, buf)
   {
   }

   // (9)
   explicit STATIC_REWARD_GROUP_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       RewardSid reward_sid,
       RewardValue reward_value,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD_GROUP, buf)
   {
       WriteValue(id);

       WriteValue(reward_sid);

       WriteValue(reward_value);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   // (8)
   explicit STATIC_REWARD_GROUP_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       RewardSid reward_sid,
       RewardValue reward_value,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
   {
       WriteValue(id);

       WriteValue(reward_sid);

       WriteValue(reward_value);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   STATIC_REWARD_GROUP& GetPacket() const noexcept { return *reinterpret_cast<STATIC_REWARD_GROUP*>(GetPacketBufPtr()); }
   STATIC_REWARD_GROUP* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_REWARD_GROUP*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_REWARD_GROUP : public Packet
{
   public:
      using Writer = STATIC_REWARD_GROUP_WRITER;


private:
   int64_t m_c_id;

   RewardSid m_c_reward_sid;

   RewardValue m_c_reward_value;

   UserExp m_c_user_exp;

   ItemSid m_c_item_sid;

   ItemQuantity m_c_item_quantity;

public:
   STATIC_REWARD_GROUP()
   {
       memset(this, 0, sizeof(STATIC_REWARD_GROUP));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_REWARD_GROUP));
   }
   STATIC_REWARD_GROUP(const STATIC_REWARD_GROUP&) = delete;
   STATIC_REWARD_GROUP(STATIC_REWARD_GROUP &&) = delete;
   STATIC_REWARD_GROUP& operator = (const STATIC_REWARD_GROUP&) = delete;
   STATIC_REWARD_GROUP& operator = (STATIC_REWARD_GROUP &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   RewardSid Get_c_reward_sid() const noexcept { return m_c_reward_sid; }
   RewardSid& Ref_c_reward_sid() noexcept { return m_c_reward_sid; }
   const RewardSid& ConstRef_c_reward_sid() const noexcept { return m_c_reward_sid; }

   RewardValue Get_c_reward_value() const noexcept { return m_c_reward_value; }
   RewardValue& Ref_c_reward_value() noexcept { return m_c_reward_value; }
   const RewardValue& ConstRef_c_reward_value() const noexcept { return m_c_reward_value; }

   UserExp Get_c_user_exp() const noexcept { return m_c_user_exp; }
   UserExp& Ref_c_user_exp() noexcept { return m_c_user_exp; }
   const UserExp& ConstRef_c_user_exp() const noexcept { return m_c_user_exp; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   ItemQuantity Get_c_item_quantity() const noexcept { return m_c_item_quantity; }
   ItemQuantity& Ref_c_item_quantity() noexcept { return m_c_item_quantity; }
   const ItemQuantity& ConstRef_c_item_quantity() const noexcept { return m_c_item_quantity; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_REWARD_GROUP) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_REWARD_GROUP) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_REWARD_GROUP); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_REWARD_GROUP))
           {
               size += sizeof(STATIC_REWARD_GROUP);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(RewardSid);
       }

       {
           thisSize += sizeof(RewardValue);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_REWARD_GROUP>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_reward_sid: {}", tTabStr, t->Get_c_reward_sid());
       result += std::format(L"\n{}c_reward_value: {}", tTabStr, t->Get_c_reward_value());
       result += std::format(L"\n{}c_user_exp: {}", tTabStr, t->Get_c_user_exp());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_item_quantity: {}", tTabStr, t->Get_c_item_quantity());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_REWARD_GROUP_ALL;

class STATIC_REWARD_GROUP_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_REWARD_GROUP_ALL;

public:
   // (7)
   explicit STATIC_REWARD_GROUP_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD_GROUP_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_REWARD_GROUP_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       RewardSid reward_sid,
       RewardValue reward_value,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD_GROUP_ALL, buf)
   {
       WriteValue(id);

       WriteValue(reward_sid);

       WriteValue(reward_value);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   // (8)
   explicit STATIC_REWARD_GROUP_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       RewardSid reward_sid,
       RewardValue reward_value,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
   {
       WriteValue(id);

       WriteValue(reward_sid);

       WriteValue(reward_value);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   STATIC_REWARD_GROUP_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_REWARD_GROUP_ALL*>(GetPacketBufPtr()); }
   STATIC_REWARD_GROUP_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_REWARD_GROUP_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_REWARD_GROUP_ALL : public Packet
{
   public:
      using Writer = STATIC_REWARD_GROUP_ALL_WRITER;


private:
   int64_t m_c_id;

   RewardSid m_c_reward_sid;

   RewardValue m_c_reward_value;

   UserExp m_c_user_exp;

   ItemSid m_c_item_sid;

   ItemQuantity m_c_item_quantity;

public:
   STATIC_REWARD_GROUP_ALL()
   {
       memset(this, 0, sizeof(STATIC_REWARD_GROUP_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_REWARD_GROUP_ALL));
   }
   STATIC_REWARD_GROUP_ALL(const STATIC_REWARD_GROUP_ALL&) = delete;
   STATIC_REWARD_GROUP_ALL(STATIC_REWARD_GROUP_ALL &&) = delete;
   STATIC_REWARD_GROUP_ALL& operator = (const STATIC_REWARD_GROUP_ALL&) = delete;
   STATIC_REWARD_GROUP_ALL& operator = (STATIC_REWARD_GROUP_ALL &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   RewardSid Get_c_reward_sid() const noexcept { return m_c_reward_sid; }
   RewardSid& Ref_c_reward_sid() noexcept { return m_c_reward_sid; }
   const RewardSid& ConstRef_c_reward_sid() const noexcept { return m_c_reward_sid; }

   RewardValue Get_c_reward_value() const noexcept { return m_c_reward_value; }
   RewardValue& Ref_c_reward_value() noexcept { return m_c_reward_value; }
   const RewardValue& ConstRef_c_reward_value() const noexcept { return m_c_reward_value; }

   UserExp Get_c_user_exp() const noexcept { return m_c_user_exp; }
   UserExp& Ref_c_user_exp() noexcept { return m_c_user_exp; }
   const UserExp& ConstRef_c_user_exp() const noexcept { return m_c_user_exp; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   ItemQuantity Get_c_item_quantity() const noexcept { return m_c_item_quantity; }
   ItemQuantity& Ref_c_item_quantity() noexcept { return m_c_item_quantity; }
   const ItemQuantity& ConstRef_c_item_quantity() const noexcept { return m_c_item_quantity; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_REWARD_GROUP_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_REWARD_GROUP_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_REWARD_GROUP_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_REWARD_GROUP_ALL))
           {
               size += sizeof(STATIC_REWARD_GROUP_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(RewardSid);
       }

       {
           thisSize += sizeof(RewardValue);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_REWARD_GROUP_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_reward_sid: {}", tTabStr, t->Get_c_reward_sid());
       result += std::format(L"\n{}c_reward_value: {}", tTabStr, t->Get_c_reward_value());
       result += std::format(L"\n{}c_user_exp: {}", tTabStr, t->Get_c_user_exp());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_item_quantity: {}", tTabStr, t->Get_c_item_quantity());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_REWARD_RATIO;

class STATIC_REWARD_RATIO_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_REWARD_RATIO;

public:
   // (7)
   explicit STATIC_REWARD_RATIO_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD_RATIO, buf)
   {
   }

   // (9)
   explicit STATIC_REWARD_RATIO_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       RewardSid reward_sid,
       RewardValue reward_value,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD_RATIO, buf)
   {
       WriteValue(id);

       WriteValue(reward_sid);

       WriteValue(reward_value);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   // (8)
   explicit STATIC_REWARD_RATIO_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       RewardSid reward_sid,
       RewardValue reward_value,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
   {
       WriteValue(id);

       WriteValue(reward_sid);

       WriteValue(reward_value);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   STATIC_REWARD_RATIO& GetPacket() const noexcept { return *reinterpret_cast<STATIC_REWARD_RATIO*>(GetPacketBufPtr()); }
   STATIC_REWARD_RATIO* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_REWARD_RATIO*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_REWARD_RATIO : public Packet
{
   public:
      using Writer = STATIC_REWARD_RATIO_WRITER;


private:
   int64_t m_c_id;

   RewardSid m_c_reward_sid;

   RewardValue m_c_reward_value;

   UserExp m_c_user_exp;

   ItemSid m_c_item_sid;

   ItemQuantity m_c_item_quantity;

public:
   STATIC_REWARD_RATIO()
   {
       memset(this, 0, sizeof(STATIC_REWARD_RATIO));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_REWARD_RATIO));
   }
   STATIC_REWARD_RATIO(const STATIC_REWARD_RATIO&) = delete;
   STATIC_REWARD_RATIO(STATIC_REWARD_RATIO &&) = delete;
   STATIC_REWARD_RATIO& operator = (const STATIC_REWARD_RATIO&) = delete;
   STATIC_REWARD_RATIO& operator = (STATIC_REWARD_RATIO &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   RewardSid Get_c_reward_sid() const noexcept { return m_c_reward_sid; }
   RewardSid& Ref_c_reward_sid() noexcept { return m_c_reward_sid; }
   const RewardSid& ConstRef_c_reward_sid() const noexcept { return m_c_reward_sid; }

   RewardValue Get_c_reward_value() const noexcept { return m_c_reward_value; }
   RewardValue& Ref_c_reward_value() noexcept { return m_c_reward_value; }
   const RewardValue& ConstRef_c_reward_value() const noexcept { return m_c_reward_value; }

   UserExp Get_c_user_exp() const noexcept { return m_c_user_exp; }
   UserExp& Ref_c_user_exp() noexcept { return m_c_user_exp; }
   const UserExp& ConstRef_c_user_exp() const noexcept { return m_c_user_exp; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   ItemQuantity Get_c_item_quantity() const noexcept { return m_c_item_quantity; }
   ItemQuantity& Ref_c_item_quantity() noexcept { return m_c_item_quantity; }
   const ItemQuantity& ConstRef_c_item_quantity() const noexcept { return m_c_item_quantity; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_REWARD_RATIO) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_REWARD_RATIO) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_REWARD_RATIO); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_REWARD_RATIO))
           {
               size += sizeof(STATIC_REWARD_RATIO);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(RewardSid);
       }

       {
           thisSize += sizeof(RewardValue);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_REWARD_RATIO>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_reward_sid: {}", tTabStr, t->Get_c_reward_sid());
       result += std::format(L"\n{}c_reward_value: {}", tTabStr, t->Get_c_reward_value());
       result += std::format(L"\n{}c_user_exp: {}", tTabStr, t->Get_c_user_exp());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_item_quantity: {}", tTabStr, t->Get_c_item_quantity());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_REWARD_RATIO_ALL;

class STATIC_REWARD_RATIO_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_REWARD_RATIO_ALL;

public:
   // (7)
   explicit STATIC_REWARD_RATIO_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD_RATIO_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_REWARD_RATIO_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       RewardSid reward_sid,
       RewardValue reward_value,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD_RATIO_ALL, buf)
   {
       WriteValue(id);

       WriteValue(reward_sid);

       WriteValue(reward_value);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   // (8)
   explicit STATIC_REWARD_RATIO_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       RewardSid reward_sid,
       RewardValue reward_value,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
   {
       WriteValue(id);

       WriteValue(reward_sid);

       WriteValue(reward_value);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   STATIC_REWARD_RATIO_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_REWARD_RATIO_ALL*>(GetPacketBufPtr()); }
   STATIC_REWARD_RATIO_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_REWARD_RATIO_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_REWARD_RATIO_ALL : public Packet
{
   public:
      using Writer = STATIC_REWARD_RATIO_ALL_WRITER;


private:
   int64_t m_c_id;

   RewardSid m_c_reward_sid;

   RewardValue m_c_reward_value;

   UserExp m_c_user_exp;

   ItemSid m_c_item_sid;

   ItemQuantity m_c_item_quantity;

public:
   STATIC_REWARD_RATIO_ALL()
   {
       memset(this, 0, sizeof(STATIC_REWARD_RATIO_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_REWARD_RATIO_ALL));
   }
   STATIC_REWARD_RATIO_ALL(const STATIC_REWARD_RATIO_ALL&) = delete;
   STATIC_REWARD_RATIO_ALL(STATIC_REWARD_RATIO_ALL &&) = delete;
   STATIC_REWARD_RATIO_ALL& operator = (const STATIC_REWARD_RATIO_ALL&) = delete;
   STATIC_REWARD_RATIO_ALL& operator = (STATIC_REWARD_RATIO_ALL &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   RewardSid Get_c_reward_sid() const noexcept { return m_c_reward_sid; }
   RewardSid& Ref_c_reward_sid() noexcept { return m_c_reward_sid; }
   const RewardSid& ConstRef_c_reward_sid() const noexcept { return m_c_reward_sid; }

   RewardValue Get_c_reward_value() const noexcept { return m_c_reward_value; }
   RewardValue& Ref_c_reward_value() noexcept { return m_c_reward_value; }
   const RewardValue& ConstRef_c_reward_value() const noexcept { return m_c_reward_value; }

   UserExp Get_c_user_exp() const noexcept { return m_c_user_exp; }
   UserExp& Ref_c_user_exp() noexcept { return m_c_user_exp; }
   const UserExp& ConstRef_c_user_exp() const noexcept { return m_c_user_exp; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   ItemQuantity Get_c_item_quantity() const noexcept { return m_c_item_quantity; }
   ItemQuantity& Ref_c_item_quantity() noexcept { return m_c_item_quantity; }
   const ItemQuantity& ConstRef_c_item_quantity() const noexcept { return m_c_item_quantity; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_REWARD_RATIO_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_REWARD_RATIO_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_REWARD_RATIO_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_REWARD_RATIO_ALL))
           {
               size += sizeof(STATIC_REWARD_RATIO_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(RewardSid);
       }

       {
           thisSize += sizeof(RewardValue);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_REWARD_RATIO_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_reward_sid: {}", tTabStr, t->Get_c_reward_sid());
       result += std::format(L"\n{}c_reward_value: {}", tTabStr, t->Get_c_reward_value());
       result += std::format(L"\n{}c_user_exp: {}", tTabStr, t->Get_c_user_exp());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_item_quantity: {}", tTabStr, t->Get_c_item_quantity());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_REWARD;

class STATIC_REWARD_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_REWARD;

public:
   // (7)
   explicit STATIC_REWARD_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD, buf)
   {
   }

   // (9)
   explicit STATIC_REWARD_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       RewardSid reward_sid,
       RewardType reward_type,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD, buf)
   {
       WriteValue(reward_sid);

       WriteValue(reward_type);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   // (8)
   explicit STATIC_REWARD_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       RewardSid reward_sid,
       RewardType reward_type,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
   {
       WriteValue(reward_sid);

       WriteValue(reward_type);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   STATIC_REWARD& GetPacket() const noexcept { return *reinterpret_cast<STATIC_REWARD*>(GetPacketBufPtr()); }
   STATIC_REWARD* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_REWARD*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_REWARD : public Packet
{
   public:
      using Writer = STATIC_REWARD_WRITER;


private:
   RewardSid m_c_reward_sid;

   RewardType m_c_reward_type;

   UserExp m_c_user_exp;

   ItemSid m_c_item_sid;

   ItemQuantity m_c_item_quantity;

public:
   STATIC_REWARD()
   {
       memset(this, 0, sizeof(STATIC_REWARD));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_REWARD));
   }
   STATIC_REWARD(const STATIC_REWARD&) = delete;
   STATIC_REWARD(STATIC_REWARD &&) = delete;
   STATIC_REWARD& operator = (const STATIC_REWARD&) = delete;
   STATIC_REWARD& operator = (STATIC_REWARD &&) = delete;

   RewardSid Get_c_reward_sid() const noexcept { return m_c_reward_sid; }
   RewardSid& Ref_c_reward_sid() noexcept { return m_c_reward_sid; }
   const RewardSid& ConstRef_c_reward_sid() const noexcept { return m_c_reward_sid; }

   RewardType Get_c_reward_type() const noexcept { return m_c_reward_type; }
   RewardType& Ref_c_reward_type() noexcept { return m_c_reward_type; }
   const RewardType& ConstRef_c_reward_type() const noexcept { return m_c_reward_type; }

   UserExp Get_c_user_exp() const noexcept { return m_c_user_exp; }
   UserExp& Ref_c_user_exp() noexcept { return m_c_user_exp; }
   const UserExp& ConstRef_c_user_exp() const noexcept { return m_c_user_exp; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   ItemQuantity Get_c_item_quantity() const noexcept { return m_c_item_quantity; }
   ItemQuantity& Ref_c_item_quantity() noexcept { return m_c_item_quantity; }
   const ItemQuantity& ConstRef_c_item_quantity() const noexcept { return m_c_item_quantity; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_REWARD) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_REWARD) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_REWARD); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_REWARD))
           {
               size += sizeof(STATIC_REWARD);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(RewardSid);
       }

       {
           thisSize += sizeof(RewardType);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_REWARD>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_reward_sid: {}", tTabStr, t->Get_c_reward_sid());
       result += std::format(L"\n{}c_reward_type: {}", tTabStr, t->Get_c_reward_type());
       result += std::format(L"\n{}c_user_exp: {}", tTabStr, t->Get_c_user_exp());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_item_quantity: {}", tTabStr, t->Get_c_item_quantity());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_REWARD_ALL;

class STATIC_REWARD_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_REWARD_ALL;

public:
   // (7)
   explicit STATIC_REWARD_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_REWARD_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       RewardSid reward_sid,
       RewardType reward_type,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_REWARD_ALL, buf)
   {
       WriteValue(reward_sid);

       WriteValue(reward_type);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   // (8)
   explicit STATIC_REWARD_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       RewardSid reward_sid,
       RewardType reward_type,
       UserExp user_exp,
       ItemSid item_sid,
       ItemQuantity item_quantity
   )
   {
       WriteValue(reward_sid);

       WriteValue(reward_type);

       WriteValue(user_exp);

       WriteValue(item_sid);

       WriteValue(item_quantity);


   }

   STATIC_REWARD_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_REWARD_ALL*>(GetPacketBufPtr()); }
   STATIC_REWARD_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_REWARD_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_REWARD_ALL : public Packet
{
   public:
      using Writer = STATIC_REWARD_ALL_WRITER;


private:
   RewardSid m_c_reward_sid;

   RewardType m_c_reward_type;

   UserExp m_c_user_exp;

   ItemSid m_c_item_sid;

   ItemQuantity m_c_item_quantity;

public:
   STATIC_REWARD_ALL()
   {
       memset(this, 0, sizeof(STATIC_REWARD_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_REWARD_ALL));
   }
   STATIC_REWARD_ALL(const STATIC_REWARD_ALL&) = delete;
   STATIC_REWARD_ALL(STATIC_REWARD_ALL &&) = delete;
   STATIC_REWARD_ALL& operator = (const STATIC_REWARD_ALL&) = delete;
   STATIC_REWARD_ALL& operator = (STATIC_REWARD_ALL &&) = delete;

   RewardSid Get_c_reward_sid() const noexcept { return m_c_reward_sid; }
   RewardSid& Ref_c_reward_sid() noexcept { return m_c_reward_sid; }
   const RewardSid& ConstRef_c_reward_sid() const noexcept { return m_c_reward_sid; }

   RewardType Get_c_reward_type() const noexcept { return m_c_reward_type; }
   RewardType& Ref_c_reward_type() noexcept { return m_c_reward_type; }
   const RewardType& ConstRef_c_reward_type() const noexcept { return m_c_reward_type; }

   UserExp Get_c_user_exp() const noexcept { return m_c_user_exp; }
   UserExp& Ref_c_user_exp() noexcept { return m_c_user_exp; }
   const UserExp& ConstRef_c_user_exp() const noexcept { return m_c_user_exp; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   ItemQuantity Get_c_item_quantity() const noexcept { return m_c_item_quantity; }
   ItemQuantity& Ref_c_item_quantity() noexcept { return m_c_item_quantity; }
   const ItemQuantity& ConstRef_c_item_quantity() const noexcept { return m_c_item_quantity; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_REWARD_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_REWARD_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_REWARD_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_REWARD_ALL))
           {
               size += sizeof(STATIC_REWARD_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(RewardSid);
       }

       {
           thisSize += sizeof(RewardType);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_REWARD_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_reward_sid: {}", tTabStr, t->Get_c_reward_sid());
       result += std::format(L"\n{}c_reward_type: {}", tTabStr, t->Get_c_reward_type());
       result += std::format(L"\n{}c_user_exp: {}", tTabStr, t->Get_c_user_exp());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_item_quantity: {}", tTabStr, t->Get_c_item_quantity());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_SCHEDULE;

class STATIC_SCHEDULE_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_SCHEDULE;

public:
   // (7)
   explicit STATIC_SCHEDULE_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_SCHEDULE, buf)
   {
   }

   // (9)
   explicit STATIC_SCHEDULE_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       SchedureSid schedule_sid,
       const wchar_t* name,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_SCHEDULE, buf)
   {
       WriteValue(schedule_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_SCHEDULE_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       SchedureSid schedule_sid,
       const wchar_t* name,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
   {
       WriteValue(schedule_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_SCHEDULE& GetPacket() const noexcept { return *reinterpret_cast<STATIC_SCHEDULE*>(GetPacketBufPtr()); }
   STATIC_SCHEDULE* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_SCHEDULE*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_SCHEDULE : public Packet
{
   public:
      using Writer = STATIC_SCHEDULE_WRITER;


private:
   SchedureSid m_c_schedule_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_SCHEDULE()
   {
       memset(this, 0, sizeof(STATIC_SCHEDULE));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_SCHEDULE));
   }
   STATIC_SCHEDULE(const STATIC_SCHEDULE&) = delete;
   STATIC_SCHEDULE(STATIC_SCHEDULE &&) = delete;
   STATIC_SCHEDULE& operator = (const STATIC_SCHEDULE&) = delete;
   STATIC_SCHEDULE& operator = (STATIC_SCHEDULE &&) = delete;

   SchedureSid Get_c_schedule_sid() const noexcept { return m_c_schedule_sid; }
   SchedureSid& Ref_c_schedule_sid() noexcept { return m_c_schedule_sid; }
   const SchedureSid& ConstRef_c_schedule_sid() const noexcept { return m_c_schedule_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_SCHEDULE) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_SCHEDULE) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_SCHEDULE); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_SCHEDULE))
           {
               size += sizeof(STATIC_SCHEDULE);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(SchedureSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_SCHEDULE>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_schedule_sid: {}", tTabStr, t->Get_c_schedule_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_SCHEDULE_ALL;

class STATIC_SCHEDULE_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_SCHEDULE_ALL;

public:
   // (7)
   explicit STATIC_SCHEDULE_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_SCHEDULE_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_SCHEDULE_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       SchedureSid schedule_sid,
       const wchar_t* name,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_SCHEDULE_ALL, buf)
   {
       WriteValue(schedule_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_SCHEDULE_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       SchedureSid schedule_sid,
       const wchar_t* name,
       const wchar_t* condition_script,
       const wchar_t* action_script
   )
   {
       WriteValue(schedule_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_SCHEDULE_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_SCHEDULE_ALL*>(GetPacketBufPtr()); }
   STATIC_SCHEDULE_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_SCHEDULE_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_SCHEDULE_ALL : public Packet
{
   public:
      using Writer = STATIC_SCHEDULE_ALL_WRITER;


private:
   SchedureSid m_c_schedule_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_SCHEDULE_ALL()
   {
       memset(this, 0, sizeof(STATIC_SCHEDULE_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_SCHEDULE_ALL));
   }
   STATIC_SCHEDULE_ALL(const STATIC_SCHEDULE_ALL&) = delete;
   STATIC_SCHEDULE_ALL(STATIC_SCHEDULE_ALL &&) = delete;
   STATIC_SCHEDULE_ALL& operator = (const STATIC_SCHEDULE_ALL&) = delete;
   STATIC_SCHEDULE_ALL& operator = (STATIC_SCHEDULE_ALL &&) = delete;

   SchedureSid Get_c_schedule_sid() const noexcept { return m_c_schedule_sid; }
   SchedureSid& Ref_c_schedule_sid() noexcept { return m_c_schedule_sid; }
   const SchedureSid& ConstRef_c_schedule_sid() const noexcept { return m_c_schedule_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_SCHEDULE_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_SCHEDULE_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_SCHEDULE_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_SCHEDULE_ALL))
           {
               size += sizeof(STATIC_SCHEDULE_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(SchedureSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_SCHEDULE_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_schedule_sid: {}", tTabStr, t->Get_c_schedule_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_STAT;

class STATIC_STAT_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_STAT;

public:
   // (7)
   explicit STATIC_STAT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_STAT, buf)
   {
   }

   // (9)
   explicit STATIC_STAT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       StatType stat_type,
       const wchar_t* name
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_STAT, buf)
   {
       WriteValue(id);

       WriteValue(stat_type);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   // (8)
   explicit STATIC_STAT_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       StatType stat_type,
       const wchar_t* name
   )
   {
       WriteValue(id);

       WriteValue(stat_type);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   STATIC_STAT& GetPacket() const noexcept { return *reinterpret_cast<STATIC_STAT*>(GetPacketBufPtr()); }
   STATIC_STAT* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_STAT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_STAT : public Packet
{
   public:
      using Writer = STATIC_STAT_WRITER;


private:
   int64_t m_c_id;

   StatType m_c_stat_type;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

public:
   STATIC_STAT()
   {
       memset(this, 0, sizeof(STATIC_STAT));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_STAT));
   }
   STATIC_STAT(const STATIC_STAT&) = delete;
   STATIC_STAT(STATIC_STAT &&) = delete;
   STATIC_STAT& operator = (const STATIC_STAT&) = delete;
   STATIC_STAT& operator = (STATIC_STAT &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   StatType Get_c_stat_type() const noexcept { return m_c_stat_type; }
   StatType& Ref_c_stat_type() noexcept { return m_c_stat_type; }
   const StatType& ConstRef_c_stat_type() const noexcept { return m_c_stat_type; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_STAT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_STAT) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_STAT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_STAT))
           {
               size += sizeof(STATIC_STAT);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(StatType);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_STAT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_stat_type: {}", tTabStr, t->Get_c_stat_type());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_STAT_ALL;

class STATIC_STAT_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_STAT_ALL;

public:
   // (7)
   explicit STATIC_STAT_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_STAT_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_STAT_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       StatType stat_type,
       const wchar_t* name
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_STAT_ALL, buf)
   {
       WriteValue(id);

       WriteValue(stat_type);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   // (8)
   explicit STATIC_STAT_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       StatType stat_type,
       const wchar_t* name
   )
   {
       WriteValue(id);

       WriteValue(stat_type);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

   }

   STATIC_STAT_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_STAT_ALL*>(GetPacketBufPtr()); }
   STATIC_STAT_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_STAT_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_STAT_ALL : public Packet
{
   public:
      using Writer = STATIC_STAT_ALL_WRITER;


private:
   int64_t m_c_id;

   StatType m_c_stat_type;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

public:
   STATIC_STAT_ALL()
   {
       memset(this, 0, sizeof(STATIC_STAT_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_STAT_ALL));
   }
   STATIC_STAT_ALL(const STATIC_STAT_ALL&) = delete;
   STATIC_STAT_ALL(STATIC_STAT_ALL &&) = delete;
   STATIC_STAT_ALL& operator = (const STATIC_STAT_ALL&) = delete;
   STATIC_STAT_ALL& operator = (STATIC_STAT_ALL &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   StatType Get_c_stat_type() const noexcept { return m_c_stat_type; }
   StatType& Ref_c_stat_type() noexcept { return m_c_stat_type; }
   const StatType& ConstRef_c_stat_type() const noexcept { return m_c_stat_type; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_STAT_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_STAT_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_STAT_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_STAT_ALL))
           {
               size += sizeof(STATIC_STAT_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(StatType);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_STAT_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_stat_type: {}", tTabStr, t->Get_c_stat_type());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_STOCK;

class STATIC_STOCK_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_STOCK;

public:
   // (7)
   explicit STATIC_STOCK_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_STOCK, buf)
   {
   }

   // (9)
   explicit STATIC_STOCK_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       StockSid stock_sid,
       const wchar_t* name,
       StockSlot stock_slot,
       const wchar_t* condition_script,
       Ratio ratio,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_STOCK, buf)
   {
       WriteValue(stock_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(stock_slot);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(ratio);

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_STOCK_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       StockSid stock_sid,
       const wchar_t* name,
       StockSlot stock_slot,
       const wchar_t* condition_script,
       Ratio ratio,
       const wchar_t* action_script
   )
   {
       WriteValue(stock_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(stock_slot);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(ratio);

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_STOCK& GetPacket() const noexcept { return *reinterpret_cast<STATIC_STOCK*>(GetPacketBufPtr()); }
   STATIC_STOCK* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_STOCK*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_STOCK : public Packet
{
   public:
      using Writer = STATIC_STOCK_WRITER;


private:
   StockSid m_c_stock_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   StockSlot m_c_stock_slot;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   Ratio m_c_ratio;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_STOCK()
   {
       memset(this, 0, sizeof(STATIC_STOCK));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_STOCK));
   }
   STATIC_STOCK(const STATIC_STOCK&) = delete;
   STATIC_STOCK(STATIC_STOCK &&) = delete;
   STATIC_STOCK& operator = (const STATIC_STOCK&) = delete;
   STATIC_STOCK& operator = (STATIC_STOCK &&) = delete;

   StockSid Get_c_stock_sid() const noexcept { return m_c_stock_sid; }
   StockSid& Ref_c_stock_sid() noexcept { return m_c_stock_sid; }
   const StockSid& ConstRef_c_stock_sid() const noexcept { return m_c_stock_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   StockSlot Get_c_stock_slot() const noexcept { return m_c_stock_slot; }
   StockSlot& Ref_c_stock_slot() noexcept { return m_c_stock_slot; }
   const StockSlot& ConstRef_c_stock_slot() const noexcept { return m_c_stock_slot; }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   Ratio Get_c_ratio() const noexcept { return m_c_ratio; }
   Ratio& Ref_c_ratio() noexcept { return m_c_ratio; }
   const Ratio& ConstRef_c_ratio() const noexcept { return m_c_ratio; }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_STOCK) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_STOCK) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_STOCK); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_STOCK))
           {
               size += sizeof(STATIC_STOCK);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(StockSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(StockSlot);
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(Ratio);
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_STOCK>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_stock_sid: {}", tTabStr, t->Get_c_stock_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_stock_slot: {}", tTabStr, t->Get_c_stock_slot());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_ratio: {}", tTabStr, t->Get_c_ratio());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_STOCK_ALL;

class STATIC_STOCK_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_STOCK_ALL;

public:
   // (7)
   explicit STATIC_STOCK_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_STOCK_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_STOCK_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       StockSid stock_sid,
       const wchar_t* name,
       StockSlot stock_slot,
       const wchar_t* condition_script,
       Ratio ratio,
       const wchar_t* action_script
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_STOCK_ALL, buf)
   {
       WriteValue(stock_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(stock_slot);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(ratio);

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   // (8)
   explicit STATIC_STOCK_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       StockSid stock_sid,
       const wchar_t* name,
       StockSlot stock_slot,
       const wchar_t* condition_script,
       Ratio ratio,
       const wchar_t* action_script
   )
   {
       WriteValue(stock_sid);

       PacketSize& nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(stock_slot);

       PacketSize& condition_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& condition_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(ratio);

       PacketSize& action_scriptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& action_scriptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       nameOffset = GetPacketSize();
       nameLength = (PacketSize)wcsnlen_s(name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == nameLength) WriteString(L"", 0);
       else WriteString(name, nameLength); 

       condition_scriptOffset = GetPacketSize();
       condition_scriptLength = (PacketSize)wcsnlen_s(condition_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == condition_scriptLength) WriteString(L"", 0);
       else WriteString(condition_script, condition_scriptLength); 

       action_scriptOffset = GetPacketSize();
       action_scriptLength = (PacketSize)wcsnlen_s(action_script, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == action_scriptLength) WriteString(L"", 0);
       else WriteString(action_script, action_scriptLength); 

   }

   STATIC_STOCK_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_STOCK_ALL*>(GetPacketBufPtr()); }
   STATIC_STOCK_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_STOCK_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_STOCK_ALL : public Packet
{
   public:
      using Writer = STATIC_STOCK_ALL_WRITER;


private:
   StockSid m_c_stock_sid;

   PacketSize m_c_name_offset = 0;
   PacketSize m_c_name_length = 0;

   StockSlot m_c_stock_slot;

   PacketSize m_c_condition_script_offset = 0;
   PacketSize m_c_condition_script_length = 0;

   Ratio m_c_ratio;

   PacketSize m_c_action_script_offset = 0;
   PacketSize m_c_action_script_length = 0;

public:
   STATIC_STOCK_ALL()
   {
       memset(this, 0, sizeof(STATIC_STOCK_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_STOCK_ALL));
   }
   STATIC_STOCK_ALL(const STATIC_STOCK_ALL&) = delete;
   STATIC_STOCK_ALL(STATIC_STOCK_ALL &&) = delete;
   STATIC_STOCK_ALL& operator = (const STATIC_STOCK_ALL&) = delete;
   STATIC_STOCK_ALL& operator = (STATIC_STOCK_ALL &&) = delete;

   StockSid Get_c_stock_sid() const noexcept { return m_c_stock_sid; }
   StockSid& Ref_c_stock_sid() noexcept { return m_c_stock_sid; }
   const StockSid& ConstRef_c_stock_sid() const noexcept { return m_c_stock_sid; }

   const wchar_t* Get_c_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   wchar_t* Ref_c_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_name_offset) + m_c_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_name_offset);
   }

   StockSlot Get_c_stock_slot() const noexcept { return m_c_stock_slot; }
   StockSlot& Ref_c_stock_slot() noexcept { return m_c_stock_slot; }
   const StockSlot& ConstRef_c_stock_slot() const noexcept { return m_c_stock_slot; }

   const wchar_t* Get_c_condition_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   wchar_t* Ref_c_condition_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_condition_script_offset) + m_c_condition_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_condition_script_offset);
   }

   Ratio Get_c_ratio() const noexcept { return m_c_ratio; }
   Ratio& Ref_c_ratio() noexcept { return m_c_ratio; }
   const Ratio& ConstRef_c_ratio() const noexcept { return m_c_ratio; }

   const wchar_t* Get_c_action_script() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }

   wchar_t* Ref_c_action_script()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_action_script_offset) + m_c_action_script_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_action_script_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_STOCK_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_STOCK_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_STOCK_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_STOCK_ALL))
           {
               size += sizeof(STATIC_STOCK_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(StockSid);
       }

       {
           if (GetPacketSize() < m_c_name_offset)
               return false;

           if (0 < m_c_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(StockSlot);
       }

       {
           if (GetPacketSize() < m_c_condition_script_offset)
               return false;

           if (0 < m_c_condition_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_condition_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(Ratio);
       }

       {
           if (GetPacketSize() < m_c_action_script_offset)
               return false;

           if (0 < m_c_action_script_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_action_script_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_STOCK_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_stock_sid: {}", tTabStr, t->Get_c_stock_sid());
       result += std::format(L"\n{}c_name: {}", tTabStr, t->Get_c_name());
       result += std::format(L"\n{}c_stock_slot: {}", tTabStr, t->Get_c_stock_slot());
       result += std::format(L"\n{}c_condition_script: {}", tTabStr, t->Get_c_condition_script());
       result += std::format(L"\n{}c_ratio: {}", tTabStr, t->Get_c_ratio());
       result += std::format(L"\n{}c_action_script: {}", tTabStr, t->Get_c_action_script());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_TRANSLATION;

class STATIC_TRANSLATION_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_TRANSLATION;

public:
   // (7)
   explicit STATIC_TRANSLATION_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_TRANSLATION, buf)
   {
   }

   // (9)
   explicit STATIC_TRANSLATION_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       const wchar_t* display_key,
       LanguageType language_type,
       const wchar_t* language_text
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_TRANSLATION, buf)
   {
       WriteValue(id);

       PacketSize& display_keyOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& display_keyLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(language_type);

       PacketSize& language_textOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& language_textLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       display_keyOffset = GetPacketSize();
       display_keyLength = (PacketSize)wcsnlen_s(display_key, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == display_keyLength) WriteString(L"", 0);
       else WriteString(display_key, display_keyLength); 

       language_textOffset = GetPacketSize();
       language_textLength = (PacketSize)wcsnlen_s(language_text, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == language_textLength) WriteString(L"", 0);
       else WriteString(language_text, language_textLength); 

   }

   // (8)
   explicit STATIC_TRANSLATION_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       const wchar_t* display_key,
       LanguageType language_type,
       const wchar_t* language_text
   )
   {
       WriteValue(id);

       PacketSize& display_keyOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& display_keyLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(language_type);

       PacketSize& language_textOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& language_textLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       display_keyOffset = GetPacketSize();
       display_keyLength = (PacketSize)wcsnlen_s(display_key, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == display_keyLength) WriteString(L"", 0);
       else WriteString(display_key, display_keyLength); 

       language_textOffset = GetPacketSize();
       language_textLength = (PacketSize)wcsnlen_s(language_text, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == language_textLength) WriteString(L"", 0);
       else WriteString(language_text, language_textLength); 

   }

   STATIC_TRANSLATION& GetPacket() const noexcept { return *reinterpret_cast<STATIC_TRANSLATION*>(GetPacketBufPtr()); }
   STATIC_TRANSLATION* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_TRANSLATION*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_TRANSLATION : public Packet
{
   public:
      using Writer = STATIC_TRANSLATION_WRITER;


private:
   int64_t m_c_id;

   PacketSize m_c_display_key_offset = 0;
   PacketSize m_c_display_key_length = 0;

   LanguageType m_c_language_type;

   PacketSize m_c_language_text_offset = 0;
   PacketSize m_c_language_text_length = 0;

public:
   STATIC_TRANSLATION()
   {
       memset(this, 0, sizeof(STATIC_TRANSLATION));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_TRANSLATION));
   }
   STATIC_TRANSLATION(const STATIC_TRANSLATION&) = delete;
   STATIC_TRANSLATION(STATIC_TRANSLATION &&) = delete;
   STATIC_TRANSLATION& operator = (const STATIC_TRANSLATION&) = delete;
   STATIC_TRANSLATION& operator = (STATIC_TRANSLATION &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   const wchar_t* Get_c_display_key() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_display_key_offset) + m_c_display_key_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_display_key_offset);
   }

   wchar_t* Ref_c_display_key()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_display_key_offset) + m_c_display_key_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_display_key_offset);
   }

   LanguageType Get_c_language_type() const noexcept { return m_c_language_type; }
   LanguageType& Ref_c_language_type() noexcept { return m_c_language_type; }
   const LanguageType& ConstRef_c_language_type() const noexcept { return m_c_language_type; }

   const wchar_t* Get_c_language_text() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_language_text_offset) + m_c_language_text_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_language_text_offset);
   }

   wchar_t* Ref_c_language_text()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_language_text_offset) + m_c_language_text_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_language_text_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_TRANSLATION) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_TRANSLATION) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_TRANSLATION); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_TRANSLATION))
           {
               size += sizeof(STATIC_TRANSLATION);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           if (GetPacketSize() < m_c_display_key_offset)
               return false;

           if (0 < m_c_display_key_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_display_key_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(LanguageType);
       }

       {
           if (GetPacketSize() < m_c_language_text_offset)
               return false;

           if (0 < m_c_language_text_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_language_text_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_TRANSLATION>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_display_key: {}", tTabStr, t->Get_c_display_key());
       result += std::format(L"\n{}c_language_type: {}", tTabStr, t->Get_c_language_type());
       result += std::format(L"\n{}c_language_text: {}", tTabStr, t->Get_c_language_text());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_TRANSLATION_ALL;

class STATIC_TRANSLATION_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_TRANSLATION_ALL;

public:
   // (7)
   explicit STATIC_TRANSLATION_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_TRANSLATION_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_TRANSLATION_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       const wchar_t* display_key,
       LanguageType language_type,
       const wchar_t* language_text
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_TRANSLATION_ALL, buf)
   {
       WriteValue(id);

       PacketSize& display_keyOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& display_keyLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(language_type);

       PacketSize& language_textOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& language_textLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       display_keyOffset = GetPacketSize();
       display_keyLength = (PacketSize)wcsnlen_s(display_key, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == display_keyLength) WriteString(L"", 0);
       else WriteString(display_key, display_keyLength); 

       language_textOffset = GetPacketSize();
       language_textLength = (PacketSize)wcsnlen_s(language_text, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == language_textLength) WriteString(L"", 0);
       else WriteString(language_text, language_textLength); 

   }

   // (8)
   explicit STATIC_TRANSLATION_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       const wchar_t* display_key,
       LanguageType language_type,
       const wchar_t* language_text
   )
   {
       WriteValue(id);

       PacketSize& display_keyOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& display_keyLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(language_type);

       PacketSize& language_textOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& language_textLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       display_keyOffset = GetPacketSize();
       display_keyLength = (PacketSize)wcsnlen_s(display_key, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == display_keyLength) WriteString(L"", 0);
       else WriteString(display_key, display_keyLength); 

       language_textOffset = GetPacketSize();
       language_textLength = (PacketSize)wcsnlen_s(language_text, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == language_textLength) WriteString(L"", 0);
       else WriteString(language_text, language_textLength); 

   }

   STATIC_TRANSLATION_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_TRANSLATION_ALL*>(GetPacketBufPtr()); }
   STATIC_TRANSLATION_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_TRANSLATION_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_TRANSLATION_ALL : public Packet
{
   public:
      using Writer = STATIC_TRANSLATION_ALL_WRITER;


private:
   int64_t m_c_id;

   PacketSize m_c_display_key_offset = 0;
   PacketSize m_c_display_key_length = 0;

   LanguageType m_c_language_type;

   PacketSize m_c_language_text_offset = 0;
   PacketSize m_c_language_text_length = 0;

public:
   STATIC_TRANSLATION_ALL()
   {
       memset(this, 0, sizeof(STATIC_TRANSLATION_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_TRANSLATION_ALL));
   }
   STATIC_TRANSLATION_ALL(const STATIC_TRANSLATION_ALL&) = delete;
   STATIC_TRANSLATION_ALL(STATIC_TRANSLATION_ALL &&) = delete;
   STATIC_TRANSLATION_ALL& operator = (const STATIC_TRANSLATION_ALL&) = delete;
   STATIC_TRANSLATION_ALL& operator = (STATIC_TRANSLATION_ALL &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   const wchar_t* Get_c_display_key() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_display_key_offset) + m_c_display_key_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_display_key_offset);
   }

   wchar_t* Ref_c_display_key()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_display_key_offset) + m_c_display_key_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_display_key_offset);
   }

   LanguageType Get_c_language_type() const noexcept { return m_c_language_type; }
   LanguageType& Ref_c_language_type() noexcept { return m_c_language_type; }
   const LanguageType& ConstRef_c_language_type() const noexcept { return m_c_language_type; }

   const wchar_t* Get_c_language_text() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_language_text_offset) + m_c_language_text_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_language_text_offset);
   }

   wchar_t* Ref_c_language_text()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_language_text_offset) + m_c_language_text_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_language_text_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_TRANSLATION_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_TRANSLATION_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_TRANSLATION_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_TRANSLATION_ALL))
           {
               size += sizeof(STATIC_TRANSLATION_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           if (GetPacketSize() < m_c_display_key_offset)
               return false;

           if (0 < m_c_display_key_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_display_key_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(LanguageType);
       }

       {
           if (GetPacketSize() < m_c_language_text_offset)
               return false;

           if (0 < m_c_language_text_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_language_text_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_TRANSLATION_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_display_key: {}", tTabStr, t->Get_c_display_key());
       result += std::format(L"\n{}c_language_type: {}", tTabStr, t->Get_c_language_type());
       result += std::format(L"\n{}c_language_text: {}", tTabStr, t->Get_c_language_text());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_USER_EXP;

class STATIC_USER_EXP_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_USER_EXP;

public:
   // (7)
   explicit STATIC_USER_EXP_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_USER_EXP, buf)
   {
   }

   // (9)
   explicit STATIC_USER_EXP_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       UserExp user_exp,
       UserLevel user_level,
       MailSid mail_sid
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_USER_EXP, buf)
   {
       WriteValue(id);

       WriteValue(user_exp);

       WriteValue(user_level);

       WriteValue(mail_sid);


   }

   // (8)
   explicit STATIC_USER_EXP_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       UserExp user_exp,
       UserLevel user_level,
       MailSid mail_sid
   )
   {
       WriteValue(id);

       WriteValue(user_exp);

       WriteValue(user_level);

       WriteValue(mail_sid);


   }

   STATIC_USER_EXP& GetPacket() const noexcept { return *reinterpret_cast<STATIC_USER_EXP*>(GetPacketBufPtr()); }
   STATIC_USER_EXP* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_USER_EXP*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_USER_EXP : public Packet
{
   public:
      using Writer = STATIC_USER_EXP_WRITER;


private:
   int64_t m_c_id;

   UserExp m_c_user_exp;

   UserLevel m_c_user_level;

   MailSid m_c_mail_sid;

public:
   STATIC_USER_EXP()
   {
       memset(this, 0, sizeof(STATIC_USER_EXP));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_USER_EXP));
   }
   STATIC_USER_EXP(const STATIC_USER_EXP&) = delete;
   STATIC_USER_EXP(STATIC_USER_EXP &&) = delete;
   STATIC_USER_EXP& operator = (const STATIC_USER_EXP&) = delete;
   STATIC_USER_EXP& operator = (STATIC_USER_EXP &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   UserExp Get_c_user_exp() const noexcept { return m_c_user_exp; }
   UserExp& Ref_c_user_exp() noexcept { return m_c_user_exp; }
   const UserExp& ConstRef_c_user_exp() const noexcept { return m_c_user_exp; }

   UserLevel Get_c_user_level() const noexcept { return m_c_user_level; }
   UserLevel& Ref_c_user_level() noexcept { return m_c_user_level; }
   const UserLevel& ConstRef_c_user_level() const noexcept { return m_c_user_level; }

   MailSid Get_c_mail_sid() const noexcept { return m_c_mail_sid; }
   MailSid& Ref_c_mail_sid() noexcept { return m_c_mail_sid; }
   const MailSid& ConstRef_c_mail_sid() const noexcept { return m_c_mail_sid; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_USER_EXP) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_USER_EXP) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_USER_EXP); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_USER_EXP))
           {
               size += sizeof(STATIC_USER_EXP);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(MailSid);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_USER_EXP>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_user_exp: {}", tTabStr, t->Get_c_user_exp());
       result += std::format(L"\n{}c_user_level: {}", tTabStr, t->Get_c_user_level());
       result += std::format(L"\n{}c_mail_sid: {}", tTabStr, t->Get_c_mail_sid());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STATIC_USER_EXP_ALL;

class STATIC_USER_EXP_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STATIC_USER_EXP_ALL;

public:
   // (7)
   explicit STATIC_USER_EXP_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STATIC_USER_EXP_ALL, buf)
   {
   }

   // (9)
   explicit STATIC_USER_EXP_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       int64_t id,
       UserExp user_exp,
       UserLevel user_level,
       MailSid mail_sid
   )
       :
       PacketWriter(trait, PacketTypes::STATIC_USER_EXP_ALL, buf)
   {
       WriteValue(id);

       WriteValue(user_exp);

       WriteValue(user_level);

       WriteValue(mail_sid);


   }

   // (8)
   explicit STATIC_USER_EXP_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       int64_t id,
       UserExp user_exp,
       UserLevel user_level,
       MailSid mail_sid
   )
   {
       WriteValue(id);

       WriteValue(user_exp);

       WriteValue(user_level);

       WriteValue(mail_sid);


   }

   STATIC_USER_EXP_ALL& GetPacket() const noexcept { return *reinterpret_cast<STATIC_USER_EXP_ALL*>(GetPacketBufPtr()); }
   STATIC_USER_EXP_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<STATIC_USER_EXP_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STATIC_USER_EXP_ALL : public Packet
{
   public:
      using Writer = STATIC_USER_EXP_ALL_WRITER;


private:
   int64_t m_c_id;

   UserExp m_c_user_exp;

   UserLevel m_c_user_level;

   MailSid m_c_mail_sid;

public:
   STATIC_USER_EXP_ALL()
   {
       memset(this, 0, sizeof(STATIC_USER_EXP_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(STATIC_USER_EXP_ALL));
   }
   STATIC_USER_EXP_ALL(const STATIC_USER_EXP_ALL&) = delete;
   STATIC_USER_EXP_ALL(STATIC_USER_EXP_ALL &&) = delete;
   STATIC_USER_EXP_ALL& operator = (const STATIC_USER_EXP_ALL&) = delete;
   STATIC_USER_EXP_ALL& operator = (STATIC_USER_EXP_ALL &&) = delete;

   int64_t Get_c_id() const noexcept { return m_c_id; }
   int64_t& Ref_c_id() noexcept { return m_c_id; }
   const int64_t& ConstRef_c_id() const noexcept { return m_c_id; }

   UserExp Get_c_user_exp() const noexcept { return m_c_user_exp; }
   UserExp& Ref_c_user_exp() noexcept { return m_c_user_exp; }
   const UserExp& ConstRef_c_user_exp() const noexcept { return m_c_user_exp; }

   UserLevel Get_c_user_level() const noexcept { return m_c_user_level; }
   UserLevel& Ref_c_user_level() noexcept { return m_c_user_level; }
   const UserLevel& ConstRef_c_user_level() const noexcept { return m_c_user_level; }

   MailSid Get_c_mail_sid() const noexcept { return m_c_mail_sid; }
   MailSid& Ref_c_mail_sid() noexcept { return m_c_mail_sid; }
   const MailSid& ConstRef_c_mail_sid() const noexcept { return m_c_mail_sid; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STATIC_USER_EXP_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STATIC_USER_EXP_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STATIC_USER_EXP_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STATIC_USER_EXP_ALL))
           {
               size += sizeof(STATIC_USER_EXP_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(MailSid);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STATIC_USER_EXP_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_user_exp: {}", tTabStr, t->Get_c_user_exp());
       result += std::format(L"\n{}c_user_level: {}", tTabStr, t->Get_c_user_level());
       result += std::format(L"\n{}c_mail_sid: {}", tTabStr, t->Get_c_mail_sid());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT_DELETED;

class ACCOUNT_DELETED_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT_DELETED;

public:
   // (7)
   explicit ACCOUNT_DELETED_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_DELETED, buf)
   {
   }

   // (9)
   explicit ACCOUNT_DELETED_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       AccountId account_id,
       const wchar_t* device_token,
       const wchar_t* google_token,
       const wchar_t* apple_token,
       AccountState account_state,
       DbShardIdx db_shard_idx,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_DELETED, buf)
   {
       WriteValue(id);

       WriteValue(account_id);

       PacketSize& device_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& device_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& google_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& google_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& apple_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& apple_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(account_state);

       WriteValue(db_shard_idx);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       device_tokenOffset = GetPacketSize();
       device_tokenLength = (PacketSize)wcsnlen_s(device_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == device_tokenLength) WriteString(L"", 0);
       else WriteString(device_token, device_tokenLength); 

       google_tokenOffset = GetPacketSize();
       google_tokenLength = (PacketSize)wcsnlen_s(google_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == google_tokenLength) WriteString(L"", 0);
       else WriteString(google_token, google_tokenLength); 

       apple_tokenOffset = GetPacketSize();
       apple_tokenLength = (PacketSize)wcsnlen_s(apple_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == apple_tokenLength) WriteString(L"", 0);
       else WriteString(apple_token, apple_tokenLength); 

   }

   // (8)
   explicit ACCOUNT_DELETED_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       AccountId account_id,
       const wchar_t* device_token,
       const wchar_t* google_token,
       const wchar_t* apple_token,
       AccountState account_state,
       DbShardIdx db_shard_idx,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(account_id);

       PacketSize& device_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& device_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& google_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& google_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& apple_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& apple_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(account_state);

       WriteValue(db_shard_idx);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       device_tokenOffset = GetPacketSize();
       device_tokenLength = (PacketSize)wcsnlen_s(device_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == device_tokenLength) WriteString(L"", 0);
       else WriteString(device_token, device_tokenLength); 

       google_tokenOffset = GetPacketSize();
       google_tokenLength = (PacketSize)wcsnlen_s(google_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == google_tokenLength) WriteString(L"", 0);
       else WriteString(google_token, google_tokenLength); 

       apple_tokenOffset = GetPacketSize();
       apple_tokenLength = (PacketSize)wcsnlen_s(apple_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == apple_tokenLength) WriteString(L"", 0);
       else WriteString(apple_token, apple_tokenLength); 

   }

   ACCOUNT_DELETED& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT_DELETED*>(GetPacketBufPtr()); }
   ACCOUNT_DELETED* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT_DELETED*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT_DELETED : public Packet
{
   public:
      using Writer = ACCOUNT_DELETED_WRITER;


private:
   Uuid m_c_id;

   AccountId m_c_account_id;

   PacketSize m_c_device_token_offset = 0;
   PacketSize m_c_device_token_length = 0;

   PacketSize m_c_google_token_offset = 0;
   PacketSize m_c_google_token_length = 0;

   PacketSize m_c_apple_token_offset = 0;
   PacketSize m_c_apple_token_length = 0;

   AccountState m_c_account_state;

   DbShardIdx m_c_db_shard_idx;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   ACCOUNT_DELETED()
   {
       memset(this, 0, sizeof(ACCOUNT_DELETED));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT_DELETED));
   }
   ACCOUNT_DELETED(const ACCOUNT_DELETED&) = delete;
   ACCOUNT_DELETED(ACCOUNT_DELETED &&) = delete;
   ACCOUNT_DELETED& operator = (const ACCOUNT_DELETED&) = delete;
   ACCOUNT_DELETED& operator = (ACCOUNT_DELETED &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   const wchar_t* Get_c_device_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_device_token_offset) + m_c_device_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_device_token_offset);
   }

   wchar_t* Ref_c_device_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_device_token_offset) + m_c_device_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_device_token_offset);
   }

   const wchar_t* Get_c_google_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_google_token_offset) + m_c_google_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_google_token_offset);
   }

   wchar_t* Ref_c_google_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_google_token_offset) + m_c_google_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_google_token_offset);
   }

   const wchar_t* Get_c_apple_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_apple_token_offset) + m_c_apple_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_apple_token_offset);
   }

   wchar_t* Ref_c_apple_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_apple_token_offset) + m_c_apple_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_apple_token_offset);
   }

   AccountState Get_c_account_state() const noexcept { return m_c_account_state; }
   AccountState& Ref_c_account_state() noexcept { return m_c_account_state; }
   const AccountState& ConstRef_c_account_state() const noexcept { return m_c_account_state; }

   DbShardIdx Get_c_db_shard_idx() const noexcept { return m_c_db_shard_idx; }
   DbShardIdx& Ref_c_db_shard_idx() noexcept { return m_c_db_shard_idx; }
   const DbShardIdx& ConstRef_c_db_shard_idx() const noexcept { return m_c_db_shard_idx; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT_DELETED) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT_DELETED) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT_DELETED); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT_DELETED))
           {
               size += sizeof(ACCOUNT_DELETED);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(AccountId);
       }

       {
           if (GetPacketSize() < m_c_device_token_offset)
               return false;

           if (0 < m_c_device_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_device_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_google_token_offset)
               return false;

           if (0 < m_c_google_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_google_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_apple_token_offset)
               return false;

           if (0 < m_c_apple_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_apple_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(AccountState);
       }

       {
           thisSize += sizeof(DbShardIdx);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT_DELETED>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_device_token: {}", tTabStr, t->Get_c_device_token());
       result += std::format(L"\n{}c_google_token: {}", tTabStr, t->Get_c_google_token());
       result += std::format(L"\n{}c_apple_token: {}", tTabStr, t->Get_c_apple_token());
       result += std::format(L"\n{}c_account_state: {}", tTabStr, t->Get_c_account_state());
       result += std::format(L"\n{}c_db_shard_idx: {}", tTabStr, t->Get_c_db_shard_idx());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT_DELETED_ALL;

class ACCOUNT_DELETED_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT_DELETED_ALL;

public:
   // (7)
   explicit ACCOUNT_DELETED_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_DELETED_ALL, buf)
   {
   }

   // (9)
   explicit ACCOUNT_DELETED_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       AccountId account_id,
       const wchar_t* device_token,
       const wchar_t* google_token,
       const wchar_t* apple_token,
       AccountState account_state,
       DbShardIdx db_shard_idx,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_DELETED_ALL, buf)
   {
       WriteValue(id);

       WriteValue(account_id);

       PacketSize& device_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& device_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& google_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& google_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& apple_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& apple_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(account_state);

       WriteValue(db_shard_idx);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       device_tokenOffset = GetPacketSize();
       device_tokenLength = (PacketSize)wcsnlen_s(device_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == device_tokenLength) WriteString(L"", 0);
       else WriteString(device_token, device_tokenLength); 

       google_tokenOffset = GetPacketSize();
       google_tokenLength = (PacketSize)wcsnlen_s(google_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == google_tokenLength) WriteString(L"", 0);
       else WriteString(google_token, google_tokenLength); 

       apple_tokenOffset = GetPacketSize();
       apple_tokenLength = (PacketSize)wcsnlen_s(apple_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == apple_tokenLength) WriteString(L"", 0);
       else WriteString(apple_token, apple_tokenLength); 

   }

   // (8)
   explicit ACCOUNT_DELETED_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       AccountId account_id,
       const wchar_t* device_token,
       const wchar_t* google_token,
       const wchar_t* apple_token,
       AccountState account_state,
       DbShardIdx db_shard_idx,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(account_id);

       PacketSize& device_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& device_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& google_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& google_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& apple_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& apple_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(account_state);

       WriteValue(db_shard_idx);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       device_tokenOffset = GetPacketSize();
       device_tokenLength = (PacketSize)wcsnlen_s(device_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == device_tokenLength) WriteString(L"", 0);
       else WriteString(device_token, device_tokenLength); 

       google_tokenOffset = GetPacketSize();
       google_tokenLength = (PacketSize)wcsnlen_s(google_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == google_tokenLength) WriteString(L"", 0);
       else WriteString(google_token, google_tokenLength); 

       apple_tokenOffset = GetPacketSize();
       apple_tokenLength = (PacketSize)wcsnlen_s(apple_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == apple_tokenLength) WriteString(L"", 0);
       else WriteString(apple_token, apple_tokenLength); 

   }

   ACCOUNT_DELETED_ALL& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT_DELETED_ALL*>(GetPacketBufPtr()); }
   ACCOUNT_DELETED_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT_DELETED_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT_DELETED_ALL : public Packet
{
   public:
      using Writer = ACCOUNT_DELETED_ALL_WRITER;


private:
   Uuid m_c_id;

   AccountId m_c_account_id;

   PacketSize m_c_device_token_offset = 0;
   PacketSize m_c_device_token_length = 0;

   PacketSize m_c_google_token_offset = 0;
   PacketSize m_c_google_token_length = 0;

   PacketSize m_c_apple_token_offset = 0;
   PacketSize m_c_apple_token_length = 0;

   AccountState m_c_account_state;

   DbShardIdx m_c_db_shard_idx;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   ACCOUNT_DELETED_ALL()
   {
       memset(this, 0, sizeof(ACCOUNT_DELETED_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT_DELETED_ALL));
   }
   ACCOUNT_DELETED_ALL(const ACCOUNT_DELETED_ALL&) = delete;
   ACCOUNT_DELETED_ALL(ACCOUNT_DELETED_ALL &&) = delete;
   ACCOUNT_DELETED_ALL& operator = (const ACCOUNT_DELETED_ALL&) = delete;
   ACCOUNT_DELETED_ALL& operator = (ACCOUNT_DELETED_ALL &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   const wchar_t* Get_c_device_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_device_token_offset) + m_c_device_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_device_token_offset);
   }

   wchar_t* Ref_c_device_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_device_token_offset) + m_c_device_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_device_token_offset);
   }

   const wchar_t* Get_c_google_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_google_token_offset) + m_c_google_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_google_token_offset);
   }

   wchar_t* Ref_c_google_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_google_token_offset) + m_c_google_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_google_token_offset);
   }

   const wchar_t* Get_c_apple_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_apple_token_offset) + m_c_apple_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_apple_token_offset);
   }

   wchar_t* Ref_c_apple_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_apple_token_offset) + m_c_apple_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_apple_token_offset);
   }

   AccountState Get_c_account_state() const noexcept { return m_c_account_state; }
   AccountState& Ref_c_account_state() noexcept { return m_c_account_state; }
   const AccountState& ConstRef_c_account_state() const noexcept { return m_c_account_state; }

   DbShardIdx Get_c_db_shard_idx() const noexcept { return m_c_db_shard_idx; }
   DbShardIdx& Ref_c_db_shard_idx() noexcept { return m_c_db_shard_idx; }
   const DbShardIdx& ConstRef_c_db_shard_idx() const noexcept { return m_c_db_shard_idx; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT_DELETED_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT_DELETED_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT_DELETED_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT_DELETED_ALL))
           {
               size += sizeof(ACCOUNT_DELETED_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(AccountId);
       }

       {
           if (GetPacketSize() < m_c_device_token_offset)
               return false;

           if (0 < m_c_device_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_device_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_google_token_offset)
               return false;

           if (0 < m_c_google_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_google_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_apple_token_offset)
               return false;

           if (0 < m_c_apple_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_apple_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(AccountState);
       }

       {
           thisSize += sizeof(DbShardIdx);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT_DELETED_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_device_token: {}", tTabStr, t->Get_c_device_token());
       result += std::format(L"\n{}c_google_token: {}", tTabStr, t->Get_c_google_token());
       result += std::format(L"\n{}c_apple_token: {}", tTabStr, t->Get_c_apple_token());
       result += std::format(L"\n{}c_account_state: {}", tTabStr, t->Get_c_account_state());
       result += std::format(L"\n{}c_db_shard_idx: {}", tTabStr, t->Get_c_db_shard_idx());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT;

class ACCOUNT_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT;

public:
   // (7)
   explicit ACCOUNT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT, buf)
   {
   }

   // (9)
   explicit ACCOUNT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AccountId account_id,
       const wchar_t* device_token,
       const wchar_t* google_token,
       const wchar_t* apple_token,
       DbShardIdx db_shard_idx,
       AccountState account_state,
       time_t date_ban,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT, buf)
   {
       WriteValue(account_id);

       PacketSize& device_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& device_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& google_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& google_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& apple_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& apple_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(db_shard_idx);

       WriteValue(account_state);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);


       device_tokenOffset = GetPacketSize();
       device_tokenLength = (PacketSize)wcsnlen_s(device_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == device_tokenLength) WriteString(L"", 0);
       else WriteString(device_token, device_tokenLength); 

       google_tokenOffset = GetPacketSize();
       google_tokenLength = (PacketSize)wcsnlen_s(google_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == google_tokenLength) WriteString(L"", 0);
       else WriteString(google_token, google_tokenLength); 

       apple_tokenOffset = GetPacketSize();
       apple_tokenLength = (PacketSize)wcsnlen_s(apple_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == apple_tokenLength) WriteString(L"", 0);
       else WriteString(apple_token, apple_tokenLength); 

   }

   // (8)
   explicit ACCOUNT_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       AccountId account_id,
       const wchar_t* device_token,
       const wchar_t* google_token,
       const wchar_t* apple_token,
       DbShardIdx db_shard_idx,
       AccountState account_state,
       time_t date_ban,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(account_id);

       PacketSize& device_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& device_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& google_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& google_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& apple_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& apple_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(db_shard_idx);

       WriteValue(account_state);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);


       device_tokenOffset = GetPacketSize();
       device_tokenLength = (PacketSize)wcsnlen_s(device_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == device_tokenLength) WriteString(L"", 0);
       else WriteString(device_token, device_tokenLength); 

       google_tokenOffset = GetPacketSize();
       google_tokenLength = (PacketSize)wcsnlen_s(google_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == google_tokenLength) WriteString(L"", 0);
       else WriteString(google_token, google_tokenLength); 

       apple_tokenOffset = GetPacketSize();
       apple_tokenLength = (PacketSize)wcsnlen_s(apple_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == apple_tokenLength) WriteString(L"", 0);
       else WriteString(apple_token, apple_tokenLength); 

   }

   ACCOUNT& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT*>(GetPacketBufPtr()); }
   ACCOUNT* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT : public Packet
{
   public:
      using Writer = ACCOUNT_WRITER;


private:
   AccountId m_c_account_id;

   PacketSize m_c_device_token_offset = 0;
   PacketSize m_c_device_token_length = 0;

   PacketSize m_c_google_token_offset = 0;
   PacketSize m_c_google_token_length = 0;

   PacketSize m_c_apple_token_offset = 0;
   PacketSize m_c_apple_token_length = 0;

   DbShardIdx m_c_db_shard_idx;

   AccountState m_c_account_state;

   time_t m_c_date_ban;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ACCOUNT()
   {
       memset(this, 0, sizeof(ACCOUNT));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT));
   }
   ACCOUNT(const ACCOUNT&) = delete;
   ACCOUNT(ACCOUNT &&) = delete;
   ACCOUNT& operator = (const ACCOUNT&) = delete;
   ACCOUNT& operator = (ACCOUNT &&) = delete;

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   const wchar_t* Get_c_device_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_device_token_offset) + m_c_device_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_device_token_offset);
   }

   wchar_t* Ref_c_device_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_device_token_offset) + m_c_device_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_device_token_offset);
   }

   const wchar_t* Get_c_google_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_google_token_offset) + m_c_google_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_google_token_offset);
   }

   wchar_t* Ref_c_google_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_google_token_offset) + m_c_google_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_google_token_offset);
   }

   const wchar_t* Get_c_apple_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_apple_token_offset) + m_c_apple_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_apple_token_offset);
   }

   wchar_t* Ref_c_apple_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_apple_token_offset) + m_c_apple_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_apple_token_offset);
   }

   DbShardIdx Get_c_db_shard_idx() const noexcept { return m_c_db_shard_idx; }
   DbShardIdx& Ref_c_db_shard_idx() noexcept { return m_c_db_shard_idx; }
   const DbShardIdx& ConstRef_c_db_shard_idx() const noexcept { return m_c_db_shard_idx; }

   AccountState Get_c_account_state() const noexcept { return m_c_account_state; }
   AccountState& Ref_c_account_state() noexcept { return m_c_account_state; }
   const AccountState& ConstRef_c_account_state() const noexcept { return m_c_account_state; }

   time_t Get_c_date_ban() const noexcept { return m_c_date_ban; }
   time_t& Ref_c_date_ban() noexcept { return m_c_date_ban; }
   const time_t& ConstRef_c_date_ban() const noexcept { return m_c_date_ban; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT))
           {
               size += sizeof(ACCOUNT);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(AccountId);
       }

       {
           if (GetPacketSize() < m_c_device_token_offset)
               return false;

           if (0 < m_c_device_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_device_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_google_token_offset)
               return false;

           if (0 < m_c_google_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_google_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_apple_token_offset)
               return false;

           if (0 < m_c_apple_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_apple_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(DbShardIdx);
       }

       {
           thisSize += sizeof(AccountState);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_device_token: {}", tTabStr, t->Get_c_device_token());
       result += std::format(L"\n{}c_google_token: {}", tTabStr, t->Get_c_google_token());
       result += std::format(L"\n{}c_apple_token: {}", tTabStr, t->Get_c_apple_token());
       result += std::format(L"\n{}c_db_shard_idx: {}", tTabStr, t->Get_c_db_shard_idx());
       result += std::format(L"\n{}c_account_state: {}", tTabStr, t->Get_c_account_state());
       result += std::format(L"\n{}c_date_ban: {}", tTabStr, t->Get_c_date_ban());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT_ALL;

class ACCOUNT_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT_ALL;

public:
   // (7)
   explicit ACCOUNT_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_ALL, buf)
   {
   }

   // (9)
   explicit ACCOUNT_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AccountId account_id,
       const wchar_t* device_token,
       const wchar_t* google_token,
       const wchar_t* apple_token,
       DbShardIdx db_shard_idx,
       AccountState account_state,
       time_t date_ban,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_ALL, buf)
   {
       WriteValue(account_id);

       PacketSize& device_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& device_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& google_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& google_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& apple_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& apple_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(db_shard_idx);

       WriteValue(account_state);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);


       device_tokenOffset = GetPacketSize();
       device_tokenLength = (PacketSize)wcsnlen_s(device_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == device_tokenLength) WriteString(L"", 0);
       else WriteString(device_token, device_tokenLength); 

       google_tokenOffset = GetPacketSize();
       google_tokenLength = (PacketSize)wcsnlen_s(google_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == google_tokenLength) WriteString(L"", 0);
       else WriteString(google_token, google_tokenLength); 

       apple_tokenOffset = GetPacketSize();
       apple_tokenLength = (PacketSize)wcsnlen_s(apple_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == apple_tokenLength) WriteString(L"", 0);
       else WriteString(apple_token, apple_tokenLength); 

   }

   // (8)
   explicit ACCOUNT_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       AccountId account_id,
       const wchar_t* device_token,
       const wchar_t* google_token,
       const wchar_t* apple_token,
       DbShardIdx db_shard_idx,
       AccountState account_state,
       time_t date_ban,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(account_id);

       PacketSize& device_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& device_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& google_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& google_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& apple_tokenOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& apple_tokenLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(db_shard_idx);

       WriteValue(account_state);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);


       device_tokenOffset = GetPacketSize();
       device_tokenLength = (PacketSize)wcsnlen_s(device_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == device_tokenLength) WriteString(L"", 0);
       else WriteString(device_token, device_tokenLength); 

       google_tokenOffset = GetPacketSize();
       google_tokenLength = (PacketSize)wcsnlen_s(google_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == google_tokenLength) WriteString(L"", 0);
       else WriteString(google_token, google_tokenLength); 

       apple_tokenOffset = GetPacketSize();
       apple_tokenLength = (PacketSize)wcsnlen_s(apple_token, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == apple_tokenLength) WriteString(L"", 0);
       else WriteString(apple_token, apple_tokenLength); 

   }

   ACCOUNT_ALL& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT_ALL*>(GetPacketBufPtr()); }
   ACCOUNT_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT_ALL : public Packet
{
   public:
      using Writer = ACCOUNT_ALL_WRITER;


private:
   AccountId m_c_account_id;

   PacketSize m_c_device_token_offset = 0;
   PacketSize m_c_device_token_length = 0;

   PacketSize m_c_google_token_offset = 0;
   PacketSize m_c_google_token_length = 0;

   PacketSize m_c_apple_token_offset = 0;
   PacketSize m_c_apple_token_length = 0;

   DbShardIdx m_c_db_shard_idx;

   AccountState m_c_account_state;

   time_t m_c_date_ban;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ACCOUNT_ALL()
   {
       memset(this, 0, sizeof(ACCOUNT_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT_ALL));
   }
   ACCOUNT_ALL(const ACCOUNT_ALL&) = delete;
   ACCOUNT_ALL(ACCOUNT_ALL &&) = delete;
   ACCOUNT_ALL& operator = (const ACCOUNT_ALL&) = delete;
   ACCOUNT_ALL& operator = (ACCOUNT_ALL &&) = delete;

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   const wchar_t* Get_c_device_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_device_token_offset) + m_c_device_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_device_token_offset);
   }

   wchar_t* Ref_c_device_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_device_token_offset) + m_c_device_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_device_token_offset);
   }

   const wchar_t* Get_c_google_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_google_token_offset) + m_c_google_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_google_token_offset);
   }

   wchar_t* Ref_c_google_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_google_token_offset) + m_c_google_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_google_token_offset);
   }

   const wchar_t* Get_c_apple_token() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_apple_token_offset) + m_c_apple_token_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_apple_token_offset);
   }

   wchar_t* Ref_c_apple_token()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_apple_token_offset) + m_c_apple_token_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_apple_token_offset);
   }

   DbShardIdx Get_c_db_shard_idx() const noexcept { return m_c_db_shard_idx; }
   DbShardIdx& Ref_c_db_shard_idx() noexcept { return m_c_db_shard_idx; }
   const DbShardIdx& ConstRef_c_db_shard_idx() const noexcept { return m_c_db_shard_idx; }

   AccountState Get_c_account_state() const noexcept { return m_c_account_state; }
   AccountState& Ref_c_account_state() noexcept { return m_c_account_state; }
   const AccountState& ConstRef_c_account_state() const noexcept { return m_c_account_state; }

   time_t Get_c_date_ban() const noexcept { return m_c_date_ban; }
   time_t& Ref_c_date_ban() noexcept { return m_c_date_ban; }
   const time_t& ConstRef_c_date_ban() const noexcept { return m_c_date_ban; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT_ALL))
           {
               size += sizeof(ACCOUNT_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(AccountId);
       }

       {
           if (GetPacketSize() < m_c_device_token_offset)
               return false;

           if (0 < m_c_device_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_device_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_google_token_offset)
               return false;

           if (0 < m_c_google_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_google_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_apple_token_offset)
               return false;

           if (0 < m_c_apple_token_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_apple_token_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(DbShardIdx);
       }

       {
           thisSize += sizeof(AccountState);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_device_token: {}", tTabStr, t->Get_c_device_token());
       result += std::format(L"\n{}c_google_token: {}", tTabStr, t->Get_c_google_token());
       result += std::format(L"\n{}c_apple_token: {}", tTabStr, t->Get_c_apple_token());
       result += std::format(L"\n{}c_db_shard_idx: {}", tTabStr, t->Get_c_db_shard_idx());
       result += std::format(L"\n{}c_account_state: {}", tTabStr, t->Get_c_account_state());
       result += std::format(L"\n{}c_date_ban: {}", tTabStr, t->Get_c_date_ban());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT_USER_DELETED;

class ACCOUNT_USER_DELETED_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT_USER_DELETED;

public:
   // (7)
   explicit ACCOUNT_USER_DELETED_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_DELETED, buf)
   {
   }

   // (9)
   explicit ACCOUNT_USER_DELETED_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       AccountId account_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_DELETED, buf)
   {
       WriteValue(id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   // (8)
   explicit ACCOUNT_USER_DELETED_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       AccountId account_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   ACCOUNT_USER_DELETED& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT_USER_DELETED*>(GetPacketBufPtr()); }
   ACCOUNT_USER_DELETED* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT_USER_DELETED*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT_USER_DELETED : public Packet
{
   public:
      using Writer = ACCOUNT_USER_DELETED_WRITER;


private:
   Uuid m_c_id;

   AccountId m_c_account_id;

   UserId m_c_user_id;

   UserType m_c_user_type;

   UserState m_c_user_state;

   PacketSize m_c_user_name_offset = 0;
   PacketSize m_c_user_name_length = 0;

   UserLevel m_c_user_level;

   UserLevel m_c_user_level_highest;

   time_t m_c_date_ban;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   ACCOUNT_USER_DELETED()
   {
       memset(this, 0, sizeof(ACCOUNT_USER_DELETED));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT_USER_DELETED));
   }
   ACCOUNT_USER_DELETED(const ACCOUNT_USER_DELETED&) = delete;
   ACCOUNT_USER_DELETED(ACCOUNT_USER_DELETED &&) = delete;
   ACCOUNT_USER_DELETED& operator = (const ACCOUNT_USER_DELETED&) = delete;
   ACCOUNT_USER_DELETED& operator = (ACCOUNT_USER_DELETED &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   UserType Get_c_user_type() const noexcept { return m_c_user_type; }
   UserType& Ref_c_user_type() noexcept { return m_c_user_type; }
   const UserType& ConstRef_c_user_type() const noexcept { return m_c_user_type; }

   UserState Get_c_user_state() const noexcept { return m_c_user_state; }
   UserState& Ref_c_user_state() noexcept { return m_c_user_state; }
   const UserState& ConstRef_c_user_state() const noexcept { return m_c_user_state; }

   const wchar_t* Get_c_user_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   wchar_t* Ref_c_user_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   UserLevel Get_c_user_level() const noexcept { return m_c_user_level; }
   UserLevel& Ref_c_user_level() noexcept { return m_c_user_level; }
   const UserLevel& ConstRef_c_user_level() const noexcept { return m_c_user_level; }

   UserLevel Get_c_user_level_highest() const noexcept { return m_c_user_level_highest; }
   UserLevel& Ref_c_user_level_highest() noexcept { return m_c_user_level_highest; }
   const UserLevel& ConstRef_c_user_level_highest() const noexcept { return m_c_user_level_highest; }

   time_t Get_c_date_ban() const noexcept { return m_c_date_ban; }
   time_t& Ref_c_date_ban() noexcept { return m_c_date_ban; }
   const time_t& ConstRef_c_date_ban() const noexcept { return m_c_date_ban; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT_USER_DELETED) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT_USER_DELETED) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT_USER_DELETED); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT_USER_DELETED))
           {
               size += sizeof(ACCOUNT_USER_DELETED);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(UserType);
       }

       {
           thisSize += sizeof(UserState);
       }

       {
           if (GetPacketSize() < m_c_user_name_offset)
               return false;

           if (0 < m_c_user_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_user_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT_USER_DELETED>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_user_type: {}", tTabStr, t->Get_c_user_type());
       result += std::format(L"\n{}c_user_state: {}", tTabStr, t->Get_c_user_state());
       result += std::format(L"\n{}c_user_name: {}", tTabStr, t->Get_c_user_name());
       result += std::format(L"\n{}c_user_level: {}", tTabStr, t->Get_c_user_level());
       result += std::format(L"\n{}c_user_level_highest: {}", tTabStr, t->Get_c_user_level_highest());
       result += std::format(L"\n{}c_date_ban: {}", tTabStr, t->Get_c_date_ban());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT_USER_DELETED_ALL;

class ACCOUNT_USER_DELETED_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT_USER_DELETED_ALL;

public:
   // (7)
   explicit ACCOUNT_USER_DELETED_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_DELETED_ALL, buf)
   {
   }

   // (9)
   explicit ACCOUNT_USER_DELETED_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       AccountId account_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_DELETED_ALL, buf)
   {
       WriteValue(id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   // (8)
   explicit ACCOUNT_USER_DELETED_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       AccountId account_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   ACCOUNT_USER_DELETED_ALL& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT_USER_DELETED_ALL*>(GetPacketBufPtr()); }
   ACCOUNT_USER_DELETED_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT_USER_DELETED_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT_USER_DELETED_ALL : public Packet
{
   public:
      using Writer = ACCOUNT_USER_DELETED_ALL_WRITER;


private:
   Uuid m_c_id;

   AccountId m_c_account_id;

   UserId m_c_user_id;

   UserType m_c_user_type;

   UserState m_c_user_state;

   PacketSize m_c_user_name_offset = 0;
   PacketSize m_c_user_name_length = 0;

   UserLevel m_c_user_level;

   UserLevel m_c_user_level_highest;

   time_t m_c_date_ban;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   ACCOUNT_USER_DELETED_ALL()
   {
       memset(this, 0, sizeof(ACCOUNT_USER_DELETED_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT_USER_DELETED_ALL));
   }
   ACCOUNT_USER_DELETED_ALL(const ACCOUNT_USER_DELETED_ALL&) = delete;
   ACCOUNT_USER_DELETED_ALL(ACCOUNT_USER_DELETED_ALL &&) = delete;
   ACCOUNT_USER_DELETED_ALL& operator = (const ACCOUNT_USER_DELETED_ALL&) = delete;
   ACCOUNT_USER_DELETED_ALL& operator = (ACCOUNT_USER_DELETED_ALL &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   UserType Get_c_user_type() const noexcept { return m_c_user_type; }
   UserType& Ref_c_user_type() noexcept { return m_c_user_type; }
   const UserType& ConstRef_c_user_type() const noexcept { return m_c_user_type; }

   UserState Get_c_user_state() const noexcept { return m_c_user_state; }
   UserState& Ref_c_user_state() noexcept { return m_c_user_state; }
   const UserState& ConstRef_c_user_state() const noexcept { return m_c_user_state; }

   const wchar_t* Get_c_user_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   wchar_t* Ref_c_user_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   UserLevel Get_c_user_level() const noexcept { return m_c_user_level; }
   UserLevel& Ref_c_user_level() noexcept { return m_c_user_level; }
   const UserLevel& ConstRef_c_user_level() const noexcept { return m_c_user_level; }

   UserLevel Get_c_user_level_highest() const noexcept { return m_c_user_level_highest; }
   UserLevel& Ref_c_user_level_highest() noexcept { return m_c_user_level_highest; }
   const UserLevel& ConstRef_c_user_level_highest() const noexcept { return m_c_user_level_highest; }

   time_t Get_c_date_ban() const noexcept { return m_c_date_ban; }
   time_t& Ref_c_date_ban() noexcept { return m_c_date_ban; }
   const time_t& ConstRef_c_date_ban() const noexcept { return m_c_date_ban; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT_USER_DELETED_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT_USER_DELETED_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT_USER_DELETED_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT_USER_DELETED_ALL))
           {
               size += sizeof(ACCOUNT_USER_DELETED_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(UserType);
       }

       {
           thisSize += sizeof(UserState);
       }

       {
           if (GetPacketSize() < m_c_user_name_offset)
               return false;

           if (0 < m_c_user_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_user_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT_USER_DELETED_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_user_type: {}", tTabStr, t->Get_c_user_type());
       result += std::format(L"\n{}c_user_state: {}", tTabStr, t->Get_c_user_state());
       result += std::format(L"\n{}c_user_name: {}", tTabStr, t->Get_c_user_name());
       result += std::format(L"\n{}c_user_level: {}", tTabStr, t->Get_c_user_level());
       result += std::format(L"\n{}c_user_level_highest: {}", tTabStr, t->Get_c_user_level_highest());
       result += std::format(L"\n{}c_date_ban: {}", tTabStr, t->Get_c_date_ban());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT_USER_DELETED_BY_ACCOUNT;

class ACCOUNT_USER_DELETED_BY_ACCOUNT_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT_USER_DELETED_BY_ACCOUNT;

public:
   // (7)
   explicit ACCOUNT_USER_DELETED_BY_ACCOUNT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_DELETED_BY_ACCOUNT, buf)
   {
   }

   // (9)
   explicit ACCOUNT_USER_DELETED_BY_ACCOUNT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       AccountId account_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_DELETED_BY_ACCOUNT, buf)
   {
       WriteValue(id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   // (8)
   explicit ACCOUNT_USER_DELETED_BY_ACCOUNT_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       AccountId account_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   ACCOUNT_USER_DELETED_BY_ACCOUNT& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT_USER_DELETED_BY_ACCOUNT*>(GetPacketBufPtr()); }
   ACCOUNT_USER_DELETED_BY_ACCOUNT* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT_USER_DELETED_BY_ACCOUNT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT_USER_DELETED_BY_ACCOUNT : public Packet
{
   public:
      using Writer = ACCOUNT_USER_DELETED_BY_ACCOUNT_WRITER;


private:
   Uuid m_c_id;

   AccountId m_c_account_id;

   UserId m_c_user_id;

   UserType m_c_user_type;

   UserState m_c_user_state;

   PacketSize m_c_user_name_offset = 0;
   PacketSize m_c_user_name_length = 0;

   UserLevel m_c_user_level;

   UserLevel m_c_user_level_highest;

   time_t m_c_date_ban;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   ACCOUNT_USER_DELETED_BY_ACCOUNT()
   {
       memset(this, 0, sizeof(ACCOUNT_USER_DELETED_BY_ACCOUNT));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT_USER_DELETED_BY_ACCOUNT));
   }
   ACCOUNT_USER_DELETED_BY_ACCOUNT(const ACCOUNT_USER_DELETED_BY_ACCOUNT&) = delete;
   ACCOUNT_USER_DELETED_BY_ACCOUNT(ACCOUNT_USER_DELETED_BY_ACCOUNT &&) = delete;
   ACCOUNT_USER_DELETED_BY_ACCOUNT& operator = (const ACCOUNT_USER_DELETED_BY_ACCOUNT&) = delete;
   ACCOUNT_USER_DELETED_BY_ACCOUNT& operator = (ACCOUNT_USER_DELETED_BY_ACCOUNT &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   UserType Get_c_user_type() const noexcept { return m_c_user_type; }
   UserType& Ref_c_user_type() noexcept { return m_c_user_type; }
   const UserType& ConstRef_c_user_type() const noexcept { return m_c_user_type; }

   UserState Get_c_user_state() const noexcept { return m_c_user_state; }
   UserState& Ref_c_user_state() noexcept { return m_c_user_state; }
   const UserState& ConstRef_c_user_state() const noexcept { return m_c_user_state; }

   const wchar_t* Get_c_user_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   wchar_t* Ref_c_user_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   UserLevel Get_c_user_level() const noexcept { return m_c_user_level; }
   UserLevel& Ref_c_user_level() noexcept { return m_c_user_level; }
   const UserLevel& ConstRef_c_user_level() const noexcept { return m_c_user_level; }

   UserLevel Get_c_user_level_highest() const noexcept { return m_c_user_level_highest; }
   UserLevel& Ref_c_user_level_highest() noexcept { return m_c_user_level_highest; }
   const UserLevel& ConstRef_c_user_level_highest() const noexcept { return m_c_user_level_highest; }

   time_t Get_c_date_ban() const noexcept { return m_c_date_ban; }
   time_t& Ref_c_date_ban() noexcept { return m_c_date_ban; }
   const time_t& ConstRef_c_date_ban() const noexcept { return m_c_date_ban; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT_USER_DELETED_BY_ACCOUNT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT_USER_DELETED_BY_ACCOUNT) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT_USER_DELETED_BY_ACCOUNT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT_USER_DELETED_BY_ACCOUNT))
           {
               size += sizeof(ACCOUNT_USER_DELETED_BY_ACCOUNT);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(UserType);
       }

       {
           thisSize += sizeof(UserState);
       }

       {
           if (GetPacketSize() < m_c_user_name_offset)
               return false;

           if (0 < m_c_user_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_user_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT_USER_DELETED_BY_ACCOUNT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_user_type: {}", tTabStr, t->Get_c_user_type());
       result += std::format(L"\n{}c_user_state: {}", tTabStr, t->Get_c_user_state());
       result += std::format(L"\n{}c_user_name: {}", tTabStr, t->Get_c_user_name());
       result += std::format(L"\n{}c_user_level: {}", tTabStr, t->Get_c_user_level());
       result += std::format(L"\n{}c_user_level_highest: {}", tTabStr, t->Get_c_user_level_highest());
       result += std::format(L"\n{}c_date_ban: {}", tTabStr, t->Get_c_date_ban());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT_USER_GAME;

class ACCOUNT_USER_GAME_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT_USER_GAME;

public:
   // (7)
   explicit ACCOUNT_USER_GAME_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_GAME, buf)
   {
   }

   // (9)
   explicit ACCOUNT_USER_GAME_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       GameId game_id,
       time_t date_updated
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_GAME, buf)
   {
       WriteValue(user_id);

       WriteValue(game_id);

       WriteValue(date_updated);


   }

   // (8)
   explicit ACCOUNT_USER_GAME_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       GameId game_id,
       time_t date_updated
   )
   {
       WriteValue(user_id);

       WriteValue(game_id);

       WriteValue(date_updated);


   }

   ACCOUNT_USER_GAME& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT_USER_GAME*>(GetPacketBufPtr()); }
   ACCOUNT_USER_GAME* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT_USER_GAME*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT_USER_GAME : public Packet
{
   public:
      using Writer = ACCOUNT_USER_GAME_WRITER;


private:
   UserId m_c_user_id;

   GameId m_c_game_id;

   time_t m_c_date_updated;

public:
   ACCOUNT_USER_GAME()
   {
       memset(this, 0, sizeof(ACCOUNT_USER_GAME));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT_USER_GAME));
   }
   ACCOUNT_USER_GAME(const ACCOUNT_USER_GAME&) = delete;
   ACCOUNT_USER_GAME(ACCOUNT_USER_GAME &&) = delete;
   ACCOUNT_USER_GAME& operator = (const ACCOUNT_USER_GAME&) = delete;
   ACCOUNT_USER_GAME& operator = (ACCOUNT_USER_GAME &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GameId Get_c_game_id() const noexcept { return m_c_game_id; }
   GameId& Ref_c_game_id() noexcept { return m_c_game_id; }
   const GameId& ConstRef_c_game_id() const noexcept { return m_c_game_id; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT_USER_GAME) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT_USER_GAME) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT_USER_GAME); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT_USER_GAME))
           {
               size += sizeof(ACCOUNT_USER_GAME);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GameId);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT_USER_GAME>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_game_id: {}", tTabStr, t->Get_c_game_id());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT_USER_GAME_ALL;

class ACCOUNT_USER_GAME_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT_USER_GAME_ALL;

public:
   // (7)
   explicit ACCOUNT_USER_GAME_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_GAME_ALL, buf)
   {
   }

   // (9)
   explicit ACCOUNT_USER_GAME_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       GameId game_id,
       time_t date_updated
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_GAME_ALL, buf)
   {
       WriteValue(user_id);

       WriteValue(game_id);

       WriteValue(date_updated);


   }

   // (8)
   explicit ACCOUNT_USER_GAME_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       GameId game_id,
       time_t date_updated
   )
   {
       WriteValue(user_id);

       WriteValue(game_id);

       WriteValue(date_updated);


   }

   ACCOUNT_USER_GAME_ALL& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT_USER_GAME_ALL*>(GetPacketBufPtr()); }
   ACCOUNT_USER_GAME_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT_USER_GAME_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT_USER_GAME_ALL : public Packet
{
   public:
      using Writer = ACCOUNT_USER_GAME_ALL_WRITER;


private:
   UserId m_c_user_id;

   GameId m_c_game_id;

   time_t m_c_date_updated;

public:
   ACCOUNT_USER_GAME_ALL()
   {
       memset(this, 0, sizeof(ACCOUNT_USER_GAME_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT_USER_GAME_ALL));
   }
   ACCOUNT_USER_GAME_ALL(const ACCOUNT_USER_GAME_ALL&) = delete;
   ACCOUNT_USER_GAME_ALL(ACCOUNT_USER_GAME_ALL &&) = delete;
   ACCOUNT_USER_GAME_ALL& operator = (const ACCOUNT_USER_GAME_ALL&) = delete;
   ACCOUNT_USER_GAME_ALL& operator = (ACCOUNT_USER_GAME_ALL &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GameId Get_c_game_id() const noexcept { return m_c_game_id; }
   GameId& Ref_c_game_id() noexcept { return m_c_game_id; }
   const GameId& ConstRef_c_game_id() const noexcept { return m_c_game_id; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT_USER_GAME_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT_USER_GAME_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT_USER_GAME_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT_USER_GAME_ALL))
           {
               size += sizeof(ACCOUNT_USER_GAME_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GameId);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT_USER_GAME_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_game_id: {}", tTabStr, t->Get_c_game_id());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT_USER;

class ACCOUNT_USER_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT_USER;

public:
   // (7)
   explicit ACCOUNT_USER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER, buf)
   {
   }

   // (9)
   explicit ACCOUNT_USER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AccountId account_id,
       ServerId server_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER, buf)
   {
       WriteValue(account_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   // (8)
   explicit ACCOUNT_USER_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       AccountId account_id,
       ServerId server_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(account_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   ACCOUNT_USER& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT_USER*>(GetPacketBufPtr()); }
   ACCOUNT_USER* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT_USER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT_USER : public Packet
{
   public:
      using Writer = ACCOUNT_USER_WRITER;


private:
   AccountId m_c_account_id;

   ServerId m_c_server_id;

   UserId m_c_user_id;

   UserType m_c_user_type;

   UserState m_c_user_state;

   PacketSize m_c_user_name_offset = 0;
   PacketSize m_c_user_name_length = 0;

   UserLevel m_c_user_level;

   UserLevel m_c_user_level_highest;

   time_t m_c_date_ban;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ACCOUNT_USER()
   {
       memset(this, 0, sizeof(ACCOUNT_USER));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT_USER));
   }
   ACCOUNT_USER(const ACCOUNT_USER&) = delete;
   ACCOUNT_USER(ACCOUNT_USER &&) = delete;
   ACCOUNT_USER& operator = (const ACCOUNT_USER&) = delete;
   ACCOUNT_USER& operator = (ACCOUNT_USER &&) = delete;

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   ServerId Get_c_server_id() const noexcept { return m_c_server_id; }
   ServerId& Ref_c_server_id() noexcept { return m_c_server_id; }
   const ServerId& ConstRef_c_server_id() const noexcept { return m_c_server_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   UserType Get_c_user_type() const noexcept { return m_c_user_type; }
   UserType& Ref_c_user_type() noexcept { return m_c_user_type; }
   const UserType& ConstRef_c_user_type() const noexcept { return m_c_user_type; }

   UserState Get_c_user_state() const noexcept { return m_c_user_state; }
   UserState& Ref_c_user_state() noexcept { return m_c_user_state; }
   const UserState& ConstRef_c_user_state() const noexcept { return m_c_user_state; }

   const wchar_t* Get_c_user_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   wchar_t* Ref_c_user_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   UserLevel Get_c_user_level() const noexcept { return m_c_user_level; }
   UserLevel& Ref_c_user_level() noexcept { return m_c_user_level; }
   const UserLevel& ConstRef_c_user_level() const noexcept { return m_c_user_level; }

   UserLevel Get_c_user_level_highest() const noexcept { return m_c_user_level_highest; }
   UserLevel& Ref_c_user_level_highest() noexcept { return m_c_user_level_highest; }
   const UserLevel& ConstRef_c_user_level_highest() const noexcept { return m_c_user_level_highest; }

   time_t Get_c_date_ban() const noexcept { return m_c_date_ban; }
   time_t& Ref_c_date_ban() noexcept { return m_c_date_ban; }
   const time_t& ConstRef_c_date_ban() const noexcept { return m_c_date_ban; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT_USER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT_USER) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT_USER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT_USER))
           {
               size += sizeof(ACCOUNT_USER);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(ServerId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(UserType);
       }

       {
           thisSize += sizeof(UserState);
       }

       {
           if (GetPacketSize() < m_c_user_name_offset)
               return false;

           if (0 < m_c_user_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_user_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT_USER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_server_id: {}", tTabStr, t->Get_c_server_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_user_type: {}", tTabStr, t->Get_c_user_type());
       result += std::format(L"\n{}c_user_state: {}", tTabStr, t->Get_c_user_state());
       result += std::format(L"\n{}c_user_name: {}", tTabStr, t->Get_c_user_name());
       result += std::format(L"\n{}c_user_level: {}", tTabStr, t->Get_c_user_level());
       result += std::format(L"\n{}c_user_level_highest: {}", tTabStr, t->Get_c_user_level_highest());
       result += std::format(L"\n{}c_date_ban: {}", tTabStr, t->Get_c_date_ban());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT_USER_ALL;

class ACCOUNT_USER_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT_USER_ALL;

public:
   // (7)
   explicit ACCOUNT_USER_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_ALL, buf)
   {
   }

   // (9)
   explicit ACCOUNT_USER_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AccountId account_id,
       ServerId server_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_ALL, buf)
   {
       WriteValue(account_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   // (8)
   explicit ACCOUNT_USER_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       AccountId account_id,
       ServerId server_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(account_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   ACCOUNT_USER_ALL& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT_USER_ALL*>(GetPacketBufPtr()); }
   ACCOUNT_USER_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT_USER_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT_USER_ALL : public Packet
{
   public:
      using Writer = ACCOUNT_USER_ALL_WRITER;


private:
   AccountId m_c_account_id;

   ServerId m_c_server_id;

   UserId m_c_user_id;

   UserType m_c_user_type;

   UserState m_c_user_state;

   PacketSize m_c_user_name_offset = 0;
   PacketSize m_c_user_name_length = 0;

   UserLevel m_c_user_level;

   UserLevel m_c_user_level_highest;

   time_t m_c_date_ban;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ACCOUNT_USER_ALL()
   {
       memset(this, 0, sizeof(ACCOUNT_USER_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT_USER_ALL));
   }
   ACCOUNT_USER_ALL(const ACCOUNT_USER_ALL&) = delete;
   ACCOUNT_USER_ALL(ACCOUNT_USER_ALL &&) = delete;
   ACCOUNT_USER_ALL& operator = (const ACCOUNT_USER_ALL&) = delete;
   ACCOUNT_USER_ALL& operator = (ACCOUNT_USER_ALL &&) = delete;

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   ServerId Get_c_server_id() const noexcept { return m_c_server_id; }
   ServerId& Ref_c_server_id() noexcept { return m_c_server_id; }
   const ServerId& ConstRef_c_server_id() const noexcept { return m_c_server_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   UserType Get_c_user_type() const noexcept { return m_c_user_type; }
   UserType& Ref_c_user_type() noexcept { return m_c_user_type; }
   const UserType& ConstRef_c_user_type() const noexcept { return m_c_user_type; }

   UserState Get_c_user_state() const noexcept { return m_c_user_state; }
   UserState& Ref_c_user_state() noexcept { return m_c_user_state; }
   const UserState& ConstRef_c_user_state() const noexcept { return m_c_user_state; }

   const wchar_t* Get_c_user_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   wchar_t* Ref_c_user_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   UserLevel Get_c_user_level() const noexcept { return m_c_user_level; }
   UserLevel& Ref_c_user_level() noexcept { return m_c_user_level; }
   const UserLevel& ConstRef_c_user_level() const noexcept { return m_c_user_level; }

   UserLevel Get_c_user_level_highest() const noexcept { return m_c_user_level_highest; }
   UserLevel& Ref_c_user_level_highest() noexcept { return m_c_user_level_highest; }
   const UserLevel& ConstRef_c_user_level_highest() const noexcept { return m_c_user_level_highest; }

   time_t Get_c_date_ban() const noexcept { return m_c_date_ban; }
   time_t& Ref_c_date_ban() noexcept { return m_c_date_ban; }
   const time_t& ConstRef_c_date_ban() const noexcept { return m_c_date_ban; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT_USER_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT_USER_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT_USER_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT_USER_ALL))
           {
               size += sizeof(ACCOUNT_USER_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(ServerId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(UserType);
       }

       {
           thisSize += sizeof(UserState);
       }

       {
           if (GetPacketSize() < m_c_user_name_offset)
               return false;

           if (0 < m_c_user_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_user_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT_USER_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_server_id: {}", tTabStr, t->Get_c_server_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_user_type: {}", tTabStr, t->Get_c_user_type());
       result += std::format(L"\n{}c_user_state: {}", tTabStr, t->Get_c_user_state());
       result += std::format(L"\n{}c_user_name: {}", tTabStr, t->Get_c_user_name());
       result += std::format(L"\n{}c_user_level: {}", tTabStr, t->Get_c_user_level());
       result += std::format(L"\n{}c_user_level_highest: {}", tTabStr, t->Get_c_user_level_highest());
       result += std::format(L"\n{}c_date_ban: {}", tTabStr, t->Get_c_date_ban());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACCOUNT_USER_BY_ACCOUNT;

class ACCOUNT_USER_BY_ACCOUNT_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACCOUNT_USER_BY_ACCOUNT;

public:
   // (7)
   explicit ACCOUNT_USER_BY_ACCOUNT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_BY_ACCOUNT, buf)
   {
   }

   // (9)
   explicit ACCOUNT_USER_BY_ACCOUNT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AccountId account_id,
       ServerId server_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ACCOUNT_USER_BY_ACCOUNT, buf)
   {
       WriteValue(account_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   // (8)
   explicit ACCOUNT_USER_BY_ACCOUNT_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       AccountId account_id,
       ServerId server_id,
       UserId user_id,
       UserType user_type,
       UserState user_state,
       const wchar_t* user_name,
       UserLevel user_level,
       UserLevel user_level_highest,
       time_t date_ban,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(account_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(user_type);

       WriteValue(user_state);

       PacketSize& user_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& user_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(user_level);

       WriteValue(user_level_highest);

       WriteValue(date_ban);

       WriteValue(date_updated);

       WriteValue(date_created);


       user_nameOffset = GetPacketSize();
       user_nameLength = (PacketSize)wcsnlen_s(user_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == user_nameLength) WriteString(L"", 0);
       else WriteString(user_name, user_nameLength); 

   }

   ACCOUNT_USER_BY_ACCOUNT& GetPacket() const noexcept { return *reinterpret_cast<ACCOUNT_USER_BY_ACCOUNT*>(GetPacketBufPtr()); }
   ACCOUNT_USER_BY_ACCOUNT* GetPacketPtr() const noexcept { return reinterpret_cast<ACCOUNT_USER_BY_ACCOUNT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACCOUNT_USER_BY_ACCOUNT : public Packet
{
   public:
      using Writer = ACCOUNT_USER_BY_ACCOUNT_WRITER;


private:
   AccountId m_c_account_id;

   ServerId m_c_server_id;

   UserId m_c_user_id;

   UserType m_c_user_type;

   UserState m_c_user_state;

   PacketSize m_c_user_name_offset = 0;
   PacketSize m_c_user_name_length = 0;

   UserLevel m_c_user_level;

   UserLevel m_c_user_level_highest;

   time_t m_c_date_ban;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ACCOUNT_USER_BY_ACCOUNT()
   {
       memset(this, 0, sizeof(ACCOUNT_USER_BY_ACCOUNT));
       mPacketSize = static_cast<PacketSize>(sizeof(ACCOUNT_USER_BY_ACCOUNT));
   }
   ACCOUNT_USER_BY_ACCOUNT(const ACCOUNT_USER_BY_ACCOUNT&) = delete;
   ACCOUNT_USER_BY_ACCOUNT(ACCOUNT_USER_BY_ACCOUNT &&) = delete;
   ACCOUNT_USER_BY_ACCOUNT& operator = (const ACCOUNT_USER_BY_ACCOUNT&) = delete;
   ACCOUNT_USER_BY_ACCOUNT& operator = (ACCOUNT_USER_BY_ACCOUNT &&) = delete;

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   ServerId Get_c_server_id() const noexcept { return m_c_server_id; }
   ServerId& Ref_c_server_id() noexcept { return m_c_server_id; }
   const ServerId& ConstRef_c_server_id() const noexcept { return m_c_server_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   UserType Get_c_user_type() const noexcept { return m_c_user_type; }
   UserType& Ref_c_user_type() noexcept { return m_c_user_type; }
   const UserType& ConstRef_c_user_type() const noexcept { return m_c_user_type; }

   UserState Get_c_user_state() const noexcept { return m_c_user_state; }
   UserState& Ref_c_user_state() noexcept { return m_c_user_state; }
   const UserState& ConstRef_c_user_state() const noexcept { return m_c_user_state; }

   const wchar_t* Get_c_user_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   wchar_t* Ref_c_user_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_user_name_offset) + m_c_user_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_user_name_offset);
   }

   UserLevel Get_c_user_level() const noexcept { return m_c_user_level; }
   UserLevel& Ref_c_user_level() noexcept { return m_c_user_level; }
   const UserLevel& ConstRef_c_user_level() const noexcept { return m_c_user_level; }

   UserLevel Get_c_user_level_highest() const noexcept { return m_c_user_level_highest; }
   UserLevel& Ref_c_user_level_highest() noexcept { return m_c_user_level_highest; }
   const UserLevel& ConstRef_c_user_level_highest() const noexcept { return m_c_user_level_highest; }

   time_t Get_c_date_ban() const noexcept { return m_c_date_ban; }
   time_t& Ref_c_date_ban() noexcept { return m_c_date_ban; }
   const time_t& ConstRef_c_date_ban() const noexcept { return m_c_date_ban; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACCOUNT_USER_BY_ACCOUNT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACCOUNT_USER_BY_ACCOUNT) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACCOUNT_USER_BY_ACCOUNT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACCOUNT_USER_BY_ACCOUNT))
           {
               size += sizeof(ACCOUNT_USER_BY_ACCOUNT);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(ServerId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(UserType);
       }

       {
           thisSize += sizeof(UserState);
       }

       {
           if (GetPacketSize() < m_c_user_name_offset)
               return false;

           if (0 < m_c_user_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_user_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(UserLevel);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACCOUNT_USER_BY_ACCOUNT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_server_id: {}", tTabStr, t->Get_c_server_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_user_type: {}", tTabStr, t->Get_c_user_type());
       result += std::format(L"\n{}c_user_state: {}", tTabStr, t->Get_c_user_state());
       result += std::format(L"\n{}c_user_name: {}", tTabStr, t->Get_c_user_name());
       result += std::format(L"\n{}c_user_level: {}", tTabStr, t->Get_c_user_level());
       result += std::format(L"\n{}c_user_level_highest: {}", tTabStr, t->Get_c_user_level_highest());
       result += std::format(L"\n{}c_date_ban: {}", tTabStr, t->Get_c_date_ban());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class GAME_DELETED;

class GAME_DELETED_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::GAME_DELETED;

public:
   // (7)
   explicit GAME_DELETED_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::GAME_DELETED, buf)
   {
   }

   // (9)
   explicit GAME_DELETED_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       GameId game_id,
       ServerId server_id,
       UserId user_id,
       GameSid game_sid,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::GAME_DELETED, buf)
   {
       WriteValue(id);

       WriteValue(game_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(game_sid);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   // (8)
   explicit GAME_DELETED_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       GameId game_id,
       ServerId server_id,
       UserId user_id,
       GameSid game_sid,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(game_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(game_sid);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   GAME_DELETED& GetPacket() const noexcept { return *reinterpret_cast<GAME_DELETED*>(GetPacketBufPtr()); }
   GAME_DELETED* GetPacketPtr() const noexcept { return reinterpret_cast<GAME_DELETED*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class GAME_DELETED : public Packet
{
   public:
      using Writer = GAME_DELETED_WRITER;


private:
   Uuid m_c_id;

   GameId m_c_game_id;

   ServerId m_c_server_id;

   UserId m_c_user_id;

   GameSid m_c_game_sid;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   GAME_DELETED()
   {
       memset(this, 0, sizeof(GAME_DELETED));
       mPacketSize = static_cast<PacketSize>(sizeof(GAME_DELETED));
   }
   GAME_DELETED(const GAME_DELETED&) = delete;
   GAME_DELETED(GAME_DELETED &&) = delete;
   GAME_DELETED& operator = (const GAME_DELETED&) = delete;
   GAME_DELETED& operator = (GAME_DELETED &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   GameId Get_c_game_id() const noexcept { return m_c_game_id; }
   GameId& Ref_c_game_id() noexcept { return m_c_game_id; }
   const GameId& ConstRef_c_game_id() const noexcept { return m_c_game_id; }

   ServerId Get_c_server_id() const noexcept { return m_c_server_id; }
   ServerId& Ref_c_server_id() noexcept { return m_c_server_id; }
   const ServerId& ConstRef_c_server_id() const noexcept { return m_c_server_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GameSid Get_c_game_sid() const noexcept { return m_c_game_sid; }
   GameSid& Ref_c_game_sid() noexcept { return m_c_game_sid; }
   const GameSid& ConstRef_c_game_sid() const noexcept { return m_c_game_sid; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(GAME_DELETED) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(GAME_DELETED) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(GAME_DELETED); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(GAME_DELETED))
           {
               size += sizeof(GAME_DELETED);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(GameId);
       }

       {
           thisSize += sizeof(ServerId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GameSid);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, GAME_DELETED>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_game_id: {}", tTabStr, t->Get_c_game_id());
       result += std::format(L"\n{}c_server_id: {}", tTabStr, t->Get_c_server_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_game_sid: {}", tTabStr, t->Get_c_game_sid());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class GAME_DELETED_ALL;

class GAME_DELETED_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::GAME_DELETED_ALL;

public:
   // (7)
   explicit GAME_DELETED_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::GAME_DELETED_ALL, buf)
   {
   }

   // (9)
   explicit GAME_DELETED_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       GameId game_id,
       ServerId server_id,
       UserId user_id,
       GameSid game_sid,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::GAME_DELETED_ALL, buf)
   {
       WriteValue(id);

       WriteValue(game_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(game_sid);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   // (8)
   explicit GAME_DELETED_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       GameId game_id,
       ServerId server_id,
       UserId user_id,
       GameSid game_sid,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(game_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(game_sid);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   GAME_DELETED_ALL& GetPacket() const noexcept { return *reinterpret_cast<GAME_DELETED_ALL*>(GetPacketBufPtr()); }
   GAME_DELETED_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<GAME_DELETED_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class GAME_DELETED_ALL : public Packet
{
   public:
      using Writer = GAME_DELETED_ALL_WRITER;


private:
   Uuid m_c_id;

   GameId m_c_game_id;

   ServerId m_c_server_id;

   UserId m_c_user_id;

   GameSid m_c_game_sid;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   GAME_DELETED_ALL()
   {
       memset(this, 0, sizeof(GAME_DELETED_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(GAME_DELETED_ALL));
   }
   GAME_DELETED_ALL(const GAME_DELETED_ALL&) = delete;
   GAME_DELETED_ALL(GAME_DELETED_ALL &&) = delete;
   GAME_DELETED_ALL& operator = (const GAME_DELETED_ALL&) = delete;
   GAME_DELETED_ALL& operator = (GAME_DELETED_ALL &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   GameId Get_c_game_id() const noexcept { return m_c_game_id; }
   GameId& Ref_c_game_id() noexcept { return m_c_game_id; }
   const GameId& ConstRef_c_game_id() const noexcept { return m_c_game_id; }

   ServerId Get_c_server_id() const noexcept { return m_c_server_id; }
   ServerId& Ref_c_server_id() noexcept { return m_c_server_id; }
   const ServerId& ConstRef_c_server_id() const noexcept { return m_c_server_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GameSid Get_c_game_sid() const noexcept { return m_c_game_sid; }
   GameSid& Ref_c_game_sid() noexcept { return m_c_game_sid; }
   const GameSid& ConstRef_c_game_sid() const noexcept { return m_c_game_sid; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(GAME_DELETED_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(GAME_DELETED_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(GAME_DELETED_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(GAME_DELETED_ALL))
           {
               size += sizeof(GAME_DELETED_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(GameId);
       }

       {
           thisSize += sizeof(ServerId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GameSid);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, GAME_DELETED_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_game_id: {}", tTabStr, t->Get_c_game_id());
       result += std::format(L"\n{}c_server_id: {}", tTabStr, t->Get_c_server_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_game_sid: {}", tTabStr, t->Get_c_game_sid());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class GAME;

class GAME_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::GAME;

public:
   // (7)
   explicit GAME_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::GAME, buf)
   {
   }

   // (9)
   explicit GAME_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       GameId game_id,
       ServerId server_id,
       UserId user_id,
       GameSid game_sid,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::GAME, buf)
   {
       WriteValue(game_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(game_sid);

       WriteValue(date_created);


   }

   // (8)
   explicit GAME_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       GameId game_id,
       ServerId server_id,
       UserId user_id,
       GameSid game_sid,
       time_t date_created
   )
   {
       WriteValue(game_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(game_sid);

       WriteValue(date_created);


   }

   GAME& GetPacket() const noexcept { return *reinterpret_cast<GAME*>(GetPacketBufPtr()); }
   GAME* GetPacketPtr() const noexcept { return reinterpret_cast<GAME*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class GAME : public Packet
{
   public:
      using Writer = GAME_WRITER;


private:
   GameId m_c_game_id;

   ServerId m_c_server_id;

   UserId m_c_user_id;

   GameSid m_c_game_sid;

   time_t m_c_date_created;

public:
   GAME()
   {
       memset(this, 0, sizeof(GAME));
       mPacketSize = static_cast<PacketSize>(sizeof(GAME));
   }
   GAME(const GAME&) = delete;
   GAME(GAME &&) = delete;
   GAME& operator = (const GAME&) = delete;
   GAME& operator = (GAME &&) = delete;

   GameId Get_c_game_id() const noexcept { return m_c_game_id; }
   GameId& Ref_c_game_id() noexcept { return m_c_game_id; }
   const GameId& ConstRef_c_game_id() const noexcept { return m_c_game_id; }

   ServerId Get_c_server_id() const noexcept { return m_c_server_id; }
   ServerId& Ref_c_server_id() noexcept { return m_c_server_id; }
   const ServerId& ConstRef_c_server_id() const noexcept { return m_c_server_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GameSid Get_c_game_sid() const noexcept { return m_c_game_sid; }
   GameSid& Ref_c_game_sid() noexcept { return m_c_game_sid; }
   const GameSid& ConstRef_c_game_sid() const noexcept { return m_c_game_sid; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(GAME) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(GAME) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(GAME); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(GAME))
           {
               size += sizeof(GAME);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(GameId);
       }

       {
           thisSize += sizeof(ServerId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GameSid);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, GAME>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_game_id: {}", tTabStr, t->Get_c_game_id());
       result += std::format(L"\n{}c_server_id: {}", tTabStr, t->Get_c_server_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_game_sid: {}", tTabStr, t->Get_c_game_sid());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class GAME_ALL;

class GAME_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::GAME_ALL;

public:
   // (7)
   explicit GAME_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::GAME_ALL, buf)
   {
   }

   // (9)
   explicit GAME_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       GameId game_id,
       ServerId server_id,
       UserId user_id,
       GameSid game_sid,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::GAME_ALL, buf)
   {
       WriteValue(game_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(game_sid);

       WriteValue(date_created);


   }

   // (8)
   explicit GAME_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       GameId game_id,
       ServerId server_id,
       UserId user_id,
       GameSid game_sid,
       time_t date_created
   )
   {
       WriteValue(game_id);

       WriteValue(server_id);

       WriteValue(user_id);

       WriteValue(game_sid);

       WriteValue(date_created);


   }

   GAME_ALL& GetPacket() const noexcept { return *reinterpret_cast<GAME_ALL*>(GetPacketBufPtr()); }
   GAME_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<GAME_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class GAME_ALL : public Packet
{
   public:
      using Writer = GAME_ALL_WRITER;


private:
   GameId m_c_game_id;

   ServerId m_c_server_id;

   UserId m_c_user_id;

   GameSid m_c_game_sid;

   time_t m_c_date_created;

public:
   GAME_ALL()
   {
       memset(this, 0, sizeof(GAME_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(GAME_ALL));
   }
   GAME_ALL(const GAME_ALL&) = delete;
   GAME_ALL(GAME_ALL &&) = delete;
   GAME_ALL& operator = (const GAME_ALL&) = delete;
   GAME_ALL& operator = (GAME_ALL &&) = delete;

   GameId Get_c_game_id() const noexcept { return m_c_game_id; }
   GameId& Ref_c_game_id() noexcept { return m_c_game_id; }
   const GameId& ConstRef_c_game_id() const noexcept { return m_c_game_id; }

   ServerId Get_c_server_id() const noexcept { return m_c_server_id; }
   ServerId& Ref_c_server_id() noexcept { return m_c_server_id; }
   const ServerId& ConstRef_c_server_id() const noexcept { return m_c_server_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GameSid Get_c_game_sid() const noexcept { return m_c_game_sid; }
   GameSid& Ref_c_game_sid() noexcept { return m_c_game_sid; }
   const GameSid& ConstRef_c_game_sid() const noexcept { return m_c_game_sid; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(GAME_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(GAME_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(GAME_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(GAME_ALL))
           {
               size += sizeof(GAME_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(GameId);
       }

       {
           thisSize += sizeof(ServerId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GameSid);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, GAME_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_game_id: {}", tTabStr, t->Get_c_game_id());
       result += std::format(L"\n{}c_server_id: {}", tTabStr, t->Get_c_server_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_game_sid: {}", tTabStr, t->Get_c_game_sid());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class PURCHASE_DELETED;

class PURCHASE_DELETED_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::PURCHASE_DELETED;

public:
   // (7)
   explicit PURCHASE_DELETED_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::PURCHASE_DELETED, buf)
   {
   }

   // (9)
   explicit PURCHASE_DELETED_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       PurchaseId purchase_id,
       AccountId account_id,
       UserId user_id,
       GuildId guild_id,
       ProductSid product_sid,
       const wchar_t* product_receipt,
       PurchaseState purchase_state,
       MailId mail_id,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::PURCHASE_DELETED, buf)
   {
       WriteValue(id);

       WriteValue(purchase_id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(guild_id);

       WriteValue(product_sid);

       PacketSize& product_receiptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_receiptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(purchase_state);

       WriteValue(mail_id);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       product_receiptOffset = GetPacketSize();
       product_receiptLength = (PacketSize)wcsnlen_s(product_receipt, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_receiptLength) WriteString(L"", 0);
       else WriteString(product_receipt, product_receiptLength); 

   }

   // (8)
   explicit PURCHASE_DELETED_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       PurchaseId purchase_id,
       AccountId account_id,
       UserId user_id,
       GuildId guild_id,
       ProductSid product_sid,
       const wchar_t* product_receipt,
       PurchaseState purchase_state,
       MailId mail_id,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(purchase_id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(guild_id);

       WriteValue(product_sid);

       PacketSize& product_receiptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_receiptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(purchase_state);

       WriteValue(mail_id);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       product_receiptOffset = GetPacketSize();
       product_receiptLength = (PacketSize)wcsnlen_s(product_receipt, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_receiptLength) WriteString(L"", 0);
       else WriteString(product_receipt, product_receiptLength); 

   }

   PURCHASE_DELETED& GetPacket() const noexcept { return *reinterpret_cast<PURCHASE_DELETED*>(GetPacketBufPtr()); }
   PURCHASE_DELETED* GetPacketPtr() const noexcept { return reinterpret_cast<PURCHASE_DELETED*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class PURCHASE_DELETED : public Packet
{
   public:
      using Writer = PURCHASE_DELETED_WRITER;


private:
   Uuid m_c_id;

   PurchaseId m_c_purchase_id;

   AccountId m_c_account_id;

   UserId m_c_user_id;

   GuildId m_c_guild_id;

   ProductSid m_c_product_sid;

   PacketSize m_c_product_receipt_offset = 0;
   PacketSize m_c_product_receipt_length = 0;

   PurchaseState m_c_purchase_state;

   MailId m_c_mail_id;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   PURCHASE_DELETED()
   {
       memset(this, 0, sizeof(PURCHASE_DELETED));
       mPacketSize = static_cast<PacketSize>(sizeof(PURCHASE_DELETED));
   }
   PURCHASE_DELETED(const PURCHASE_DELETED&) = delete;
   PURCHASE_DELETED(PURCHASE_DELETED &&) = delete;
   PURCHASE_DELETED& operator = (const PURCHASE_DELETED&) = delete;
   PURCHASE_DELETED& operator = (PURCHASE_DELETED &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   PurchaseId Get_c_purchase_id() const noexcept { return m_c_purchase_id; }
   PurchaseId& Ref_c_purchase_id() noexcept { return m_c_purchase_id; }
   const PurchaseId& ConstRef_c_purchase_id() const noexcept { return m_c_purchase_id; }

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GuildId Get_c_guild_id() const noexcept { return m_c_guild_id; }
   GuildId& Ref_c_guild_id() noexcept { return m_c_guild_id; }
   const GuildId& ConstRef_c_guild_id() const noexcept { return m_c_guild_id; }

   ProductSid Get_c_product_sid() const noexcept { return m_c_product_sid; }
   ProductSid& Ref_c_product_sid() noexcept { return m_c_product_sid; }
   const ProductSid& ConstRef_c_product_sid() const noexcept { return m_c_product_sid; }

   const wchar_t* Get_c_product_receipt() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_product_receipt_offset) + m_c_product_receipt_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_product_receipt_offset);
   }

   wchar_t* Ref_c_product_receipt()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_product_receipt_offset) + m_c_product_receipt_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_product_receipt_offset);
   }

   PurchaseState Get_c_purchase_state() const noexcept { return m_c_purchase_state; }
   PurchaseState& Ref_c_purchase_state() noexcept { return m_c_purchase_state; }
   const PurchaseState& ConstRef_c_purchase_state() const noexcept { return m_c_purchase_state; }

   MailId Get_c_mail_id() const noexcept { return m_c_mail_id; }
   MailId& Ref_c_mail_id() noexcept { return m_c_mail_id; }
   const MailId& ConstRef_c_mail_id() const noexcept { return m_c_mail_id; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(PURCHASE_DELETED) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(PURCHASE_DELETED) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(PURCHASE_DELETED); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(PURCHASE_DELETED))
           {
               size += sizeof(PURCHASE_DELETED);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(PurchaseId);
       }

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GuildId);
       }

       {
           thisSize += sizeof(ProductSid);
       }

       {
           if (GetPacketSize() < m_c_product_receipt_offset)
               return false;

           if (0 < m_c_product_receipt_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_product_receipt_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(PurchaseState);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, PURCHASE_DELETED>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_purchase_id: {}", tTabStr, t->Get_c_purchase_id());
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_guild_id: {}", tTabStr, t->Get_c_guild_id());
       result += std::format(L"\n{}c_product_sid: {}", tTabStr, t->Get_c_product_sid());
       result += std::format(L"\n{}c_product_receipt: {}", tTabStr, t->Get_c_product_receipt());
       result += std::format(L"\n{}c_purchase_state: {}", tTabStr, t->Get_c_purchase_state());
       result += std::format(L"\n{}c_mail_id: {}", tTabStr, t->Get_c_mail_id());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class PURCHASE_DELETED_ALL;

class PURCHASE_DELETED_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::PURCHASE_DELETED_ALL;

public:
   // (7)
   explicit PURCHASE_DELETED_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::PURCHASE_DELETED_ALL, buf)
   {
   }

   // (9)
   explicit PURCHASE_DELETED_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       PurchaseId purchase_id,
       AccountId account_id,
       UserId user_id,
       GuildId guild_id,
       ProductSid product_sid,
       const wchar_t* product_receipt,
       PurchaseState purchase_state,
       MailId mail_id,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::PURCHASE_DELETED_ALL, buf)
   {
       WriteValue(id);

       WriteValue(purchase_id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(guild_id);

       WriteValue(product_sid);

       PacketSize& product_receiptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_receiptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(purchase_state);

       WriteValue(mail_id);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       product_receiptOffset = GetPacketSize();
       product_receiptLength = (PacketSize)wcsnlen_s(product_receipt, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_receiptLength) WriteString(L"", 0);
       else WriteString(product_receipt, product_receiptLength); 

   }

   // (8)
   explicit PURCHASE_DELETED_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       PurchaseId purchase_id,
       AccountId account_id,
       UserId user_id,
       GuildId guild_id,
       ProductSid product_sid,
       const wchar_t* product_receipt,
       PurchaseState purchase_state,
       MailId mail_id,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(purchase_id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(guild_id);

       WriteValue(product_sid);

       PacketSize& product_receiptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_receiptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(purchase_state);

       WriteValue(mail_id);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       product_receiptOffset = GetPacketSize();
       product_receiptLength = (PacketSize)wcsnlen_s(product_receipt, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_receiptLength) WriteString(L"", 0);
       else WriteString(product_receipt, product_receiptLength); 

   }

   PURCHASE_DELETED_ALL& GetPacket() const noexcept { return *reinterpret_cast<PURCHASE_DELETED_ALL*>(GetPacketBufPtr()); }
   PURCHASE_DELETED_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<PURCHASE_DELETED_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class PURCHASE_DELETED_ALL : public Packet
{
   public:
      using Writer = PURCHASE_DELETED_ALL_WRITER;


private:
   Uuid m_c_id;

   PurchaseId m_c_purchase_id;

   AccountId m_c_account_id;

   UserId m_c_user_id;

   GuildId m_c_guild_id;

   ProductSid m_c_product_sid;

   PacketSize m_c_product_receipt_offset = 0;
   PacketSize m_c_product_receipt_length = 0;

   PurchaseState m_c_purchase_state;

   MailId m_c_mail_id;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   PURCHASE_DELETED_ALL()
   {
       memset(this, 0, sizeof(PURCHASE_DELETED_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(PURCHASE_DELETED_ALL));
   }
   PURCHASE_DELETED_ALL(const PURCHASE_DELETED_ALL&) = delete;
   PURCHASE_DELETED_ALL(PURCHASE_DELETED_ALL &&) = delete;
   PURCHASE_DELETED_ALL& operator = (const PURCHASE_DELETED_ALL&) = delete;
   PURCHASE_DELETED_ALL& operator = (PURCHASE_DELETED_ALL &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   PurchaseId Get_c_purchase_id() const noexcept { return m_c_purchase_id; }
   PurchaseId& Ref_c_purchase_id() noexcept { return m_c_purchase_id; }
   const PurchaseId& ConstRef_c_purchase_id() const noexcept { return m_c_purchase_id; }

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GuildId Get_c_guild_id() const noexcept { return m_c_guild_id; }
   GuildId& Ref_c_guild_id() noexcept { return m_c_guild_id; }
   const GuildId& ConstRef_c_guild_id() const noexcept { return m_c_guild_id; }

   ProductSid Get_c_product_sid() const noexcept { return m_c_product_sid; }
   ProductSid& Ref_c_product_sid() noexcept { return m_c_product_sid; }
   const ProductSid& ConstRef_c_product_sid() const noexcept { return m_c_product_sid; }

   const wchar_t* Get_c_product_receipt() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_product_receipt_offset) + m_c_product_receipt_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_product_receipt_offset);
   }

   wchar_t* Ref_c_product_receipt()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_product_receipt_offset) + m_c_product_receipt_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_product_receipt_offset);
   }

   PurchaseState Get_c_purchase_state() const noexcept { return m_c_purchase_state; }
   PurchaseState& Ref_c_purchase_state() noexcept { return m_c_purchase_state; }
   const PurchaseState& ConstRef_c_purchase_state() const noexcept { return m_c_purchase_state; }

   MailId Get_c_mail_id() const noexcept { return m_c_mail_id; }
   MailId& Ref_c_mail_id() noexcept { return m_c_mail_id; }
   const MailId& ConstRef_c_mail_id() const noexcept { return m_c_mail_id; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(PURCHASE_DELETED_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(PURCHASE_DELETED_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(PURCHASE_DELETED_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(PURCHASE_DELETED_ALL))
           {
               size += sizeof(PURCHASE_DELETED_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(PurchaseId);
       }

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GuildId);
       }

       {
           thisSize += sizeof(ProductSid);
       }

       {
           if (GetPacketSize() < m_c_product_receipt_offset)
               return false;

           if (0 < m_c_product_receipt_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_product_receipt_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(PurchaseState);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, PURCHASE_DELETED_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_purchase_id: {}", tTabStr, t->Get_c_purchase_id());
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_guild_id: {}", tTabStr, t->Get_c_guild_id());
       result += std::format(L"\n{}c_product_sid: {}", tTabStr, t->Get_c_product_sid());
       result += std::format(L"\n{}c_product_receipt: {}", tTabStr, t->Get_c_product_receipt());
       result += std::format(L"\n{}c_purchase_state: {}", tTabStr, t->Get_c_purchase_state());
       result += std::format(L"\n{}c_mail_id: {}", tTabStr, t->Get_c_mail_id());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class PURCHASE;

class PURCHASE_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::PURCHASE;

public:
   // (7)
   explicit PURCHASE_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::PURCHASE, buf)
   {
   }

   // (9)
   explicit PURCHASE_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       PurchaseId purchase_id,
       AccountId account_id,
       UserId user_id,
       GuildId guild_id,
       ProductSid product_sid,
       const wchar_t* product_receipt,
       PurchaseState purchase_state,
       MailId mail_id,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::PURCHASE, buf)
   {
       WriteValue(purchase_id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(guild_id);

       WriteValue(product_sid);

       PacketSize& product_receiptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_receiptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(purchase_state);

       WriteValue(mail_id);

       WriteValue(date_updated);

       WriteValue(date_created);


       product_receiptOffset = GetPacketSize();
       product_receiptLength = (PacketSize)wcsnlen_s(product_receipt, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_receiptLength) WriteString(L"", 0);
       else WriteString(product_receipt, product_receiptLength); 

   }

   // (8)
   explicit PURCHASE_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       PurchaseId purchase_id,
       AccountId account_id,
       UserId user_id,
       GuildId guild_id,
       ProductSid product_sid,
       const wchar_t* product_receipt,
       PurchaseState purchase_state,
       MailId mail_id,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(purchase_id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(guild_id);

       WriteValue(product_sid);

       PacketSize& product_receiptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_receiptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(purchase_state);

       WriteValue(mail_id);

       WriteValue(date_updated);

       WriteValue(date_created);


       product_receiptOffset = GetPacketSize();
       product_receiptLength = (PacketSize)wcsnlen_s(product_receipt, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_receiptLength) WriteString(L"", 0);
       else WriteString(product_receipt, product_receiptLength); 

   }

   PURCHASE& GetPacket() const noexcept { return *reinterpret_cast<PURCHASE*>(GetPacketBufPtr()); }
   PURCHASE* GetPacketPtr() const noexcept { return reinterpret_cast<PURCHASE*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class PURCHASE : public Packet
{
   public:
      using Writer = PURCHASE_WRITER;


private:
   PurchaseId m_c_purchase_id;

   AccountId m_c_account_id;

   UserId m_c_user_id;

   GuildId m_c_guild_id;

   ProductSid m_c_product_sid;

   PacketSize m_c_product_receipt_offset = 0;
   PacketSize m_c_product_receipt_length = 0;

   PurchaseState m_c_purchase_state;

   MailId m_c_mail_id;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   PURCHASE()
   {
       memset(this, 0, sizeof(PURCHASE));
       mPacketSize = static_cast<PacketSize>(sizeof(PURCHASE));
   }
   PURCHASE(const PURCHASE&) = delete;
   PURCHASE(PURCHASE &&) = delete;
   PURCHASE& operator = (const PURCHASE&) = delete;
   PURCHASE& operator = (PURCHASE &&) = delete;

   PurchaseId Get_c_purchase_id() const noexcept { return m_c_purchase_id; }
   PurchaseId& Ref_c_purchase_id() noexcept { return m_c_purchase_id; }
   const PurchaseId& ConstRef_c_purchase_id() const noexcept { return m_c_purchase_id; }

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GuildId Get_c_guild_id() const noexcept { return m_c_guild_id; }
   GuildId& Ref_c_guild_id() noexcept { return m_c_guild_id; }
   const GuildId& ConstRef_c_guild_id() const noexcept { return m_c_guild_id; }

   ProductSid Get_c_product_sid() const noexcept { return m_c_product_sid; }
   ProductSid& Ref_c_product_sid() noexcept { return m_c_product_sid; }
   const ProductSid& ConstRef_c_product_sid() const noexcept { return m_c_product_sid; }

   const wchar_t* Get_c_product_receipt() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_product_receipt_offset) + m_c_product_receipt_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_product_receipt_offset);
   }

   wchar_t* Ref_c_product_receipt()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_product_receipt_offset) + m_c_product_receipt_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_product_receipt_offset);
   }

   PurchaseState Get_c_purchase_state() const noexcept { return m_c_purchase_state; }
   PurchaseState& Ref_c_purchase_state() noexcept { return m_c_purchase_state; }
   const PurchaseState& ConstRef_c_purchase_state() const noexcept { return m_c_purchase_state; }

   MailId Get_c_mail_id() const noexcept { return m_c_mail_id; }
   MailId& Ref_c_mail_id() noexcept { return m_c_mail_id; }
   const MailId& ConstRef_c_mail_id() const noexcept { return m_c_mail_id; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(PURCHASE) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(PURCHASE) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(PURCHASE); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(PURCHASE))
           {
               size += sizeof(PURCHASE);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(PurchaseId);
       }

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GuildId);
       }

       {
           thisSize += sizeof(ProductSid);
       }

       {
           if (GetPacketSize() < m_c_product_receipt_offset)
               return false;

           if (0 < m_c_product_receipt_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_product_receipt_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(PurchaseState);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, PURCHASE>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_purchase_id: {}", tTabStr, t->Get_c_purchase_id());
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_guild_id: {}", tTabStr, t->Get_c_guild_id());
       result += std::format(L"\n{}c_product_sid: {}", tTabStr, t->Get_c_product_sid());
       result += std::format(L"\n{}c_product_receipt: {}", tTabStr, t->Get_c_product_receipt());
       result += std::format(L"\n{}c_purchase_state: {}", tTabStr, t->Get_c_purchase_state());
       result += std::format(L"\n{}c_mail_id: {}", tTabStr, t->Get_c_mail_id());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class PURCHASE_ALL;

class PURCHASE_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::PURCHASE_ALL;

public:
   // (7)
   explicit PURCHASE_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::PURCHASE_ALL, buf)
   {
   }

   // (9)
   explicit PURCHASE_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       PurchaseId purchase_id,
       AccountId account_id,
       UserId user_id,
       GuildId guild_id,
       ProductSid product_sid,
       const wchar_t* product_receipt,
       PurchaseState purchase_state,
       MailId mail_id,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::PURCHASE_ALL, buf)
   {
       WriteValue(purchase_id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(guild_id);

       WriteValue(product_sid);

       PacketSize& product_receiptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_receiptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(purchase_state);

       WriteValue(mail_id);

       WriteValue(date_updated);

       WriteValue(date_created);


       product_receiptOffset = GetPacketSize();
       product_receiptLength = (PacketSize)wcsnlen_s(product_receipt, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_receiptLength) WriteString(L"", 0);
       else WriteString(product_receipt, product_receiptLength); 

   }

   // (8)
   explicit PURCHASE_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       PurchaseId purchase_id,
       AccountId account_id,
       UserId user_id,
       GuildId guild_id,
       ProductSid product_sid,
       const wchar_t* product_receipt,
       PurchaseState purchase_state,
       MailId mail_id,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(purchase_id);

       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(guild_id);

       WriteValue(product_sid);

       PacketSize& product_receiptOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& product_receiptLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(purchase_state);

       WriteValue(mail_id);

       WriteValue(date_updated);

       WriteValue(date_created);


       product_receiptOffset = GetPacketSize();
       product_receiptLength = (PacketSize)wcsnlen_s(product_receipt, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == product_receiptLength) WriteString(L"", 0);
       else WriteString(product_receipt, product_receiptLength); 

   }

   PURCHASE_ALL& GetPacket() const noexcept { return *reinterpret_cast<PURCHASE_ALL*>(GetPacketBufPtr()); }
   PURCHASE_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<PURCHASE_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class PURCHASE_ALL : public Packet
{
   public:
      using Writer = PURCHASE_ALL_WRITER;


private:
   PurchaseId m_c_purchase_id;

   AccountId m_c_account_id;

   UserId m_c_user_id;

   GuildId m_c_guild_id;

   ProductSid m_c_product_sid;

   PacketSize m_c_product_receipt_offset = 0;
   PacketSize m_c_product_receipt_length = 0;

   PurchaseState m_c_purchase_state;

   MailId m_c_mail_id;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   PURCHASE_ALL()
   {
       memset(this, 0, sizeof(PURCHASE_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(PURCHASE_ALL));
   }
   PURCHASE_ALL(const PURCHASE_ALL&) = delete;
   PURCHASE_ALL(PURCHASE_ALL &&) = delete;
   PURCHASE_ALL& operator = (const PURCHASE_ALL&) = delete;
   PURCHASE_ALL& operator = (PURCHASE_ALL &&) = delete;

   PurchaseId Get_c_purchase_id() const noexcept { return m_c_purchase_id; }
   PurchaseId& Ref_c_purchase_id() noexcept { return m_c_purchase_id; }
   const PurchaseId& ConstRef_c_purchase_id() const noexcept { return m_c_purchase_id; }

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GuildId Get_c_guild_id() const noexcept { return m_c_guild_id; }
   GuildId& Ref_c_guild_id() noexcept { return m_c_guild_id; }
   const GuildId& ConstRef_c_guild_id() const noexcept { return m_c_guild_id; }

   ProductSid Get_c_product_sid() const noexcept { return m_c_product_sid; }
   ProductSid& Ref_c_product_sid() noexcept { return m_c_product_sid; }
   const ProductSid& ConstRef_c_product_sid() const noexcept { return m_c_product_sid; }

   const wchar_t* Get_c_product_receipt() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_product_receipt_offset) + m_c_product_receipt_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_product_receipt_offset);
   }

   wchar_t* Ref_c_product_receipt()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_product_receipt_offset) + m_c_product_receipt_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_product_receipt_offset);
   }

   PurchaseState Get_c_purchase_state() const noexcept { return m_c_purchase_state; }
   PurchaseState& Ref_c_purchase_state() noexcept { return m_c_purchase_state; }
   const PurchaseState& ConstRef_c_purchase_state() const noexcept { return m_c_purchase_state; }

   MailId Get_c_mail_id() const noexcept { return m_c_mail_id; }
   MailId& Ref_c_mail_id() noexcept { return m_c_mail_id; }
   const MailId& ConstRef_c_mail_id() const noexcept { return m_c_mail_id; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(PURCHASE_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(PURCHASE_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(PURCHASE_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(PURCHASE_ALL))
           {
               size += sizeof(PURCHASE_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(PurchaseId);
       }

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GuildId);
       }

       {
           thisSize += sizeof(ProductSid);
       }

       {
           if (GetPacketSize() < m_c_product_receipt_offset)
               return false;

           if (0 < m_c_product_receipt_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_product_receipt_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(PurchaseState);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, PURCHASE_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_purchase_id: {}", tTabStr, t->Get_c_purchase_id());
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_guild_id: {}", tTabStr, t->Get_c_guild_id());
       result += std::format(L"\n{}c_product_sid: {}", tTabStr, t->Get_c_product_sid());
       result += std::format(L"\n{}c_product_receipt: {}", tTabStr, t->Get_c_product_receipt());
       result += std::format(L"\n{}c_purchase_state: {}", tTabStr, t->Get_c_purchase_state());
       result += std::format(L"\n{}c_mail_id: {}", tTabStr, t->Get_c_mail_id());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class SERVER;

class SERVER_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::SERVER;

public:
   // (7)
   explicit SERVER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::SERVER, buf)
   {
   }

   // (9)
   explicit SERVER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ServerId server_id,
       const wchar_t* server_name,
       GameSid home_game_sid,
       ServerState server_state,
       int64_t wau_count_max,
       int64_t wau_count_busy,
       int64_t ccu_count_max,
       int64_t ccu_count_busy
   )
       :
       PacketWriter(trait, PacketTypes::SERVER, buf)
   {
       WriteValue(server_id);

       PacketSize& server_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& server_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(home_game_sid);

       WriteValue(server_state);

       WriteValue(wau_count_max);

       WriteValue(wau_count_busy);

       WriteValue(ccu_count_max);

       WriteValue(ccu_count_busy);


       server_nameOffset = GetPacketSize();
       server_nameLength = (PacketSize)wcsnlen_s(server_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == server_nameLength) WriteString(L"", 0);
       else WriteString(server_name, server_nameLength); 

   }

   // (8)
   explicit SERVER_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ServerId server_id,
       const wchar_t* server_name,
       GameSid home_game_sid,
       ServerState server_state,
       int64_t wau_count_max,
       int64_t wau_count_busy,
       int64_t ccu_count_max,
       int64_t ccu_count_busy
   )
   {
       WriteValue(server_id);

       PacketSize& server_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& server_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(home_game_sid);

       WriteValue(server_state);

       WriteValue(wau_count_max);

       WriteValue(wau_count_busy);

       WriteValue(ccu_count_max);

       WriteValue(ccu_count_busy);


       server_nameOffset = GetPacketSize();
       server_nameLength = (PacketSize)wcsnlen_s(server_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == server_nameLength) WriteString(L"", 0);
       else WriteString(server_name, server_nameLength); 

   }

   SERVER& GetPacket() const noexcept { return *reinterpret_cast<SERVER*>(GetPacketBufPtr()); }
   SERVER* GetPacketPtr() const noexcept { return reinterpret_cast<SERVER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class SERVER : public Packet
{
   public:
      using Writer = SERVER_WRITER;


private:
   ServerId m_c_server_id;

   PacketSize m_c_server_name_offset = 0;
   PacketSize m_c_server_name_length = 0;

   GameSid m_c_home_game_sid;

   ServerState m_c_server_state;

   int64_t m_c_wau_count_max;

   int64_t m_c_wau_count_busy;

   int64_t m_c_ccu_count_max;

   int64_t m_c_ccu_count_busy;

public:
   SERVER()
   {
       memset(this, 0, sizeof(SERVER));
       mPacketSize = static_cast<PacketSize>(sizeof(SERVER));
   }
   SERVER(const SERVER&) = delete;
   SERVER(SERVER &&) = delete;
   SERVER& operator = (const SERVER&) = delete;
   SERVER& operator = (SERVER &&) = delete;

   ServerId Get_c_server_id() const noexcept { return m_c_server_id; }
   ServerId& Ref_c_server_id() noexcept { return m_c_server_id; }
   const ServerId& ConstRef_c_server_id() const noexcept { return m_c_server_id; }

   const wchar_t* Get_c_server_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_server_name_offset) + m_c_server_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_server_name_offset);
   }

   wchar_t* Ref_c_server_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_server_name_offset) + m_c_server_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_server_name_offset);
   }

   GameSid Get_c_home_game_sid() const noexcept { return m_c_home_game_sid; }
   GameSid& Ref_c_home_game_sid() noexcept { return m_c_home_game_sid; }
   const GameSid& ConstRef_c_home_game_sid() const noexcept { return m_c_home_game_sid; }

   ServerState Get_c_server_state() const noexcept { return m_c_server_state; }
   ServerState& Ref_c_server_state() noexcept { return m_c_server_state; }
   const ServerState& ConstRef_c_server_state() const noexcept { return m_c_server_state; }

   int64_t Get_c_wau_count_max() const noexcept { return m_c_wau_count_max; }
   int64_t& Ref_c_wau_count_max() noexcept { return m_c_wau_count_max; }
   const int64_t& ConstRef_c_wau_count_max() const noexcept { return m_c_wau_count_max; }

   int64_t Get_c_wau_count_busy() const noexcept { return m_c_wau_count_busy; }
   int64_t& Ref_c_wau_count_busy() noexcept { return m_c_wau_count_busy; }
   const int64_t& ConstRef_c_wau_count_busy() const noexcept { return m_c_wau_count_busy; }

   int64_t Get_c_ccu_count_max() const noexcept { return m_c_ccu_count_max; }
   int64_t& Ref_c_ccu_count_max() noexcept { return m_c_ccu_count_max; }
   const int64_t& ConstRef_c_ccu_count_max() const noexcept { return m_c_ccu_count_max; }

   int64_t Get_c_ccu_count_busy() const noexcept { return m_c_ccu_count_busy; }
   int64_t& Ref_c_ccu_count_busy() noexcept { return m_c_ccu_count_busy; }
   const int64_t& ConstRef_c_ccu_count_busy() const noexcept { return m_c_ccu_count_busy; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(SERVER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(SERVER) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(SERVER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(SERVER))
           {
               size += sizeof(SERVER);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ServerId);
       }

       {
           if (GetPacketSize() < m_c_server_name_offset)
               return false;

           if (0 < m_c_server_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_server_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(GameSid);
       }

       {
           thisSize += sizeof(ServerState);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, SERVER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_server_id: {}", tTabStr, t->Get_c_server_id());
       result += std::format(L"\n{}c_server_name: {}", tTabStr, t->Get_c_server_name());
       result += std::format(L"\n{}c_home_game_sid: {}", tTabStr, t->Get_c_home_game_sid());
       result += std::format(L"\n{}c_server_state: {}", tTabStr, t->Get_c_server_state());
       result += std::format(L"\n{}c_wau_count_max: {}", tTabStr, t->Get_c_wau_count_max());
       result += std::format(L"\n{}c_wau_count_busy: {}", tTabStr, t->Get_c_wau_count_busy());
       result += std::format(L"\n{}c_ccu_count_max: {}", tTabStr, t->Get_c_ccu_count_max());
       result += std::format(L"\n{}c_ccu_count_busy: {}", tTabStr, t->Get_c_ccu_count_busy());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class SERVER_ALL;

class SERVER_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::SERVER_ALL;

public:
   // (7)
   explicit SERVER_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::SERVER_ALL, buf)
   {
   }

   // (9)
   explicit SERVER_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ServerId server_id,
       const wchar_t* server_name,
       GameSid home_game_sid,
       ServerState server_state,
       int64_t wau_count_max,
       int64_t wau_count_busy,
       int64_t ccu_count_max,
       int64_t ccu_count_busy
   )
       :
       PacketWriter(trait, PacketTypes::SERVER_ALL, buf)
   {
       WriteValue(server_id);

       PacketSize& server_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& server_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(home_game_sid);

       WriteValue(server_state);

       WriteValue(wau_count_max);

       WriteValue(wau_count_busy);

       WriteValue(ccu_count_max);

       WriteValue(ccu_count_busy);


       server_nameOffset = GetPacketSize();
       server_nameLength = (PacketSize)wcsnlen_s(server_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == server_nameLength) WriteString(L"", 0);
       else WriteString(server_name, server_nameLength); 

   }

   // (8)
   explicit SERVER_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ServerId server_id,
       const wchar_t* server_name,
       GameSid home_game_sid,
       ServerState server_state,
       int64_t wau_count_max,
       int64_t wau_count_busy,
       int64_t ccu_count_max,
       int64_t ccu_count_busy
   )
   {
       WriteValue(server_id);

       PacketSize& server_nameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& server_nameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(home_game_sid);

       WriteValue(server_state);

       WriteValue(wau_count_max);

       WriteValue(wau_count_busy);

       WriteValue(ccu_count_max);

       WriteValue(ccu_count_busy);


       server_nameOffset = GetPacketSize();
       server_nameLength = (PacketSize)wcsnlen_s(server_name, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == server_nameLength) WriteString(L"", 0);
       else WriteString(server_name, server_nameLength); 

   }

   SERVER_ALL& GetPacket() const noexcept { return *reinterpret_cast<SERVER_ALL*>(GetPacketBufPtr()); }
   SERVER_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<SERVER_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class SERVER_ALL : public Packet
{
   public:
      using Writer = SERVER_ALL_WRITER;


private:
   ServerId m_c_server_id;

   PacketSize m_c_server_name_offset = 0;
   PacketSize m_c_server_name_length = 0;

   GameSid m_c_home_game_sid;

   ServerState m_c_server_state;

   int64_t m_c_wau_count_max;

   int64_t m_c_wau_count_busy;

   int64_t m_c_ccu_count_max;

   int64_t m_c_ccu_count_busy;

public:
   SERVER_ALL()
   {
       memset(this, 0, sizeof(SERVER_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(SERVER_ALL));
   }
   SERVER_ALL(const SERVER_ALL&) = delete;
   SERVER_ALL(SERVER_ALL &&) = delete;
   SERVER_ALL& operator = (const SERVER_ALL&) = delete;
   SERVER_ALL& operator = (SERVER_ALL &&) = delete;

   ServerId Get_c_server_id() const noexcept { return m_c_server_id; }
   ServerId& Ref_c_server_id() noexcept { return m_c_server_id; }
   const ServerId& ConstRef_c_server_id() const noexcept { return m_c_server_id; }

   const wchar_t* Get_c_server_name() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_server_name_offset) + m_c_server_name_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_server_name_offset);
   }

   wchar_t* Ref_c_server_name()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_server_name_offset) + m_c_server_name_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_server_name_offset);
   }

   GameSid Get_c_home_game_sid() const noexcept { return m_c_home_game_sid; }
   GameSid& Ref_c_home_game_sid() noexcept { return m_c_home_game_sid; }
   const GameSid& ConstRef_c_home_game_sid() const noexcept { return m_c_home_game_sid; }

   ServerState Get_c_server_state() const noexcept { return m_c_server_state; }
   ServerState& Ref_c_server_state() noexcept { return m_c_server_state; }
   const ServerState& ConstRef_c_server_state() const noexcept { return m_c_server_state; }

   int64_t Get_c_wau_count_max() const noexcept { return m_c_wau_count_max; }
   int64_t& Ref_c_wau_count_max() noexcept { return m_c_wau_count_max; }
   const int64_t& ConstRef_c_wau_count_max() const noexcept { return m_c_wau_count_max; }

   int64_t Get_c_wau_count_busy() const noexcept { return m_c_wau_count_busy; }
   int64_t& Ref_c_wau_count_busy() noexcept { return m_c_wau_count_busy; }
   const int64_t& ConstRef_c_wau_count_busy() const noexcept { return m_c_wau_count_busy; }

   int64_t Get_c_ccu_count_max() const noexcept { return m_c_ccu_count_max; }
   int64_t& Ref_c_ccu_count_max() noexcept { return m_c_ccu_count_max; }
   const int64_t& ConstRef_c_ccu_count_max() const noexcept { return m_c_ccu_count_max; }

   int64_t Get_c_ccu_count_busy() const noexcept { return m_c_ccu_count_busy; }
   int64_t& Ref_c_ccu_count_busy() noexcept { return m_c_ccu_count_busy; }
   const int64_t& ConstRef_c_ccu_count_busy() const noexcept { return m_c_ccu_count_busy; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(SERVER_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(SERVER_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(SERVER_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(SERVER_ALL))
           {
               size += sizeof(SERVER_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ServerId);
       }

       {
           if (GetPacketSize() < m_c_server_name_offset)
               return false;

           if (0 < m_c_server_name_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_server_name_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(GameSid);
       }

       {
           thisSize += sizeof(ServerState);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, SERVER_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_server_id: {}", tTabStr, t->Get_c_server_id());
       result += std::format(L"\n{}c_server_name: {}", tTabStr, t->Get_c_server_name());
       result += std::format(L"\n{}c_home_game_sid: {}", tTabStr, t->Get_c_home_game_sid());
       result += std::format(L"\n{}c_server_state: {}", tTabStr, t->Get_c_server_state());
       result += std::format(L"\n{}c_wau_count_max: {}", tTabStr, t->Get_c_wau_count_max());
       result += std::format(L"\n{}c_wau_count_busy: {}", tTabStr, t->Get_c_wau_count_busy());
       result += std::format(L"\n{}c_ccu_count_max: {}", tTabStr, t->Get_c_ccu_count_max());
       result += std::format(L"\n{}c_ccu_count_busy: {}", tTabStr, t->Get_c_ccu_count_busy());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class SERVER_STATISTICS;

class SERVER_STATISTICS_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::SERVER_STATISTICS;

public:
   // (7)
   explicit SERVER_STATISTICS_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::SERVER_STATISTICS, buf)
   {
   }

   // (9)
   explicit SERVER_STATISTICS_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ServerId server_id,
       int64_t tuc_count,
       int64_t mau_count,
       int64_t wau_count,
       int64_t ccu_count
   )
       :
       PacketWriter(trait, PacketTypes::SERVER_STATISTICS, buf)
   {
       WriteValue(server_id);

       WriteValue(tuc_count);

       WriteValue(mau_count);

       WriteValue(wau_count);

       WriteValue(ccu_count);


   }

   // (8)
   explicit SERVER_STATISTICS_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ServerId server_id,
       int64_t tuc_count,
       int64_t mau_count,
       int64_t wau_count,
       int64_t ccu_count
   )
   {
       WriteValue(server_id);

       WriteValue(tuc_count);

       WriteValue(mau_count);

       WriteValue(wau_count);

       WriteValue(ccu_count);


   }

   SERVER_STATISTICS& GetPacket() const noexcept { return *reinterpret_cast<SERVER_STATISTICS*>(GetPacketBufPtr()); }
   SERVER_STATISTICS* GetPacketPtr() const noexcept { return reinterpret_cast<SERVER_STATISTICS*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class SERVER_STATISTICS : public Packet
{
   public:
      using Writer = SERVER_STATISTICS_WRITER;


private:
   ServerId m_c_server_id;

   int64_t m_c_tuc_count;

   int64_t m_c_mau_count;

   int64_t m_c_wau_count;

   int64_t m_c_ccu_count;

public:
   SERVER_STATISTICS()
   {
       memset(this, 0, sizeof(SERVER_STATISTICS));
       mPacketSize = static_cast<PacketSize>(sizeof(SERVER_STATISTICS));
   }
   SERVER_STATISTICS(const SERVER_STATISTICS&) = delete;
   SERVER_STATISTICS(SERVER_STATISTICS &&) = delete;
   SERVER_STATISTICS& operator = (const SERVER_STATISTICS&) = delete;
   SERVER_STATISTICS& operator = (SERVER_STATISTICS &&) = delete;

   ServerId Get_c_server_id() const noexcept { return m_c_server_id; }
   ServerId& Ref_c_server_id() noexcept { return m_c_server_id; }
   const ServerId& ConstRef_c_server_id() const noexcept { return m_c_server_id; }

   int64_t Get_c_tuc_count() const noexcept { return m_c_tuc_count; }
   int64_t& Ref_c_tuc_count() noexcept { return m_c_tuc_count; }
   const int64_t& ConstRef_c_tuc_count() const noexcept { return m_c_tuc_count; }

   int64_t Get_c_mau_count() const noexcept { return m_c_mau_count; }
   int64_t& Ref_c_mau_count() noexcept { return m_c_mau_count; }
   const int64_t& ConstRef_c_mau_count() const noexcept { return m_c_mau_count; }

   int64_t Get_c_wau_count() const noexcept { return m_c_wau_count; }
   int64_t& Ref_c_wau_count() noexcept { return m_c_wau_count; }
   const int64_t& ConstRef_c_wau_count() const noexcept { return m_c_wau_count; }

   int64_t Get_c_ccu_count() const noexcept { return m_c_ccu_count; }
   int64_t& Ref_c_ccu_count() noexcept { return m_c_ccu_count; }
   const int64_t& ConstRef_c_ccu_count() const noexcept { return m_c_ccu_count; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(SERVER_STATISTICS) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(SERVER_STATISTICS) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(SERVER_STATISTICS); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(SERVER_STATISTICS))
           {
               size += sizeof(SERVER_STATISTICS);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ServerId);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, SERVER_STATISTICS>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_server_id: {}", tTabStr, t->Get_c_server_id());
       result += std::format(L"\n{}c_tuc_count: {}", tTabStr, t->Get_c_tuc_count());
       result += std::format(L"\n{}c_mau_count: {}", tTabStr, t->Get_c_mau_count());
       result += std::format(L"\n{}c_wau_count: {}", tTabStr, t->Get_c_wau_count());
       result += std::format(L"\n{}c_ccu_count: {}", tTabStr, t->Get_c_ccu_count());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class SERVER_STATISTICS_ALL;

class SERVER_STATISTICS_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::SERVER_STATISTICS_ALL;

public:
   // (7)
   explicit SERVER_STATISTICS_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::SERVER_STATISTICS_ALL, buf)
   {
   }

   // (9)
   explicit SERVER_STATISTICS_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ServerId server_id,
       int64_t tuc_count,
       int64_t mau_count,
       int64_t wau_count,
       int64_t ccu_count
   )
       :
       PacketWriter(trait, PacketTypes::SERVER_STATISTICS_ALL, buf)
   {
       WriteValue(server_id);

       WriteValue(tuc_count);

       WriteValue(mau_count);

       WriteValue(wau_count);

       WriteValue(ccu_count);


   }

   // (8)
   explicit SERVER_STATISTICS_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       ServerId server_id,
       int64_t tuc_count,
       int64_t mau_count,
       int64_t wau_count,
       int64_t ccu_count
   )
   {
       WriteValue(server_id);

       WriteValue(tuc_count);

       WriteValue(mau_count);

       WriteValue(wau_count);

       WriteValue(ccu_count);


   }

   SERVER_STATISTICS_ALL& GetPacket() const noexcept { return *reinterpret_cast<SERVER_STATISTICS_ALL*>(GetPacketBufPtr()); }
   SERVER_STATISTICS_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<SERVER_STATISTICS_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class SERVER_STATISTICS_ALL : public Packet
{
   public:
      using Writer = SERVER_STATISTICS_ALL_WRITER;


private:
   ServerId m_c_server_id;

   int64_t m_c_tuc_count;

   int64_t m_c_mau_count;

   int64_t m_c_wau_count;

   int64_t m_c_ccu_count;

public:
   SERVER_STATISTICS_ALL()
   {
       memset(this, 0, sizeof(SERVER_STATISTICS_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(SERVER_STATISTICS_ALL));
   }
   SERVER_STATISTICS_ALL(const SERVER_STATISTICS_ALL&) = delete;
   SERVER_STATISTICS_ALL(SERVER_STATISTICS_ALL &&) = delete;
   SERVER_STATISTICS_ALL& operator = (const SERVER_STATISTICS_ALL&) = delete;
   SERVER_STATISTICS_ALL& operator = (SERVER_STATISTICS_ALL &&) = delete;

   ServerId Get_c_server_id() const noexcept { return m_c_server_id; }
   ServerId& Ref_c_server_id() noexcept { return m_c_server_id; }
   const ServerId& ConstRef_c_server_id() const noexcept { return m_c_server_id; }

   int64_t Get_c_tuc_count() const noexcept { return m_c_tuc_count; }
   int64_t& Ref_c_tuc_count() noexcept { return m_c_tuc_count; }
   const int64_t& ConstRef_c_tuc_count() const noexcept { return m_c_tuc_count; }

   int64_t Get_c_mau_count() const noexcept { return m_c_mau_count; }
   int64_t& Ref_c_mau_count() noexcept { return m_c_mau_count; }
   const int64_t& ConstRef_c_mau_count() const noexcept { return m_c_mau_count; }

   int64_t Get_c_wau_count() const noexcept { return m_c_wau_count; }
   int64_t& Ref_c_wau_count() noexcept { return m_c_wau_count; }
   const int64_t& ConstRef_c_wau_count() const noexcept { return m_c_wau_count; }

   int64_t Get_c_ccu_count() const noexcept { return m_c_ccu_count; }
   int64_t& Ref_c_ccu_count() noexcept { return m_c_ccu_count; }
   const int64_t& ConstRef_c_ccu_count() const noexcept { return m_c_ccu_count; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(SERVER_STATISTICS_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(SERVER_STATISTICS_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(SERVER_STATISTICS_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(SERVER_STATISTICS_ALL))
           {
               size += sizeof(SERVER_STATISTICS_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(ServerId);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, SERVER_STATISTICS_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_server_id: {}", tTabStr, t->Get_c_server_id());
       result += std::format(L"\n{}c_tuc_count: {}", tTabStr, t->Get_c_tuc_count());
       result += std::format(L"\n{}c_mau_count: {}", tTabStr, t->Get_c_mau_count());
       result += std::format(L"\n{}c_wau_count: {}", tTabStr, t->Get_c_wau_count());
       result += std::format(L"\n{}c_ccu_count: {}", tTabStr, t->Get_c_ccu_count());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACHIEVEMENT;

class ACHIEVEMENT_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACHIEVEMENT;

public:
   // (7)
   explicit ACHIEVEMENT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACHIEVEMENT, buf)
   {
   }

   // (9)
   explicit ACHIEVEMENT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       AchievementId achievement_id,
       AchievementSid achievement_sid,
       AchievementStep achievement_step,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ACHIEVEMENT, buf)
   {
       WriteValue(user_id);

       WriteValue(achievement_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit ACHIEVEMENT_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       AchievementId achievement_id,
       AchievementSid achievement_sid,
       AchievementStep achievement_step,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(achievement_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   ACHIEVEMENT& GetPacket() const noexcept { return *reinterpret_cast<ACHIEVEMENT*>(GetPacketBufPtr()); }
   ACHIEVEMENT* GetPacketPtr() const noexcept { return reinterpret_cast<ACHIEVEMENT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACHIEVEMENT : public Packet
{
   public:
      using Writer = ACHIEVEMENT_WRITER;


private:
   UserId m_c_user_id;

   AchievementId m_c_achievement_id;

   AchievementSid m_c_achievement_sid;

   AchievementStep m_c_achievement_step;

   RewardFlag m_c_reward_flag;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ACHIEVEMENT()
   {
       memset(this, 0, sizeof(ACHIEVEMENT));
       mPacketSize = static_cast<PacketSize>(sizeof(ACHIEVEMENT));
   }
   ACHIEVEMENT(const ACHIEVEMENT&) = delete;
   ACHIEVEMENT(ACHIEVEMENT &&) = delete;
   ACHIEVEMENT& operator = (const ACHIEVEMENT&) = delete;
   ACHIEVEMENT& operator = (ACHIEVEMENT &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   AchievementId Get_c_achievement_id() const noexcept { return m_c_achievement_id; }
   AchievementId& Ref_c_achievement_id() noexcept { return m_c_achievement_id; }
   const AchievementId& ConstRef_c_achievement_id() const noexcept { return m_c_achievement_id; }

   AchievementSid Get_c_achievement_sid() const noexcept { return m_c_achievement_sid; }
   AchievementSid& Ref_c_achievement_sid() noexcept { return m_c_achievement_sid; }
   const AchievementSid& ConstRef_c_achievement_sid() const noexcept { return m_c_achievement_sid; }

   AchievementStep Get_c_achievement_step() const noexcept { return m_c_achievement_step; }
   AchievementStep& Ref_c_achievement_step() noexcept { return m_c_achievement_step; }
   const AchievementStep& ConstRef_c_achievement_step() const noexcept { return m_c_achievement_step; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACHIEVEMENT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACHIEVEMENT) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACHIEVEMENT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACHIEVEMENT))
           {
               size += sizeof(ACHIEVEMENT);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(AchievementId);
       }

       {
           thisSize += sizeof(AchievementSid);
       }

       {
           thisSize += sizeof(AchievementStep);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACHIEVEMENT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_achievement_id: {}", tTabStr, t->Get_c_achievement_id());
       result += std::format(L"\n{}c_achievement_sid: {}", tTabStr, t->Get_c_achievement_sid());
       result += std::format(L"\n{}c_achievement_step: {}", tTabStr, t->Get_c_achievement_step());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACHIEVEMENT_ALL;

class ACHIEVEMENT_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACHIEVEMENT_ALL;

public:
   // (7)
   explicit ACHIEVEMENT_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACHIEVEMENT_ALL, buf)
   {
   }

   // (9)
   explicit ACHIEVEMENT_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       AchievementId achievement_id,
       AchievementSid achievement_sid,
       AchievementStep achievement_step,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ACHIEVEMENT_ALL, buf)
   {
       WriteValue(user_id);

       WriteValue(achievement_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit ACHIEVEMENT_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       AchievementId achievement_id,
       AchievementSid achievement_sid,
       AchievementStep achievement_step,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(achievement_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   ACHIEVEMENT_ALL& GetPacket() const noexcept { return *reinterpret_cast<ACHIEVEMENT_ALL*>(GetPacketBufPtr()); }
   ACHIEVEMENT_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<ACHIEVEMENT_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACHIEVEMENT_ALL : public Packet
{
   public:
      using Writer = ACHIEVEMENT_ALL_WRITER;


private:
   UserId m_c_user_id;

   AchievementId m_c_achievement_id;

   AchievementSid m_c_achievement_sid;

   AchievementStep m_c_achievement_step;

   RewardFlag m_c_reward_flag;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ACHIEVEMENT_ALL()
   {
       memset(this, 0, sizeof(ACHIEVEMENT_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(ACHIEVEMENT_ALL));
   }
   ACHIEVEMENT_ALL(const ACHIEVEMENT_ALL&) = delete;
   ACHIEVEMENT_ALL(ACHIEVEMENT_ALL &&) = delete;
   ACHIEVEMENT_ALL& operator = (const ACHIEVEMENT_ALL&) = delete;
   ACHIEVEMENT_ALL& operator = (ACHIEVEMENT_ALL &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   AchievementId Get_c_achievement_id() const noexcept { return m_c_achievement_id; }
   AchievementId& Ref_c_achievement_id() noexcept { return m_c_achievement_id; }
   const AchievementId& ConstRef_c_achievement_id() const noexcept { return m_c_achievement_id; }

   AchievementSid Get_c_achievement_sid() const noexcept { return m_c_achievement_sid; }
   AchievementSid& Ref_c_achievement_sid() noexcept { return m_c_achievement_sid; }
   const AchievementSid& ConstRef_c_achievement_sid() const noexcept { return m_c_achievement_sid; }

   AchievementStep Get_c_achievement_step() const noexcept { return m_c_achievement_step; }
   AchievementStep& Ref_c_achievement_step() noexcept { return m_c_achievement_step; }
   const AchievementStep& ConstRef_c_achievement_step() const noexcept { return m_c_achievement_step; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACHIEVEMENT_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACHIEVEMENT_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACHIEVEMENT_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACHIEVEMENT_ALL))
           {
               size += sizeof(ACHIEVEMENT_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(AchievementId);
       }

       {
           thisSize += sizeof(AchievementSid);
       }

       {
           thisSize += sizeof(AchievementStep);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACHIEVEMENT_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_achievement_id: {}", tTabStr, t->Get_c_achievement_id());
       result += std::format(L"\n{}c_achievement_sid: {}", tTabStr, t->Get_c_achievement_sid());
       result += std::format(L"\n{}c_achievement_step: {}", tTabStr, t->Get_c_achievement_step());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACHIEVEMENT_BY_USER;

class ACHIEVEMENT_BY_USER_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACHIEVEMENT_BY_USER;

public:
   // (7)
   explicit ACHIEVEMENT_BY_USER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACHIEVEMENT_BY_USER, buf)
   {
   }

   // (9)
   explicit ACHIEVEMENT_BY_USER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       AchievementId achievement_id,
       AchievementSid achievement_sid,
       AchievementStep achievement_step,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ACHIEVEMENT_BY_USER, buf)
   {
       WriteValue(user_id);

       WriteValue(achievement_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit ACHIEVEMENT_BY_USER_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       AchievementId achievement_id,
       AchievementSid achievement_sid,
       AchievementStep achievement_step,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(achievement_id);

       WriteValue(achievement_sid);

       WriteValue(achievement_step);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   ACHIEVEMENT_BY_USER& GetPacket() const noexcept { return *reinterpret_cast<ACHIEVEMENT_BY_USER*>(GetPacketBufPtr()); }
   ACHIEVEMENT_BY_USER* GetPacketPtr() const noexcept { return reinterpret_cast<ACHIEVEMENT_BY_USER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACHIEVEMENT_BY_USER : public Packet
{
   public:
      using Writer = ACHIEVEMENT_BY_USER_WRITER;


private:
   UserId m_c_user_id;

   AchievementId m_c_achievement_id;

   AchievementSid m_c_achievement_sid;

   AchievementStep m_c_achievement_step;

   RewardFlag m_c_reward_flag;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ACHIEVEMENT_BY_USER()
   {
       memset(this, 0, sizeof(ACHIEVEMENT_BY_USER));
       mPacketSize = static_cast<PacketSize>(sizeof(ACHIEVEMENT_BY_USER));
   }
   ACHIEVEMENT_BY_USER(const ACHIEVEMENT_BY_USER&) = delete;
   ACHIEVEMENT_BY_USER(ACHIEVEMENT_BY_USER &&) = delete;
   ACHIEVEMENT_BY_USER& operator = (const ACHIEVEMENT_BY_USER&) = delete;
   ACHIEVEMENT_BY_USER& operator = (ACHIEVEMENT_BY_USER &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   AchievementId Get_c_achievement_id() const noexcept { return m_c_achievement_id; }
   AchievementId& Ref_c_achievement_id() noexcept { return m_c_achievement_id; }
   const AchievementId& ConstRef_c_achievement_id() const noexcept { return m_c_achievement_id; }

   AchievementSid Get_c_achievement_sid() const noexcept { return m_c_achievement_sid; }
   AchievementSid& Ref_c_achievement_sid() noexcept { return m_c_achievement_sid; }
   const AchievementSid& ConstRef_c_achievement_sid() const noexcept { return m_c_achievement_sid; }

   AchievementStep Get_c_achievement_step() const noexcept { return m_c_achievement_step; }
   AchievementStep& Ref_c_achievement_step() noexcept { return m_c_achievement_step; }
   const AchievementStep& ConstRef_c_achievement_step() const noexcept { return m_c_achievement_step; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACHIEVEMENT_BY_USER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACHIEVEMENT_BY_USER) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACHIEVEMENT_BY_USER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACHIEVEMENT_BY_USER))
           {
               size += sizeof(ACHIEVEMENT_BY_USER);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(AchievementId);
       }

       {
           thisSize += sizeof(AchievementSid);
       }

       {
           thisSize += sizeof(AchievementStep);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACHIEVEMENT_BY_USER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_achievement_id: {}", tTabStr, t->Get_c_achievement_id());
       result += std::format(L"\n{}c_achievement_sid: {}", tTabStr, t->Get_c_achievement_sid());
       result += std::format(L"\n{}c_achievement_step: {}", tTabStr, t->Get_c_achievement_step());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACTIVITY;

class ACTIVITY_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACTIVITY;

public:
   // (7)
   explicit ACTIVITY_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACTIVITY, buf)
   {
   }

   // (9)
   explicit ACTIVITY_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       ActivityId activity_id,
       ActivitySid activity_sid,
       ActivityPoint activity_point,
       ActivityPoint activity_point_increased,
       ActivityPoint activity_point_decreased,
       ActivityPoint activity_point_min,
       ActivityPoint activity_point_max,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ACTIVITY, buf)
   {
       WriteValue(user_id);

       WriteValue(activity_id);

       WriteValue(activity_sid);

       WriteValue(activity_point);

       WriteValue(activity_point_increased);

       WriteValue(activity_point_decreased);

       WriteValue(activity_point_min);

       WriteValue(activity_point_max);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit ACTIVITY_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       ActivityId activity_id,
       ActivitySid activity_sid,
       ActivityPoint activity_point,
       ActivityPoint activity_point_increased,
       ActivityPoint activity_point_decreased,
       ActivityPoint activity_point_min,
       ActivityPoint activity_point_max,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(activity_id);

       WriteValue(activity_sid);

       WriteValue(activity_point);

       WriteValue(activity_point_increased);

       WriteValue(activity_point_decreased);

       WriteValue(activity_point_min);

       WriteValue(activity_point_max);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   ACTIVITY& GetPacket() const noexcept { return *reinterpret_cast<ACTIVITY*>(GetPacketBufPtr()); }
   ACTIVITY* GetPacketPtr() const noexcept { return reinterpret_cast<ACTIVITY*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACTIVITY : public Packet
{
   public:
      using Writer = ACTIVITY_WRITER;


private:
   UserId m_c_user_id;

   ActivityId m_c_activity_id;

   ActivitySid m_c_activity_sid;

   ActivityPoint m_c_activity_point;

   ActivityPoint m_c_activity_point_increased;

   ActivityPoint m_c_activity_point_decreased;

   ActivityPoint m_c_activity_point_min;

   ActivityPoint m_c_activity_point_max;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ACTIVITY()
   {
       memset(this, 0, sizeof(ACTIVITY));
       mPacketSize = static_cast<PacketSize>(sizeof(ACTIVITY));
   }
   ACTIVITY(const ACTIVITY&) = delete;
   ACTIVITY(ACTIVITY &&) = delete;
   ACTIVITY& operator = (const ACTIVITY&) = delete;
   ACTIVITY& operator = (ACTIVITY &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   ActivityId Get_c_activity_id() const noexcept { return m_c_activity_id; }
   ActivityId& Ref_c_activity_id() noexcept { return m_c_activity_id; }
   const ActivityId& ConstRef_c_activity_id() const noexcept { return m_c_activity_id; }

   ActivitySid Get_c_activity_sid() const noexcept { return m_c_activity_sid; }
   ActivitySid& Ref_c_activity_sid() noexcept { return m_c_activity_sid; }
   const ActivitySid& ConstRef_c_activity_sid() const noexcept { return m_c_activity_sid; }

   ActivityPoint Get_c_activity_point() const noexcept { return m_c_activity_point; }
   ActivityPoint& Ref_c_activity_point() noexcept { return m_c_activity_point; }
   const ActivityPoint& ConstRef_c_activity_point() const noexcept { return m_c_activity_point; }

   ActivityPoint Get_c_activity_point_increased() const noexcept { return m_c_activity_point_increased; }
   ActivityPoint& Ref_c_activity_point_increased() noexcept { return m_c_activity_point_increased; }
   const ActivityPoint& ConstRef_c_activity_point_increased() const noexcept { return m_c_activity_point_increased; }

   ActivityPoint Get_c_activity_point_decreased() const noexcept { return m_c_activity_point_decreased; }
   ActivityPoint& Ref_c_activity_point_decreased() noexcept { return m_c_activity_point_decreased; }
   const ActivityPoint& ConstRef_c_activity_point_decreased() const noexcept { return m_c_activity_point_decreased; }

   ActivityPoint Get_c_activity_point_min() const noexcept { return m_c_activity_point_min; }
   ActivityPoint& Ref_c_activity_point_min() noexcept { return m_c_activity_point_min; }
   const ActivityPoint& ConstRef_c_activity_point_min() const noexcept { return m_c_activity_point_min; }

   ActivityPoint Get_c_activity_point_max() const noexcept { return m_c_activity_point_max; }
   ActivityPoint& Ref_c_activity_point_max() noexcept { return m_c_activity_point_max; }
   const ActivityPoint& ConstRef_c_activity_point_max() const noexcept { return m_c_activity_point_max; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACTIVITY) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACTIVITY) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACTIVITY); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACTIVITY))
           {
               size += sizeof(ACTIVITY);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(ActivityId);
       }

       {
           thisSize += sizeof(ActivitySid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACTIVITY>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_activity_id: {}", tTabStr, t->Get_c_activity_id());
       result += std::format(L"\n{}c_activity_sid: {}", tTabStr, t->Get_c_activity_sid());
       result += std::format(L"\n{}c_activity_point: {}", tTabStr, t->Get_c_activity_point());
       result += std::format(L"\n{}c_activity_point_increased: {}", tTabStr, t->Get_c_activity_point_increased());
       result += std::format(L"\n{}c_activity_point_decreased: {}", tTabStr, t->Get_c_activity_point_decreased());
       result += std::format(L"\n{}c_activity_point_min: {}", tTabStr, t->Get_c_activity_point_min());
       result += std::format(L"\n{}c_activity_point_max: {}", tTabStr, t->Get_c_activity_point_max());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACTIVITY_ALL;

class ACTIVITY_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACTIVITY_ALL;

public:
   // (7)
   explicit ACTIVITY_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACTIVITY_ALL, buf)
   {
   }

   // (9)
   explicit ACTIVITY_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       ActivityId activity_id,
       ActivitySid activity_sid,
       ActivityPoint activity_point,
       ActivityPoint activity_point_increased,
       ActivityPoint activity_point_decreased,
       ActivityPoint activity_point_min,
       ActivityPoint activity_point_max,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ACTIVITY_ALL, buf)
   {
       WriteValue(user_id);

       WriteValue(activity_id);

       WriteValue(activity_sid);

       WriteValue(activity_point);

       WriteValue(activity_point_increased);

       WriteValue(activity_point_decreased);

       WriteValue(activity_point_min);

       WriteValue(activity_point_max);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit ACTIVITY_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       ActivityId activity_id,
       ActivitySid activity_sid,
       ActivityPoint activity_point,
       ActivityPoint activity_point_increased,
       ActivityPoint activity_point_decreased,
       ActivityPoint activity_point_min,
       ActivityPoint activity_point_max,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(activity_id);

       WriteValue(activity_sid);

       WriteValue(activity_point);

       WriteValue(activity_point_increased);

       WriteValue(activity_point_decreased);

       WriteValue(activity_point_min);

       WriteValue(activity_point_max);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   ACTIVITY_ALL& GetPacket() const noexcept { return *reinterpret_cast<ACTIVITY_ALL*>(GetPacketBufPtr()); }
   ACTIVITY_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<ACTIVITY_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACTIVITY_ALL : public Packet
{
   public:
      using Writer = ACTIVITY_ALL_WRITER;


private:
   UserId m_c_user_id;

   ActivityId m_c_activity_id;

   ActivitySid m_c_activity_sid;

   ActivityPoint m_c_activity_point;

   ActivityPoint m_c_activity_point_increased;

   ActivityPoint m_c_activity_point_decreased;

   ActivityPoint m_c_activity_point_min;

   ActivityPoint m_c_activity_point_max;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ACTIVITY_ALL()
   {
       memset(this, 0, sizeof(ACTIVITY_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(ACTIVITY_ALL));
   }
   ACTIVITY_ALL(const ACTIVITY_ALL&) = delete;
   ACTIVITY_ALL(ACTIVITY_ALL &&) = delete;
   ACTIVITY_ALL& operator = (const ACTIVITY_ALL&) = delete;
   ACTIVITY_ALL& operator = (ACTIVITY_ALL &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   ActivityId Get_c_activity_id() const noexcept { return m_c_activity_id; }
   ActivityId& Ref_c_activity_id() noexcept { return m_c_activity_id; }
   const ActivityId& ConstRef_c_activity_id() const noexcept { return m_c_activity_id; }

   ActivitySid Get_c_activity_sid() const noexcept { return m_c_activity_sid; }
   ActivitySid& Ref_c_activity_sid() noexcept { return m_c_activity_sid; }
   const ActivitySid& ConstRef_c_activity_sid() const noexcept { return m_c_activity_sid; }

   ActivityPoint Get_c_activity_point() const noexcept { return m_c_activity_point; }
   ActivityPoint& Ref_c_activity_point() noexcept { return m_c_activity_point; }
   const ActivityPoint& ConstRef_c_activity_point() const noexcept { return m_c_activity_point; }

   ActivityPoint Get_c_activity_point_increased() const noexcept { return m_c_activity_point_increased; }
   ActivityPoint& Ref_c_activity_point_increased() noexcept { return m_c_activity_point_increased; }
   const ActivityPoint& ConstRef_c_activity_point_increased() const noexcept { return m_c_activity_point_increased; }

   ActivityPoint Get_c_activity_point_decreased() const noexcept { return m_c_activity_point_decreased; }
   ActivityPoint& Ref_c_activity_point_decreased() noexcept { return m_c_activity_point_decreased; }
   const ActivityPoint& ConstRef_c_activity_point_decreased() const noexcept { return m_c_activity_point_decreased; }

   ActivityPoint Get_c_activity_point_min() const noexcept { return m_c_activity_point_min; }
   ActivityPoint& Ref_c_activity_point_min() noexcept { return m_c_activity_point_min; }
   const ActivityPoint& ConstRef_c_activity_point_min() const noexcept { return m_c_activity_point_min; }

   ActivityPoint Get_c_activity_point_max() const noexcept { return m_c_activity_point_max; }
   ActivityPoint& Ref_c_activity_point_max() noexcept { return m_c_activity_point_max; }
   const ActivityPoint& ConstRef_c_activity_point_max() const noexcept { return m_c_activity_point_max; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACTIVITY_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACTIVITY_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACTIVITY_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACTIVITY_ALL))
           {
               size += sizeof(ACTIVITY_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(ActivityId);
       }

       {
           thisSize += sizeof(ActivitySid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACTIVITY_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_activity_id: {}", tTabStr, t->Get_c_activity_id());
       result += std::format(L"\n{}c_activity_sid: {}", tTabStr, t->Get_c_activity_sid());
       result += std::format(L"\n{}c_activity_point: {}", tTabStr, t->Get_c_activity_point());
       result += std::format(L"\n{}c_activity_point_increased: {}", tTabStr, t->Get_c_activity_point_increased());
       result += std::format(L"\n{}c_activity_point_decreased: {}", tTabStr, t->Get_c_activity_point_decreased());
       result += std::format(L"\n{}c_activity_point_min: {}", tTabStr, t->Get_c_activity_point_min());
       result += std::format(L"\n{}c_activity_point_max: {}", tTabStr, t->Get_c_activity_point_max());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACTIVITY_BY_USER;

class ACTIVITY_BY_USER_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACTIVITY_BY_USER;

public:
   // (7)
   explicit ACTIVITY_BY_USER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ACTIVITY_BY_USER, buf)
   {
   }

   // (9)
   explicit ACTIVITY_BY_USER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       ActivityId activity_id,
       ActivitySid activity_sid,
       ActivityPoint activity_point,
       ActivityPoint activity_point_increased,
       ActivityPoint activity_point_decreased,
       ActivityPoint activity_point_min,
       ActivityPoint activity_point_max,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ACTIVITY_BY_USER, buf)
   {
       WriteValue(user_id);

       WriteValue(activity_id);

       WriteValue(activity_sid);

       WriteValue(activity_point);

       WriteValue(activity_point_increased);

       WriteValue(activity_point_decreased);

       WriteValue(activity_point_min);

       WriteValue(activity_point_max);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit ACTIVITY_BY_USER_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       ActivityId activity_id,
       ActivitySid activity_sid,
       ActivityPoint activity_point,
       ActivityPoint activity_point_increased,
       ActivityPoint activity_point_decreased,
       ActivityPoint activity_point_min,
       ActivityPoint activity_point_max,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(activity_id);

       WriteValue(activity_sid);

       WriteValue(activity_point);

       WriteValue(activity_point_increased);

       WriteValue(activity_point_decreased);

       WriteValue(activity_point_min);

       WriteValue(activity_point_max);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   ACTIVITY_BY_USER& GetPacket() const noexcept { return *reinterpret_cast<ACTIVITY_BY_USER*>(GetPacketBufPtr()); }
   ACTIVITY_BY_USER* GetPacketPtr() const noexcept { return reinterpret_cast<ACTIVITY_BY_USER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACTIVITY_BY_USER : public Packet
{
   public:
      using Writer = ACTIVITY_BY_USER_WRITER;


private:
   UserId m_c_user_id;

   ActivityId m_c_activity_id;

   ActivitySid m_c_activity_sid;

   ActivityPoint m_c_activity_point;

   ActivityPoint m_c_activity_point_increased;

   ActivityPoint m_c_activity_point_decreased;

   ActivityPoint m_c_activity_point_min;

   ActivityPoint m_c_activity_point_max;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ACTIVITY_BY_USER()
   {
       memset(this, 0, sizeof(ACTIVITY_BY_USER));
       mPacketSize = static_cast<PacketSize>(sizeof(ACTIVITY_BY_USER));
   }
   ACTIVITY_BY_USER(const ACTIVITY_BY_USER&) = delete;
   ACTIVITY_BY_USER(ACTIVITY_BY_USER &&) = delete;
   ACTIVITY_BY_USER& operator = (const ACTIVITY_BY_USER&) = delete;
   ACTIVITY_BY_USER& operator = (ACTIVITY_BY_USER &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   ActivityId Get_c_activity_id() const noexcept { return m_c_activity_id; }
   ActivityId& Ref_c_activity_id() noexcept { return m_c_activity_id; }
   const ActivityId& ConstRef_c_activity_id() const noexcept { return m_c_activity_id; }

   ActivitySid Get_c_activity_sid() const noexcept { return m_c_activity_sid; }
   ActivitySid& Ref_c_activity_sid() noexcept { return m_c_activity_sid; }
   const ActivitySid& ConstRef_c_activity_sid() const noexcept { return m_c_activity_sid; }

   ActivityPoint Get_c_activity_point() const noexcept { return m_c_activity_point; }
   ActivityPoint& Ref_c_activity_point() noexcept { return m_c_activity_point; }
   const ActivityPoint& ConstRef_c_activity_point() const noexcept { return m_c_activity_point; }

   ActivityPoint Get_c_activity_point_increased() const noexcept { return m_c_activity_point_increased; }
   ActivityPoint& Ref_c_activity_point_increased() noexcept { return m_c_activity_point_increased; }
   const ActivityPoint& ConstRef_c_activity_point_increased() const noexcept { return m_c_activity_point_increased; }

   ActivityPoint Get_c_activity_point_decreased() const noexcept { return m_c_activity_point_decreased; }
   ActivityPoint& Ref_c_activity_point_decreased() noexcept { return m_c_activity_point_decreased; }
   const ActivityPoint& ConstRef_c_activity_point_decreased() const noexcept { return m_c_activity_point_decreased; }

   ActivityPoint Get_c_activity_point_min() const noexcept { return m_c_activity_point_min; }
   ActivityPoint& Ref_c_activity_point_min() noexcept { return m_c_activity_point_min; }
   const ActivityPoint& ConstRef_c_activity_point_min() const noexcept { return m_c_activity_point_min; }

   ActivityPoint Get_c_activity_point_max() const noexcept { return m_c_activity_point_max; }
   ActivityPoint& Ref_c_activity_point_max() noexcept { return m_c_activity_point_max; }
   const ActivityPoint& ConstRef_c_activity_point_max() const noexcept { return m_c_activity_point_max; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACTIVITY_BY_USER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACTIVITY_BY_USER) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ACTIVITY_BY_USER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACTIVITY_BY_USER))
           {
               size += sizeof(ACTIVITY_BY_USER);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(ActivityId);
       }

       {
           thisSize += sizeof(ActivitySid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACTIVITY_BY_USER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_activity_id: {}", tTabStr, t->Get_c_activity_id());
       result += std::format(L"\n{}c_activity_sid: {}", tTabStr, t->Get_c_activity_sid());
       result += std::format(L"\n{}c_activity_point: {}", tTabStr, t->Get_c_activity_point());
       result += std::format(L"\n{}c_activity_point_increased: {}", tTabStr, t->Get_c_activity_point_increased());
       result += std::format(L"\n{}c_activity_point_decreased: {}", tTabStr, t->Get_c_activity_point_decreased());
       result += std::format(L"\n{}c_activity_point_min: {}", tTabStr, t->Get_c_activity_point_min());
       result += std::format(L"\n{}c_activity_point_max: {}", tTabStr, t->Get_c_activity_point_max());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ITEM_DELETED;

class ITEM_DELETED_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ITEM_DELETED;

public:
   // (7)
   explicit ITEM_DELETED_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ITEM_DELETED, buf)
   {
   }

   // (9)
   explicit ITEM_DELETED_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       UserId user_id,
       ItemId item_id,
       ItemId parent_item_id,
       MailId parent_mail_id,
       ItemSid item_sid,
       int64_t inventory_slot_index,
       int64_t quick_slot_index,
       int64_t equip_slot_index,
       ItemQuantity item_quantity,
       ItemLevel item_level,
       ItemColorUnion item_color_union,
       ItemStatUnion item_stat_union,
       ItemEnchantUnion item_enchant_union,
       ItemStateFlag item_state_flag,
       time_t date_cooltime,
       time_t date_expire,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::ITEM_DELETED, buf)
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(parent_item_id);

       WriteValue(parent_mail_id);

       WriteValue(item_sid);

       WriteValue(inventory_slot_index);

       WriteValue(quick_slot_index);

       WriteValue(equip_slot_index);

       WriteValue(item_quantity);

       WriteValue(item_level);

       WriteValue(item_color_union);

       WriteValue(item_stat_union);

       WriteValue(item_enchant_union);

       WriteValue(item_state_flag);

       WriteValue(date_cooltime);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   // (8)
   explicit ITEM_DELETED_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       UserId user_id,
       ItemId item_id,
       ItemId parent_item_id,
       MailId parent_mail_id,
       ItemSid item_sid,
       int64_t inventory_slot_index,
       int64_t quick_slot_index,
       int64_t equip_slot_index,
       ItemQuantity item_quantity,
       ItemLevel item_level,
       ItemColorUnion item_color_union,
       ItemStatUnion item_stat_union,
       ItemEnchantUnion item_enchant_union,
       ItemStateFlag item_state_flag,
       time_t date_cooltime,
       time_t date_expire,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(parent_item_id);

       WriteValue(parent_mail_id);

       WriteValue(item_sid);

       WriteValue(inventory_slot_index);

       WriteValue(quick_slot_index);

       WriteValue(equip_slot_index);

       WriteValue(item_quantity);

       WriteValue(item_level);

       WriteValue(item_color_union);

       WriteValue(item_stat_union);

       WriteValue(item_enchant_union);

       WriteValue(item_state_flag);

       WriteValue(date_cooltime);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   ITEM_DELETED& GetPacket() const noexcept { return *reinterpret_cast<ITEM_DELETED*>(GetPacketBufPtr()); }
   ITEM_DELETED* GetPacketPtr() const noexcept { return reinterpret_cast<ITEM_DELETED*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ITEM_DELETED : public Packet
{
   public:
      using Writer = ITEM_DELETED_WRITER;


private:
   Uuid m_c_id;

   UserId m_c_user_id;

   ItemId m_c_item_id;

   ItemId m_c_parent_item_id;

   MailId m_c_parent_mail_id;

   ItemSid m_c_item_sid;

   int64_t m_c_inventory_slot_index;

   int64_t m_c_quick_slot_index;

   int64_t m_c_equip_slot_index;

   ItemQuantity m_c_item_quantity;

   ItemLevel m_c_item_level;

   ItemColorUnion m_c_item_color_union;

   ItemStatUnion m_c_item_stat_union;

   ItemEnchantUnion m_c_item_enchant_union;

   ItemStateFlag m_c_item_state_flag;

   time_t m_c_date_cooltime;

   time_t m_c_date_expire;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   ITEM_DELETED()
   {
       memset(this, 0, sizeof(ITEM_DELETED));
       mPacketSize = static_cast<PacketSize>(sizeof(ITEM_DELETED));
   }
   ITEM_DELETED(const ITEM_DELETED&) = delete;
   ITEM_DELETED(ITEM_DELETED &&) = delete;
   ITEM_DELETED& operator = (const ITEM_DELETED&) = delete;
   ITEM_DELETED& operator = (ITEM_DELETED &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   ItemId Get_c_item_id() const noexcept { return m_c_item_id; }
   ItemId& Ref_c_item_id() noexcept { return m_c_item_id; }
   const ItemId& ConstRef_c_item_id() const noexcept { return m_c_item_id; }

   ItemId Get_c_parent_item_id() const noexcept { return m_c_parent_item_id; }
   ItemId& Ref_c_parent_item_id() noexcept { return m_c_parent_item_id; }
   const ItemId& ConstRef_c_parent_item_id() const noexcept { return m_c_parent_item_id; }

   MailId Get_c_parent_mail_id() const noexcept { return m_c_parent_mail_id; }
   MailId& Ref_c_parent_mail_id() noexcept { return m_c_parent_mail_id; }
   const MailId& ConstRef_c_parent_mail_id() const noexcept { return m_c_parent_mail_id; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   int64_t Get_c_inventory_slot_index() const noexcept { return m_c_inventory_slot_index; }
   int64_t& Ref_c_inventory_slot_index() noexcept { return m_c_inventory_slot_index; }
   const int64_t& ConstRef_c_inventory_slot_index() const noexcept { return m_c_inventory_slot_index; }

   int64_t Get_c_quick_slot_index() const noexcept { return m_c_quick_slot_index; }
   int64_t& Ref_c_quick_slot_index() noexcept { return m_c_quick_slot_index; }
   const int64_t& ConstRef_c_quick_slot_index() const noexcept { return m_c_quick_slot_index; }

   int64_t Get_c_equip_slot_index() const noexcept { return m_c_equip_slot_index; }
   int64_t& Ref_c_equip_slot_index() noexcept { return m_c_equip_slot_index; }
   const int64_t& ConstRef_c_equip_slot_index() const noexcept { return m_c_equip_slot_index; }

   ItemQuantity Get_c_item_quantity() const noexcept { return m_c_item_quantity; }
   ItemQuantity& Ref_c_item_quantity() noexcept { return m_c_item_quantity; }
   const ItemQuantity& ConstRef_c_item_quantity() const noexcept { return m_c_item_quantity; }

   ItemLevel Get_c_item_level() const noexcept { return m_c_item_level; }
   ItemLevel& Ref_c_item_level() noexcept { return m_c_item_level; }
   const ItemLevel& ConstRef_c_item_level() const noexcept { return m_c_item_level; }

   ItemColorUnion Get_c_item_color_union() const noexcept { return m_c_item_color_union; }
   ItemColorUnion& Ref_c_item_color_union() noexcept { return m_c_item_color_union; }
   const ItemColorUnion& ConstRef_c_item_color_union() const noexcept { return m_c_item_color_union; }

   ItemStatUnion Get_c_item_stat_union() const noexcept { return m_c_item_stat_union; }
   ItemStatUnion& Ref_c_item_stat_union() noexcept { return m_c_item_stat_union; }
   const ItemStatUnion& ConstRef_c_item_stat_union() const noexcept { return m_c_item_stat_union; }

   ItemEnchantUnion Get_c_item_enchant_union() const noexcept { return m_c_item_enchant_union; }
   ItemEnchantUnion& Ref_c_item_enchant_union() noexcept { return m_c_item_enchant_union; }
   const ItemEnchantUnion& ConstRef_c_item_enchant_union() const noexcept { return m_c_item_enchant_union; }

   ItemStateFlag Get_c_item_state_flag() const noexcept { return m_c_item_state_flag; }
   ItemStateFlag& Ref_c_item_state_flag() noexcept { return m_c_item_state_flag; }
   const ItemStateFlag& ConstRef_c_item_state_flag() const noexcept { return m_c_item_state_flag; }

   time_t Get_c_date_cooltime() const noexcept { return m_c_date_cooltime; }
   time_t& Ref_c_date_cooltime() noexcept { return m_c_date_cooltime; }
   const time_t& ConstRef_c_date_cooltime() const noexcept { return m_c_date_cooltime; }

   time_t Get_c_date_expire() const noexcept { return m_c_date_expire; }
   time_t& Ref_c_date_expire() noexcept { return m_c_date_expire; }
   const time_t& ConstRef_c_date_expire() const noexcept { return m_c_date_expire; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ITEM_DELETED) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ITEM_DELETED) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ITEM_DELETED); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ITEM_DELETED))
           {
               size += sizeof(ITEM_DELETED);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       {
           thisSize += sizeof(ItemLevel);
       }

       {
           thisSize += sizeof(ItemColorUnion);
       }

       {
           thisSize += sizeof(ItemStatUnion);
       }

       {
           thisSize += sizeof(ItemEnchantUnion);
       }

       {
           thisSize += sizeof(ItemStateFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ITEM_DELETED>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_item_id: {}", tTabStr, t->Get_c_item_id());
       result += std::format(L"\n{}c_parent_item_id: {}", tTabStr, t->Get_c_parent_item_id());
       result += std::format(L"\n{}c_parent_mail_id: {}", tTabStr, t->Get_c_parent_mail_id());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_inventory_slot_index: {}", tTabStr, t->Get_c_inventory_slot_index());
       result += std::format(L"\n{}c_quick_slot_index: {}", tTabStr, t->Get_c_quick_slot_index());
       result += std::format(L"\n{}c_equip_slot_index: {}", tTabStr, t->Get_c_equip_slot_index());
       result += std::format(L"\n{}c_item_quantity: {}", tTabStr, t->Get_c_item_quantity());
       result += std::format(L"\n{}c_item_level: {}", tTabStr, t->Get_c_item_level());
       result += std::format(L"\n{}c_item_color_union: {}", tTabStr, t->Get_c_item_color_union());
       result += std::format(L"\n{}c_item_stat_union: {}", tTabStr, t->Get_c_item_stat_union());
       result += std::format(L"\n{}c_item_enchant_union: {}", tTabStr, t->Get_c_item_enchant_union());
       result += std::format(L"\n{}c_item_state_flag: {}", tTabStr, t->Get_c_item_state_flag());
       result += std::format(L"\n{}c_date_cooltime: {}", tTabStr, t->Get_c_date_cooltime());
       result += std::format(L"\n{}c_date_expire: {}", tTabStr, t->Get_c_date_expire());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ITEM_DELETED_ALL;

class ITEM_DELETED_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ITEM_DELETED_ALL;

public:
   // (7)
   explicit ITEM_DELETED_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ITEM_DELETED_ALL, buf)
   {
   }

   // (9)
   explicit ITEM_DELETED_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       UserId user_id,
       ItemId item_id,
       ItemId parent_item_id,
       MailId parent_mail_id,
       ItemSid item_sid,
       int64_t inventory_slot_index,
       int64_t quick_slot_index,
       int64_t equip_slot_index,
       ItemQuantity item_quantity,
       ItemLevel item_level,
       ItemColorUnion item_color_union,
       ItemStatUnion item_stat_union,
       ItemEnchantUnion item_enchant_union,
       ItemStateFlag item_state_flag,
       time_t date_cooltime,
       time_t date_expire,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::ITEM_DELETED_ALL, buf)
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(parent_item_id);

       WriteValue(parent_mail_id);

       WriteValue(item_sid);

       WriteValue(inventory_slot_index);

       WriteValue(quick_slot_index);

       WriteValue(equip_slot_index);

       WriteValue(item_quantity);

       WriteValue(item_level);

       WriteValue(item_color_union);

       WriteValue(item_stat_union);

       WriteValue(item_enchant_union);

       WriteValue(item_state_flag);

       WriteValue(date_cooltime);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   // (8)
   explicit ITEM_DELETED_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       UserId user_id,
       ItemId item_id,
       ItemId parent_item_id,
       MailId parent_mail_id,
       ItemSid item_sid,
       int64_t inventory_slot_index,
       int64_t quick_slot_index,
       int64_t equip_slot_index,
       ItemQuantity item_quantity,
       ItemLevel item_level,
       ItemColorUnion item_color_union,
       ItemStatUnion item_stat_union,
       ItemEnchantUnion item_enchant_union,
       ItemStateFlag item_state_flag,
       time_t date_cooltime,
       time_t date_expire,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(parent_item_id);

       WriteValue(parent_mail_id);

       WriteValue(item_sid);

       WriteValue(inventory_slot_index);

       WriteValue(quick_slot_index);

       WriteValue(equip_slot_index);

       WriteValue(item_quantity);

       WriteValue(item_level);

       WriteValue(item_color_union);

       WriteValue(item_stat_union);

       WriteValue(item_enchant_union);

       WriteValue(item_state_flag);

       WriteValue(date_cooltime);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   ITEM_DELETED_ALL& GetPacket() const noexcept { return *reinterpret_cast<ITEM_DELETED_ALL*>(GetPacketBufPtr()); }
   ITEM_DELETED_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<ITEM_DELETED_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ITEM_DELETED_ALL : public Packet
{
   public:
      using Writer = ITEM_DELETED_ALL_WRITER;


private:
   Uuid m_c_id;

   UserId m_c_user_id;

   ItemId m_c_item_id;

   ItemId m_c_parent_item_id;

   MailId m_c_parent_mail_id;

   ItemSid m_c_item_sid;

   int64_t m_c_inventory_slot_index;

   int64_t m_c_quick_slot_index;

   int64_t m_c_equip_slot_index;

   ItemQuantity m_c_item_quantity;

   ItemLevel m_c_item_level;

   ItemColorUnion m_c_item_color_union;

   ItemStatUnion m_c_item_stat_union;

   ItemEnchantUnion m_c_item_enchant_union;

   ItemStateFlag m_c_item_state_flag;

   time_t m_c_date_cooltime;

   time_t m_c_date_expire;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   ITEM_DELETED_ALL()
   {
       memset(this, 0, sizeof(ITEM_DELETED_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(ITEM_DELETED_ALL));
   }
   ITEM_DELETED_ALL(const ITEM_DELETED_ALL&) = delete;
   ITEM_DELETED_ALL(ITEM_DELETED_ALL &&) = delete;
   ITEM_DELETED_ALL& operator = (const ITEM_DELETED_ALL&) = delete;
   ITEM_DELETED_ALL& operator = (ITEM_DELETED_ALL &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   ItemId Get_c_item_id() const noexcept { return m_c_item_id; }
   ItemId& Ref_c_item_id() noexcept { return m_c_item_id; }
   const ItemId& ConstRef_c_item_id() const noexcept { return m_c_item_id; }

   ItemId Get_c_parent_item_id() const noexcept { return m_c_parent_item_id; }
   ItemId& Ref_c_parent_item_id() noexcept { return m_c_parent_item_id; }
   const ItemId& ConstRef_c_parent_item_id() const noexcept { return m_c_parent_item_id; }

   MailId Get_c_parent_mail_id() const noexcept { return m_c_parent_mail_id; }
   MailId& Ref_c_parent_mail_id() noexcept { return m_c_parent_mail_id; }
   const MailId& ConstRef_c_parent_mail_id() const noexcept { return m_c_parent_mail_id; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   int64_t Get_c_inventory_slot_index() const noexcept { return m_c_inventory_slot_index; }
   int64_t& Ref_c_inventory_slot_index() noexcept { return m_c_inventory_slot_index; }
   const int64_t& ConstRef_c_inventory_slot_index() const noexcept { return m_c_inventory_slot_index; }

   int64_t Get_c_quick_slot_index() const noexcept { return m_c_quick_slot_index; }
   int64_t& Ref_c_quick_slot_index() noexcept { return m_c_quick_slot_index; }
   const int64_t& ConstRef_c_quick_slot_index() const noexcept { return m_c_quick_slot_index; }

   int64_t Get_c_equip_slot_index() const noexcept { return m_c_equip_slot_index; }
   int64_t& Ref_c_equip_slot_index() noexcept { return m_c_equip_slot_index; }
   const int64_t& ConstRef_c_equip_slot_index() const noexcept { return m_c_equip_slot_index; }

   ItemQuantity Get_c_item_quantity() const noexcept { return m_c_item_quantity; }
   ItemQuantity& Ref_c_item_quantity() noexcept { return m_c_item_quantity; }
   const ItemQuantity& ConstRef_c_item_quantity() const noexcept { return m_c_item_quantity; }

   ItemLevel Get_c_item_level() const noexcept { return m_c_item_level; }
   ItemLevel& Ref_c_item_level() noexcept { return m_c_item_level; }
   const ItemLevel& ConstRef_c_item_level() const noexcept { return m_c_item_level; }

   ItemColorUnion Get_c_item_color_union() const noexcept { return m_c_item_color_union; }
   ItemColorUnion& Ref_c_item_color_union() noexcept { return m_c_item_color_union; }
   const ItemColorUnion& ConstRef_c_item_color_union() const noexcept { return m_c_item_color_union; }

   ItemStatUnion Get_c_item_stat_union() const noexcept { return m_c_item_stat_union; }
   ItemStatUnion& Ref_c_item_stat_union() noexcept { return m_c_item_stat_union; }
   const ItemStatUnion& ConstRef_c_item_stat_union() const noexcept { return m_c_item_stat_union; }

   ItemEnchantUnion Get_c_item_enchant_union() const noexcept { return m_c_item_enchant_union; }
   ItemEnchantUnion& Ref_c_item_enchant_union() noexcept { return m_c_item_enchant_union; }
   const ItemEnchantUnion& ConstRef_c_item_enchant_union() const noexcept { return m_c_item_enchant_union; }

   ItemStateFlag Get_c_item_state_flag() const noexcept { return m_c_item_state_flag; }
   ItemStateFlag& Ref_c_item_state_flag() noexcept { return m_c_item_state_flag; }
   const ItemStateFlag& ConstRef_c_item_state_flag() const noexcept { return m_c_item_state_flag; }

   time_t Get_c_date_cooltime() const noexcept { return m_c_date_cooltime; }
   time_t& Ref_c_date_cooltime() noexcept { return m_c_date_cooltime; }
   const time_t& ConstRef_c_date_cooltime() const noexcept { return m_c_date_cooltime; }

   time_t Get_c_date_expire() const noexcept { return m_c_date_expire; }
   time_t& Ref_c_date_expire() noexcept { return m_c_date_expire; }
   const time_t& ConstRef_c_date_expire() const noexcept { return m_c_date_expire; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ITEM_DELETED_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ITEM_DELETED_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ITEM_DELETED_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ITEM_DELETED_ALL))
           {
               size += sizeof(ITEM_DELETED_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       {
           thisSize += sizeof(ItemLevel);
       }

       {
           thisSize += sizeof(ItemColorUnion);
       }

       {
           thisSize += sizeof(ItemStatUnion);
       }

       {
           thisSize += sizeof(ItemEnchantUnion);
       }

       {
           thisSize += sizeof(ItemStateFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ITEM_DELETED_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_item_id: {}", tTabStr, t->Get_c_item_id());
       result += std::format(L"\n{}c_parent_item_id: {}", tTabStr, t->Get_c_parent_item_id());
       result += std::format(L"\n{}c_parent_mail_id: {}", tTabStr, t->Get_c_parent_mail_id());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_inventory_slot_index: {}", tTabStr, t->Get_c_inventory_slot_index());
       result += std::format(L"\n{}c_quick_slot_index: {}", tTabStr, t->Get_c_quick_slot_index());
       result += std::format(L"\n{}c_equip_slot_index: {}", tTabStr, t->Get_c_equip_slot_index());
       result += std::format(L"\n{}c_item_quantity: {}", tTabStr, t->Get_c_item_quantity());
       result += std::format(L"\n{}c_item_level: {}", tTabStr, t->Get_c_item_level());
       result += std::format(L"\n{}c_item_color_union: {}", tTabStr, t->Get_c_item_color_union());
       result += std::format(L"\n{}c_item_stat_union: {}", tTabStr, t->Get_c_item_stat_union());
       result += std::format(L"\n{}c_item_enchant_union: {}", tTabStr, t->Get_c_item_enchant_union());
       result += std::format(L"\n{}c_item_state_flag: {}", tTabStr, t->Get_c_item_state_flag());
       result += std::format(L"\n{}c_date_cooltime: {}", tTabStr, t->Get_c_date_cooltime());
       result += std::format(L"\n{}c_date_expire: {}", tTabStr, t->Get_c_date_expire());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ITEM;

class ITEM_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ITEM;

public:
   // (7)
   explicit ITEM_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ITEM, buf)
   {
   }

   // (9)
   explicit ITEM_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       ItemId item_id,
       ItemId parent_item_id,
       MailId parent_mail_id,
       ItemSid item_sid,
       int64_t inventory_slot_index,
       int64_t quick_slot_index,
       int64_t equip_slot_index,
       ItemQuantity item_quantity,
       ItemLevel item_level,
       ItemColorUnion item_color_union,
       ItemStatUnion item_stat_union,
       ItemEnchantUnion item_enchant_union,
       ItemStateFlag item_state_flag,
       time_t date_cooltime,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ITEM, buf)
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(parent_item_id);

       WriteValue(parent_mail_id);

       WriteValue(item_sid);

       WriteValue(inventory_slot_index);

       WriteValue(quick_slot_index);

       WriteValue(equip_slot_index);

       WriteValue(item_quantity);

       WriteValue(item_level);

       WriteValue(item_color_union);

       WriteValue(item_stat_union);

       WriteValue(item_enchant_union);

       WriteValue(item_state_flag);

       WriteValue(date_cooltime);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit ITEM_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       ItemId item_id,
       ItemId parent_item_id,
       MailId parent_mail_id,
       ItemSid item_sid,
       int64_t inventory_slot_index,
       int64_t quick_slot_index,
       int64_t equip_slot_index,
       ItemQuantity item_quantity,
       ItemLevel item_level,
       ItemColorUnion item_color_union,
       ItemStatUnion item_stat_union,
       ItemEnchantUnion item_enchant_union,
       ItemStateFlag item_state_flag,
       time_t date_cooltime,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(parent_item_id);

       WriteValue(parent_mail_id);

       WriteValue(item_sid);

       WriteValue(inventory_slot_index);

       WriteValue(quick_slot_index);

       WriteValue(equip_slot_index);

       WriteValue(item_quantity);

       WriteValue(item_level);

       WriteValue(item_color_union);

       WriteValue(item_stat_union);

       WriteValue(item_enchant_union);

       WriteValue(item_state_flag);

       WriteValue(date_cooltime);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   ITEM& GetPacket() const noexcept { return *reinterpret_cast<ITEM*>(GetPacketBufPtr()); }
   ITEM* GetPacketPtr() const noexcept { return reinterpret_cast<ITEM*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ITEM : public Packet
{
   public:
      using Writer = ITEM_WRITER;


private:
   UserId m_c_user_id;

   ItemId m_c_item_id;

   ItemId m_c_parent_item_id;

   MailId m_c_parent_mail_id;

   ItemSid m_c_item_sid;

   int64_t m_c_inventory_slot_index;

   int64_t m_c_quick_slot_index;

   int64_t m_c_equip_slot_index;

   ItemQuantity m_c_item_quantity;

   ItemLevel m_c_item_level;

   ItemColorUnion m_c_item_color_union;

   ItemStatUnion m_c_item_stat_union;

   ItemEnchantUnion m_c_item_enchant_union;

   ItemStateFlag m_c_item_state_flag;

   time_t m_c_date_cooltime;

   time_t m_c_date_expire;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ITEM()
   {
       memset(this, 0, sizeof(ITEM));
       mPacketSize = static_cast<PacketSize>(sizeof(ITEM));
   }
   ITEM(const ITEM&) = delete;
   ITEM(ITEM &&) = delete;
   ITEM& operator = (const ITEM&) = delete;
   ITEM& operator = (ITEM &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   ItemId Get_c_item_id() const noexcept { return m_c_item_id; }
   ItemId& Ref_c_item_id() noexcept { return m_c_item_id; }
   const ItemId& ConstRef_c_item_id() const noexcept { return m_c_item_id; }

   ItemId Get_c_parent_item_id() const noexcept { return m_c_parent_item_id; }
   ItemId& Ref_c_parent_item_id() noexcept { return m_c_parent_item_id; }
   const ItemId& ConstRef_c_parent_item_id() const noexcept { return m_c_parent_item_id; }

   MailId Get_c_parent_mail_id() const noexcept { return m_c_parent_mail_id; }
   MailId& Ref_c_parent_mail_id() noexcept { return m_c_parent_mail_id; }
   const MailId& ConstRef_c_parent_mail_id() const noexcept { return m_c_parent_mail_id; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   int64_t Get_c_inventory_slot_index() const noexcept { return m_c_inventory_slot_index; }
   int64_t& Ref_c_inventory_slot_index() noexcept { return m_c_inventory_slot_index; }
   const int64_t& ConstRef_c_inventory_slot_index() const noexcept { return m_c_inventory_slot_index; }

   int64_t Get_c_quick_slot_index() const noexcept { return m_c_quick_slot_index; }
   int64_t& Ref_c_quick_slot_index() noexcept { return m_c_quick_slot_index; }
   const int64_t& ConstRef_c_quick_slot_index() const noexcept { return m_c_quick_slot_index; }

   int64_t Get_c_equip_slot_index() const noexcept { return m_c_equip_slot_index; }
   int64_t& Ref_c_equip_slot_index() noexcept { return m_c_equip_slot_index; }
   const int64_t& ConstRef_c_equip_slot_index() const noexcept { return m_c_equip_slot_index; }

   ItemQuantity Get_c_item_quantity() const noexcept { return m_c_item_quantity; }
   ItemQuantity& Ref_c_item_quantity() noexcept { return m_c_item_quantity; }
   const ItemQuantity& ConstRef_c_item_quantity() const noexcept { return m_c_item_quantity; }

   ItemLevel Get_c_item_level() const noexcept { return m_c_item_level; }
   ItemLevel& Ref_c_item_level() noexcept { return m_c_item_level; }
   const ItemLevel& ConstRef_c_item_level() const noexcept { return m_c_item_level; }

   ItemColorUnion Get_c_item_color_union() const noexcept { return m_c_item_color_union; }
   ItemColorUnion& Ref_c_item_color_union() noexcept { return m_c_item_color_union; }
   const ItemColorUnion& ConstRef_c_item_color_union() const noexcept { return m_c_item_color_union; }

   ItemStatUnion Get_c_item_stat_union() const noexcept { return m_c_item_stat_union; }
   ItemStatUnion& Ref_c_item_stat_union() noexcept { return m_c_item_stat_union; }
   const ItemStatUnion& ConstRef_c_item_stat_union() const noexcept { return m_c_item_stat_union; }

   ItemEnchantUnion Get_c_item_enchant_union() const noexcept { return m_c_item_enchant_union; }
   ItemEnchantUnion& Ref_c_item_enchant_union() noexcept { return m_c_item_enchant_union; }
   const ItemEnchantUnion& ConstRef_c_item_enchant_union() const noexcept { return m_c_item_enchant_union; }

   ItemStateFlag Get_c_item_state_flag() const noexcept { return m_c_item_state_flag; }
   ItemStateFlag& Ref_c_item_state_flag() noexcept { return m_c_item_state_flag; }
   const ItemStateFlag& ConstRef_c_item_state_flag() const noexcept { return m_c_item_state_flag; }

   time_t Get_c_date_cooltime() const noexcept { return m_c_date_cooltime; }
   time_t& Ref_c_date_cooltime() noexcept { return m_c_date_cooltime; }
   const time_t& ConstRef_c_date_cooltime() const noexcept { return m_c_date_cooltime; }

   time_t Get_c_date_expire() const noexcept { return m_c_date_expire; }
   time_t& Ref_c_date_expire() noexcept { return m_c_date_expire; }
   const time_t& ConstRef_c_date_expire() const noexcept { return m_c_date_expire; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ITEM) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ITEM) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ITEM); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ITEM))
           {
               size += sizeof(ITEM);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       {
           thisSize += sizeof(ItemLevel);
       }

       {
           thisSize += sizeof(ItemColorUnion);
       }

       {
           thisSize += sizeof(ItemStatUnion);
       }

       {
           thisSize += sizeof(ItemEnchantUnion);
       }

       {
           thisSize += sizeof(ItemStateFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ITEM>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_item_id: {}", tTabStr, t->Get_c_item_id());
       result += std::format(L"\n{}c_parent_item_id: {}", tTabStr, t->Get_c_parent_item_id());
       result += std::format(L"\n{}c_parent_mail_id: {}", tTabStr, t->Get_c_parent_mail_id());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_inventory_slot_index: {}", tTabStr, t->Get_c_inventory_slot_index());
       result += std::format(L"\n{}c_quick_slot_index: {}", tTabStr, t->Get_c_quick_slot_index());
       result += std::format(L"\n{}c_equip_slot_index: {}", tTabStr, t->Get_c_equip_slot_index());
       result += std::format(L"\n{}c_item_quantity: {}", tTabStr, t->Get_c_item_quantity());
       result += std::format(L"\n{}c_item_level: {}", tTabStr, t->Get_c_item_level());
       result += std::format(L"\n{}c_item_color_union: {}", tTabStr, t->Get_c_item_color_union());
       result += std::format(L"\n{}c_item_stat_union: {}", tTabStr, t->Get_c_item_stat_union());
       result += std::format(L"\n{}c_item_enchant_union: {}", tTabStr, t->Get_c_item_enchant_union());
       result += std::format(L"\n{}c_item_state_flag: {}", tTabStr, t->Get_c_item_state_flag());
       result += std::format(L"\n{}c_date_cooltime: {}", tTabStr, t->Get_c_date_cooltime());
       result += std::format(L"\n{}c_date_expire: {}", tTabStr, t->Get_c_date_expire());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ITEM_ALL;

class ITEM_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ITEM_ALL;

public:
   // (7)
   explicit ITEM_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ITEM_ALL, buf)
   {
   }

   // (9)
   explicit ITEM_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       ItemId item_id,
       ItemId parent_item_id,
       MailId parent_mail_id,
       ItemSid item_sid,
       int64_t inventory_slot_index,
       int64_t quick_slot_index,
       int64_t equip_slot_index,
       ItemQuantity item_quantity,
       ItemLevel item_level,
       ItemColorUnion item_color_union,
       ItemStatUnion item_stat_union,
       ItemEnchantUnion item_enchant_union,
       ItemStateFlag item_state_flag,
       time_t date_cooltime,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ITEM_ALL, buf)
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(parent_item_id);

       WriteValue(parent_mail_id);

       WriteValue(item_sid);

       WriteValue(inventory_slot_index);

       WriteValue(quick_slot_index);

       WriteValue(equip_slot_index);

       WriteValue(item_quantity);

       WriteValue(item_level);

       WriteValue(item_color_union);

       WriteValue(item_stat_union);

       WriteValue(item_enchant_union);

       WriteValue(item_state_flag);

       WriteValue(date_cooltime);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit ITEM_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       ItemId item_id,
       ItemId parent_item_id,
       MailId parent_mail_id,
       ItemSid item_sid,
       int64_t inventory_slot_index,
       int64_t quick_slot_index,
       int64_t equip_slot_index,
       ItemQuantity item_quantity,
       ItemLevel item_level,
       ItemColorUnion item_color_union,
       ItemStatUnion item_stat_union,
       ItemEnchantUnion item_enchant_union,
       ItemStateFlag item_state_flag,
       time_t date_cooltime,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(parent_item_id);

       WriteValue(parent_mail_id);

       WriteValue(item_sid);

       WriteValue(inventory_slot_index);

       WriteValue(quick_slot_index);

       WriteValue(equip_slot_index);

       WriteValue(item_quantity);

       WriteValue(item_level);

       WriteValue(item_color_union);

       WriteValue(item_stat_union);

       WriteValue(item_enchant_union);

       WriteValue(item_state_flag);

       WriteValue(date_cooltime);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   ITEM_ALL& GetPacket() const noexcept { return *reinterpret_cast<ITEM_ALL*>(GetPacketBufPtr()); }
   ITEM_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<ITEM_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ITEM_ALL : public Packet
{
   public:
      using Writer = ITEM_ALL_WRITER;


private:
   UserId m_c_user_id;

   ItemId m_c_item_id;

   ItemId m_c_parent_item_id;

   MailId m_c_parent_mail_id;

   ItemSid m_c_item_sid;

   int64_t m_c_inventory_slot_index;

   int64_t m_c_quick_slot_index;

   int64_t m_c_equip_slot_index;

   ItemQuantity m_c_item_quantity;

   ItemLevel m_c_item_level;

   ItemColorUnion m_c_item_color_union;

   ItemStatUnion m_c_item_stat_union;

   ItemEnchantUnion m_c_item_enchant_union;

   ItemStateFlag m_c_item_state_flag;

   time_t m_c_date_cooltime;

   time_t m_c_date_expire;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ITEM_ALL()
   {
       memset(this, 0, sizeof(ITEM_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(ITEM_ALL));
   }
   ITEM_ALL(const ITEM_ALL&) = delete;
   ITEM_ALL(ITEM_ALL &&) = delete;
   ITEM_ALL& operator = (const ITEM_ALL&) = delete;
   ITEM_ALL& operator = (ITEM_ALL &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   ItemId Get_c_item_id() const noexcept { return m_c_item_id; }
   ItemId& Ref_c_item_id() noexcept { return m_c_item_id; }
   const ItemId& ConstRef_c_item_id() const noexcept { return m_c_item_id; }

   ItemId Get_c_parent_item_id() const noexcept { return m_c_parent_item_id; }
   ItemId& Ref_c_parent_item_id() noexcept { return m_c_parent_item_id; }
   const ItemId& ConstRef_c_parent_item_id() const noexcept { return m_c_parent_item_id; }

   MailId Get_c_parent_mail_id() const noexcept { return m_c_parent_mail_id; }
   MailId& Ref_c_parent_mail_id() noexcept { return m_c_parent_mail_id; }
   const MailId& ConstRef_c_parent_mail_id() const noexcept { return m_c_parent_mail_id; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   int64_t Get_c_inventory_slot_index() const noexcept { return m_c_inventory_slot_index; }
   int64_t& Ref_c_inventory_slot_index() noexcept { return m_c_inventory_slot_index; }
   const int64_t& ConstRef_c_inventory_slot_index() const noexcept { return m_c_inventory_slot_index; }

   int64_t Get_c_quick_slot_index() const noexcept { return m_c_quick_slot_index; }
   int64_t& Ref_c_quick_slot_index() noexcept { return m_c_quick_slot_index; }
   const int64_t& ConstRef_c_quick_slot_index() const noexcept { return m_c_quick_slot_index; }

   int64_t Get_c_equip_slot_index() const noexcept { return m_c_equip_slot_index; }
   int64_t& Ref_c_equip_slot_index() noexcept { return m_c_equip_slot_index; }
   const int64_t& ConstRef_c_equip_slot_index() const noexcept { return m_c_equip_slot_index; }

   ItemQuantity Get_c_item_quantity() const noexcept { return m_c_item_quantity; }
   ItemQuantity& Ref_c_item_quantity() noexcept { return m_c_item_quantity; }
   const ItemQuantity& ConstRef_c_item_quantity() const noexcept { return m_c_item_quantity; }

   ItemLevel Get_c_item_level() const noexcept { return m_c_item_level; }
   ItemLevel& Ref_c_item_level() noexcept { return m_c_item_level; }
   const ItemLevel& ConstRef_c_item_level() const noexcept { return m_c_item_level; }

   ItemColorUnion Get_c_item_color_union() const noexcept { return m_c_item_color_union; }
   ItemColorUnion& Ref_c_item_color_union() noexcept { return m_c_item_color_union; }
   const ItemColorUnion& ConstRef_c_item_color_union() const noexcept { return m_c_item_color_union; }

   ItemStatUnion Get_c_item_stat_union() const noexcept { return m_c_item_stat_union; }
   ItemStatUnion& Ref_c_item_stat_union() noexcept { return m_c_item_stat_union; }
   const ItemStatUnion& ConstRef_c_item_stat_union() const noexcept { return m_c_item_stat_union; }

   ItemEnchantUnion Get_c_item_enchant_union() const noexcept { return m_c_item_enchant_union; }
   ItemEnchantUnion& Ref_c_item_enchant_union() noexcept { return m_c_item_enchant_union; }
   const ItemEnchantUnion& ConstRef_c_item_enchant_union() const noexcept { return m_c_item_enchant_union; }

   ItemStateFlag Get_c_item_state_flag() const noexcept { return m_c_item_state_flag; }
   ItemStateFlag& Ref_c_item_state_flag() noexcept { return m_c_item_state_flag; }
   const ItemStateFlag& ConstRef_c_item_state_flag() const noexcept { return m_c_item_state_flag; }

   time_t Get_c_date_cooltime() const noexcept { return m_c_date_cooltime; }
   time_t& Ref_c_date_cooltime() noexcept { return m_c_date_cooltime; }
   const time_t& ConstRef_c_date_cooltime() const noexcept { return m_c_date_cooltime; }

   time_t Get_c_date_expire() const noexcept { return m_c_date_expire; }
   time_t& Ref_c_date_expire() noexcept { return m_c_date_expire; }
   const time_t& ConstRef_c_date_expire() const noexcept { return m_c_date_expire; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ITEM_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ITEM_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ITEM_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ITEM_ALL))
           {
               size += sizeof(ITEM_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       {
           thisSize += sizeof(ItemLevel);
       }

       {
           thisSize += sizeof(ItemColorUnion);
       }

       {
           thisSize += sizeof(ItemStatUnion);
       }

       {
           thisSize += sizeof(ItemEnchantUnion);
       }

       {
           thisSize += sizeof(ItemStateFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ITEM_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_item_id: {}", tTabStr, t->Get_c_item_id());
       result += std::format(L"\n{}c_parent_item_id: {}", tTabStr, t->Get_c_parent_item_id());
       result += std::format(L"\n{}c_parent_mail_id: {}", tTabStr, t->Get_c_parent_mail_id());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_inventory_slot_index: {}", tTabStr, t->Get_c_inventory_slot_index());
       result += std::format(L"\n{}c_quick_slot_index: {}", tTabStr, t->Get_c_quick_slot_index());
       result += std::format(L"\n{}c_equip_slot_index: {}", tTabStr, t->Get_c_equip_slot_index());
       result += std::format(L"\n{}c_item_quantity: {}", tTabStr, t->Get_c_item_quantity());
       result += std::format(L"\n{}c_item_level: {}", tTabStr, t->Get_c_item_level());
       result += std::format(L"\n{}c_item_color_union: {}", tTabStr, t->Get_c_item_color_union());
       result += std::format(L"\n{}c_item_stat_union: {}", tTabStr, t->Get_c_item_stat_union());
       result += std::format(L"\n{}c_item_enchant_union: {}", tTabStr, t->Get_c_item_enchant_union());
       result += std::format(L"\n{}c_item_state_flag: {}", tTabStr, t->Get_c_item_state_flag());
       result += std::format(L"\n{}c_date_cooltime: {}", tTabStr, t->Get_c_date_cooltime());
       result += std::format(L"\n{}c_date_expire: {}", tTabStr, t->Get_c_date_expire());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ITEM_BY_USER;

class ITEM_BY_USER_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ITEM_BY_USER;

public:
   // (7)
   explicit ITEM_BY_USER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ITEM_BY_USER, buf)
   {
   }

   // (9)
   explicit ITEM_BY_USER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       ItemId item_id,
       ItemId parent_item_id,
       MailId parent_mail_id,
       ItemSid item_sid,
       int64_t inventory_slot_index,
       int64_t quick_slot_index,
       int64_t equip_slot_index,
       ItemQuantity item_quantity,
       ItemLevel item_level,
       ItemColorUnion item_color_union,
       ItemStatUnion item_stat_union,
       ItemEnchantUnion item_enchant_union,
       ItemStateFlag item_state_flag,
       time_t date_cooltime,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::ITEM_BY_USER, buf)
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(parent_item_id);

       WriteValue(parent_mail_id);

       WriteValue(item_sid);

       WriteValue(inventory_slot_index);

       WriteValue(quick_slot_index);

       WriteValue(equip_slot_index);

       WriteValue(item_quantity);

       WriteValue(item_level);

       WriteValue(item_color_union);

       WriteValue(item_stat_union);

       WriteValue(item_enchant_union);

       WriteValue(item_state_flag);

       WriteValue(date_cooltime);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit ITEM_BY_USER_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       ItemId item_id,
       ItemId parent_item_id,
       MailId parent_mail_id,
       ItemSid item_sid,
       int64_t inventory_slot_index,
       int64_t quick_slot_index,
       int64_t equip_slot_index,
       ItemQuantity item_quantity,
       ItemLevel item_level,
       ItemColorUnion item_color_union,
       ItemStatUnion item_stat_union,
       ItemEnchantUnion item_enchant_union,
       ItemStateFlag item_state_flag,
       time_t date_cooltime,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(parent_item_id);

       WriteValue(parent_mail_id);

       WriteValue(item_sid);

       WriteValue(inventory_slot_index);

       WriteValue(quick_slot_index);

       WriteValue(equip_slot_index);

       WriteValue(item_quantity);

       WriteValue(item_level);

       WriteValue(item_color_union);

       WriteValue(item_stat_union);

       WriteValue(item_enchant_union);

       WriteValue(item_state_flag);

       WriteValue(date_cooltime);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   ITEM_BY_USER& GetPacket() const noexcept { return *reinterpret_cast<ITEM_BY_USER*>(GetPacketBufPtr()); }
   ITEM_BY_USER* GetPacketPtr() const noexcept { return reinterpret_cast<ITEM_BY_USER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ITEM_BY_USER : public Packet
{
   public:
      using Writer = ITEM_BY_USER_WRITER;


private:
   UserId m_c_user_id;

   ItemId m_c_item_id;

   ItemId m_c_parent_item_id;

   MailId m_c_parent_mail_id;

   ItemSid m_c_item_sid;

   int64_t m_c_inventory_slot_index;

   int64_t m_c_quick_slot_index;

   int64_t m_c_equip_slot_index;

   ItemQuantity m_c_item_quantity;

   ItemLevel m_c_item_level;

   ItemColorUnion m_c_item_color_union;

   ItemStatUnion m_c_item_stat_union;

   ItemEnchantUnion m_c_item_enchant_union;

   ItemStateFlag m_c_item_state_flag;

   time_t m_c_date_cooltime;

   time_t m_c_date_expire;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   ITEM_BY_USER()
   {
       memset(this, 0, sizeof(ITEM_BY_USER));
       mPacketSize = static_cast<PacketSize>(sizeof(ITEM_BY_USER));
   }
   ITEM_BY_USER(const ITEM_BY_USER&) = delete;
   ITEM_BY_USER(ITEM_BY_USER &&) = delete;
   ITEM_BY_USER& operator = (const ITEM_BY_USER&) = delete;
   ITEM_BY_USER& operator = (ITEM_BY_USER &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   ItemId Get_c_item_id() const noexcept { return m_c_item_id; }
   ItemId& Ref_c_item_id() noexcept { return m_c_item_id; }
   const ItemId& ConstRef_c_item_id() const noexcept { return m_c_item_id; }

   ItemId Get_c_parent_item_id() const noexcept { return m_c_parent_item_id; }
   ItemId& Ref_c_parent_item_id() noexcept { return m_c_parent_item_id; }
   const ItemId& ConstRef_c_parent_item_id() const noexcept { return m_c_parent_item_id; }

   MailId Get_c_parent_mail_id() const noexcept { return m_c_parent_mail_id; }
   MailId& Ref_c_parent_mail_id() noexcept { return m_c_parent_mail_id; }
   const MailId& ConstRef_c_parent_mail_id() const noexcept { return m_c_parent_mail_id; }

   ItemSid Get_c_item_sid() const noexcept { return m_c_item_sid; }
   ItemSid& Ref_c_item_sid() noexcept { return m_c_item_sid; }
   const ItemSid& ConstRef_c_item_sid() const noexcept { return m_c_item_sid; }

   int64_t Get_c_inventory_slot_index() const noexcept { return m_c_inventory_slot_index; }
   int64_t& Ref_c_inventory_slot_index() noexcept { return m_c_inventory_slot_index; }
   const int64_t& ConstRef_c_inventory_slot_index() const noexcept { return m_c_inventory_slot_index; }

   int64_t Get_c_quick_slot_index() const noexcept { return m_c_quick_slot_index; }
   int64_t& Ref_c_quick_slot_index() noexcept { return m_c_quick_slot_index; }
   const int64_t& ConstRef_c_quick_slot_index() const noexcept { return m_c_quick_slot_index; }

   int64_t Get_c_equip_slot_index() const noexcept { return m_c_equip_slot_index; }
   int64_t& Ref_c_equip_slot_index() noexcept { return m_c_equip_slot_index; }
   const int64_t& ConstRef_c_equip_slot_index() const noexcept { return m_c_equip_slot_index; }

   ItemQuantity Get_c_item_quantity() const noexcept { return m_c_item_quantity; }
   ItemQuantity& Ref_c_item_quantity() noexcept { return m_c_item_quantity; }
   const ItemQuantity& ConstRef_c_item_quantity() const noexcept { return m_c_item_quantity; }

   ItemLevel Get_c_item_level() const noexcept { return m_c_item_level; }
   ItemLevel& Ref_c_item_level() noexcept { return m_c_item_level; }
   const ItemLevel& ConstRef_c_item_level() const noexcept { return m_c_item_level; }

   ItemColorUnion Get_c_item_color_union() const noexcept { return m_c_item_color_union; }
   ItemColorUnion& Ref_c_item_color_union() noexcept { return m_c_item_color_union; }
   const ItemColorUnion& ConstRef_c_item_color_union() const noexcept { return m_c_item_color_union; }

   ItemStatUnion Get_c_item_stat_union() const noexcept { return m_c_item_stat_union; }
   ItemStatUnion& Ref_c_item_stat_union() noexcept { return m_c_item_stat_union; }
   const ItemStatUnion& ConstRef_c_item_stat_union() const noexcept { return m_c_item_stat_union; }

   ItemEnchantUnion Get_c_item_enchant_union() const noexcept { return m_c_item_enchant_union; }
   ItemEnchantUnion& Ref_c_item_enchant_union() noexcept { return m_c_item_enchant_union; }
   const ItemEnchantUnion& ConstRef_c_item_enchant_union() const noexcept { return m_c_item_enchant_union; }

   ItemStateFlag Get_c_item_state_flag() const noexcept { return m_c_item_state_flag; }
   ItemStateFlag& Ref_c_item_state_flag() noexcept { return m_c_item_state_flag; }
   const ItemStateFlag& ConstRef_c_item_state_flag() const noexcept { return m_c_item_state_flag; }

   time_t Get_c_date_cooltime() const noexcept { return m_c_date_cooltime; }
   time_t& Ref_c_date_cooltime() noexcept { return m_c_date_cooltime; }
   const time_t& ConstRef_c_date_cooltime() const noexcept { return m_c_date_cooltime; }

   time_t Get_c_date_expire() const noexcept { return m_c_date_expire; }
   time_t& Ref_c_date_expire() noexcept { return m_c_date_expire; }
   const time_t& ConstRef_c_date_expire() const noexcept { return m_c_date_expire; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ITEM_BY_USER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ITEM_BY_USER) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ITEM_BY_USER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ITEM_BY_USER))
           {
               size += sizeof(ITEM_BY_USER);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(ItemQuantity);
       }

       {
           thisSize += sizeof(ItemLevel);
       }

       {
           thisSize += sizeof(ItemColorUnion);
       }

       {
           thisSize += sizeof(ItemStatUnion);
       }

       {
           thisSize += sizeof(ItemEnchantUnion);
       }

       {
           thisSize += sizeof(ItemStateFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ITEM_BY_USER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_item_id: {}", tTabStr, t->Get_c_item_id());
       result += std::format(L"\n{}c_parent_item_id: {}", tTabStr, t->Get_c_parent_item_id());
       result += std::format(L"\n{}c_parent_mail_id: {}", tTabStr, t->Get_c_parent_mail_id());
       result += std::format(L"\n{}c_item_sid: {}", tTabStr, t->Get_c_item_sid());
       result += std::format(L"\n{}c_inventory_slot_index: {}", tTabStr, t->Get_c_inventory_slot_index());
       result += std::format(L"\n{}c_quick_slot_index: {}", tTabStr, t->Get_c_quick_slot_index());
       result += std::format(L"\n{}c_equip_slot_index: {}", tTabStr, t->Get_c_equip_slot_index());
       result += std::format(L"\n{}c_item_quantity: {}", tTabStr, t->Get_c_item_quantity());
       result += std::format(L"\n{}c_item_level: {}", tTabStr, t->Get_c_item_level());
       result += std::format(L"\n{}c_item_color_union: {}", tTabStr, t->Get_c_item_color_union());
       result += std::format(L"\n{}c_item_stat_union: {}", tTabStr, t->Get_c_item_stat_union());
       result += std::format(L"\n{}c_item_enchant_union: {}", tTabStr, t->Get_c_item_enchant_union());
       result += std::format(L"\n{}c_item_state_flag: {}", tTabStr, t->Get_c_item_state_flag());
       result += std::format(L"\n{}c_date_cooltime: {}", tTabStr, t->Get_c_date_cooltime());
       result += std::format(L"\n{}c_date_expire: {}", tTabStr, t->Get_c_date_expire());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ITEM_STAT;

class ITEM_STAT_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ITEM_STAT;

public:
   // (7)
   explicit ITEM_STAT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ITEM_STAT, buf)
   {
   }

   // (9)
   explicit ITEM_STAT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       ItemId item_id,
       StatType stat_type,
       StatValue stat_value,
       time_t date_updated
   )
       :
       PacketWriter(trait, PacketTypes::ITEM_STAT, buf)
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(stat_type);

       WriteValue(stat_value);

       WriteValue(date_updated);


   }

   // (8)
   explicit ITEM_STAT_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       ItemId item_id,
       StatType stat_type,
       StatValue stat_value,
       time_t date_updated
   )
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(stat_type);

       WriteValue(stat_value);

       WriteValue(date_updated);


   }

   ITEM_STAT& GetPacket() const noexcept { return *reinterpret_cast<ITEM_STAT*>(GetPacketBufPtr()); }
   ITEM_STAT* GetPacketPtr() const noexcept { return reinterpret_cast<ITEM_STAT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ITEM_STAT : public Packet
{
   public:
      using Writer = ITEM_STAT_WRITER;


private:
   UserId m_c_user_id;

   ItemId m_c_item_id;

   StatType m_c_stat_type;

   StatValue m_c_stat_value;

   time_t m_c_date_updated;

public:
   ITEM_STAT()
   {
       memset(this, 0, sizeof(ITEM_STAT));
       mPacketSize = static_cast<PacketSize>(sizeof(ITEM_STAT));
   }
   ITEM_STAT(const ITEM_STAT&) = delete;
   ITEM_STAT(ITEM_STAT &&) = delete;
   ITEM_STAT& operator = (const ITEM_STAT&) = delete;
   ITEM_STAT& operator = (ITEM_STAT &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   ItemId Get_c_item_id() const noexcept { return m_c_item_id; }
   ItemId& Ref_c_item_id() noexcept { return m_c_item_id; }
   const ItemId& ConstRef_c_item_id() const noexcept { return m_c_item_id; }

   StatType Get_c_stat_type() const noexcept { return m_c_stat_type; }
   StatType& Ref_c_stat_type() noexcept { return m_c_stat_type; }
   const StatType& ConstRef_c_stat_type() const noexcept { return m_c_stat_type; }

   StatValue Get_c_stat_value() const noexcept { return m_c_stat_value; }
   StatValue& Ref_c_stat_value() noexcept { return m_c_stat_value; }
   const StatValue& ConstRef_c_stat_value() const noexcept { return m_c_stat_value; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ITEM_STAT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ITEM_STAT) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ITEM_STAT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ITEM_STAT))
           {
               size += sizeof(ITEM_STAT);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(StatType);
       }

       {
           thisSize += sizeof(StatValue);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ITEM_STAT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_item_id: {}", tTabStr, t->Get_c_item_id());
       result += std::format(L"\n{}c_stat_type: {}", tTabStr, t->Get_c_stat_type());
       result += std::format(L"\n{}c_stat_value: {}", tTabStr, t->Get_c_stat_value());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ITEM_STAT_ALL;

class ITEM_STAT_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ITEM_STAT_ALL;

public:
   // (7)
   explicit ITEM_STAT_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ITEM_STAT_ALL, buf)
   {
   }

   // (9)
   explicit ITEM_STAT_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       ItemId item_id,
       StatType stat_type,
       StatValue stat_value,
       time_t date_updated
   )
       :
       PacketWriter(trait, PacketTypes::ITEM_STAT_ALL, buf)
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(stat_type);

       WriteValue(stat_value);

       WriteValue(date_updated);


   }

   // (8)
   explicit ITEM_STAT_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       ItemId item_id,
       StatType stat_type,
       StatValue stat_value,
       time_t date_updated
   )
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(stat_type);

       WriteValue(stat_value);

       WriteValue(date_updated);


   }

   ITEM_STAT_ALL& GetPacket() const noexcept { return *reinterpret_cast<ITEM_STAT_ALL*>(GetPacketBufPtr()); }
   ITEM_STAT_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<ITEM_STAT_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ITEM_STAT_ALL : public Packet
{
   public:
      using Writer = ITEM_STAT_ALL_WRITER;


private:
   UserId m_c_user_id;

   ItemId m_c_item_id;

   StatType m_c_stat_type;

   StatValue m_c_stat_value;

   time_t m_c_date_updated;

public:
   ITEM_STAT_ALL()
   {
       memset(this, 0, sizeof(ITEM_STAT_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(ITEM_STAT_ALL));
   }
   ITEM_STAT_ALL(const ITEM_STAT_ALL&) = delete;
   ITEM_STAT_ALL(ITEM_STAT_ALL &&) = delete;
   ITEM_STAT_ALL& operator = (const ITEM_STAT_ALL&) = delete;
   ITEM_STAT_ALL& operator = (ITEM_STAT_ALL &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   ItemId Get_c_item_id() const noexcept { return m_c_item_id; }
   ItemId& Ref_c_item_id() noexcept { return m_c_item_id; }
   const ItemId& ConstRef_c_item_id() const noexcept { return m_c_item_id; }

   StatType Get_c_stat_type() const noexcept { return m_c_stat_type; }
   StatType& Ref_c_stat_type() noexcept { return m_c_stat_type; }
   const StatType& ConstRef_c_stat_type() const noexcept { return m_c_stat_type; }

   StatValue Get_c_stat_value() const noexcept { return m_c_stat_value; }
   StatValue& Ref_c_stat_value() noexcept { return m_c_stat_value; }
   const StatValue& ConstRef_c_stat_value() const noexcept { return m_c_stat_value; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ITEM_STAT_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ITEM_STAT_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ITEM_STAT_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ITEM_STAT_ALL))
           {
               size += sizeof(ITEM_STAT_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(StatType);
       }

       {
           thisSize += sizeof(StatValue);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ITEM_STAT_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_item_id: {}", tTabStr, t->Get_c_item_id());
       result += std::format(L"\n{}c_stat_type: {}", tTabStr, t->Get_c_stat_type());
       result += std::format(L"\n{}c_stat_value: {}", tTabStr, t->Get_c_stat_value());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ITEM_STAT_BY_USER;

class ITEM_STAT_BY_USER_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ITEM_STAT_BY_USER;

public:
   // (7)
   explicit ITEM_STAT_BY_USER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::ITEM_STAT_BY_USER, buf)
   {
   }

   // (9)
   explicit ITEM_STAT_BY_USER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       ItemId item_id,
       StatType stat_type,
       StatValue stat_value,
       time_t date_updated
   )
       :
       PacketWriter(trait, PacketTypes::ITEM_STAT_BY_USER, buf)
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(stat_type);

       WriteValue(stat_value);

       WriteValue(date_updated);


   }

   // (8)
   explicit ITEM_STAT_BY_USER_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       ItemId item_id,
       StatType stat_type,
       StatValue stat_value,
       time_t date_updated
   )
   {
       WriteValue(user_id);

       WriteValue(item_id);

       WriteValue(stat_type);

       WriteValue(stat_value);

       WriteValue(date_updated);


   }

   ITEM_STAT_BY_USER& GetPacket() const noexcept { return *reinterpret_cast<ITEM_STAT_BY_USER*>(GetPacketBufPtr()); }
   ITEM_STAT_BY_USER* GetPacketPtr() const noexcept { return reinterpret_cast<ITEM_STAT_BY_USER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ITEM_STAT_BY_USER : public Packet
{
   public:
      using Writer = ITEM_STAT_BY_USER_WRITER;


private:
   UserId m_c_user_id;

   ItemId m_c_item_id;

   StatType m_c_stat_type;

   StatValue m_c_stat_value;

   time_t m_c_date_updated;

public:
   ITEM_STAT_BY_USER()
   {
       memset(this, 0, sizeof(ITEM_STAT_BY_USER));
       mPacketSize = static_cast<PacketSize>(sizeof(ITEM_STAT_BY_USER));
   }
   ITEM_STAT_BY_USER(const ITEM_STAT_BY_USER&) = delete;
   ITEM_STAT_BY_USER(ITEM_STAT_BY_USER &&) = delete;
   ITEM_STAT_BY_USER& operator = (const ITEM_STAT_BY_USER&) = delete;
   ITEM_STAT_BY_USER& operator = (ITEM_STAT_BY_USER &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   ItemId Get_c_item_id() const noexcept { return m_c_item_id; }
   ItemId& Ref_c_item_id() noexcept { return m_c_item_id; }
   const ItemId& ConstRef_c_item_id() const noexcept { return m_c_item_id; }

   StatType Get_c_stat_type() const noexcept { return m_c_stat_type; }
   StatType& Ref_c_stat_type() noexcept { return m_c_stat_type; }
   const StatType& ConstRef_c_stat_type() const noexcept { return m_c_stat_type; }

   StatValue Get_c_stat_value() const noexcept { return m_c_stat_value; }
   StatValue& Ref_c_stat_value() noexcept { return m_c_stat_value; }
   const StatValue& ConstRef_c_stat_value() const noexcept { return m_c_stat_value; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ITEM_STAT_BY_USER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ITEM_STAT_BY_USER) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(ITEM_STAT_BY_USER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ITEM_STAT_BY_USER))
           {
               size += sizeof(ITEM_STAT_BY_USER);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(ItemId);
       }

       {
           thisSize += sizeof(StatType);
       }

       {
           thisSize += sizeof(StatValue);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ITEM_STAT_BY_USER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_item_id: {}", tTabStr, t->Get_c_item_id());
       result += std::format(L"\n{}c_stat_type: {}", tTabStr, t->Get_c_stat_type());
       result += std::format(L"\n{}c_stat_value: {}", tTabStr, t->Get_c_stat_value());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class MAIL_DELETED;

class MAIL_DELETED_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::MAIL_DELETED;

public:
   // (7)
   explicit MAIL_DELETED_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::MAIL_DELETED, buf)
   {
   }

   // (9)
   explicit MAIL_DELETED_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       UserId user_id,
       MailId mail_id,
       ItemSid mail_sid,
       ReadFlag read_flag,
       RewardFlag reward_flag,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* msg_args,
       time_t date_expire,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::MAIL_DELETED, buf)
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(mail_id);

       WriteValue(mail_sid);

       WriteValue(read_flag);

       WriteValue(reward_flag);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msg_argsOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msg_argsLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       msg_argsOffset = GetPacketSize();
       msg_argsLength = (PacketSize)wcsnlen_s(msg_args, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msg_argsLength) WriteString(L"", 0);
       else WriteString(msg_args, msg_argsLength); 

   }

   // (8)
   explicit MAIL_DELETED_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       UserId user_id,
       MailId mail_id,
       ItemSid mail_sid,
       ReadFlag read_flag,
       RewardFlag reward_flag,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* msg_args,
       time_t date_expire,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(mail_id);

       WriteValue(mail_sid);

       WriteValue(read_flag);

       WriteValue(reward_flag);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msg_argsOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msg_argsLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       msg_argsOffset = GetPacketSize();
       msg_argsLength = (PacketSize)wcsnlen_s(msg_args, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msg_argsLength) WriteString(L"", 0);
       else WriteString(msg_args, msg_argsLength); 

   }

   MAIL_DELETED& GetPacket() const noexcept { return *reinterpret_cast<MAIL_DELETED*>(GetPacketBufPtr()); }
   MAIL_DELETED* GetPacketPtr() const noexcept { return reinterpret_cast<MAIL_DELETED*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class MAIL_DELETED : public Packet
{
   public:
      using Writer = MAIL_DELETED_WRITER;


private:
   Uuid m_c_id;

   UserId m_c_user_id;

   MailId m_c_mail_id;

   ItemSid m_c_mail_sid;

   ReadFlag m_c_read_flag;

   RewardFlag m_c_reward_flag;

   PacketSize m_c_title_offset = 0;
   PacketSize m_c_title_length = 0;

   PacketSize m_c_msg_offset = 0;
   PacketSize m_c_msg_length = 0;

   PacketSize m_c_msg_args_offset = 0;
   PacketSize m_c_msg_args_length = 0;

   time_t m_c_date_expire;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   MAIL_DELETED()
   {
       memset(this, 0, sizeof(MAIL_DELETED));
       mPacketSize = static_cast<PacketSize>(sizeof(MAIL_DELETED));
   }
   MAIL_DELETED(const MAIL_DELETED&) = delete;
   MAIL_DELETED(MAIL_DELETED &&) = delete;
   MAIL_DELETED& operator = (const MAIL_DELETED&) = delete;
   MAIL_DELETED& operator = (MAIL_DELETED &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   MailId Get_c_mail_id() const noexcept { return m_c_mail_id; }
   MailId& Ref_c_mail_id() noexcept { return m_c_mail_id; }
   const MailId& ConstRef_c_mail_id() const noexcept { return m_c_mail_id; }

   ItemSid Get_c_mail_sid() const noexcept { return m_c_mail_sid; }
   ItemSid& Ref_c_mail_sid() noexcept { return m_c_mail_sid; }
   const ItemSid& ConstRef_c_mail_sid() const noexcept { return m_c_mail_sid; }

   ReadFlag Get_c_read_flag() const noexcept { return m_c_read_flag; }
   ReadFlag& Ref_c_read_flag() noexcept { return m_c_read_flag; }
   const ReadFlag& ConstRef_c_read_flag() const noexcept { return m_c_read_flag; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   const wchar_t* Get_c_title() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   wchar_t* Ref_c_title()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   const wchar_t* Get_c_msg() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   wchar_t* Ref_c_msg()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   const wchar_t* Get_c_msg_args() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_args_offset) + m_c_msg_args_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_args_offset);
   }

   wchar_t* Ref_c_msg_args()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_args_offset) + m_c_msg_args_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_args_offset);
   }

   time_t Get_c_date_expire() const noexcept { return m_c_date_expire; }
   time_t& Ref_c_date_expire() noexcept { return m_c_date_expire; }
   const time_t& ConstRef_c_date_expire() const noexcept { return m_c_date_expire; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(MAIL_DELETED) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(MAIL_DELETED) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(MAIL_DELETED); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(MAIL_DELETED))
           {
               size += sizeof(MAIL_DELETED);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ReadFlag);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           if (GetPacketSize() < m_c_title_offset)
               return false;

           if (0 < m_c_title_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_title_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_offset)
               return false;

           if (0 < m_c_msg_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_args_offset)
               return false;

           if (0 < m_c_msg_args_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_args_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, MAIL_DELETED>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_mail_id: {}", tTabStr, t->Get_c_mail_id());
       result += std::format(L"\n{}c_mail_sid: {}", tTabStr, t->Get_c_mail_sid());
       result += std::format(L"\n{}c_read_flag: {}", tTabStr, t->Get_c_read_flag());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_title: {}", tTabStr, t->Get_c_title());
       result += std::format(L"\n{}c_msg: {}", tTabStr, t->Get_c_msg());
       result += std::format(L"\n{}c_msg_args: {}", tTabStr, t->Get_c_msg_args());
       result += std::format(L"\n{}c_date_expire: {}", tTabStr, t->Get_c_date_expire());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class MAIL_DELETED_ALL;

class MAIL_DELETED_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::MAIL_DELETED_ALL;

public:
   // (7)
   explicit MAIL_DELETED_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::MAIL_DELETED_ALL, buf)
   {
   }

   // (9)
   explicit MAIL_DELETED_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       UserId user_id,
       MailId mail_id,
       ItemSid mail_sid,
       ReadFlag read_flag,
       RewardFlag reward_flag,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* msg_args,
       time_t date_expire,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::MAIL_DELETED_ALL, buf)
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(mail_id);

       WriteValue(mail_sid);

       WriteValue(read_flag);

       WriteValue(reward_flag);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msg_argsOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msg_argsLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       msg_argsOffset = GetPacketSize();
       msg_argsLength = (PacketSize)wcsnlen_s(msg_args, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msg_argsLength) WriteString(L"", 0);
       else WriteString(msg_args, msg_argsLength); 

   }

   // (8)
   explicit MAIL_DELETED_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       UserId user_id,
       MailId mail_id,
       ItemSid mail_sid,
       ReadFlag read_flag,
       RewardFlag reward_flag,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* msg_args,
       time_t date_expire,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(mail_id);

       WriteValue(mail_sid);

       WriteValue(read_flag);

       WriteValue(reward_flag);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msg_argsOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msg_argsLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       msg_argsOffset = GetPacketSize();
       msg_argsLength = (PacketSize)wcsnlen_s(msg_args, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msg_argsLength) WriteString(L"", 0);
       else WriteString(msg_args, msg_argsLength); 

   }

   MAIL_DELETED_ALL& GetPacket() const noexcept { return *reinterpret_cast<MAIL_DELETED_ALL*>(GetPacketBufPtr()); }
   MAIL_DELETED_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<MAIL_DELETED_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class MAIL_DELETED_ALL : public Packet
{
   public:
      using Writer = MAIL_DELETED_ALL_WRITER;


private:
   Uuid m_c_id;

   UserId m_c_user_id;

   MailId m_c_mail_id;

   ItemSid m_c_mail_sid;

   ReadFlag m_c_read_flag;

   RewardFlag m_c_reward_flag;

   PacketSize m_c_title_offset = 0;
   PacketSize m_c_title_length = 0;

   PacketSize m_c_msg_offset = 0;
   PacketSize m_c_msg_length = 0;

   PacketSize m_c_msg_args_offset = 0;
   PacketSize m_c_msg_args_length = 0;

   time_t m_c_date_expire;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   MAIL_DELETED_ALL()
   {
       memset(this, 0, sizeof(MAIL_DELETED_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(MAIL_DELETED_ALL));
   }
   MAIL_DELETED_ALL(const MAIL_DELETED_ALL&) = delete;
   MAIL_DELETED_ALL(MAIL_DELETED_ALL &&) = delete;
   MAIL_DELETED_ALL& operator = (const MAIL_DELETED_ALL&) = delete;
   MAIL_DELETED_ALL& operator = (MAIL_DELETED_ALL &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   MailId Get_c_mail_id() const noexcept { return m_c_mail_id; }
   MailId& Ref_c_mail_id() noexcept { return m_c_mail_id; }
   const MailId& ConstRef_c_mail_id() const noexcept { return m_c_mail_id; }

   ItemSid Get_c_mail_sid() const noexcept { return m_c_mail_sid; }
   ItemSid& Ref_c_mail_sid() noexcept { return m_c_mail_sid; }
   const ItemSid& ConstRef_c_mail_sid() const noexcept { return m_c_mail_sid; }

   ReadFlag Get_c_read_flag() const noexcept { return m_c_read_flag; }
   ReadFlag& Ref_c_read_flag() noexcept { return m_c_read_flag; }
   const ReadFlag& ConstRef_c_read_flag() const noexcept { return m_c_read_flag; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   const wchar_t* Get_c_title() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   wchar_t* Ref_c_title()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   const wchar_t* Get_c_msg() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   wchar_t* Ref_c_msg()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   const wchar_t* Get_c_msg_args() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_args_offset) + m_c_msg_args_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_args_offset);
   }

   wchar_t* Ref_c_msg_args()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_args_offset) + m_c_msg_args_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_args_offset);
   }

   time_t Get_c_date_expire() const noexcept { return m_c_date_expire; }
   time_t& Ref_c_date_expire() noexcept { return m_c_date_expire; }
   const time_t& ConstRef_c_date_expire() const noexcept { return m_c_date_expire; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(MAIL_DELETED_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(MAIL_DELETED_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(MAIL_DELETED_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(MAIL_DELETED_ALL))
           {
               size += sizeof(MAIL_DELETED_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ReadFlag);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           if (GetPacketSize() < m_c_title_offset)
               return false;

           if (0 < m_c_title_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_title_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_offset)
               return false;

           if (0 < m_c_msg_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_args_offset)
               return false;

           if (0 < m_c_msg_args_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_args_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, MAIL_DELETED_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_mail_id: {}", tTabStr, t->Get_c_mail_id());
       result += std::format(L"\n{}c_mail_sid: {}", tTabStr, t->Get_c_mail_sid());
       result += std::format(L"\n{}c_read_flag: {}", tTabStr, t->Get_c_read_flag());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_title: {}", tTabStr, t->Get_c_title());
       result += std::format(L"\n{}c_msg: {}", tTabStr, t->Get_c_msg());
       result += std::format(L"\n{}c_msg_args: {}", tTabStr, t->Get_c_msg_args());
       result += std::format(L"\n{}c_date_expire: {}", tTabStr, t->Get_c_date_expire());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class MAIL;

class MAIL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::MAIL;

public:
   // (7)
   explicit MAIL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::MAIL, buf)
   {
   }

   // (9)
   explicit MAIL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       MailId mail_id,
       ItemSid mail_sid,
       ReadFlag read_flag,
       RewardFlag reward_flag,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* msg_args,
       int64_t reward_sid,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::MAIL, buf)
   {
       WriteValue(user_id);

       WriteValue(mail_id);

       WriteValue(mail_sid);

       WriteValue(read_flag);

       WriteValue(reward_flag);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msg_argsOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msg_argsLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(reward_sid);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       msg_argsOffset = GetPacketSize();
       msg_argsLength = (PacketSize)wcsnlen_s(msg_args, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msg_argsLength) WriteString(L"", 0);
       else WriteString(msg_args, msg_argsLength); 

   }

   // (8)
   explicit MAIL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       MailId mail_id,
       ItemSid mail_sid,
       ReadFlag read_flag,
       RewardFlag reward_flag,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* msg_args,
       int64_t reward_sid,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(mail_id);

       WriteValue(mail_sid);

       WriteValue(read_flag);

       WriteValue(reward_flag);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msg_argsOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msg_argsLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(reward_sid);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       msg_argsOffset = GetPacketSize();
       msg_argsLength = (PacketSize)wcsnlen_s(msg_args, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msg_argsLength) WriteString(L"", 0);
       else WriteString(msg_args, msg_argsLength); 

   }

   MAIL& GetPacket() const noexcept { return *reinterpret_cast<MAIL*>(GetPacketBufPtr()); }
   MAIL* GetPacketPtr() const noexcept { return reinterpret_cast<MAIL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class MAIL : public Packet
{
   public:
      using Writer = MAIL_WRITER;


private:
   UserId m_c_user_id;

   MailId m_c_mail_id;

   ItemSid m_c_mail_sid;

   ReadFlag m_c_read_flag;

   RewardFlag m_c_reward_flag;

   PacketSize m_c_title_offset = 0;
   PacketSize m_c_title_length = 0;

   PacketSize m_c_msg_offset = 0;
   PacketSize m_c_msg_length = 0;

   PacketSize m_c_msg_args_offset = 0;
   PacketSize m_c_msg_args_length = 0;

   int64_t m_c_reward_sid;

   time_t m_c_date_expire;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   MAIL()
   {
       memset(this, 0, sizeof(MAIL));
       mPacketSize = static_cast<PacketSize>(sizeof(MAIL));
   }
   MAIL(const MAIL&) = delete;
   MAIL(MAIL &&) = delete;
   MAIL& operator = (const MAIL&) = delete;
   MAIL& operator = (MAIL &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   MailId Get_c_mail_id() const noexcept { return m_c_mail_id; }
   MailId& Ref_c_mail_id() noexcept { return m_c_mail_id; }
   const MailId& ConstRef_c_mail_id() const noexcept { return m_c_mail_id; }

   ItemSid Get_c_mail_sid() const noexcept { return m_c_mail_sid; }
   ItemSid& Ref_c_mail_sid() noexcept { return m_c_mail_sid; }
   const ItemSid& ConstRef_c_mail_sid() const noexcept { return m_c_mail_sid; }

   ReadFlag Get_c_read_flag() const noexcept { return m_c_read_flag; }
   ReadFlag& Ref_c_read_flag() noexcept { return m_c_read_flag; }
   const ReadFlag& ConstRef_c_read_flag() const noexcept { return m_c_read_flag; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   const wchar_t* Get_c_title() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   wchar_t* Ref_c_title()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   const wchar_t* Get_c_msg() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   wchar_t* Ref_c_msg()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   const wchar_t* Get_c_msg_args() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_args_offset) + m_c_msg_args_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_args_offset);
   }

   wchar_t* Ref_c_msg_args()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_args_offset) + m_c_msg_args_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_args_offset);
   }

   int64_t Get_c_reward_sid() const noexcept { return m_c_reward_sid; }
   int64_t& Ref_c_reward_sid() noexcept { return m_c_reward_sid; }
   const int64_t& ConstRef_c_reward_sid() const noexcept { return m_c_reward_sid; }

   time_t Get_c_date_expire() const noexcept { return m_c_date_expire; }
   time_t& Ref_c_date_expire() noexcept { return m_c_date_expire; }
   const time_t& ConstRef_c_date_expire() const noexcept { return m_c_date_expire; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(MAIL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(MAIL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(MAIL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(MAIL))
           {
               size += sizeof(MAIL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ReadFlag);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           if (GetPacketSize() < m_c_title_offset)
               return false;

           if (0 < m_c_title_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_title_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_offset)
               return false;

           if (0 < m_c_msg_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_args_offset)
               return false;

           if (0 < m_c_msg_args_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_args_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, MAIL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_mail_id: {}", tTabStr, t->Get_c_mail_id());
       result += std::format(L"\n{}c_mail_sid: {}", tTabStr, t->Get_c_mail_sid());
       result += std::format(L"\n{}c_read_flag: {}", tTabStr, t->Get_c_read_flag());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_title: {}", tTabStr, t->Get_c_title());
       result += std::format(L"\n{}c_msg: {}", tTabStr, t->Get_c_msg());
       result += std::format(L"\n{}c_msg_args: {}", tTabStr, t->Get_c_msg_args());
       result += std::format(L"\n{}c_reward_sid: {}", tTabStr, t->Get_c_reward_sid());
       result += std::format(L"\n{}c_date_expire: {}", tTabStr, t->Get_c_date_expire());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class MAIL_ALL;

class MAIL_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::MAIL_ALL;

public:
   // (7)
   explicit MAIL_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::MAIL_ALL, buf)
   {
   }

   // (9)
   explicit MAIL_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       MailId mail_id,
       ItemSid mail_sid,
       ReadFlag read_flag,
       RewardFlag reward_flag,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* msg_args,
       int64_t reward_sid,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::MAIL_ALL, buf)
   {
       WriteValue(user_id);

       WriteValue(mail_id);

       WriteValue(mail_sid);

       WriteValue(read_flag);

       WriteValue(reward_flag);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msg_argsOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msg_argsLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(reward_sid);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       msg_argsOffset = GetPacketSize();
       msg_argsLength = (PacketSize)wcsnlen_s(msg_args, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msg_argsLength) WriteString(L"", 0);
       else WriteString(msg_args, msg_argsLength); 

   }

   // (8)
   explicit MAIL_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       MailId mail_id,
       ItemSid mail_sid,
       ReadFlag read_flag,
       RewardFlag reward_flag,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* msg_args,
       int64_t reward_sid,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(mail_id);

       WriteValue(mail_sid);

       WriteValue(read_flag);

       WriteValue(reward_flag);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msg_argsOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msg_argsLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(reward_sid);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       msg_argsOffset = GetPacketSize();
       msg_argsLength = (PacketSize)wcsnlen_s(msg_args, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msg_argsLength) WriteString(L"", 0);
       else WriteString(msg_args, msg_argsLength); 

   }

   MAIL_ALL& GetPacket() const noexcept { return *reinterpret_cast<MAIL_ALL*>(GetPacketBufPtr()); }
   MAIL_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<MAIL_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class MAIL_ALL : public Packet
{
   public:
      using Writer = MAIL_ALL_WRITER;


private:
   UserId m_c_user_id;

   MailId m_c_mail_id;

   ItemSid m_c_mail_sid;

   ReadFlag m_c_read_flag;

   RewardFlag m_c_reward_flag;

   PacketSize m_c_title_offset = 0;
   PacketSize m_c_title_length = 0;

   PacketSize m_c_msg_offset = 0;
   PacketSize m_c_msg_length = 0;

   PacketSize m_c_msg_args_offset = 0;
   PacketSize m_c_msg_args_length = 0;

   int64_t m_c_reward_sid;

   time_t m_c_date_expire;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   MAIL_ALL()
   {
       memset(this, 0, sizeof(MAIL_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(MAIL_ALL));
   }
   MAIL_ALL(const MAIL_ALL&) = delete;
   MAIL_ALL(MAIL_ALL &&) = delete;
   MAIL_ALL& operator = (const MAIL_ALL&) = delete;
   MAIL_ALL& operator = (MAIL_ALL &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   MailId Get_c_mail_id() const noexcept { return m_c_mail_id; }
   MailId& Ref_c_mail_id() noexcept { return m_c_mail_id; }
   const MailId& ConstRef_c_mail_id() const noexcept { return m_c_mail_id; }

   ItemSid Get_c_mail_sid() const noexcept { return m_c_mail_sid; }
   ItemSid& Ref_c_mail_sid() noexcept { return m_c_mail_sid; }
   const ItemSid& ConstRef_c_mail_sid() const noexcept { return m_c_mail_sid; }

   ReadFlag Get_c_read_flag() const noexcept { return m_c_read_flag; }
   ReadFlag& Ref_c_read_flag() noexcept { return m_c_read_flag; }
   const ReadFlag& ConstRef_c_read_flag() const noexcept { return m_c_read_flag; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   const wchar_t* Get_c_title() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   wchar_t* Ref_c_title()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   const wchar_t* Get_c_msg() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   wchar_t* Ref_c_msg()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   const wchar_t* Get_c_msg_args() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_args_offset) + m_c_msg_args_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_args_offset);
   }

   wchar_t* Ref_c_msg_args()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_args_offset) + m_c_msg_args_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_args_offset);
   }

   int64_t Get_c_reward_sid() const noexcept { return m_c_reward_sid; }
   int64_t& Ref_c_reward_sid() noexcept { return m_c_reward_sid; }
   const int64_t& ConstRef_c_reward_sid() const noexcept { return m_c_reward_sid; }

   time_t Get_c_date_expire() const noexcept { return m_c_date_expire; }
   time_t& Ref_c_date_expire() noexcept { return m_c_date_expire; }
   const time_t& ConstRef_c_date_expire() const noexcept { return m_c_date_expire; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(MAIL_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(MAIL_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(MAIL_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(MAIL_ALL))
           {
               size += sizeof(MAIL_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ReadFlag);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           if (GetPacketSize() < m_c_title_offset)
               return false;

           if (0 < m_c_title_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_title_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_offset)
               return false;

           if (0 < m_c_msg_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_args_offset)
               return false;

           if (0 < m_c_msg_args_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_args_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, MAIL_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_mail_id: {}", tTabStr, t->Get_c_mail_id());
       result += std::format(L"\n{}c_mail_sid: {}", tTabStr, t->Get_c_mail_sid());
       result += std::format(L"\n{}c_read_flag: {}", tTabStr, t->Get_c_read_flag());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_title: {}", tTabStr, t->Get_c_title());
       result += std::format(L"\n{}c_msg: {}", tTabStr, t->Get_c_msg());
       result += std::format(L"\n{}c_msg_args: {}", tTabStr, t->Get_c_msg_args());
       result += std::format(L"\n{}c_reward_sid: {}", tTabStr, t->Get_c_reward_sid());
       result += std::format(L"\n{}c_date_expire: {}", tTabStr, t->Get_c_date_expire());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class MAIL_BY_USER;

class MAIL_BY_USER_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::MAIL_BY_USER;

public:
   // (7)
   explicit MAIL_BY_USER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::MAIL_BY_USER, buf)
   {
   }

   // (9)
   explicit MAIL_BY_USER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       MailId mail_id,
       ItemSid mail_sid,
       ReadFlag read_flag,
       RewardFlag reward_flag,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* msg_args,
       int64_t reward_sid,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::MAIL_BY_USER, buf)
   {
       WriteValue(user_id);

       WriteValue(mail_id);

       WriteValue(mail_sid);

       WriteValue(read_flag);

       WriteValue(reward_flag);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msg_argsOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msg_argsLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(reward_sid);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       msg_argsOffset = GetPacketSize();
       msg_argsLength = (PacketSize)wcsnlen_s(msg_args, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msg_argsLength) WriteString(L"", 0);
       else WriteString(msg_args, msg_argsLength); 

   }

   // (8)
   explicit MAIL_BY_USER_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       MailId mail_id,
       ItemSid mail_sid,
       ReadFlag read_flag,
       RewardFlag reward_flag,
       const wchar_t* title,
       const wchar_t* msg,
       const wchar_t* msg_args,
       int64_t reward_sid,
       time_t date_expire,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(mail_id);

       WriteValue(mail_sid);

       WriteValue(read_flag);

       WriteValue(reward_flag);

       PacketSize& titleOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& titleLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msg_argsOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msg_argsLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(reward_sid);

       WriteValue(date_expire);

       WriteValue(date_updated);

       WriteValue(date_created);


       titleOffset = GetPacketSize();
       titleLength = (PacketSize)wcsnlen_s(title, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == titleLength) WriteString(L"", 0);
       else WriteString(title, titleLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

       msg_argsOffset = GetPacketSize();
       msg_argsLength = (PacketSize)wcsnlen_s(msg_args, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msg_argsLength) WriteString(L"", 0);
       else WriteString(msg_args, msg_argsLength); 

   }

   MAIL_BY_USER& GetPacket() const noexcept { return *reinterpret_cast<MAIL_BY_USER*>(GetPacketBufPtr()); }
   MAIL_BY_USER* GetPacketPtr() const noexcept { return reinterpret_cast<MAIL_BY_USER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class MAIL_BY_USER : public Packet
{
   public:
      using Writer = MAIL_BY_USER_WRITER;


private:
   UserId m_c_user_id;

   MailId m_c_mail_id;

   ItemSid m_c_mail_sid;

   ReadFlag m_c_read_flag;

   RewardFlag m_c_reward_flag;

   PacketSize m_c_title_offset = 0;
   PacketSize m_c_title_length = 0;

   PacketSize m_c_msg_offset = 0;
   PacketSize m_c_msg_length = 0;

   PacketSize m_c_msg_args_offset = 0;
   PacketSize m_c_msg_args_length = 0;

   int64_t m_c_reward_sid;

   time_t m_c_date_expire;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   MAIL_BY_USER()
   {
       memset(this, 0, sizeof(MAIL_BY_USER));
       mPacketSize = static_cast<PacketSize>(sizeof(MAIL_BY_USER));
   }
   MAIL_BY_USER(const MAIL_BY_USER&) = delete;
   MAIL_BY_USER(MAIL_BY_USER &&) = delete;
   MAIL_BY_USER& operator = (const MAIL_BY_USER&) = delete;
   MAIL_BY_USER& operator = (MAIL_BY_USER &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   MailId Get_c_mail_id() const noexcept { return m_c_mail_id; }
   MailId& Ref_c_mail_id() noexcept { return m_c_mail_id; }
   const MailId& ConstRef_c_mail_id() const noexcept { return m_c_mail_id; }

   ItemSid Get_c_mail_sid() const noexcept { return m_c_mail_sid; }
   ItemSid& Ref_c_mail_sid() noexcept { return m_c_mail_sid; }
   const ItemSid& ConstRef_c_mail_sid() const noexcept { return m_c_mail_sid; }

   ReadFlag Get_c_read_flag() const noexcept { return m_c_read_flag; }
   ReadFlag& Ref_c_read_flag() noexcept { return m_c_read_flag; }
   const ReadFlag& ConstRef_c_read_flag() const noexcept { return m_c_read_flag; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   const wchar_t* Get_c_title() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   wchar_t* Ref_c_title()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_title_offset) + m_c_title_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_title_offset);
   }

   const wchar_t* Get_c_msg() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   wchar_t* Ref_c_msg()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_offset) + m_c_msg_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_offset);
   }

   const wchar_t* Get_c_msg_args() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_c_msg_args_offset) + m_c_msg_args_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_c_msg_args_offset);
   }

   wchar_t* Ref_c_msg_args()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_c_msg_args_offset) + m_c_msg_args_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_c_msg_args_offset);
   }

   int64_t Get_c_reward_sid() const noexcept { return m_c_reward_sid; }
   int64_t& Ref_c_reward_sid() noexcept { return m_c_reward_sid; }
   const int64_t& ConstRef_c_reward_sid() const noexcept { return m_c_reward_sid; }

   time_t Get_c_date_expire() const noexcept { return m_c_date_expire; }
   time_t& Ref_c_date_expire() noexcept { return m_c_date_expire; }
   const time_t& ConstRef_c_date_expire() const noexcept { return m_c_date_expire; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(MAIL_BY_USER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(MAIL_BY_USER) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(MAIL_BY_USER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(MAIL_BY_USER))
           {
               size += sizeof(MAIL_BY_USER);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(MailId);
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ReadFlag);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           if (GetPacketSize() < m_c_title_offset)
               return false;

           if (0 < m_c_title_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_title_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_offset)
               return false;

           if (0 < m_c_msg_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_c_msg_args_offset)
               return false;

           if (0 < m_c_msg_args_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_c_msg_args_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(int64_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, MAIL_BY_USER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_mail_id: {}", tTabStr, t->Get_c_mail_id());
       result += std::format(L"\n{}c_mail_sid: {}", tTabStr, t->Get_c_mail_sid());
       result += std::format(L"\n{}c_read_flag: {}", tTabStr, t->Get_c_read_flag());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_title: {}", tTabStr, t->Get_c_title());
       result += std::format(L"\n{}c_msg: {}", tTabStr, t->Get_c_msg());
       result += std::format(L"\n{}c_msg_args: {}", tTabStr, t->Get_c_msg_args());
       result += std::format(L"\n{}c_reward_sid: {}", tTabStr, t->Get_c_reward_sid());
       result += std::format(L"\n{}c_date_expire: {}", tTabStr, t->Get_c_date_expire());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class MISSION_DELETED;

class MISSION_DELETED_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::MISSION_DELETED;

public:
   // (7)
   explicit MISSION_DELETED_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::MISSION_DELETED, buf)
   {
   }

   // (9)
   explicit MISSION_DELETED_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       UserId user_id,
       MissionId mission_id,
       MissionSid mission_sid,
       ActivityPoint activity_point_delta,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::MISSION_DELETED, buf)
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(mission_id);

       WriteValue(mission_sid);

       WriteValue(activity_point_delta);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   // (8)
   explicit MISSION_DELETED_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       UserId user_id,
       MissionId mission_id,
       MissionSid mission_sid,
       ActivityPoint activity_point_delta,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(mission_id);

       WriteValue(mission_sid);

       WriteValue(activity_point_delta);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   MISSION_DELETED& GetPacket() const noexcept { return *reinterpret_cast<MISSION_DELETED*>(GetPacketBufPtr()); }
   MISSION_DELETED* GetPacketPtr() const noexcept { return reinterpret_cast<MISSION_DELETED*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class MISSION_DELETED : public Packet
{
   public:
      using Writer = MISSION_DELETED_WRITER;


private:
   Uuid m_c_id;

   UserId m_c_user_id;

   MissionId m_c_mission_id;

   MissionSid m_c_mission_sid;

   ActivityPoint m_c_activity_point_delta;

   RewardFlag m_c_reward_flag;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   MISSION_DELETED()
   {
       memset(this, 0, sizeof(MISSION_DELETED));
       mPacketSize = static_cast<PacketSize>(sizeof(MISSION_DELETED));
   }
   MISSION_DELETED(const MISSION_DELETED&) = delete;
   MISSION_DELETED(MISSION_DELETED &&) = delete;
   MISSION_DELETED& operator = (const MISSION_DELETED&) = delete;
   MISSION_DELETED& operator = (MISSION_DELETED &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   MissionId Get_c_mission_id() const noexcept { return m_c_mission_id; }
   MissionId& Ref_c_mission_id() noexcept { return m_c_mission_id; }
   const MissionId& ConstRef_c_mission_id() const noexcept { return m_c_mission_id; }

   MissionSid Get_c_mission_sid() const noexcept { return m_c_mission_sid; }
   MissionSid& Ref_c_mission_sid() noexcept { return m_c_mission_sid; }
   const MissionSid& ConstRef_c_mission_sid() const noexcept { return m_c_mission_sid; }

   ActivityPoint Get_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }
   ActivityPoint& Ref_c_activity_point_delta() noexcept { return m_c_activity_point_delta; }
   const ActivityPoint& ConstRef_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(MISSION_DELETED) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(MISSION_DELETED) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(MISSION_DELETED); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(MISSION_DELETED))
           {
               size += sizeof(MISSION_DELETED);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(MissionId);
       }

       {
           thisSize += sizeof(MissionSid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, MISSION_DELETED>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_mission_id: {}", tTabStr, t->Get_c_mission_id());
       result += std::format(L"\n{}c_mission_sid: {}", tTabStr, t->Get_c_mission_sid());
       result += std::format(L"\n{}c_activity_point_delta: {}", tTabStr, t->Get_c_activity_point_delta());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class MISSION_DELETED_ALL;

class MISSION_DELETED_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::MISSION_DELETED_ALL;

public:
   // (7)
   explicit MISSION_DELETED_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::MISSION_DELETED_ALL, buf)
   {
   }

   // (9)
   explicit MISSION_DELETED_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       UserId user_id,
       MissionId mission_id,
       MissionSid mission_sid,
       ActivityPoint activity_point_delta,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::MISSION_DELETED_ALL, buf)
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(mission_id);

       WriteValue(mission_sid);

       WriteValue(activity_point_delta);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   // (8)
   explicit MISSION_DELETED_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       UserId user_id,
       MissionId mission_id,
       MissionSid mission_sid,
       ActivityPoint activity_point_delta,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(mission_id);

       WriteValue(mission_sid);

       WriteValue(activity_point_delta);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   MISSION_DELETED_ALL& GetPacket() const noexcept { return *reinterpret_cast<MISSION_DELETED_ALL*>(GetPacketBufPtr()); }
   MISSION_DELETED_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<MISSION_DELETED_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class MISSION_DELETED_ALL : public Packet
{
   public:
      using Writer = MISSION_DELETED_ALL_WRITER;


private:
   Uuid m_c_id;

   UserId m_c_user_id;

   MissionId m_c_mission_id;

   MissionSid m_c_mission_sid;

   ActivityPoint m_c_activity_point_delta;

   RewardFlag m_c_reward_flag;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   MISSION_DELETED_ALL()
   {
       memset(this, 0, sizeof(MISSION_DELETED_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(MISSION_DELETED_ALL));
   }
   MISSION_DELETED_ALL(const MISSION_DELETED_ALL&) = delete;
   MISSION_DELETED_ALL(MISSION_DELETED_ALL &&) = delete;
   MISSION_DELETED_ALL& operator = (const MISSION_DELETED_ALL&) = delete;
   MISSION_DELETED_ALL& operator = (MISSION_DELETED_ALL &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   MissionId Get_c_mission_id() const noexcept { return m_c_mission_id; }
   MissionId& Ref_c_mission_id() noexcept { return m_c_mission_id; }
   const MissionId& ConstRef_c_mission_id() const noexcept { return m_c_mission_id; }

   MissionSid Get_c_mission_sid() const noexcept { return m_c_mission_sid; }
   MissionSid& Ref_c_mission_sid() noexcept { return m_c_mission_sid; }
   const MissionSid& ConstRef_c_mission_sid() const noexcept { return m_c_mission_sid; }

   ActivityPoint Get_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }
   ActivityPoint& Ref_c_activity_point_delta() noexcept { return m_c_activity_point_delta; }
   const ActivityPoint& ConstRef_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(MISSION_DELETED_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(MISSION_DELETED_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(MISSION_DELETED_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(MISSION_DELETED_ALL))
           {
               size += sizeof(MISSION_DELETED_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(MissionId);
       }

       {
           thisSize += sizeof(MissionSid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, MISSION_DELETED_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_mission_id: {}", tTabStr, t->Get_c_mission_id());
       result += std::format(L"\n{}c_mission_sid: {}", tTabStr, t->Get_c_mission_sid());
       result += std::format(L"\n{}c_activity_point_delta: {}", tTabStr, t->Get_c_activity_point_delta());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class MISSION;

class MISSION_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::MISSION;

public:
   // (7)
   explicit MISSION_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::MISSION, buf)
   {
   }

   // (9)
   explicit MISSION_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       MissionId mission_id,
       MissionSid mission_sid,
       ActivityPoint activity_point_delta,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::MISSION, buf)
   {
       WriteValue(user_id);

       WriteValue(mission_id);

       WriteValue(mission_sid);

       WriteValue(activity_point_delta);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit MISSION_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       MissionId mission_id,
       MissionSid mission_sid,
       ActivityPoint activity_point_delta,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(mission_id);

       WriteValue(mission_sid);

       WriteValue(activity_point_delta);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   MISSION& GetPacket() const noexcept { return *reinterpret_cast<MISSION*>(GetPacketBufPtr()); }
   MISSION* GetPacketPtr() const noexcept { return reinterpret_cast<MISSION*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class MISSION : public Packet
{
   public:
      using Writer = MISSION_WRITER;


private:
   UserId m_c_user_id;

   MissionId m_c_mission_id;

   MissionSid m_c_mission_sid;

   ActivityPoint m_c_activity_point_delta;

   RewardFlag m_c_reward_flag;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   MISSION()
   {
       memset(this, 0, sizeof(MISSION));
       mPacketSize = static_cast<PacketSize>(sizeof(MISSION));
   }
   MISSION(const MISSION&) = delete;
   MISSION(MISSION &&) = delete;
   MISSION& operator = (const MISSION&) = delete;
   MISSION& operator = (MISSION &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   MissionId Get_c_mission_id() const noexcept { return m_c_mission_id; }
   MissionId& Ref_c_mission_id() noexcept { return m_c_mission_id; }
   const MissionId& ConstRef_c_mission_id() const noexcept { return m_c_mission_id; }

   MissionSid Get_c_mission_sid() const noexcept { return m_c_mission_sid; }
   MissionSid& Ref_c_mission_sid() noexcept { return m_c_mission_sid; }
   const MissionSid& ConstRef_c_mission_sid() const noexcept { return m_c_mission_sid; }

   ActivityPoint Get_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }
   ActivityPoint& Ref_c_activity_point_delta() noexcept { return m_c_activity_point_delta; }
   const ActivityPoint& ConstRef_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(MISSION) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(MISSION) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(MISSION); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(MISSION))
           {
               size += sizeof(MISSION);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(MissionId);
       }

       {
           thisSize += sizeof(MissionSid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, MISSION>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_mission_id: {}", tTabStr, t->Get_c_mission_id());
       result += std::format(L"\n{}c_mission_sid: {}", tTabStr, t->Get_c_mission_sid());
       result += std::format(L"\n{}c_activity_point_delta: {}", tTabStr, t->Get_c_activity_point_delta());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class MISSION_ALL;

class MISSION_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::MISSION_ALL;

public:
   // (7)
   explicit MISSION_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::MISSION_ALL, buf)
   {
   }

   // (9)
   explicit MISSION_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       MissionId mission_id,
       MissionSid mission_sid,
       ActivityPoint activity_point_delta,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::MISSION_ALL, buf)
   {
       WriteValue(user_id);

       WriteValue(mission_id);

       WriteValue(mission_sid);

       WriteValue(activity_point_delta);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit MISSION_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       MissionId mission_id,
       MissionSid mission_sid,
       ActivityPoint activity_point_delta,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(mission_id);

       WriteValue(mission_sid);

       WriteValue(activity_point_delta);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   MISSION_ALL& GetPacket() const noexcept { return *reinterpret_cast<MISSION_ALL*>(GetPacketBufPtr()); }
   MISSION_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<MISSION_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class MISSION_ALL : public Packet
{
   public:
      using Writer = MISSION_ALL_WRITER;


private:
   UserId m_c_user_id;

   MissionId m_c_mission_id;

   MissionSid m_c_mission_sid;

   ActivityPoint m_c_activity_point_delta;

   RewardFlag m_c_reward_flag;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   MISSION_ALL()
   {
       memset(this, 0, sizeof(MISSION_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(MISSION_ALL));
   }
   MISSION_ALL(const MISSION_ALL&) = delete;
   MISSION_ALL(MISSION_ALL &&) = delete;
   MISSION_ALL& operator = (const MISSION_ALL&) = delete;
   MISSION_ALL& operator = (MISSION_ALL &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   MissionId Get_c_mission_id() const noexcept { return m_c_mission_id; }
   MissionId& Ref_c_mission_id() noexcept { return m_c_mission_id; }
   const MissionId& ConstRef_c_mission_id() const noexcept { return m_c_mission_id; }

   MissionSid Get_c_mission_sid() const noexcept { return m_c_mission_sid; }
   MissionSid& Ref_c_mission_sid() noexcept { return m_c_mission_sid; }
   const MissionSid& ConstRef_c_mission_sid() const noexcept { return m_c_mission_sid; }

   ActivityPoint Get_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }
   ActivityPoint& Ref_c_activity_point_delta() noexcept { return m_c_activity_point_delta; }
   const ActivityPoint& ConstRef_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(MISSION_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(MISSION_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(MISSION_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(MISSION_ALL))
           {
               size += sizeof(MISSION_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(MissionId);
       }

       {
           thisSize += sizeof(MissionSid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, MISSION_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_mission_id: {}", tTabStr, t->Get_c_mission_id());
       result += std::format(L"\n{}c_mission_sid: {}", tTabStr, t->Get_c_mission_sid());
       result += std::format(L"\n{}c_activity_point_delta: {}", tTabStr, t->Get_c_activity_point_delta());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class MISSION_BY_USER;

class MISSION_BY_USER_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::MISSION_BY_USER;

public:
   // (7)
   explicit MISSION_BY_USER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::MISSION_BY_USER, buf)
   {
   }

   // (9)
   explicit MISSION_BY_USER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       MissionId mission_id,
       MissionSid mission_sid,
       ActivityPoint activity_point_delta,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::MISSION_BY_USER, buf)
   {
       WriteValue(user_id);

       WriteValue(mission_id);

       WriteValue(mission_sid);

       WriteValue(activity_point_delta);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit MISSION_BY_USER_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       MissionId mission_id,
       MissionSid mission_sid,
       ActivityPoint activity_point_delta,
       RewardFlag reward_flag,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(mission_id);

       WriteValue(mission_sid);

       WriteValue(activity_point_delta);

       WriteValue(reward_flag);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   MISSION_BY_USER& GetPacket() const noexcept { return *reinterpret_cast<MISSION_BY_USER*>(GetPacketBufPtr()); }
   MISSION_BY_USER* GetPacketPtr() const noexcept { return reinterpret_cast<MISSION_BY_USER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class MISSION_BY_USER : public Packet
{
   public:
      using Writer = MISSION_BY_USER_WRITER;


private:
   UserId m_c_user_id;

   MissionId m_c_mission_id;

   MissionSid m_c_mission_sid;

   ActivityPoint m_c_activity_point_delta;

   RewardFlag m_c_reward_flag;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   MISSION_BY_USER()
   {
       memset(this, 0, sizeof(MISSION_BY_USER));
       mPacketSize = static_cast<PacketSize>(sizeof(MISSION_BY_USER));
   }
   MISSION_BY_USER(const MISSION_BY_USER&) = delete;
   MISSION_BY_USER(MISSION_BY_USER &&) = delete;
   MISSION_BY_USER& operator = (const MISSION_BY_USER&) = delete;
   MISSION_BY_USER& operator = (MISSION_BY_USER &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   MissionId Get_c_mission_id() const noexcept { return m_c_mission_id; }
   MissionId& Ref_c_mission_id() noexcept { return m_c_mission_id; }
   const MissionId& ConstRef_c_mission_id() const noexcept { return m_c_mission_id; }

   MissionSid Get_c_mission_sid() const noexcept { return m_c_mission_sid; }
   MissionSid& Ref_c_mission_sid() noexcept { return m_c_mission_sid; }
   const MissionSid& ConstRef_c_mission_sid() const noexcept { return m_c_mission_sid; }

   ActivityPoint Get_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }
   ActivityPoint& Ref_c_activity_point_delta() noexcept { return m_c_activity_point_delta; }
   const ActivityPoint& ConstRef_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }

   RewardFlag Get_c_reward_flag() const noexcept { return m_c_reward_flag; }
   RewardFlag& Ref_c_reward_flag() noexcept { return m_c_reward_flag; }
   const RewardFlag& ConstRef_c_reward_flag() const noexcept { return m_c_reward_flag; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(MISSION_BY_USER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(MISSION_BY_USER) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(MISSION_BY_USER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(MISSION_BY_USER))
           {
               size += sizeof(MISSION_BY_USER);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(MissionId);
       }

       {
           thisSize += sizeof(MissionSid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(RewardFlag);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, MISSION_BY_USER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_mission_id: {}", tTabStr, t->Get_c_mission_id());
       result += std::format(L"\n{}c_mission_sid: {}", tTabStr, t->Get_c_mission_sid());
       result += std::format(L"\n{}c_activity_point_delta: {}", tTabStr, t->Get_c_activity_point_delta());
       result += std::format(L"\n{}c_reward_flag: {}", tTabStr, t->Get_c_reward_flag());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class QUEST_DELETED;

class QUEST_DELETED_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::QUEST_DELETED;

public:
   // (7)
   explicit QUEST_DELETED_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::QUEST_DELETED, buf)
   {
   }

   // (9)
   explicit QUEST_DELETED_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       UserId user_id,
       QuestId quest_id,
       QuestSid quest_sid,
       ActivityPoint activity_point_delta,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::QUEST_DELETED, buf)
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(quest_id);

       WriteValue(quest_sid);

       WriteValue(activity_point_delta);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   // (8)
   explicit QUEST_DELETED_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       UserId user_id,
       QuestId quest_id,
       QuestSid quest_sid,
       ActivityPoint activity_point_delta,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(quest_id);

       WriteValue(quest_sid);

       WriteValue(activity_point_delta);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   QUEST_DELETED& GetPacket() const noexcept { return *reinterpret_cast<QUEST_DELETED*>(GetPacketBufPtr()); }
   QUEST_DELETED* GetPacketPtr() const noexcept { return reinterpret_cast<QUEST_DELETED*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class QUEST_DELETED : public Packet
{
   public:
      using Writer = QUEST_DELETED_WRITER;


private:
   Uuid m_c_id;

   UserId m_c_user_id;

   QuestId m_c_quest_id;

   QuestSid m_c_quest_sid;

   ActivityPoint m_c_activity_point_delta;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   QUEST_DELETED()
   {
       memset(this, 0, sizeof(QUEST_DELETED));
       mPacketSize = static_cast<PacketSize>(sizeof(QUEST_DELETED));
   }
   QUEST_DELETED(const QUEST_DELETED&) = delete;
   QUEST_DELETED(QUEST_DELETED &&) = delete;
   QUEST_DELETED& operator = (const QUEST_DELETED&) = delete;
   QUEST_DELETED& operator = (QUEST_DELETED &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   QuestId Get_c_quest_id() const noexcept { return m_c_quest_id; }
   QuestId& Ref_c_quest_id() noexcept { return m_c_quest_id; }
   const QuestId& ConstRef_c_quest_id() const noexcept { return m_c_quest_id; }

   QuestSid Get_c_quest_sid() const noexcept { return m_c_quest_sid; }
   QuestSid& Ref_c_quest_sid() noexcept { return m_c_quest_sid; }
   const QuestSid& ConstRef_c_quest_sid() const noexcept { return m_c_quest_sid; }

   ActivityPoint Get_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }
   ActivityPoint& Ref_c_activity_point_delta() noexcept { return m_c_activity_point_delta; }
   const ActivityPoint& ConstRef_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(QUEST_DELETED) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(QUEST_DELETED) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(QUEST_DELETED); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(QUEST_DELETED))
           {
               size += sizeof(QUEST_DELETED);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(QuestId);
       }

       {
           thisSize += sizeof(QuestSid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, QUEST_DELETED>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_quest_id: {}", tTabStr, t->Get_c_quest_id());
       result += std::format(L"\n{}c_quest_sid: {}", tTabStr, t->Get_c_quest_sid());
       result += std::format(L"\n{}c_activity_point_delta: {}", tTabStr, t->Get_c_activity_point_delta());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class QUEST_DELETED_ALL;

class QUEST_DELETED_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::QUEST_DELETED_ALL;

public:
   // (7)
   explicit QUEST_DELETED_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::QUEST_DELETED_ALL, buf)
   {
   }

   // (9)
   explicit QUEST_DELETED_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       Uuid id,
       UserId user_id,
       QuestId quest_id,
       QuestSid quest_sid,
       ActivityPoint activity_point_delta,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
       :
       PacketWriter(trait, PacketTypes::QUEST_DELETED_ALL, buf)
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(quest_id);

       WriteValue(quest_sid);

       WriteValue(activity_point_delta);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   // (8)
   explicit QUEST_DELETED_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       Uuid id,
       UserId user_id,
       QuestId quest_id,
       QuestSid quest_sid,
       ActivityPoint activity_point_delta,
       time_t date_updated,
       time_t date_created,
       time_t date_deleted
   )
   {
       WriteValue(id);

       WriteValue(user_id);

       WriteValue(quest_id);

       WriteValue(quest_sid);

       WriteValue(activity_point_delta);

       WriteValue(date_updated);

       WriteValue(date_created);

       WriteValue(date_deleted);


   }

   QUEST_DELETED_ALL& GetPacket() const noexcept { return *reinterpret_cast<QUEST_DELETED_ALL*>(GetPacketBufPtr()); }
   QUEST_DELETED_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<QUEST_DELETED_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class QUEST_DELETED_ALL : public Packet
{
   public:
      using Writer = QUEST_DELETED_ALL_WRITER;


private:
   Uuid m_c_id;

   UserId m_c_user_id;

   QuestId m_c_quest_id;

   QuestSid m_c_quest_sid;

   ActivityPoint m_c_activity_point_delta;

   time_t m_c_date_updated;

   time_t m_c_date_created;

   time_t m_c_date_deleted;

public:
   QUEST_DELETED_ALL()
   {
       memset(this, 0, sizeof(QUEST_DELETED_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(QUEST_DELETED_ALL));
   }
   QUEST_DELETED_ALL(const QUEST_DELETED_ALL&) = delete;
   QUEST_DELETED_ALL(QUEST_DELETED_ALL &&) = delete;
   QUEST_DELETED_ALL& operator = (const QUEST_DELETED_ALL&) = delete;
   QUEST_DELETED_ALL& operator = (QUEST_DELETED_ALL &&) = delete;

   Uuid Get_c_id() const noexcept { return m_c_id; }
   Uuid& Ref_c_id() noexcept { return m_c_id; }
   const Uuid& ConstRef_c_id() const noexcept { return m_c_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   QuestId Get_c_quest_id() const noexcept { return m_c_quest_id; }
   QuestId& Ref_c_quest_id() noexcept { return m_c_quest_id; }
   const QuestId& ConstRef_c_quest_id() const noexcept { return m_c_quest_id; }

   QuestSid Get_c_quest_sid() const noexcept { return m_c_quest_sid; }
   QuestSid& Ref_c_quest_sid() noexcept { return m_c_quest_sid; }
   const QuestSid& ConstRef_c_quest_sid() const noexcept { return m_c_quest_sid; }

   ActivityPoint Get_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }
   ActivityPoint& Ref_c_activity_point_delta() noexcept { return m_c_activity_point_delta; }
   const ActivityPoint& ConstRef_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }

   time_t Get_c_date_deleted() const noexcept { return m_c_date_deleted; }
   time_t& Ref_c_date_deleted() noexcept { return m_c_date_deleted; }
   const time_t& ConstRef_c_date_deleted() const noexcept { return m_c_date_deleted; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(QUEST_DELETED_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(QUEST_DELETED_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(QUEST_DELETED_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(QUEST_DELETED_ALL))
           {
               size += sizeof(QUEST_DELETED_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(Uuid);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(QuestId);
       }

       {
           thisSize += sizeof(QuestSid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, QUEST_DELETED_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_id: {}", tTabStr, t->Get_c_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_quest_id: {}", tTabStr, t->Get_c_quest_id());
       result += std::format(L"\n{}c_quest_sid: {}", tTabStr, t->Get_c_quest_sid());
       result += std::format(L"\n{}c_activity_point_delta: {}", tTabStr, t->Get_c_activity_point_delta());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       result += std::format(L"\n{}c_date_deleted: {}", tTabStr, t->Get_c_date_deleted());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class QUEST;

class QUEST_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::QUEST;

public:
   // (7)
   explicit QUEST_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::QUEST, buf)
   {
   }

   // (9)
   explicit QUEST_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       QuestId quest_id,
       QuestSid quest_sid,
       ActivityPoint activity_point_delta,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::QUEST, buf)
   {
       WriteValue(user_id);

       WriteValue(quest_id);

       WriteValue(quest_sid);

       WriteValue(activity_point_delta);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit QUEST_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       QuestId quest_id,
       QuestSid quest_sid,
       ActivityPoint activity_point_delta,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(quest_id);

       WriteValue(quest_sid);

       WriteValue(activity_point_delta);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   QUEST& GetPacket() const noexcept { return *reinterpret_cast<QUEST*>(GetPacketBufPtr()); }
   QUEST* GetPacketPtr() const noexcept { return reinterpret_cast<QUEST*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class QUEST : public Packet
{
   public:
      using Writer = QUEST_WRITER;


private:
   UserId m_c_user_id;

   QuestId m_c_quest_id;

   QuestSid m_c_quest_sid;

   ActivityPoint m_c_activity_point_delta;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   QUEST()
   {
       memset(this, 0, sizeof(QUEST));
       mPacketSize = static_cast<PacketSize>(sizeof(QUEST));
   }
   QUEST(const QUEST&) = delete;
   QUEST(QUEST &&) = delete;
   QUEST& operator = (const QUEST&) = delete;
   QUEST& operator = (QUEST &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   QuestId Get_c_quest_id() const noexcept { return m_c_quest_id; }
   QuestId& Ref_c_quest_id() noexcept { return m_c_quest_id; }
   const QuestId& ConstRef_c_quest_id() const noexcept { return m_c_quest_id; }

   QuestSid Get_c_quest_sid() const noexcept { return m_c_quest_sid; }
   QuestSid& Ref_c_quest_sid() noexcept { return m_c_quest_sid; }
   const QuestSid& ConstRef_c_quest_sid() const noexcept { return m_c_quest_sid; }

   ActivityPoint Get_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }
   ActivityPoint& Ref_c_activity_point_delta() noexcept { return m_c_activity_point_delta; }
   const ActivityPoint& ConstRef_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(QUEST) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(QUEST) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(QUEST); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(QUEST))
           {
               size += sizeof(QUEST);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(QuestId);
       }

       {
           thisSize += sizeof(QuestSid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, QUEST>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_quest_id: {}", tTabStr, t->Get_c_quest_id());
       result += std::format(L"\n{}c_quest_sid: {}", tTabStr, t->Get_c_quest_sid());
       result += std::format(L"\n{}c_activity_point_delta: {}", tTabStr, t->Get_c_activity_point_delta());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class QUEST_ALL;

class QUEST_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::QUEST_ALL;

public:
   // (7)
   explicit QUEST_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::QUEST_ALL, buf)
   {
   }

   // (9)
   explicit QUEST_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       QuestId quest_id,
       QuestSid quest_sid,
       ActivityPoint activity_point_delta,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::QUEST_ALL, buf)
   {
       WriteValue(user_id);

       WriteValue(quest_id);

       WriteValue(quest_sid);

       WriteValue(activity_point_delta);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit QUEST_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       QuestId quest_id,
       QuestSid quest_sid,
       ActivityPoint activity_point_delta,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(quest_id);

       WriteValue(quest_sid);

       WriteValue(activity_point_delta);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   QUEST_ALL& GetPacket() const noexcept { return *reinterpret_cast<QUEST_ALL*>(GetPacketBufPtr()); }
   QUEST_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<QUEST_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class QUEST_ALL : public Packet
{
   public:
      using Writer = QUEST_ALL_WRITER;


private:
   UserId m_c_user_id;

   QuestId m_c_quest_id;

   QuestSid m_c_quest_sid;

   ActivityPoint m_c_activity_point_delta;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   QUEST_ALL()
   {
       memset(this, 0, sizeof(QUEST_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(QUEST_ALL));
   }
   QUEST_ALL(const QUEST_ALL&) = delete;
   QUEST_ALL(QUEST_ALL &&) = delete;
   QUEST_ALL& operator = (const QUEST_ALL&) = delete;
   QUEST_ALL& operator = (QUEST_ALL &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   QuestId Get_c_quest_id() const noexcept { return m_c_quest_id; }
   QuestId& Ref_c_quest_id() noexcept { return m_c_quest_id; }
   const QuestId& ConstRef_c_quest_id() const noexcept { return m_c_quest_id; }

   QuestSid Get_c_quest_sid() const noexcept { return m_c_quest_sid; }
   QuestSid& Ref_c_quest_sid() noexcept { return m_c_quest_sid; }
   const QuestSid& ConstRef_c_quest_sid() const noexcept { return m_c_quest_sid; }

   ActivityPoint Get_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }
   ActivityPoint& Ref_c_activity_point_delta() noexcept { return m_c_activity_point_delta; }
   const ActivityPoint& ConstRef_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(QUEST_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(QUEST_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(QUEST_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(QUEST_ALL))
           {
               size += sizeof(QUEST_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(QuestId);
       }

       {
           thisSize += sizeof(QuestSid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, QUEST_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_quest_id: {}", tTabStr, t->Get_c_quest_id());
       result += std::format(L"\n{}c_quest_sid: {}", tTabStr, t->Get_c_quest_sid());
       result += std::format(L"\n{}c_activity_point_delta: {}", tTabStr, t->Get_c_activity_point_delta());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class QUEST_BY_USER;

class QUEST_BY_USER_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::QUEST_BY_USER;

public:
   // (7)
   explicit QUEST_BY_USER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::QUEST_BY_USER, buf)
   {
   }

   // (9)
   explicit QUEST_BY_USER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       QuestId quest_id,
       QuestSid quest_sid,
       ActivityPoint activity_point_delta,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::QUEST_BY_USER, buf)
   {
       WriteValue(user_id);

       WriteValue(quest_id);

       WriteValue(quest_sid);

       WriteValue(activity_point_delta);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit QUEST_BY_USER_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       QuestId quest_id,
       QuestSid quest_sid,
       ActivityPoint activity_point_delta,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(user_id);

       WriteValue(quest_id);

       WriteValue(quest_sid);

       WriteValue(activity_point_delta);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   QUEST_BY_USER& GetPacket() const noexcept { return *reinterpret_cast<QUEST_BY_USER*>(GetPacketBufPtr()); }
   QUEST_BY_USER* GetPacketPtr() const noexcept { return reinterpret_cast<QUEST_BY_USER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class QUEST_BY_USER : public Packet
{
   public:
      using Writer = QUEST_BY_USER_WRITER;


private:
   UserId m_c_user_id;

   QuestId m_c_quest_id;

   QuestSid m_c_quest_sid;

   ActivityPoint m_c_activity_point_delta;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   QUEST_BY_USER()
   {
       memset(this, 0, sizeof(QUEST_BY_USER));
       mPacketSize = static_cast<PacketSize>(sizeof(QUEST_BY_USER));
   }
   QUEST_BY_USER(const QUEST_BY_USER&) = delete;
   QUEST_BY_USER(QUEST_BY_USER &&) = delete;
   QUEST_BY_USER& operator = (const QUEST_BY_USER&) = delete;
   QUEST_BY_USER& operator = (QUEST_BY_USER &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   QuestId Get_c_quest_id() const noexcept { return m_c_quest_id; }
   QuestId& Ref_c_quest_id() noexcept { return m_c_quest_id; }
   const QuestId& ConstRef_c_quest_id() const noexcept { return m_c_quest_id; }

   QuestSid Get_c_quest_sid() const noexcept { return m_c_quest_sid; }
   QuestSid& Ref_c_quest_sid() noexcept { return m_c_quest_sid; }
   const QuestSid& ConstRef_c_quest_sid() const noexcept { return m_c_quest_sid; }

   ActivityPoint Get_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }
   ActivityPoint& Ref_c_activity_point_delta() noexcept { return m_c_activity_point_delta; }
   const ActivityPoint& ConstRef_c_activity_point_delta() const noexcept { return m_c_activity_point_delta; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(QUEST_BY_USER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(QUEST_BY_USER) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(QUEST_BY_USER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(QUEST_BY_USER))
           {
               size += sizeof(QUEST_BY_USER);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(QuestId);
       }

       {
           thisSize += sizeof(QuestSid);
       }

       {
           thisSize += sizeof(ActivityPoint);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, QUEST_BY_USER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_quest_id: {}", tTabStr, t->Get_c_quest_id());
       result += std::format(L"\n{}c_quest_sid: {}", tTabStr, t->Get_c_quest_sid());
       result += std::format(L"\n{}c_activity_point_delta: {}", tTabStr, t->Get_c_activity_point_delta());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class USER_POS;

class USER_POS_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::USER_POS;

public:
   // (7)
   explicit USER_POS_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::USER_POS, buf)
   {
   }

   // (9)
   explicit USER_POS_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       GameType game_type,
       GameId game_id,
       Scalar x,
       Scalar y,
       time_t date_updated
   )
       :
       PacketWriter(trait, PacketTypes::USER_POS, buf)
   {
       WriteValue(user_id);

       WriteValue(game_type);

       WriteValue(game_id);

       WriteValue(x);

       WriteValue(y);

       WriteValue(date_updated);


   }

   // (8)
   explicit USER_POS_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       GameType game_type,
       GameId game_id,
       Scalar x,
       Scalar y,
       time_t date_updated
   )
   {
       WriteValue(user_id);

       WriteValue(game_type);

       WriteValue(game_id);

       WriteValue(x);

       WriteValue(y);

       WriteValue(date_updated);


   }

   USER_POS& GetPacket() const noexcept { return *reinterpret_cast<USER_POS*>(GetPacketBufPtr()); }
   USER_POS* GetPacketPtr() const noexcept { return reinterpret_cast<USER_POS*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class USER_POS : public Packet
{
   public:
      using Writer = USER_POS_WRITER;


private:
   UserId m_c_user_id;

   GameType m_c_game_type;

   GameId m_c_game_id;

   Scalar m_c_x;

   Scalar m_c_y;

   time_t m_c_date_updated;

public:
   USER_POS()
   {
       memset(this, 0, sizeof(USER_POS));
       mPacketSize = static_cast<PacketSize>(sizeof(USER_POS));
   }
   USER_POS(const USER_POS&) = delete;
   USER_POS(USER_POS &&) = delete;
   USER_POS& operator = (const USER_POS&) = delete;
   USER_POS& operator = (USER_POS &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GameType Get_c_game_type() const noexcept { return m_c_game_type; }
   GameType& Ref_c_game_type() noexcept { return m_c_game_type; }
   const GameType& ConstRef_c_game_type() const noexcept { return m_c_game_type; }

   GameId Get_c_game_id() const noexcept { return m_c_game_id; }
   GameId& Ref_c_game_id() noexcept { return m_c_game_id; }
   const GameId& ConstRef_c_game_id() const noexcept { return m_c_game_id; }

   Scalar Get_c_x() const noexcept { return m_c_x; }
   Scalar& Ref_c_x() noexcept { return m_c_x; }
   const Scalar& ConstRef_c_x() const noexcept { return m_c_x; }

   Scalar Get_c_y() const noexcept { return m_c_y; }
   Scalar& Ref_c_y() noexcept { return m_c_y; }
   const Scalar& ConstRef_c_y() const noexcept { return m_c_y; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(USER_POS) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(USER_POS) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(USER_POS); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(USER_POS))
           {
               size += sizeof(USER_POS);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GameType);
       }

       {
           thisSize += sizeof(GameId);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, USER_POS>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_game_type: {}", tTabStr, t->Get_c_game_type());
       result += std::format(L"\n{}c_game_id: {}", tTabStr, t->Get_c_game_id());
       result += std::format(L"\n{}c_x: {}", tTabStr, t->Get_c_x());
       result += std::format(L"\n{}c_y: {}", tTabStr, t->Get_c_y());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class USER_POS_ALL;

class USER_POS_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::USER_POS_ALL;

public:
   // (7)
   explicit USER_POS_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::USER_POS_ALL, buf)
   {
   }

   // (9)
   explicit USER_POS_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserId user_id,
       GameType game_type,
       GameId game_id,
       Scalar x,
       Scalar y,
       time_t date_updated
   )
       :
       PacketWriter(trait, PacketTypes::USER_POS_ALL, buf)
   {
       WriteValue(user_id);

       WriteValue(game_type);

       WriteValue(game_id);

       WriteValue(x);

       WriteValue(y);

       WriteValue(date_updated);


   }

   // (8)
   explicit USER_POS_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserId user_id,
       GameType game_type,
       GameId game_id,
       Scalar x,
       Scalar y,
       time_t date_updated
   )
   {
       WriteValue(user_id);

       WriteValue(game_type);

       WriteValue(game_id);

       WriteValue(x);

       WriteValue(y);

       WriteValue(date_updated);


   }

   USER_POS_ALL& GetPacket() const noexcept { return *reinterpret_cast<USER_POS_ALL*>(GetPacketBufPtr()); }
   USER_POS_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<USER_POS_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class USER_POS_ALL : public Packet
{
   public:
      using Writer = USER_POS_ALL_WRITER;


private:
   UserId m_c_user_id;

   GameType m_c_game_type;

   GameId m_c_game_id;

   Scalar m_c_x;

   Scalar m_c_y;

   time_t m_c_date_updated;

public:
   USER_POS_ALL()
   {
       memset(this, 0, sizeof(USER_POS_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(USER_POS_ALL));
   }
   USER_POS_ALL(const USER_POS_ALL&) = delete;
   USER_POS_ALL(USER_POS_ALL &&) = delete;
   USER_POS_ALL& operator = (const USER_POS_ALL&) = delete;
   USER_POS_ALL& operator = (USER_POS_ALL &&) = delete;

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   GameType Get_c_game_type() const noexcept { return m_c_game_type; }
   GameType& Ref_c_game_type() noexcept { return m_c_game_type; }
   const GameType& ConstRef_c_game_type() const noexcept { return m_c_game_type; }

   GameId Get_c_game_id() const noexcept { return m_c_game_id; }
   GameId& Ref_c_game_id() noexcept { return m_c_game_id; }
   const GameId& ConstRef_c_game_id() const noexcept { return m_c_game_id; }

   Scalar Get_c_x() const noexcept { return m_c_x; }
   Scalar& Ref_c_x() noexcept { return m_c_x; }
   const Scalar& ConstRef_c_x() const noexcept { return m_c_x; }

   Scalar Get_c_y() const noexcept { return m_c_y; }
   Scalar& Ref_c_y() noexcept { return m_c_y; }
   const Scalar& ConstRef_c_y() const noexcept { return m_c_y; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(USER_POS_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(USER_POS_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(USER_POS_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(USER_POS_ALL))
           {
               size += sizeof(USER_POS_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(GameType);
       }

       {
           thisSize += sizeof(GameId);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, USER_POS_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_game_type: {}", tTabStr, t->Get_c_game_type());
       result += std::format(L"\n{}c_game_id: {}", tTabStr, t->Get_c_game_id());
       result += std::format(L"\n{}c_x: {}", tTabStr, t->Get_c_x());
       result += std::format(L"\n{}c_y: {}", tTabStr, t->Get_c_y());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class USER;

class USER_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::USER;

public:
   // (7)
   explicit USER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::USER, buf)
   {
   }

   // (9)
   explicit USER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AccountId account_id,
       UserId user_id,
       UserExp user_exp,
       UserExp user_exp_total,
       time_t play_second_total,
       time_t date_login,
       time_t date_logout,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::USER, buf)
   {
       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_exp);

       WriteValue(user_exp_total);

       WriteValue(play_second_total);

       WriteValue(date_login);

       WriteValue(date_logout);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit USER_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       AccountId account_id,
       UserId user_id,
       UserExp user_exp,
       UserExp user_exp_total,
       time_t play_second_total,
       time_t date_login,
       time_t date_logout,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_exp);

       WriteValue(user_exp_total);

       WriteValue(play_second_total);

       WriteValue(date_login);

       WriteValue(date_logout);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   USER& GetPacket() const noexcept { return *reinterpret_cast<USER*>(GetPacketBufPtr()); }
   USER* GetPacketPtr() const noexcept { return reinterpret_cast<USER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class USER : public Packet
{
   public:
      using Writer = USER_WRITER;


private:
   AccountId m_c_account_id;

   UserId m_c_user_id;

   UserExp m_c_user_exp;

   UserExp m_c_user_exp_total;

   time_t m_c_play_second_total;

   time_t m_c_date_login;

   time_t m_c_date_logout;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   USER()
   {
       memset(this, 0, sizeof(USER));
       mPacketSize = static_cast<PacketSize>(sizeof(USER));
   }
   USER(const USER&) = delete;
   USER(USER &&) = delete;
   USER& operator = (const USER&) = delete;
   USER& operator = (USER &&) = delete;

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   UserExp Get_c_user_exp() const noexcept { return m_c_user_exp; }
   UserExp& Ref_c_user_exp() noexcept { return m_c_user_exp; }
   const UserExp& ConstRef_c_user_exp() const noexcept { return m_c_user_exp; }

   UserExp Get_c_user_exp_total() const noexcept { return m_c_user_exp_total; }
   UserExp& Ref_c_user_exp_total() noexcept { return m_c_user_exp_total; }
   const UserExp& ConstRef_c_user_exp_total() const noexcept { return m_c_user_exp_total; }

   time_t Get_c_play_second_total() const noexcept { return m_c_play_second_total; }
   time_t& Ref_c_play_second_total() noexcept { return m_c_play_second_total; }
   const time_t& ConstRef_c_play_second_total() const noexcept { return m_c_play_second_total; }

   time_t Get_c_date_login() const noexcept { return m_c_date_login; }
   time_t& Ref_c_date_login() noexcept { return m_c_date_login; }
   const time_t& ConstRef_c_date_login() const noexcept { return m_c_date_login; }

   time_t Get_c_date_logout() const noexcept { return m_c_date_logout; }
   time_t& Ref_c_date_logout() noexcept { return m_c_date_logout; }
   const time_t& ConstRef_c_date_logout() const noexcept { return m_c_date_logout; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(USER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(USER) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(USER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(USER))
           {
               size += sizeof(USER);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, USER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_user_exp: {}", tTabStr, t->Get_c_user_exp());
       result += std::format(L"\n{}c_user_exp_total: {}", tTabStr, t->Get_c_user_exp_total());
       result += std::format(L"\n{}c_play_second_total: {}", tTabStr, t->Get_c_play_second_total());
       result += std::format(L"\n{}c_date_login: {}", tTabStr, t->Get_c_date_login());
       result += std::format(L"\n{}c_date_logout: {}", tTabStr, t->Get_c_date_logout());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class USER_ALL;

class USER_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::USER_ALL;

public:
   // (7)
   explicit USER_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::USER_ALL, buf)
   {
   }

   // (9)
   explicit USER_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AccountId account_id,
       UserId user_id,
       UserExp user_exp,
       UserExp user_exp_total,
       time_t play_second_total,
       time_t date_login,
       time_t date_logout,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::USER_ALL, buf)
   {
       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_exp);

       WriteValue(user_exp_total);

       WriteValue(play_second_total);

       WriteValue(date_login);

       WriteValue(date_logout);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit USER_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       AccountId account_id,
       UserId user_id,
       UserExp user_exp,
       UserExp user_exp_total,
       time_t play_second_total,
       time_t date_login,
       time_t date_logout,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_exp);

       WriteValue(user_exp_total);

       WriteValue(play_second_total);

       WriteValue(date_login);

       WriteValue(date_logout);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   USER_ALL& GetPacket() const noexcept { return *reinterpret_cast<USER_ALL*>(GetPacketBufPtr()); }
   USER_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<USER_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class USER_ALL : public Packet
{
   public:
      using Writer = USER_ALL_WRITER;


private:
   AccountId m_c_account_id;

   UserId m_c_user_id;

   UserExp m_c_user_exp;

   UserExp m_c_user_exp_total;

   time_t m_c_play_second_total;

   time_t m_c_date_login;

   time_t m_c_date_logout;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   USER_ALL()
   {
       memset(this, 0, sizeof(USER_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(USER_ALL));
   }
   USER_ALL(const USER_ALL&) = delete;
   USER_ALL(USER_ALL &&) = delete;
   USER_ALL& operator = (const USER_ALL&) = delete;
   USER_ALL& operator = (USER_ALL &&) = delete;

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   UserExp Get_c_user_exp() const noexcept { return m_c_user_exp; }
   UserExp& Ref_c_user_exp() noexcept { return m_c_user_exp; }
   const UserExp& ConstRef_c_user_exp() const noexcept { return m_c_user_exp; }

   UserExp Get_c_user_exp_total() const noexcept { return m_c_user_exp_total; }
   UserExp& Ref_c_user_exp_total() noexcept { return m_c_user_exp_total; }
   const UserExp& ConstRef_c_user_exp_total() const noexcept { return m_c_user_exp_total; }

   time_t Get_c_play_second_total() const noexcept { return m_c_play_second_total; }
   time_t& Ref_c_play_second_total() noexcept { return m_c_play_second_total; }
   const time_t& ConstRef_c_play_second_total() const noexcept { return m_c_play_second_total; }

   time_t Get_c_date_login() const noexcept { return m_c_date_login; }
   time_t& Ref_c_date_login() noexcept { return m_c_date_login; }
   const time_t& ConstRef_c_date_login() const noexcept { return m_c_date_login; }

   time_t Get_c_date_logout() const noexcept { return m_c_date_logout; }
   time_t& Ref_c_date_logout() noexcept { return m_c_date_logout; }
   const time_t& ConstRef_c_date_logout() const noexcept { return m_c_date_logout; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(USER_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(USER_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(USER_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(USER_ALL))
           {
               size += sizeof(USER_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, USER_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_user_exp: {}", tTabStr, t->Get_c_user_exp());
       result += std::format(L"\n{}c_user_exp_total: {}", tTabStr, t->Get_c_user_exp_total());
       result += std::format(L"\n{}c_play_second_total: {}", tTabStr, t->Get_c_play_second_total());
       result += std::format(L"\n{}c_date_login: {}", tTabStr, t->Get_c_date_login());
       result += std::format(L"\n{}c_date_logout: {}", tTabStr, t->Get_c_date_logout());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class USER_BY_ACCOUNT;

class USER_BY_ACCOUNT_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::USER_BY_ACCOUNT;

public:
   // (7)
   explicit USER_BY_ACCOUNT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::USER_BY_ACCOUNT, buf)
   {
   }

   // (9)
   explicit USER_BY_ACCOUNT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AccountId account_id,
       UserId user_id,
       UserExp user_exp,
       UserExp user_exp_total,
       time_t play_second_total,
       time_t date_login,
       time_t date_logout,
       time_t date_updated,
       time_t date_created
   )
       :
       PacketWriter(trait, PacketTypes::USER_BY_ACCOUNT, buf)
   {
       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_exp);

       WriteValue(user_exp_total);

       WriteValue(play_second_total);

       WriteValue(date_login);

       WriteValue(date_logout);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   // (8)
   explicit USER_BY_ACCOUNT_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       AccountId account_id,
       UserId user_id,
       UserExp user_exp,
       UserExp user_exp_total,
       time_t play_second_total,
       time_t date_login,
       time_t date_logout,
       time_t date_updated,
       time_t date_created
   )
   {
       WriteValue(account_id);

       WriteValue(user_id);

       WriteValue(user_exp);

       WriteValue(user_exp_total);

       WriteValue(play_second_total);

       WriteValue(date_login);

       WriteValue(date_logout);

       WriteValue(date_updated);

       WriteValue(date_created);


   }

   USER_BY_ACCOUNT& GetPacket() const noexcept { return *reinterpret_cast<USER_BY_ACCOUNT*>(GetPacketBufPtr()); }
   USER_BY_ACCOUNT* GetPacketPtr() const noexcept { return reinterpret_cast<USER_BY_ACCOUNT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class USER_BY_ACCOUNT : public Packet
{
   public:
      using Writer = USER_BY_ACCOUNT_WRITER;


private:
   AccountId m_c_account_id;

   UserId m_c_user_id;

   UserExp m_c_user_exp;

   UserExp m_c_user_exp_total;

   time_t m_c_play_second_total;

   time_t m_c_date_login;

   time_t m_c_date_logout;

   time_t m_c_date_updated;

   time_t m_c_date_created;

public:
   USER_BY_ACCOUNT()
   {
       memset(this, 0, sizeof(USER_BY_ACCOUNT));
       mPacketSize = static_cast<PacketSize>(sizeof(USER_BY_ACCOUNT));
   }
   USER_BY_ACCOUNT(const USER_BY_ACCOUNT&) = delete;
   USER_BY_ACCOUNT(USER_BY_ACCOUNT &&) = delete;
   USER_BY_ACCOUNT& operator = (const USER_BY_ACCOUNT&) = delete;
   USER_BY_ACCOUNT& operator = (USER_BY_ACCOUNT &&) = delete;

   AccountId Get_c_account_id() const noexcept { return m_c_account_id; }
   AccountId& Ref_c_account_id() noexcept { return m_c_account_id; }
   const AccountId& ConstRef_c_account_id() const noexcept { return m_c_account_id; }

   UserId Get_c_user_id() const noexcept { return m_c_user_id; }
   UserId& Ref_c_user_id() noexcept { return m_c_user_id; }
   const UserId& ConstRef_c_user_id() const noexcept { return m_c_user_id; }

   UserExp Get_c_user_exp() const noexcept { return m_c_user_exp; }
   UserExp& Ref_c_user_exp() noexcept { return m_c_user_exp; }
   const UserExp& ConstRef_c_user_exp() const noexcept { return m_c_user_exp; }

   UserExp Get_c_user_exp_total() const noexcept { return m_c_user_exp_total; }
   UserExp& Ref_c_user_exp_total() noexcept { return m_c_user_exp_total; }
   const UserExp& ConstRef_c_user_exp_total() const noexcept { return m_c_user_exp_total; }

   time_t Get_c_play_second_total() const noexcept { return m_c_play_second_total; }
   time_t& Ref_c_play_second_total() noexcept { return m_c_play_second_total; }
   const time_t& ConstRef_c_play_second_total() const noexcept { return m_c_play_second_total; }

   time_t Get_c_date_login() const noexcept { return m_c_date_login; }
   time_t& Ref_c_date_login() noexcept { return m_c_date_login; }
   const time_t& ConstRef_c_date_login() const noexcept { return m_c_date_login; }

   time_t Get_c_date_logout() const noexcept { return m_c_date_logout; }
   time_t& Ref_c_date_logout() noexcept { return m_c_date_logout; }
   const time_t& ConstRef_c_date_logout() const noexcept { return m_c_date_logout; }

   time_t Get_c_date_updated() const noexcept { return m_c_date_updated; }
   time_t& Ref_c_date_updated() noexcept { return m_c_date_updated; }
   const time_t& ConstRef_c_date_updated() const noexcept { return m_c_date_updated; }

   time_t Get_c_date_created() const noexcept { return m_c_date_created; }
   time_t& Ref_c_date_created() noexcept { return m_c_date_created; }
   const time_t& ConstRef_c_date_created() const noexcept { return m_c_date_created; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(USER_BY_ACCOUNT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(USER_BY_ACCOUNT) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(USER_BY_ACCOUNT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(USER_BY_ACCOUNT))
           {
               size += sizeof(USER_BY_ACCOUNT);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(AccountId);
       }

       {
           thisSize += sizeof(UserId);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       {
           thisSize += sizeof(time_t);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, USER_BY_ACCOUNT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}c_account_id: {}", tTabStr, t->Get_c_account_id());
       result += std::format(L"\n{}c_user_id: {}", tTabStr, t->Get_c_user_id());
       result += std::format(L"\n{}c_user_exp: {}", tTabStr, t->Get_c_user_exp());
       result += std::format(L"\n{}c_user_exp_total: {}", tTabStr, t->Get_c_user_exp_total());
       result += std::format(L"\n{}c_play_second_total: {}", tTabStr, t->Get_c_play_second_total());
       result += std::format(L"\n{}c_date_login: {}", tTabStr, t->Get_c_date_login());
       result += std::format(L"\n{}c_date_logout: {}", tTabStr, t->Get_c_date_logout());
       result += std::format(L"\n{}c_date_updated: {}", tTabStr, t->Get_c_date_updated());
       result += std::format(L"\n{}c_date_created: {}", tTabStr, t->Get_c_date_created());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class HANDSHAKE_TO_PEER;

class HANDSHAKE_TO_PEER_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::HANDSHAKE_TO_PEER;

public:
   // (7)
   explicit HANDSHAKE_TO_PEER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::HANDSHAKE_TO_PEER, buf)
   {
   }

   // (9)
   explicit HANDSHAKE_TO_PEER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       EncryptionKey hostEncryptionKey
   )
       :
       NetworkPacketWriter(trait, PacketTypes::HANDSHAKE_TO_PEER, buf)
   {
       WriteValue(hostEncryptionKey);


   }

   // (8)
   explicit HANDSHAKE_TO_PEER_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       EncryptionKey hostEncryptionKey
   )
   {
       WriteValue(hostEncryptionKey);


   }

   HANDSHAKE_TO_PEER& GetPacket() const noexcept { return *reinterpret_cast<HANDSHAKE_TO_PEER*>(GetPacketBufPtr()); }
   HANDSHAKE_TO_PEER* GetPacketPtr() const noexcept { return reinterpret_cast<HANDSHAKE_TO_PEER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class HANDSHAKE_TO_PEER : public NetworkPacket
{
   public:
      using Writer = HANDSHAKE_TO_PEER_WRITER;


private:
   EncryptionKey m_hostEncryptionKey;

public:
   HANDSHAKE_TO_PEER()
   {
       memset(this, 0, sizeof(HANDSHAKE_TO_PEER));
       mPacketSize = static_cast<PacketSize>(sizeof(HANDSHAKE_TO_PEER));
   }
   HANDSHAKE_TO_PEER(const HANDSHAKE_TO_PEER&) = delete;
   HANDSHAKE_TO_PEER(HANDSHAKE_TO_PEER &&) = delete;
   HANDSHAKE_TO_PEER& operator = (const HANDSHAKE_TO_PEER&) = delete;
   HANDSHAKE_TO_PEER& operator = (HANDSHAKE_TO_PEER &&) = delete;

   EncryptionKey Get_hostEncryptionKey() const noexcept { return m_hostEncryptionKey; }
   EncryptionKey& Ref_hostEncryptionKey() noexcept { return m_hostEncryptionKey; }
   const EncryptionKey& ConstRef_hostEncryptionKey() const noexcept { return m_hostEncryptionKey; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(HANDSHAKE_TO_PEER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(HANDSHAKE_TO_PEER) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(HANDSHAKE_TO_PEER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(HANDSHAKE_TO_PEER))
           {
               size += sizeof(HANDSHAKE_TO_PEER);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(EncryptionKey);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, HANDSHAKE_TO_PEER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}hostEncryptionKey: {}", tTabStr, t->Get_hostEncryptionKey());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class HANDSHAKE_TO_HOST;

class HANDSHAKE_TO_HOST_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::HANDSHAKE_TO_HOST;

public:
   // (7)
   explicit HANDSHAKE_TO_HOST_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::HANDSHAKE_TO_HOST, buf)
   {
   }

   // (9)
   explicit HANDSHAKE_TO_HOST_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       EncryptionKey peerEncryptionKey,
       AppId peerAppId,
       AppType peerAppType
   )
       :
       NetworkPacketWriter(trait, PacketTypes::HANDSHAKE_TO_HOST, buf)
   {
       WriteValue(peerEncryptionKey);

       WriteValue(peerAppId);

       WriteValue(peerAppType);


   }

   // (8)
   explicit HANDSHAKE_TO_HOST_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       EncryptionKey peerEncryptionKey,
       AppId peerAppId,
       AppType peerAppType
   )
   {
       WriteValue(peerEncryptionKey);

       WriteValue(peerAppId);

       WriteValue(peerAppType);


   }

   HANDSHAKE_TO_HOST& GetPacket() const noexcept { return *reinterpret_cast<HANDSHAKE_TO_HOST*>(GetPacketBufPtr()); }
   HANDSHAKE_TO_HOST* GetPacketPtr() const noexcept { return reinterpret_cast<HANDSHAKE_TO_HOST*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class HANDSHAKE_TO_HOST : public NetworkPacket
{
   public:
      using Writer = HANDSHAKE_TO_HOST_WRITER;


private:
   EncryptionKey m_peerEncryptionKey;

   AppId m_peerAppId;

   AppType m_peerAppType;

public:
   HANDSHAKE_TO_HOST()
   {
       memset(this, 0, sizeof(HANDSHAKE_TO_HOST));
       mPacketSize = static_cast<PacketSize>(sizeof(HANDSHAKE_TO_HOST));
   }
   HANDSHAKE_TO_HOST(const HANDSHAKE_TO_HOST&) = delete;
   HANDSHAKE_TO_HOST(HANDSHAKE_TO_HOST &&) = delete;
   HANDSHAKE_TO_HOST& operator = (const HANDSHAKE_TO_HOST&) = delete;
   HANDSHAKE_TO_HOST& operator = (HANDSHAKE_TO_HOST &&) = delete;

   EncryptionKey Get_peerEncryptionKey() const noexcept { return m_peerEncryptionKey; }
   EncryptionKey& Ref_peerEncryptionKey() noexcept { return m_peerEncryptionKey; }
   const EncryptionKey& ConstRef_peerEncryptionKey() const noexcept { return m_peerEncryptionKey; }

   AppId Get_peerAppId() const noexcept { return m_peerAppId; }
   AppId& Ref_peerAppId() noexcept { return m_peerAppId; }
   const AppId& ConstRef_peerAppId() const noexcept { return m_peerAppId; }

   AppType Get_peerAppType() const noexcept { return m_peerAppType; }
   AppType& Ref_peerAppType() noexcept { return m_peerAppType; }
   const AppType& ConstRef_peerAppType() const noexcept { return m_peerAppType; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(HANDSHAKE_TO_HOST) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(HANDSHAKE_TO_HOST) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(HANDSHAKE_TO_HOST); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(HANDSHAKE_TO_HOST))
           {
               size += sizeof(HANDSHAKE_TO_HOST);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(EncryptionKey);
       }

       {
           thisSize += sizeof(AppId);
       }

       {
           thisSize += sizeof(AppType);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, HANDSHAKE_TO_HOST>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}peerEncryptionKey: {}", tTabStr, t->Get_peerEncryptionKey());
       result += std::format(L"\n{}peerAppId: {}", tTabStr, t->Get_peerAppId());
       result += std::format(L"\n{}peerAppType: {}", tTabStr, t->Get_peerAppType());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class APP_DATA;

class APP_DATA_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::APP_DATA;

public:
   // (7)
   explicit APP_DATA_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::APP_DATA, buf)
   {
   }

   // (9)
   explicit APP_DATA_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AppId appId,
       AppType appType,
       const wchar_t* ip
   )
       :
       PacketWriter(trait, PacketTypes::APP_DATA, buf)
   {
       WriteValue(appId);

       WriteValue(appType);

       PacketSize& ipOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& ipLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       ipOffset = GetPacketSize();
       ipLength = (PacketSize)wcsnlen_s(ip, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == ipLength) WriteString(L"", 0);
       else WriteString(ip, ipLength); 

   }

   // (8)
   explicit APP_DATA_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       AppId appId,
       AppType appType,
       const wchar_t* ip
   )
   {
       WriteValue(appId);

       WriteValue(appType);

       PacketSize& ipOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& ipLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       ipOffset = GetPacketSize();
       ipLength = (PacketSize)wcsnlen_s(ip, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == ipLength) WriteString(L"", 0);
       else WriteString(ip, ipLength); 

   }

   APP_DATA& GetPacket() const noexcept { return *reinterpret_cast<APP_DATA*>(GetPacketBufPtr()); }
   APP_DATA* GetPacketPtr() const noexcept { return reinterpret_cast<APP_DATA*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class APP_DATA : public Packet
{
   public:
      using Writer = APP_DATA_WRITER;


private:
   AppId m_appId;

   AppType m_appType;

   PacketSize m_ip_offset = 0;
   PacketSize m_ip_length = 0;

public:
   APP_DATA()
   {
       memset(this, 0, sizeof(APP_DATA));
       mPacketSize = static_cast<PacketSize>(sizeof(APP_DATA));
   }
   APP_DATA(const APP_DATA&) = delete;
   APP_DATA(APP_DATA &&) = delete;
   APP_DATA& operator = (const APP_DATA&) = delete;
   APP_DATA& operator = (APP_DATA &&) = delete;

   AppId Get_appId() const noexcept { return m_appId; }
   AppId& Ref_appId() noexcept { return m_appId; }
   const AppId& ConstRef_appId() const noexcept { return m_appId; }

   AppType Get_appType() const noexcept { return m_appType; }
   AppType& Ref_appType() noexcept { return m_appType; }
   const AppType& ConstRef_appType() const noexcept { return m_appType; }

   const wchar_t* Get_ip() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_ip_offset) + m_ip_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_ip_offset);
   }

   wchar_t* Ref_ip()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_ip_offset) + m_ip_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_ip_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(APP_DATA) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(APP_DATA) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(APP_DATA); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(APP_DATA))
           {
               size += sizeof(APP_DATA);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(AppId);
       }

       {
           thisSize += sizeof(AppType);
       }

       {
           if (GetPacketSize() < m_ip_offset)
               return false;

           if (0 < m_ip_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_ip_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, APP_DATA>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}appId: {}", tTabStr, t->Get_appId());
       result += std::format(L"\n{}appType: {}", tTabStr, t->Get_appType());
       result += std::format(L"\n{}ip: {}", tTabStr, t->Get_ip());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class USER_DATA;

class USER_DATA_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::USER_DATA;

private:
   PacketSize* m_achievementList_offset = nullptr;
   PacketSize* m_achievementList_count = nullptr;

   PacketSize* m_missionList_offset = nullptr;
   PacketSize* m_missionList_count = nullptr;

   PacketSize* m_questList_offset = nullptr;
   PacketSize* m_questList_count = nullptr;

   PacketSize* m_itemList_offset = nullptr;
   PacketSize* m_itemList_count = nullptr;

public:
   // (9)
   explicit USER_DATA_WRITER(
       PacketTraitParam trait, uint8_t* buf
   )
       :
       PacketWriter(trait, PacketTypes::USER_DATA, buf)
   {
       m_achievementList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       m_achievementList_count = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       m_missionList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       m_missionList_count = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       m_questList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       m_questList_count = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       m_itemList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       m_itemList_count = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));


   }

   // (8)
   explicit USER_DATA_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void Write_achievementList(const ACHIEVEMENT& packet)
   {
       *m_achievementList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_achievementList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_achievementList_count);
   }

   void Write_achievementList(ACHIEVEMENT&& packet)
   {
       *m_achievementList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_achievementList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_achievementList_count);
   }

   void Write_achievementList(const ACHIEVEMENT::Writer& packet)
   {
       *m_achievementList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_achievementList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_achievementList_count);
   }

   void Write_achievementList(ACHIEVEMENT::Writer&& packet)
   {
       *m_achievementList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_achievementList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_achievementList_count);
   }

   void Write_missionList(const MISSION& packet)
   {
       *m_missionList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_missionList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_missionList_count);
   }

   void Write_missionList(MISSION&& packet)
   {
       *m_missionList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_missionList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_missionList_count);
   }

   void Write_missionList(const MISSION::Writer& packet)
   {
       *m_missionList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_missionList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_missionList_count);
   }

   void Write_missionList(MISSION::Writer&& packet)
   {
       *m_missionList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_missionList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_missionList_count);
   }

   void Write_questList(const QUEST& packet)
   {
       *m_questList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_questList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_questList_count);
   }

   void Write_questList(QUEST&& packet)
   {
       *m_questList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_questList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_questList_count);
   }

   void Write_questList(const QUEST::Writer& packet)
   {
       *m_questList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_questList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_questList_count);
   }

   void Write_questList(QUEST::Writer&& packet)
   {
       *m_questList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_questList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_questList_count);
   }

   void Write_itemList(const ITEM& packet)
   {
       *m_itemList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_itemList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_itemList_count);
   }

   void Write_itemList(ITEM&& packet)
   {
       *m_itemList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_itemList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_itemList_count);
   }

   void Write_itemList(const ITEM::Writer& packet)
   {
       *m_itemList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_itemList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_itemList_count);
   }

   void Write_itemList(ITEM::Writer&& packet)
   {
       *m_itemList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_itemList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_itemList_count);
   }

   USER_DATA& GetPacket() const noexcept { return *reinterpret_cast<USER_DATA*>(GetPacketBufPtr()); }
   USER_DATA* GetPacketPtr() const noexcept { return reinterpret_cast<USER_DATA*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class USER_DATA : public Packet
{
   public:
      using Writer = USER_DATA_WRITER;


private:
   PacketSize m_achievementList_offset = 0;
   PacketSize m_achievementList_count = 0;

   PacketSize m_missionList_offset = 0;
   PacketSize m_missionList_count = 0;

   PacketSize m_questList_offset = 0;
   PacketSize m_questList_count = 0;

   PacketSize m_itemList_offset = 0;
   PacketSize m_itemList_count = 0;

public:
   USER_DATA()
   {
       memset(this, 0, sizeof(USER_DATA));
       mPacketSize = static_cast<PacketSize>(sizeof(USER_DATA));
   }
   USER_DATA(const USER_DATA&) = delete;
   USER_DATA(USER_DATA &&) = delete;
   USER_DATA& operator = (const USER_DATA&) = delete;
   USER_DATA& operator = (USER_DATA &&) = delete;

   PacketDataList<USER_DATA, ACHIEVEMENT> Get_achievementList() const { return PacketDataList<USER_DATA, ACHIEVEMENT>(this, m_achievementList_offset); }
   PacketSize Get_achievementList_count() const { return m_achievementList_count; }

   PacketDataList<USER_DATA, MISSION> Get_missionList() const { return PacketDataList<USER_DATA, MISSION>(this, m_missionList_offset); }
   PacketSize Get_missionList_count() const { return m_missionList_count; }

   PacketDataList<USER_DATA, QUEST> Get_questList() const { return PacketDataList<USER_DATA, QUEST>(this, m_questList_offset); }
   PacketSize Get_questList_count() const { return m_questList_count; }

   PacketDataList<USER_DATA, ITEM> Get_itemList() const { return PacketDataList<USER_DATA, ITEM>(this, m_itemList_offset); }
   PacketSize Get_itemList_count() const { return m_itemList_count; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(USER_DATA) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(USER_DATA) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(USER_DATA); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(USER_DATA))
           {
               size += sizeof(USER_DATA);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           if (GetPacketSize() < m_achievementList_offset)
               return false;

           PacketSize _offset = m_achievementList_offset;
           while (0 != _offset)
           {
               const ACHIEVEMENT* _packet = reinterpret_cast<const ACHIEVEMENT*>(GetPacketBufPtr() + _offset);
               if (!_packet->Validate(thisSize, ++depth))
                   return false;

               if (GetPacketSize() < thisSize)
                   return false;

               _offset = *reinterpret_cast<const PacketSize*>(GetPacketBufPtr() + _offset + _packet->GetPacketSize());
               thisSize += sizeof(PacketSize);

               if (GetPacketSize() < _offset)
                   return false;
           }

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < _offset)
               return false;
       }

       {
           if (GetPacketSize() < m_missionList_offset)
               return false;

           PacketSize _offset = m_missionList_offset;
           while (0 != _offset)
           {
               const MISSION* _packet = reinterpret_cast<const MISSION*>(GetPacketBufPtr() + _offset);
               if (!_packet->Validate(thisSize, ++depth))
                   return false;

               if (GetPacketSize() < thisSize)
                   return false;

               _offset = *reinterpret_cast<const PacketSize*>(GetPacketBufPtr() + _offset + _packet->GetPacketSize());
               thisSize += sizeof(PacketSize);

               if (GetPacketSize() < _offset)
                   return false;
           }

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < _offset)
               return false;
       }

       {
           if (GetPacketSize() < m_questList_offset)
               return false;

           PacketSize _offset = m_questList_offset;
           while (0 != _offset)
           {
               const QUEST* _packet = reinterpret_cast<const QUEST*>(GetPacketBufPtr() + _offset);
               if (!_packet->Validate(thisSize, ++depth))
                   return false;

               if (GetPacketSize() < thisSize)
                   return false;

               _offset = *reinterpret_cast<const PacketSize*>(GetPacketBufPtr() + _offset + _packet->GetPacketSize());
               thisSize += sizeof(PacketSize);

               if (GetPacketSize() < _offset)
                   return false;
           }

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < _offset)
               return false;
       }

       {
           if (GetPacketSize() < m_itemList_offset)
               return false;

           PacketSize _offset = m_itemList_offset;
           while (0 != _offset)
           {
               const ITEM* _packet = reinterpret_cast<const ITEM*>(GetPacketBufPtr() + _offset);
               if (!_packet->Validate(thisSize, ++depth))
                   return false;

               if (GetPacketSize() < thisSize)
                   return false;

               _offset = *reinterpret_cast<const PacketSize*>(GetPacketBufPtr() + _offset + _packet->GetPacketSize());
               thisSize += sizeof(PacketSize);

               if (GetPacketSize() < _offset)
                   return false;
           }

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < _offset)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, USER_DATA>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       if (0 == t->Get_achievementList_count())
       {
           result += std::format(L"\n{}achievementList: LIST[{}]", tTabStr, t->Get_achievementList_count());
       }
       else
       {
           result += std::format(L"\n{}achievementList: LIST[{}], ", tTabStr, t->Get_achievementList_count());
           //tTabStr += std::wstring(L"\t");
           for (const ACHIEVEMENT* it : t->Get_achievementList())
           {
               result += std::format(L"\n{}{}", tTabStr, const_cast<ACHIEVEMENT*>(it));
           }
           //tTabStr.pop_back();
       }
       if (0 == t->Get_missionList_count())
       {
           result += std::format(L"\n{}missionList: LIST[{}]", tTabStr, t->Get_missionList_count());
       }
       else
       {
           result += std::format(L"\n{}missionList: LIST[{}], ", tTabStr, t->Get_missionList_count());
           //tTabStr += std::wstring(L"\t");
           for (const MISSION* it : t->Get_missionList())
           {
               result += std::format(L"\n{}{}", tTabStr, const_cast<MISSION*>(it));
           }
           //tTabStr.pop_back();
       }
       if (0 == t->Get_questList_count())
       {
           result += std::format(L"\n{}questList: LIST[{}]", tTabStr, t->Get_questList_count());
       }
       else
       {
           result += std::format(L"\n{}questList: LIST[{}], ", tTabStr, t->Get_questList_count());
           //tTabStr += std::wstring(L"\t");
           for (const QUEST* it : t->Get_questList())
           {
               result += std::format(L"\n{}{}", tTabStr, const_cast<QUEST*>(it));
           }
           //tTabStr.pop_back();
       }
       if (0 == t->Get_itemList_count())
       {
           result += std::format(L"\n{}itemList: LIST[{}]", tTabStr, t->Get_itemList_count());
       }
       else
       {
           result += std::format(L"\n{}itemList: LIST[{}], ", tTabStr, t->Get_itemList_count());
           //tTabStr += std::wstring(L"\t");
           for (const ITEM* it : t->Get_itemList())
           {
               result += std::format(L"\n{}{}", tTabStr, const_cast<ITEM*>(it));
           }
           //tTabStr.pop_back();
       }
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class PACKET_DEV_DATA;

class PACKET_DEV_DATA_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::PACKET_DEV_DATA;

public:
   // (7)
   explicit PACKET_DEV_DATA_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::PACKET_DEV_DATA, buf)
   {
   }

   // (9)
   explicit PACKET_DEV_DATA_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       const wchar_t* packetName,
       PacketType packetType
   )
       :
       PacketWriter(trait, PacketTypes::PACKET_DEV_DATA, buf)
   {
       PacketSize& packetNameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& packetNameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(packetType);


       packetNameOffset = GetPacketSize();
       packetNameLength = (PacketSize)wcsnlen_s(packetName, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == packetNameLength) WriteString(L"", 0);
       else WriteString(packetName, packetNameLength); 

   }

   // (8)
   explicit PACKET_DEV_DATA_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       const wchar_t* packetName,
       PacketType packetType
   )
   {
       PacketSize& packetNameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& packetNameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       WriteValue(packetType);


       packetNameOffset = GetPacketSize();
       packetNameLength = (PacketSize)wcsnlen_s(packetName, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == packetNameLength) WriteString(L"", 0);
       else WriteString(packetName, packetNameLength); 

   }

   PACKET_DEV_DATA& GetPacket() const noexcept { return *reinterpret_cast<PACKET_DEV_DATA*>(GetPacketBufPtr()); }
   PACKET_DEV_DATA* GetPacketPtr() const noexcept { return reinterpret_cast<PACKET_DEV_DATA*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class PACKET_DEV_DATA : public Packet
{
   public:
      using Writer = PACKET_DEV_DATA_WRITER;


private:
   PacketSize m_packetName_offset = 0;
   PacketSize m_packetName_length = 0;

   PacketType m_packetType;

public:
   PACKET_DEV_DATA()
   {
       memset(this, 0, sizeof(PACKET_DEV_DATA));
       mPacketSize = static_cast<PacketSize>(sizeof(PACKET_DEV_DATA));
   }
   PACKET_DEV_DATA(const PACKET_DEV_DATA&) = delete;
   PACKET_DEV_DATA(PACKET_DEV_DATA &&) = delete;
   PACKET_DEV_DATA& operator = (const PACKET_DEV_DATA&) = delete;
   PACKET_DEV_DATA& operator = (PACKET_DEV_DATA &&) = delete;

   const wchar_t* Get_packetName() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_packetName_offset) + m_packetName_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_packetName_offset);
   }

   wchar_t* Ref_packetName()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_packetName_offset) + m_packetName_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_packetName_offset);
   }

   PacketType Get_packetType() const noexcept { return m_packetType; }
   PacketType& Ref_packetType() noexcept { return m_packetType; }
   const PacketType& ConstRef_packetType() const noexcept { return m_packetType; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(PACKET_DEV_DATA) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(PACKET_DEV_DATA) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(PACKET_DEV_DATA); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(PACKET_DEV_DATA))
           {
               size += sizeof(PACKET_DEV_DATA);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           if (GetPacketSize() < m_packetName_offset)
               return false;

           if (0 < m_packetName_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_packetName_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(PacketType);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, PACKET_DEV_DATA>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}packetName: {}", tTabStr, t->Get_packetName());
       result += std::format(L"\n{}packetType: {}", tTabStr, t->Get_packetType());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class SYNC_TIME;

class SYNC_TIME_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::SYNC_TIME;

public:
   // (7)
   explicit SYNC_TIME_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::SYNC_TIME, buf)
   {

   }

   // (8)
   explicit SYNC_TIME_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   SYNC_TIME& GetPacket() const noexcept { return *reinterpret_cast<SYNC_TIME*>(GetPacketBufPtr()); }
   SYNC_TIME* GetPacketPtr() const noexcept { return reinterpret_cast<SYNC_TIME*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class SYNC_TIME : public NetworkPacket
{
   public:
      using Writer = SYNC_TIME_WRITER;


private:
public:
   SYNC_TIME()
   {
       memset(this, 0, sizeof(SYNC_TIME));
       mPacketSize = static_cast<PacketSize>(sizeof(SYNC_TIME));
   }
   SYNC_TIME(const SYNC_TIME&) = delete;
   SYNC_TIME(SYNC_TIME &&) = delete;
   SYNC_TIME& operator = (const SYNC_TIME&) = delete;
   SYNC_TIME& operator = (SYNC_TIME &&) = delete;


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(SYNC_TIME) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(SYNC_TIME) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(SYNC_TIME); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(SYNC_TIME))
           {
               size += sizeof(SYNC_TIME);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, SYNC_TIME>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class REQ_GLOBAL_NOW;

class REQ_GLOBAL_NOW_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::REQ_GLOBAL_NOW;

public:
   // (1)
   explicit REQ_GLOBAL_NOW_WRITER(PacketTraitReq trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::REQ_GLOBAL_NOW, buf)
   {
   }

   // (2)
   explicit REQ_GLOBAL_NOW_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacket& rp)
       :
       NetworkPacketWriter(trait, PacketTypes::REQ_GLOBAL_NOW, buf)
   {
       SetReadPacket(rp);
   }

   // (3)
   explicit REQ_GLOBAL_NOW_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacketWriter& wp)
       :
       NetworkPacketWriter(trait, PacketTypes::REQ_GLOBAL_NOW, buf)
   {
       SetWritePacket(wp);
   }

   // (8)
   explicit REQ_GLOBAL_NOW_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       bool onlySyncTime,
       ClockMs reqMs
   )
   {
       WriteValue(onlySyncTime);

       WriteValue(reqMs);


   }

   REQ_GLOBAL_NOW& GetPacket() const noexcept { return *reinterpret_cast<REQ_GLOBAL_NOW*>(GetPacketBufPtr()); }
   REQ_GLOBAL_NOW* GetPacketPtr() const noexcept { return reinterpret_cast<REQ_GLOBAL_NOW*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class REQ_GLOBAL_NOW : public NetworkPacket
{
   public:
      using Writer = REQ_GLOBAL_NOW_WRITER;


private:
   bool m_onlySyncTime;

   ClockMs m_reqMs;

public:
   REQ_GLOBAL_NOW()
   {
       memset(this, 0, sizeof(REQ_GLOBAL_NOW));
       mPacketSize = static_cast<PacketSize>(sizeof(REQ_GLOBAL_NOW));
   }
   REQ_GLOBAL_NOW(const REQ_GLOBAL_NOW&) = delete;
   REQ_GLOBAL_NOW(REQ_GLOBAL_NOW &&) = delete;
   REQ_GLOBAL_NOW& operator = (const REQ_GLOBAL_NOW&) = delete;
   REQ_GLOBAL_NOW& operator = (REQ_GLOBAL_NOW &&) = delete;

   bool Get_onlySyncTime() const noexcept { return m_onlySyncTime; }
   bool& Ref_onlySyncTime() noexcept { return m_onlySyncTime; }
   const bool& ConstRef_onlySyncTime() const noexcept { return m_onlySyncTime; }

   ClockMs Get_reqMs() const noexcept { return m_reqMs; }
   ClockMs& Ref_reqMs() noexcept { return m_reqMs; }
   const ClockMs& ConstRef_reqMs() const noexcept { return m_reqMs; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(REQ_GLOBAL_NOW) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(REQ_GLOBAL_NOW) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(REQ_GLOBAL_NOW); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(REQ_GLOBAL_NOW))
           {
               size += sizeof(REQ_GLOBAL_NOW);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(bool);
       }

       {
           thisSize += sizeof(ClockMs);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, REQ_GLOBAL_NOW>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}onlySyncTime: {}", tTabStr, t->Get_onlySyncTime());
       result += std::format(L"\n{}reqMs: {}", tTabStr, t->Get_reqMs());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACK_GLOBAL_NOW;

class ACK_GLOBAL_NOW_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACK_GLOBAL_NOW;

public:
   // (4)
   explicit ACK_GLOBAL_NOW_WRITER(PacketTraitAck trait, uint8_t* buf, const NetworkPacket& rp, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::ACK_GLOBAL_NOW, buf)
   {
       SetReadPacket(rp);
       GetHeader().SetPacketResult(packetResult);
   }

   // (5)
   explicit ACK_GLOBAL_NOW_WRITER(PacketTraitAck trait, uint8_t* buf, const NetworkPacketWriter& wp, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::ACK_GLOBAL_NOW, buf)
   {
       SetWritePacket(wp);
       GetHeader().SetPacketResult(packetResult);
   }

   // (6)
   explicit ACK_GLOBAL_NOW_WRITER(PacketTraitAck trait, uint8_t* buf, const PacketHeader& header, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::ACK_GLOBAL_NOW, buf)
   {
       SetHeader(header);
       GetHeader().SetPacketResult(packetResult);
   }

   // (8)
   explicit ACK_GLOBAL_NOW_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       bool onlySyncTime,
       ClockMs reqMs,
       ClockMs ackNow
   )
   {
       WriteValue(onlySyncTime);

       WriteValue(reqMs);

       WriteValue(ackNow);


   }

   ACK_GLOBAL_NOW& GetPacket() const noexcept { return *reinterpret_cast<ACK_GLOBAL_NOW*>(GetPacketBufPtr()); }
   ACK_GLOBAL_NOW* GetPacketPtr() const noexcept { return reinterpret_cast<ACK_GLOBAL_NOW*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACK_GLOBAL_NOW : public NetworkPacket
{
   public:
      using Writer = ACK_GLOBAL_NOW_WRITER;


private:
   bool m_onlySyncTime;

   ClockMs m_reqMs;

   ClockMs m_ackNow;

public:
   ACK_GLOBAL_NOW()
   {
       memset(this, 0, sizeof(ACK_GLOBAL_NOW));
       mPacketSize = static_cast<PacketSize>(sizeof(ACK_GLOBAL_NOW));
   }
   ACK_GLOBAL_NOW(const ACK_GLOBAL_NOW&) = delete;
   ACK_GLOBAL_NOW(ACK_GLOBAL_NOW &&) = delete;
   ACK_GLOBAL_NOW& operator = (const ACK_GLOBAL_NOW&) = delete;
   ACK_GLOBAL_NOW& operator = (ACK_GLOBAL_NOW &&) = delete;

   bool Get_onlySyncTime() const noexcept { return m_onlySyncTime; }
   bool& Ref_onlySyncTime() noexcept { return m_onlySyncTime; }
   const bool& ConstRef_onlySyncTime() const noexcept { return m_onlySyncTime; }

   ClockMs Get_reqMs() const noexcept { return m_reqMs; }
   ClockMs& Ref_reqMs() noexcept { return m_reqMs; }
   const ClockMs& ConstRef_reqMs() const noexcept { return m_reqMs; }

   ClockMs Get_ackNow() const noexcept { return m_ackNow; }
   ClockMs& Ref_ackNow() noexcept { return m_ackNow; }
   const ClockMs& ConstRef_ackNow() const noexcept { return m_ackNow; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACK_GLOBAL_NOW) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACK_GLOBAL_NOW) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(ACK_GLOBAL_NOW); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACK_GLOBAL_NOW))
           {
               size += sizeof(ACK_GLOBAL_NOW);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(bool);
       }

       {
           thisSize += sizeof(ClockMs);
       }

       {
           thisSize += sizeof(ClockMs);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACK_GLOBAL_NOW>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}onlySyncTime: {}", tTabStr, t->Get_onlySyncTime());
       result += std::format(L"\n{}reqMs: {}", tTabStr, t->Get_reqMs());
       result += std::format(L"\n{}ackNow: {}", tTabStr, t->Get_ackNow());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class REQ_APP_JOIN;

class REQ_APP_JOIN_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::REQ_APP_JOIN;

public:
   // (1)
   explicit REQ_APP_JOIN_WRITER(PacketTraitReq trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::REQ_APP_JOIN, buf)
   {
   }

   // (2)
   explicit REQ_APP_JOIN_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacket& rp)
       :
       NetworkPacketWriter(trait, PacketTypes::REQ_APP_JOIN, buf)
   {
       SetReadPacket(rp);
   }

   // (3)
   explicit REQ_APP_JOIN_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacketWriter& wp)
       :
       NetworkPacketWriter(trait, PacketTypes::REQ_APP_JOIN, buf)
   {
       SetWritePacket(wp);
   }

   // (8)
   explicit REQ_APP_JOIN_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       const APP_DATA& appData
   )
   {
       PacketSize& appDataOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       appDataOffset = GetPacketSize();
       WriteBinary((uint8_t*)&appData, appData.GetPacketSize());

   }

   void SetValues(
       const APP_DATA::Writer& appData
   )
   {
       PacketSize& appDataOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       appDataOffset = GetPacketSize();
       WriteBinary(appData.GetPacketBufPtr(), appData.GetPacketSize());

   }

   REQ_APP_JOIN& GetPacket() const noexcept { return *reinterpret_cast<REQ_APP_JOIN*>(GetPacketBufPtr()); }
   REQ_APP_JOIN* GetPacketPtr() const noexcept { return reinterpret_cast<REQ_APP_JOIN*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class REQ_APP_JOIN : public NetworkPacket
{
   public:
      using Writer = REQ_APP_JOIN_WRITER;


private:
   PacketSize m_appData_offset = 0;

public:
   REQ_APP_JOIN()
   {
       memset(this, 0, sizeof(REQ_APP_JOIN));
       mPacketSize = static_cast<PacketSize>(sizeof(REQ_APP_JOIN));
   }
   REQ_APP_JOIN(const REQ_APP_JOIN&) = delete;
   REQ_APP_JOIN(REQ_APP_JOIN &&) = delete;
   REQ_APP_JOIN& operator = (const REQ_APP_JOIN&) = delete;
   REQ_APP_JOIN& operator = (REQ_APP_JOIN &&) = delete;

   const APP_DATA& Get_appData() const noexcept { return *(APP_DATA*)((uint8_t*)this + m_appData_offset); }
   APP_DATA* Get_appData_ptr() const noexcept { return (APP_DATA*)((uint8_t*)this + m_appData_offset); }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(REQ_APP_JOIN) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(REQ_APP_JOIN) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(REQ_APP_JOIN); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(REQ_APP_JOIN))
           {
               size += sizeof(REQ_APP_JOIN);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           if (GetPacketSize() < m_appData_offset)
               return false;

           const APP_DATA* _packet = reinterpret_cast<const APP_DATA*>(GetPacketBufPtr() + m_appData_offset);
           if (!_packet->Validate(thisSize, ++depth))
               return false;

           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, REQ_APP_JOIN>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}appData: {}", tTabStr, t->Get_appData_ptr());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACK_APP_JOIN;

class ACK_APP_JOIN_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACK_APP_JOIN;

private:
   PacketSize* m_appDataList_offset = nullptr;
   PacketSize* m_appDataList_count = nullptr;

public:
   // (4)
   explicit ACK_APP_JOIN_WRITER(PacketTraitAck trait, uint8_t* buf, const NetworkPacket& rp, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::ACK_APP_JOIN, buf)
   {
       SetReadPacket(rp);
       GetHeader().SetPacketResult(packetResult);
       m_appDataList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       m_appDataList_count = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));


   }

   // (5)
   explicit ACK_APP_JOIN_WRITER(PacketTraitAck trait, uint8_t* buf, const NetworkPacketWriter& wp, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::ACK_APP_JOIN, buf)
   {
       SetWritePacket(wp);
       GetHeader().SetPacketResult(packetResult);
       m_appDataList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       m_appDataList_count = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));


   }

   // (6)
   explicit ACK_APP_JOIN_WRITER(PacketTraitAck trait, uint8_t* buf, const PacketHeader& header, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::ACK_APP_JOIN, buf)
   {
       SetHeader(header);
       GetHeader().SetPacketResult(packetResult);
       m_appDataList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       m_appDataList_count = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));


   }

   // (8)
   explicit ACK_APP_JOIN_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void Write_appDataList(const APP_DATA& packet)
   {
       *m_appDataList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_appDataList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_appDataList_count);
   }

   void Write_appDataList(APP_DATA&& packet)
   {
       *m_appDataList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_appDataList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_appDataList_count);
   }

   void Write_appDataList(const APP_DATA::Writer& packet)
   {
       *m_appDataList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_appDataList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_appDataList_count);
   }

   void Write_appDataList(APP_DATA::Writer&& packet)
   {
       *m_appDataList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_appDataList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_appDataList_count);
   }

   ACK_APP_JOIN& GetPacket() const noexcept { return *reinterpret_cast<ACK_APP_JOIN*>(GetPacketBufPtr()); }
   ACK_APP_JOIN* GetPacketPtr() const noexcept { return reinterpret_cast<ACK_APP_JOIN*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACK_APP_JOIN : public NetworkPacket
{
   public:
      using Writer = ACK_APP_JOIN_WRITER;


private:
   PacketSize m_appDataList_offset = 0;
   PacketSize m_appDataList_count = 0;

public:
   ACK_APP_JOIN()
   {
       memset(this, 0, sizeof(ACK_APP_JOIN));
       mPacketSize = static_cast<PacketSize>(sizeof(ACK_APP_JOIN));
   }
   ACK_APP_JOIN(const ACK_APP_JOIN&) = delete;
   ACK_APP_JOIN(ACK_APP_JOIN &&) = delete;
   ACK_APP_JOIN& operator = (const ACK_APP_JOIN&) = delete;
   ACK_APP_JOIN& operator = (ACK_APP_JOIN &&) = delete;

   PacketDataList<ACK_APP_JOIN, APP_DATA> Get_appDataList() const { return PacketDataList<ACK_APP_JOIN, APP_DATA>(this, m_appDataList_offset); }
   PacketSize Get_appDataList_count() const { return m_appDataList_count; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACK_APP_JOIN) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACK_APP_JOIN) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(ACK_APP_JOIN); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACK_APP_JOIN))
           {
               size += sizeof(ACK_APP_JOIN);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           if (GetPacketSize() < m_appDataList_offset)
               return false;

           PacketSize _offset = m_appDataList_offset;
           while (0 != _offset)
           {
               const APP_DATA* _packet = reinterpret_cast<const APP_DATA*>(GetPacketBufPtr() + _offset);
               if (!_packet->Validate(thisSize, ++depth))
                   return false;

               if (GetPacketSize() < thisSize)
                   return false;

               _offset = *reinterpret_cast<const PacketSize*>(GetPacketBufPtr() + _offset + _packet->GetPacketSize());
               thisSize += sizeof(PacketSize);

               if (GetPacketSize() < _offset)
                   return false;
           }

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < _offset)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACK_APP_JOIN>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       if (0 == t->Get_appDataList_count())
       {
           result += std::format(L"\n{}appDataList: LIST[{}]", tTabStr, t->Get_appDataList_count());
       }
       else
       {
           result += std::format(L"\n{}appDataList: LIST[{}], ", tTabStr, t->Get_appDataList_count());
           //tTabStr += std::wstring(L"\t");
           for (const APP_DATA* it : t->Get_appDataList())
           {
               result += std::format(L"\n{}{}", tTabStr, const_cast<APP_DATA*>(it));
           }
           //tTabStr.pop_back();
       }
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class APP_ADD;

class APP_ADD_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::APP_ADD;

public:
   // (7)
   explicit APP_ADD_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::APP_ADD, buf)
   {
   }

   // (9)
   explicit APP_ADD_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       const APP_DATA& appData
   )
       :
       NetworkPacketWriter(trait, PacketTypes::APP_ADD, buf)
   {
       PacketSize& appDataOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       appDataOffset = GetPacketSize();
       WriteBinary((uint8_t*)&appData, appData.GetPacketSize());

   }

   // (9)
   explicit APP_ADD_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       const APP_DATA::Writer& appData
   )
       :
       NetworkPacketWriter(trait, PacketTypes::APP_ADD, buf)
   {
       PacketSize& appDataOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       appDataOffset = GetPacketSize();
       WriteBinary(appData.GetPacketBufPtr(), appData.GetPacketSize());

   }

   // (8)
   explicit APP_ADD_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       const APP_DATA& appData
   )
   {
       PacketSize& appDataOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       appDataOffset = GetPacketSize();
       WriteBinary((uint8_t*)&appData, appData.GetPacketSize());

   }

   void SetValues(
       const APP_DATA::Writer& appData
   )
   {
       PacketSize& appDataOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       appDataOffset = GetPacketSize();
       WriteBinary(appData.GetPacketBufPtr(), appData.GetPacketSize());

   }

   APP_ADD& GetPacket() const noexcept { return *reinterpret_cast<APP_ADD*>(GetPacketBufPtr()); }
   APP_ADD* GetPacketPtr() const noexcept { return reinterpret_cast<APP_ADD*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class APP_ADD : public NetworkPacket
{
   public:
      using Writer = APP_ADD_WRITER;


private:
   PacketSize m_appData_offset = 0;

public:
   APP_ADD()
   {
       memset(this, 0, sizeof(APP_ADD));
       mPacketSize = static_cast<PacketSize>(sizeof(APP_ADD));
   }
   APP_ADD(const APP_ADD&) = delete;
   APP_ADD(APP_ADD &&) = delete;
   APP_ADD& operator = (const APP_ADD&) = delete;
   APP_ADD& operator = (APP_ADD &&) = delete;

   const APP_DATA& Get_appData() const noexcept { return *(APP_DATA*)((uint8_t*)this + m_appData_offset); }
   APP_DATA* Get_appData_ptr() const noexcept { return (APP_DATA*)((uint8_t*)this + m_appData_offset); }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(APP_ADD) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(APP_ADD) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(APP_ADD); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(APP_ADD))
           {
               size += sizeof(APP_ADD);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           if (GetPacketSize() < m_appData_offset)
               return false;

           const APP_DATA* _packet = reinterpret_cast<const APP_DATA*>(GetPacketBufPtr() + m_appData_offset);
           if (!_packet->Validate(thisSize, ++depth))
               return false;

           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, APP_ADD>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}appData: {}", tTabStr, t->Get_appData_ptr());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class APP_REMOVE;

class APP_REMOVE_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::APP_REMOVE;

public:
   // (7)
   explicit APP_REMOVE_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::APP_REMOVE, buf)
   {
   }

   // (9)
   explicit APP_REMOVE_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       AppId appId
   )
       :
       NetworkPacketWriter(trait, PacketTypes::APP_REMOVE, buf)
   {
       WriteValue(appId);


   }

   // (8)
   explicit APP_REMOVE_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       AppId appId
   )
   {
       WriteValue(appId);


   }

   APP_REMOVE& GetPacket() const noexcept { return *reinterpret_cast<APP_REMOVE*>(GetPacketBufPtr()); }
   APP_REMOVE* GetPacketPtr() const noexcept { return reinterpret_cast<APP_REMOVE*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class APP_REMOVE : public NetworkPacket
{
   public:
      using Writer = APP_REMOVE_WRITER;


private:
   AppId m_appId;

public:
   APP_REMOVE()
   {
       memset(this, 0, sizeof(APP_REMOVE));
       mPacketSize = static_cast<PacketSize>(sizeof(APP_REMOVE));
   }
   APP_REMOVE(const APP_REMOVE&) = delete;
   APP_REMOVE(APP_REMOVE &&) = delete;
   APP_REMOVE& operator = (const APP_REMOVE&) = delete;
   APP_REMOVE& operator = (APP_REMOVE &&) = delete;

   AppId Get_appId() const noexcept { return m_appId; }
   AppId& Ref_appId() noexcept { return m_appId; }
   const AppId& ConstRef_appId() const noexcept { return m_appId; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(APP_REMOVE) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(APP_REMOVE) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(APP_REMOVE); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(APP_REMOVE))
           {
               size += sizeof(APP_REMOVE);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(AppId);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, APP_REMOVE>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}appId: {}", tTabStr, t->Get_appId());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class REQ_SHELL_COMMAND;

class REQ_SHELL_COMMAND_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::REQ_SHELL_COMMAND;

public:
   // (1)
   explicit REQ_SHELL_COMMAND_WRITER(PacketTraitReq trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::REQ_SHELL_COMMAND, buf)
   {
   }

   // (2)
   explicit REQ_SHELL_COMMAND_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacket& rp)
       :
       NetworkPacketWriter(trait, PacketTypes::REQ_SHELL_COMMAND, buf)
   {
       SetReadPacket(rp);
   }

   // (3)
   explicit REQ_SHELL_COMMAND_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacketWriter& wp)
       :
       NetworkPacketWriter(trait, PacketTypes::REQ_SHELL_COMMAND, buf)
   {
       SetWritePacket(wp);
   }

   // (8)
   explicit REQ_SHELL_COMMAND_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       const wchar_t* command
   )
   {
       PacketSize& commandOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& commandLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       commandOffset = GetPacketSize();
       commandLength = (PacketSize)wcsnlen_s(command, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == commandLength) WriteString(L"", 0);
       else WriteString(command, commandLength); 

   }

   REQ_SHELL_COMMAND& GetPacket() const noexcept { return *reinterpret_cast<REQ_SHELL_COMMAND*>(GetPacketBufPtr()); }
   REQ_SHELL_COMMAND* GetPacketPtr() const noexcept { return reinterpret_cast<REQ_SHELL_COMMAND*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class REQ_SHELL_COMMAND : public NetworkPacket
{
   public:
      using Writer = REQ_SHELL_COMMAND_WRITER;


private:
   PacketSize m_command_offset = 0; // Shell 명령
   PacketSize m_command_length = 0;

public:
   REQ_SHELL_COMMAND()
   {
       memset(this, 0, sizeof(REQ_SHELL_COMMAND));
       mPacketSize = static_cast<PacketSize>(sizeof(REQ_SHELL_COMMAND));
   }
   REQ_SHELL_COMMAND(const REQ_SHELL_COMMAND&) = delete;
   REQ_SHELL_COMMAND(REQ_SHELL_COMMAND &&) = delete;
   REQ_SHELL_COMMAND& operator = (const REQ_SHELL_COMMAND&) = delete;
   REQ_SHELL_COMMAND& operator = (REQ_SHELL_COMMAND &&) = delete;

   const wchar_t* Get_command() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_command_offset) + m_command_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_command_offset);
   }

   wchar_t* Ref_command()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_command_offset) + m_command_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_command_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(REQ_SHELL_COMMAND) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(REQ_SHELL_COMMAND) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(REQ_SHELL_COMMAND); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(REQ_SHELL_COMMAND))
           {
               size += sizeof(REQ_SHELL_COMMAND);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           if (GetPacketSize() < m_command_offset)
               return false;

           if (0 < m_command_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_command_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, REQ_SHELL_COMMAND>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}command: {}", tTabStr, t->Get_command());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ACK_SHELL_COMMAND;

class ACK_SHELL_COMMAND_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::ACK_SHELL_COMMAND;

public:
   // (4)
   explicit ACK_SHELL_COMMAND_WRITER(PacketTraitAck trait, uint8_t* buf, const NetworkPacket& rp, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::ACK_SHELL_COMMAND, buf)
   {
       SetReadPacket(rp);
       GetHeader().SetPacketResult(packetResult);
   }

   // (5)
   explicit ACK_SHELL_COMMAND_WRITER(PacketTraitAck trait, uint8_t* buf, const NetworkPacketWriter& wp, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::ACK_SHELL_COMMAND, buf)
   {
       SetWritePacket(wp);
       GetHeader().SetPacketResult(packetResult);
   }

   // (6)
   explicit ACK_SHELL_COMMAND_WRITER(PacketTraitAck trait, uint8_t* buf, const PacketHeader& header, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::ACK_SHELL_COMMAND, buf)
   {
       SetHeader(header);
       GetHeader().SetPacketResult(packetResult);
   }

   // (8)
   explicit ACK_SHELL_COMMAND_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       const wchar_t* result
   )
   {
       PacketSize& resultOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& resultLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       resultOffset = GetPacketSize();
       resultLength = (PacketSize)wcsnlen_s(result, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == resultLength) WriteString(L"", 0);
       else WriteString(result, resultLength); 

   }

   ACK_SHELL_COMMAND& GetPacket() const noexcept { return *reinterpret_cast<ACK_SHELL_COMMAND*>(GetPacketBufPtr()); }
   ACK_SHELL_COMMAND* GetPacketPtr() const noexcept { return reinterpret_cast<ACK_SHELL_COMMAND*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class ACK_SHELL_COMMAND : public NetworkPacket
{
   public:
      using Writer = ACK_SHELL_COMMAND_WRITER;


private:
   PacketSize m_result_offset = 0;
   PacketSize m_result_length = 0;

public:
   ACK_SHELL_COMMAND()
   {
       memset(this, 0, sizeof(ACK_SHELL_COMMAND));
       mPacketSize = static_cast<PacketSize>(sizeof(ACK_SHELL_COMMAND));
   }
   ACK_SHELL_COMMAND(const ACK_SHELL_COMMAND&) = delete;
   ACK_SHELL_COMMAND(ACK_SHELL_COMMAND &&) = delete;
   ACK_SHELL_COMMAND& operator = (const ACK_SHELL_COMMAND&) = delete;
   ACK_SHELL_COMMAND& operator = (ACK_SHELL_COMMAND &&) = delete;

   const wchar_t* Get_result() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_result_offset) + m_result_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_result_offset);
   }

   wchar_t* Ref_result()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_result_offset) + m_result_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_result_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(ACK_SHELL_COMMAND) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(ACK_SHELL_COMMAND) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(ACK_SHELL_COMMAND); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(ACK_SHELL_COMMAND))
           {
               size += sizeof(ACK_SHELL_COMMAND);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           if (GetPacketSize() < m_result_offset)
               return false;

           if (0 < m_result_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_result_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, ACK_SHELL_COMMAND>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}result: {}", tTabStr, t->Get_result());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class SHELL_NOTIFY;

class SHELL_NOTIFY_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::SHELL_NOTIFY;

public:
   // (7)
   explicit SHELL_NOTIFY_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::SHELL_NOTIFY, buf)
   {
   }

   // (9)
   explicit SHELL_NOTIFY_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       const wchar_t* command
   )
       :
       NetworkPacketWriter(trait, PacketTypes::SHELL_NOTIFY, buf)
   {
       PacketSize& commandOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& commandLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       commandOffset = GetPacketSize();
       commandLength = (PacketSize)wcsnlen_s(command, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == commandLength) WriteString(L"", 0);
       else WriteString(command, commandLength); 

   }

   // (8)
   explicit SHELL_NOTIFY_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       const wchar_t* command
   )
   {
       PacketSize& commandOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& commandLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       commandOffset = GetPacketSize();
       commandLength = (PacketSize)wcsnlen_s(command, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == commandLength) WriteString(L"", 0);
       else WriteString(command, commandLength); 

   }

   SHELL_NOTIFY& GetPacket() const noexcept { return *reinterpret_cast<SHELL_NOTIFY*>(GetPacketBufPtr()); }
   SHELL_NOTIFY* GetPacketPtr() const noexcept { return reinterpret_cast<SHELL_NOTIFY*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class SHELL_NOTIFY : public NetworkPacket
{
   public:
      using Writer = SHELL_NOTIFY_WRITER;


private:
   PacketSize m_command_offset = 0; // Shell 알림
   PacketSize m_command_length = 0;

public:
   SHELL_NOTIFY()
   {
       memset(this, 0, sizeof(SHELL_NOTIFY));
       mPacketSize = static_cast<PacketSize>(sizeof(SHELL_NOTIFY));
   }
   SHELL_NOTIFY(const SHELL_NOTIFY&) = delete;
   SHELL_NOTIFY(SHELL_NOTIFY &&) = delete;
   SHELL_NOTIFY& operator = (const SHELL_NOTIFY&) = delete;
   SHELL_NOTIFY& operator = (SHELL_NOTIFY &&) = delete;

   const wchar_t* Get_command() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_command_offset) + m_command_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_command_offset);
   }

   wchar_t* Ref_command()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_command_offset) + m_command_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_command_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(SHELL_NOTIFY) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(SHELL_NOTIFY) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(SHELL_NOTIFY); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(SHELL_NOTIFY))
           {
               size += sizeof(SHELL_NOTIFY);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           if (GetPacketSize() < m_command_offset)
               return false;

           if (0 < m_command_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_command_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, SHELL_NOTIFY>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}command: {}", tTabStr, t->Get_command());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class STRING_ELEM;

class STRING_ELEM_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::STRING_ELEM;

public:
   // (7)
   explicit STRING_ELEM_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::STRING_ELEM, buf)
   {
   }

   // (9)
   explicit STRING_ELEM_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       const wchar_t* value
   )
       :
       PacketWriter(trait, PacketTypes::STRING_ELEM, buf)
   {
       PacketSize& valueOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& valueLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       valueOffset = GetPacketSize();
       valueLength = (PacketSize)wcsnlen_s(value, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == valueLength) WriteString(L"", 0);
       else WriteString(value, valueLength); 

   }

   // (8)
   explicit STRING_ELEM_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       const wchar_t* value
   )
   {
       PacketSize& valueOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& valueLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       valueOffset = GetPacketSize();
       valueLength = (PacketSize)wcsnlen_s(value, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == valueLength) WriteString(L"", 0);
       else WriteString(value, valueLength); 

   }

   STRING_ELEM& GetPacket() const noexcept { return *reinterpret_cast<STRING_ELEM*>(GetPacketBufPtr()); }
   STRING_ELEM* GetPacketPtr() const noexcept { return reinterpret_cast<STRING_ELEM*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class STRING_ELEM : public Packet
{
   public:
      using Writer = STRING_ELEM_WRITER;


private:
   PacketSize m_value_offset = 0;
   PacketSize m_value_length = 0;

public:
   STRING_ELEM()
   {
       memset(this, 0, sizeof(STRING_ELEM));
       mPacketSize = static_cast<PacketSize>(sizeof(STRING_ELEM));
   }
   STRING_ELEM(const STRING_ELEM&) = delete;
   STRING_ELEM(STRING_ELEM &&) = delete;
   STRING_ELEM& operator = (const STRING_ELEM&) = delete;
   STRING_ELEM& operator = (STRING_ELEM &&) = delete;

   const wchar_t* Get_value() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_value_offset) + m_value_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_value_offset);
   }

   wchar_t* Ref_value()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_value_offset) + m_value_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_value_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(STRING_ELEM) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(STRING_ELEM) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(STRING_ELEM); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(STRING_ELEM))
           {
               size += sizeof(STRING_ELEM);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           if (GetPacketSize() < m_value_offset)
               return false;

           if (0 < m_value_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_value_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, STRING_ELEM>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}value: {}", tTabStr, t->Get_value());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class BIN_ELEM;

class BIN_ELEM_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::BIN_ELEM;

public:
   // (7)
   explicit BIN_ELEM_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::BIN_ELEM, buf)
   {
   }

   // (9)
   explicit BIN_ELEM_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       const uint8_t* value,
       PacketSize valueSize
   )
       :
       PacketWriter(trait, PacketTypes::BIN_ELEM, buf)
   {
       PacketSize& valueOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(valueSize);


       valueOffset = GetPacketSize();
       WriteBinary(value, valueSize);

   }

   // (8)
   explicit BIN_ELEM_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       const uint8_t* value,
       PacketSize valueSize
   )
   {
       PacketSize& valueOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(valueSize);


       valueOffset = GetPacketSize();
       WriteBinary(value, valueSize);

   }

   BIN_ELEM& GetPacket() const noexcept { return *reinterpret_cast<BIN_ELEM*>(GetPacketBufPtr()); }
   BIN_ELEM* GetPacketPtr() const noexcept { return reinterpret_cast<BIN_ELEM*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class BIN_ELEM : public Packet
{
   public:
      using Writer = BIN_ELEM_WRITER;


private:
   PacketSize m_value_offset = 0;
   PacketSize m_value_size = 0;

public:
   BIN_ELEM()
   {
       memset(this, 0, sizeof(BIN_ELEM));
       mPacketSize = static_cast<PacketSize>(sizeof(BIN_ELEM));
   }
   BIN_ELEM(const BIN_ELEM&) = delete;
   BIN_ELEM(BIN_ELEM &&) = delete;
   BIN_ELEM& operator = (const BIN_ELEM&) = delete;
   BIN_ELEM& operator = (BIN_ELEM &&) = delete;

   const uint8_t* Get_value() const noexcept { return ((uint8_t*)this + m_value_offset); }
   PacketSize Get_value_size() const noexcept { return m_value_size; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(BIN_ELEM) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(BIN_ELEM) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(BIN_ELEM); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(BIN_ELEM))
           {
               size += sizeof(BIN_ELEM);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           if (GetPacketSize() < m_value_offset)
               return false;

           thisSize += m_value_size;
           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, BIN_ELEM>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}value: BIN[{}]", tTabStr, t->Get_value_size());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class USER_CACHE_DIFF;

class USER_CACHE_DIFF_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::USER_CACHE_DIFF;

private:
   PacketSize* m_itemList_offset = nullptr;
   PacketSize* m_itemList_count = nullptr;

public:
   // (7)
   explicit USER_CACHE_DIFF_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::USER_CACHE_DIFF, buf)
   {
   }

   // (9)
   explicit USER_CACHE_DIFF_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       UserExp userExp,
       const ItemId* itemIdList,
       PacketSize itemIdListSize,
       const ItemQuantity* itemQuantityList,
       PacketSize itemQuantityListSize
   )
       :
       PacketWriter(trait, PacketTypes::USER_CACHE_DIFF, buf)
   {
       WriteValue(userExp);

       PacketSize& itemIdListOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(itemIdListSize);

       PacketSize& itemQuantityListOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(itemQuantityListSize);

       m_itemList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       m_itemList_count = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));


       itemIdListOffset = GetPacketSize();
       WriteBinary((uint8_t*)itemIdList, sizeof(ItemId) * itemIdListSize);

       itemQuantityListOffset = GetPacketSize();
       WriteBinary((uint8_t*)itemQuantityList, sizeof(ItemQuantity) * itemQuantityListSize);

   }

   // (8)
   explicit USER_CACHE_DIFF_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       UserExp userExp,
       const ItemId* itemIdList,
       PacketSize itemIdListSize,
       const ItemQuantity* itemQuantityList,
       PacketSize itemQuantityListSize
   )
   {
       WriteValue(userExp);

       PacketSize& itemIdListOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(itemIdListSize);

       PacketSize& itemQuantityListOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(itemQuantityListSize);

       m_itemList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       m_itemList_count = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));


       itemIdListOffset = GetPacketSize();
       WriteBinary((uint8_t*)itemIdList, sizeof(ItemId) * itemIdListSize);

       itemQuantityListOffset = GetPacketSize();
       WriteBinary((uint8_t*)itemQuantityList, sizeof(ItemQuantity) * itemQuantityListSize);

   }

   void Write_itemList(const ITEM& packet)
   {
       *m_itemList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_itemList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_itemList_count);
   }

   void Write_itemList(ITEM&& packet)
   {
       *m_itemList_offset = GetPacketSize();
       WriteBinary((uint8_t*)&packet, packet.GetPacketSize());
       m_itemList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_itemList_count);
   }

   void Write_itemList(const ITEM::Writer& packet)
   {
       *m_itemList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_itemList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_itemList_count);
   }

   void Write_itemList(ITEM::Writer&& packet)
   {
       *m_itemList_offset = GetPacketSize();
       WriteBinary(packet.GetPacketBufPtr(), packet.GetPacketSize());
       m_itemList_offset = (PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       ++(*m_itemList_count);
   }

   USER_CACHE_DIFF& GetPacket() const noexcept { return *reinterpret_cast<USER_CACHE_DIFF*>(GetPacketBufPtr()); }
   USER_CACHE_DIFF* GetPacketPtr() const noexcept { return reinterpret_cast<USER_CACHE_DIFF*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class USER_CACHE_DIFF : public Packet
{
   public:
      using Writer = USER_CACHE_DIFF_WRITER;


private:
   UserExp m_userExp;

   PacketSize m_itemIdList_offset = 0;
   PacketSize m_itemIdList_size = 0;

   PacketSize m_itemQuantityList_offset = 0;
   PacketSize m_itemQuantityList_size = 0;

   PacketSize m_itemList_offset = 0;
   PacketSize m_itemList_count = 0;

public:
   USER_CACHE_DIFF()
   {
       memset(this, 0, sizeof(USER_CACHE_DIFF));
       mPacketSize = static_cast<PacketSize>(sizeof(USER_CACHE_DIFF));
   }
   USER_CACHE_DIFF(const USER_CACHE_DIFF&) = delete;
   USER_CACHE_DIFF(USER_CACHE_DIFF &&) = delete;
   USER_CACHE_DIFF& operator = (const USER_CACHE_DIFF&) = delete;
   USER_CACHE_DIFF& operator = (USER_CACHE_DIFF &&) = delete;

   UserExp Get_userExp() const noexcept { return m_userExp; }
   UserExp& Ref_userExp() noexcept { return m_userExp; }
   const UserExp& ConstRef_userExp() const noexcept { return m_userExp; }

   const ItemId* Get_itemIdList() const noexcept { return (ItemId*)((uint8_t*)this + m_itemIdList_offset); }
   PacketSize Get_itemIdList_size() const noexcept { return m_itemIdList_size; }

   const ItemQuantity* Get_itemQuantityList() const noexcept { return (ItemQuantity*)((uint8_t*)this + m_itemQuantityList_offset); }
   PacketSize Get_itemQuantityList_size() const noexcept { return m_itemQuantityList_size; }

   PacketDataList<USER_CACHE_DIFF, ITEM> Get_itemList() const { return PacketDataList<USER_CACHE_DIFF, ITEM>(this, m_itemList_offset); }
   PacketSize Get_itemList_count() const { return m_itemList_count; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(USER_CACHE_DIFF) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(USER_CACHE_DIFF) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(USER_CACHE_DIFF); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(USER_CACHE_DIFF))
           {
               size += sizeof(USER_CACHE_DIFF);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(UserExp);
       }

       {
           thisSize += sizeof(ItemId) * m_itemIdList_size;
           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(ItemQuantity) * m_itemQuantityList_size;
           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_itemList_offset)
               return false;

           PacketSize _offset = m_itemList_offset;
           while (0 != _offset)
           {
               const ITEM* _packet = reinterpret_cast<const ITEM*>(GetPacketBufPtr() + _offset);
               if (!_packet->Validate(thisSize, ++depth))
                   return false;

               if (GetPacketSize() < thisSize)
                   return false;

               _offset = *reinterpret_cast<const PacketSize*>(GetPacketBufPtr() + _offset + _packet->GetPacketSize());
               thisSize += sizeof(PacketSize);

               if (GetPacketSize() < _offset)
                   return false;
           }

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < _offset)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, USER_CACHE_DIFF>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}userExp: {}", tTabStr, t->Get_userExp());
       if (0 == t->Get_itemIdList_size())
       {
           result += std::format(L"\n{}itemIdList: ARRAY[{}]", tTabStr, t->Get_itemIdList_size());
       }
       else
       {
           result += std::format(L"\n{}itemIdList: ARRAY[{}], ", tTabStr, t->Get_itemIdList_size());
           //tTabStr += std::wstring(L"\t");
           for (size_t i = 0; i < t->Get_itemIdList_size(); ++i)
           {
               result += std::format(L"\n{}{}", tTabStr, t->Get_itemIdList()[i]);
           }
           //tTabStr.pop_back();
       }
       if (0 == t->Get_itemQuantityList_size())
       {
           result += std::format(L"\n{}itemQuantityList: ARRAY[{}]", tTabStr, t->Get_itemQuantityList_size());
       }
       else
       {
           result += std::format(L"\n{}itemQuantityList: ARRAY[{}], ", tTabStr, t->Get_itemQuantityList_size());
           //tTabStr += std::wstring(L"\t");
           for (size_t i = 0; i < t->Get_itemQuantityList_size(); ++i)
           {
               result += std::format(L"\n{}{}", tTabStr, t->Get_itemQuantityList()[i]);
           }
           //tTabStr.pop_back();
       }
       if (0 == t->Get_itemList_count())
       {
           result += std::format(L"\n{}itemList: LIST[{}]", tTabStr, t->Get_itemList_count());
       }
       else
       {
           result += std::format(L"\n{}itemList: LIST[{}], ", tTabStr, t->Get_itemList_count());
           //tTabStr += std::wstring(L"\t");
           for (const ITEM* it : t->Get_itemList())
           {
               result += std::format(L"\n{}{}", tTabStr, const_cast<ITEM*>(it));
           }
           //tTabStr.pop_back();
       }
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class WORLD_USER_COMMAND;

class WORLD_USER_COMMAND_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::WORLD_USER_COMMAND;

public:
   // (7)
   explicit WORLD_USER_COMMAND_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::WORLD_USER_COMMAND, buf)
   {
   }

   // (9)
   explicit WORLD_USER_COMMAND_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       WorldUserId worldUserId,
       WorldUserCommandType commandType,
       uint8_t commandKeyEvent,
       uint8_t commandKeyState,
       Scalar commandX,
       Scalar commandY,
       EntityIdp targetEntityIdp
   )
       :
       PacketWriter(trait, PacketTypes::WORLD_USER_COMMAND, buf)
   {
       WriteValue(worldUserId);

       WriteValue(commandType);

       WriteValue(commandKeyEvent);

       WriteValue(commandKeyState);

       WriteValue(commandX);

       WriteValue(commandY);

       WriteValue(targetEntityIdp);


   }

   // (8)
   explicit WORLD_USER_COMMAND_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       WorldUserId worldUserId,
       WorldUserCommandType commandType,
       uint8_t commandKeyEvent,
       uint8_t commandKeyState,
       Scalar commandX,
       Scalar commandY,
       EntityIdp targetEntityIdp
   )
   {
       WriteValue(worldUserId);

       WriteValue(commandType);

       WriteValue(commandKeyEvent);

       WriteValue(commandKeyState);

       WriteValue(commandX);

       WriteValue(commandY);

       WriteValue(targetEntityIdp);


   }

   WORLD_USER_COMMAND& GetPacket() const noexcept { return *reinterpret_cast<WORLD_USER_COMMAND*>(GetPacketBufPtr()); }
   WORLD_USER_COMMAND* GetPacketPtr() const noexcept { return reinterpret_cast<WORLD_USER_COMMAND*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class WORLD_USER_COMMAND : public Packet
{
   public:
      using Writer = WORLD_USER_COMMAND_WRITER;


private:
   WorldUserId m_worldUserId;

   WorldUserCommandType m_commandType;

   uint8_t m_commandKeyEvent;

   uint8_t m_commandKeyState;

   Scalar m_commandX;

   Scalar m_commandY;

   EntityIdp m_targetEntityIdp;

public:
   WORLD_USER_COMMAND()
   {
       memset(this, 0, sizeof(WORLD_USER_COMMAND));
       mPacketSize = static_cast<PacketSize>(sizeof(WORLD_USER_COMMAND));
   }
   WORLD_USER_COMMAND(const WORLD_USER_COMMAND&) = delete;
   WORLD_USER_COMMAND(WORLD_USER_COMMAND &&) = delete;
   WORLD_USER_COMMAND& operator = (const WORLD_USER_COMMAND&) = delete;
   WORLD_USER_COMMAND& operator = (WORLD_USER_COMMAND &&) = delete;

   WorldUserId Get_worldUserId() const noexcept { return m_worldUserId; }
   WorldUserId& Ref_worldUserId() noexcept { return m_worldUserId; }
   const WorldUserId& ConstRef_worldUserId() const noexcept { return m_worldUserId; }

   WorldUserCommandType Get_commandType() const noexcept { return m_commandType; }
   WorldUserCommandType& Ref_commandType() noexcept { return m_commandType; }
   const WorldUserCommandType& ConstRef_commandType() const noexcept { return m_commandType; }

   uint8_t Get_commandKeyEvent() const noexcept { return m_commandKeyEvent; }
   uint8_t& Ref_commandKeyEvent() noexcept { return m_commandKeyEvent; }
   const uint8_t& ConstRef_commandKeyEvent() const noexcept { return m_commandKeyEvent; }

   uint8_t Get_commandKeyState() const noexcept { return m_commandKeyState; }
   uint8_t& Ref_commandKeyState() noexcept { return m_commandKeyState; }
   const uint8_t& ConstRef_commandKeyState() const noexcept { return m_commandKeyState; }

   Scalar Get_commandX() const noexcept { return m_commandX; }
   Scalar& Ref_commandX() noexcept { return m_commandX; }
   const Scalar& ConstRef_commandX() const noexcept { return m_commandX; }

   Scalar Get_commandY() const noexcept { return m_commandY; }
   Scalar& Ref_commandY() noexcept { return m_commandY; }
   const Scalar& ConstRef_commandY() const noexcept { return m_commandY; }

   EntityIdp Get_targetEntityIdp() const noexcept { return m_targetEntityIdp; }
   EntityIdp& Ref_targetEntityIdp() noexcept { return m_targetEntityIdp; }
   const EntityIdp& ConstRef_targetEntityIdp() const noexcept { return m_targetEntityIdp; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(WORLD_USER_COMMAND) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(WORLD_USER_COMMAND) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(WORLD_USER_COMMAND); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(WORLD_USER_COMMAND))
           {
               size += sizeof(WORLD_USER_COMMAND);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(WorldUserId);
       }

       {
           thisSize += sizeof(WorldUserCommandType);
       }

       {
           thisSize += sizeof(uint8_t);
       }

       {
           thisSize += sizeof(uint8_t);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(Scalar);
       }

       {
           thisSize += sizeof(EntityIdp);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, WORLD_USER_COMMAND>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}worldUserId: {}", tTabStr, t->Get_worldUserId());
       result += std::format(L"\n{}commandType: {}", tTabStr, t->Get_commandType());
       result += std::format(L"\n{}commandKeyEvent: {}", tTabStr, t->Get_commandKeyEvent());
       result += std::format(L"\n{}commandKeyState: {}", tTabStr, t->Get_commandKeyState());
       result += std::format(L"\n{}commandX: {}", tTabStr, t->Get_commandX());
       result += std::format(L"\n{}commandY: {}", tTabStr, t->Get_commandY());
       result += std::format(L"\n{}targetEntityIdp: {}", tTabStr, t->Get_targetEntityIdp());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class WORLD_USER_STAT_ALL;

class WORLD_USER_STAT_ALL_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::WORLD_USER_STAT_ALL;

public:
   // (7)
   explicit WORLD_USER_STAT_ALL_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::WORLD_USER_STAT_ALL, buf)
   {
   }

   // (9)
   explicit WORLD_USER_STAT_ALL_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       WorldUserId worldUserId,
       const uint8_t* statDataBin,
       PacketSize statDataBinSize,
       ItemSid characterItemSid,
       ItemLevel characterItemLevel
   )
       :
       PacketWriter(trait, PacketTypes::WORLD_USER_STAT_ALL, buf)
   {
       WriteValue(worldUserId);

       PacketSize& statDataBinOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(statDataBinSize);

       WriteValue(characterItemSid);

       WriteValue(characterItemLevel);


       statDataBinOffset = GetPacketSize();
       WriteBinary(statDataBin, statDataBinSize);

   }

   // (8)
   explicit WORLD_USER_STAT_ALL_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       WorldUserId worldUserId,
       const uint8_t* statDataBin,
       PacketSize statDataBinSize,
       ItemSid characterItemSid,
       ItemLevel characterItemLevel
   )
   {
       WriteValue(worldUserId);

       PacketSize& statDataBinOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(statDataBinSize);

       WriteValue(characterItemSid);

       WriteValue(characterItemLevel);


       statDataBinOffset = GetPacketSize();
       WriteBinary(statDataBin, statDataBinSize);

   }

   WORLD_USER_STAT_ALL& GetPacket() const noexcept { return *reinterpret_cast<WORLD_USER_STAT_ALL*>(GetPacketBufPtr()); }
   WORLD_USER_STAT_ALL* GetPacketPtr() const noexcept { return reinterpret_cast<WORLD_USER_STAT_ALL*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class WORLD_USER_STAT_ALL : public Packet
{
   public:
      using Writer = WORLD_USER_STAT_ALL_WRITER;


private:
   WorldUserId m_worldUserId;

   PacketSize m_statDataBin_offset = 0;
   PacketSize m_statDataBin_size = 0;

   ItemSid m_characterItemSid;

   ItemLevel m_characterItemLevel;

public:
   WORLD_USER_STAT_ALL()
   {
       memset(this, 0, sizeof(WORLD_USER_STAT_ALL));
       mPacketSize = static_cast<PacketSize>(sizeof(WORLD_USER_STAT_ALL));
   }
   WORLD_USER_STAT_ALL(const WORLD_USER_STAT_ALL&) = delete;
   WORLD_USER_STAT_ALL(WORLD_USER_STAT_ALL &&) = delete;
   WORLD_USER_STAT_ALL& operator = (const WORLD_USER_STAT_ALL&) = delete;
   WORLD_USER_STAT_ALL& operator = (WORLD_USER_STAT_ALL &&) = delete;

   WorldUserId Get_worldUserId() const noexcept { return m_worldUserId; }
   WorldUserId& Ref_worldUserId() noexcept { return m_worldUserId; }
   const WorldUserId& ConstRef_worldUserId() const noexcept { return m_worldUserId; }

   const uint8_t* Get_statDataBin() const noexcept { return ((uint8_t*)this + m_statDataBin_offset); }
   PacketSize Get_statDataBin_size() const noexcept { return m_statDataBin_size; }

   ItemSid Get_characterItemSid() const noexcept { return m_characterItemSid; }
   ItemSid& Ref_characterItemSid() noexcept { return m_characterItemSid; }
   const ItemSid& ConstRef_characterItemSid() const noexcept { return m_characterItemSid; }

   ItemLevel Get_characterItemLevel() const noexcept { return m_characterItemLevel; }
   ItemLevel& Ref_characterItemLevel() noexcept { return m_characterItemLevel; }
   const ItemLevel& ConstRef_characterItemLevel() const noexcept { return m_characterItemLevel; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(WORLD_USER_STAT_ALL) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(WORLD_USER_STAT_ALL) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(WORLD_USER_STAT_ALL); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(WORLD_USER_STAT_ALL))
           {
               size += sizeof(WORLD_USER_STAT_ALL);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(WorldUserId);
       }

       {
           if (GetPacketSize() < m_statDataBin_offset)
               return false;

           thisSize += m_statDataBin_size;
           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(ItemSid);
       }

       {
           thisSize += sizeof(ItemLevel);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, WORLD_USER_STAT_ALL>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}worldUserId: {}", tTabStr, t->Get_worldUserId());
       result += std::format(L"\n{}statDataBin: BIN[{}]", tTabStr, t->Get_statDataBin_size());
       result += std::format(L"\n{}characterItemSid: {}", tTabStr, t->Get_characterItemSid());
       result += std::format(L"\n{}characterItemLevel: {}", tTabStr, t->Get_characterItemLevel());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class WORLD_USER_STAT_DIFF;

class WORLD_USER_STAT_DIFF_WRITER : public PacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::WORLD_USER_STAT_DIFF;

public:
   // (7)
   explicit WORLD_USER_STAT_DIFF_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       PacketWriter(trait, PacketTypes::WORLD_USER_STAT_DIFF, buf)
   {
   }

   // (9)
   explicit WORLD_USER_STAT_DIFF_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       WorldUserId worldUserId,
       const StatType* statTypeList,
       PacketSize statTypeListSize,
       const StatValue* statValueList,
       PacketSize statValueListSize
   )
       :
       PacketWriter(trait, PacketTypes::WORLD_USER_STAT_DIFF, buf)
   {
       WriteValue(worldUserId);

       PacketSize& statTypeListOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(statTypeListSize);

       PacketSize& statValueListOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(statValueListSize);


       statTypeListOffset = GetPacketSize();
       WriteBinary((uint8_t*)statTypeList, sizeof(StatType) * statTypeListSize);

       statValueListOffset = GetPacketSize();
       WriteBinary((uint8_t*)statValueList, sizeof(StatValue) * statValueListSize);

   }

   // (8)
   explicit WORLD_USER_STAT_DIFF_WRITER(uint8_t* buf)
       :
       PacketWriter(buf) 
   {
   }

   void SetValues(
       WorldUserId worldUserId,
       const StatType* statTypeList,
       PacketSize statTypeListSize,
       const StatValue* statValueList,
       PacketSize statValueListSize
   )
   {
       WriteValue(worldUserId);

       PacketSize& statTypeListOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(statTypeListSize);

       PacketSize& statValueListOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(statValueListSize);


       statTypeListOffset = GetPacketSize();
       WriteBinary((uint8_t*)statTypeList, sizeof(StatType) * statTypeListSize);

       statValueListOffset = GetPacketSize();
       WriteBinary((uint8_t*)statValueList, sizeof(StatValue) * statValueListSize);

   }

   WORLD_USER_STAT_DIFF& GetPacket() const noexcept { return *reinterpret_cast<WORLD_USER_STAT_DIFF*>(GetPacketBufPtr()); }
   WORLD_USER_STAT_DIFF* GetPacketPtr() const noexcept { return reinterpret_cast<WORLD_USER_STAT_DIFF*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class WORLD_USER_STAT_DIFF : public Packet
{
   public:
      using Writer = WORLD_USER_STAT_DIFF_WRITER;


private:
   WorldUserId m_worldUserId;

   PacketSize m_statTypeList_offset = 0;
   PacketSize m_statTypeList_size = 0;

   PacketSize m_statValueList_offset = 0;
   PacketSize m_statValueList_size = 0;

public:
   WORLD_USER_STAT_DIFF()
   {
       memset(this, 0, sizeof(WORLD_USER_STAT_DIFF));
       mPacketSize = static_cast<PacketSize>(sizeof(WORLD_USER_STAT_DIFF));
   }
   WORLD_USER_STAT_DIFF(const WORLD_USER_STAT_DIFF&) = delete;
   WORLD_USER_STAT_DIFF(WORLD_USER_STAT_DIFF &&) = delete;
   WORLD_USER_STAT_DIFF& operator = (const WORLD_USER_STAT_DIFF&) = delete;
   WORLD_USER_STAT_DIFF& operator = (WORLD_USER_STAT_DIFF &&) = delete;

   WorldUserId Get_worldUserId() const noexcept { return m_worldUserId; }
   WorldUserId& Ref_worldUserId() noexcept { return m_worldUserId; }
   const WorldUserId& ConstRef_worldUserId() const noexcept { return m_worldUserId; }

   const StatType* Get_statTypeList() const noexcept { return (StatType*)((uint8_t*)this + m_statTypeList_offset); }
   PacketSize Get_statTypeList_size() const noexcept { return m_statTypeList_size; }

   const StatValue* Get_statValueList() const noexcept { return (StatValue*)((uint8_t*)this + m_statValueList_offset); }
   PacketSize Get_statValueList_size() const noexcept { return m_statValueList_size; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(WORLD_USER_STAT_DIFF) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(WORLD_USER_STAT_DIFF) == GetPacketSize()))
       {
           size_t i = sizeof(Packet);
           for (; i < sizeof(WORLD_USER_STAT_DIFF); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(WORLD_USER_STAT_DIFF))
           {
               size += sizeof(WORLD_USER_STAT_DIFF);
               return true;
           }
       }

       size_t thisSize = sizeof(Packet);

       {
           thisSize += sizeof(WorldUserId);
       }

       {
           thisSize += sizeof(StatType) * m_statTypeList_size;
           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           thisSize += sizeof(StatValue) * m_statValueList_size;
           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, WORLD_USER_STAT_DIFF>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}worldUserId: {}", tTabStr, t->Get_worldUserId());
       if (0 == t->Get_statTypeList_size())
       {
           result += std::format(L"\n{}statTypeList: ARRAY[{}]", tTabStr, t->Get_statTypeList_size());
       }
       else
       {
           result += std::format(L"\n{}statTypeList: ARRAY[{}], ", tTabStr, t->Get_statTypeList_size());
           //tTabStr += std::wstring(L"\t");
           for (size_t i = 0; i < t->Get_statTypeList_size(); ++i)
           {
               result += std::format(L"\n{}{}", tTabStr, t->Get_statTypeList()[i]);
           }
           //tTabStr.pop_back();
       }
       if (0 == t->Get_statValueList_size())
       {
           result += std::format(L"\n{}statValueList: ARRAY[{}]", tTabStr, t->Get_statValueList_size());
       }
       else
       {
           result += std::format(L"\n{}statValueList: ARRAY[{}], ", tTabStr, t->Get_statValueList_size());
           //tTabStr += std::wstring(L"\t");
           for (size_t i = 0; i < t->Get_statValueList_size(); ++i)
           {
               result += std::format(L"\n{}{}", tTabStr, t->Get_statValueList()[i]);
           }
           //tTabStr.pop_back();
       }
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

#pragma pack(pop)

inline bool ValidatePacketCOMMON(const Packet& rp)
{
   static constexpr bool(*ValidatePacketFuncArray[PacketTypes::COMMON_PACKET_END - PacketTypes::COMMON_PACKET_START - 1])(const Packet&) =
   {
       &ValidatePacketFunc<LOG_ACHIEVEMENT>,
       &ValidatePacketFunc<INSERT_LOG_ACHIEVEMENT>,
       &ValidatePacketFunc<LOG_ACHIEVEMENT_ALL>,
       &ValidatePacketFunc<STATIC_ACHIEVEMENT>,
       &ValidatePacketFunc<STATIC_ACHIEVEMENT_ALL>,
       &ValidatePacketFunc<STATIC_ACHIEVEMENT_STEP>,
       &ValidatePacketFunc<STATIC_ACHIEVEMENT_STEP_ALL>,
       &ValidatePacketFunc<STATIC_BUILDING>,
       &ValidatePacketFunc<STATIC_BUILDING_ALL>,
       &ValidatePacketFunc<STATIC_CHARACTER>,
       &ValidatePacketFunc<STATIC_CHARACTER_ALL>,
       &ValidatePacketFunc<STATIC_CHARACTER_SKILL>,
       &ValidatePacketFunc<STATIC_CHARACTER_SKILL_ALL>,
       &ValidatePacketFunc<STATIC_GAME>,
       &ValidatePacketFunc<STATIC_GAME_ALL>,
       &ValidatePacketFunc<STATIC_ITEM>,
       &ValidatePacketFunc<STATIC_ITEM_ALL>,
       &ValidatePacketFunc<STATIC_ITEM_STAT>,
       &ValidatePacketFunc<STATIC_ITEM_STAT_ALL>,
       &ValidatePacketFunc<STATIC_MAIL>,
       &ValidatePacketFunc<STATIC_MAIL_ALL>,
       &ValidatePacketFunc<STATIC_MAP>,
       &ValidatePacketFunc<STATIC_MAP_ALL>,
       &ValidatePacketFunc<STATIC_MISSION>,
       &ValidatePacketFunc<STATIC_MISSION_ALL>,
       &ValidatePacketFunc<STATIC_PRODUCT>,
       &ValidatePacketFunc<STATIC_PRODUCT_ALL>,
       &ValidatePacketFunc<STATIC_QUEST>,
       &ValidatePacketFunc<STATIC_QUEST_ALL>,
       &ValidatePacketFunc<STATIC_REWARD_GROUP>,
       &ValidatePacketFunc<STATIC_REWARD_GROUP_ALL>,
       &ValidatePacketFunc<STATIC_REWARD_RATIO>,
       &ValidatePacketFunc<STATIC_REWARD_RATIO_ALL>,
       &ValidatePacketFunc<STATIC_REWARD>,
       &ValidatePacketFunc<STATIC_REWARD_ALL>,
       &ValidatePacketFunc<STATIC_SCHEDULE>,
       &ValidatePacketFunc<STATIC_SCHEDULE_ALL>,
       &ValidatePacketFunc<STATIC_STAT>,
       &ValidatePacketFunc<STATIC_STAT_ALL>,
       &ValidatePacketFunc<STATIC_STOCK>,
       &ValidatePacketFunc<STATIC_STOCK_ALL>,
       &ValidatePacketFunc<STATIC_TRANSLATION>,
       &ValidatePacketFunc<STATIC_TRANSLATION_ALL>,
       &ValidatePacketFunc<STATIC_USER_EXP>,
       &ValidatePacketFunc<STATIC_USER_EXP_ALL>,
       &ValidatePacketFunc<ACCOUNT_DELETED>,
       &ValidatePacketFunc<ACCOUNT_DELETED_ALL>,
       &ValidatePacketFunc<ACCOUNT>,
       &ValidatePacketFunc<ACCOUNT_ALL>,
       &ValidatePacketFunc<ACCOUNT_USER_DELETED>,
       &ValidatePacketFunc<ACCOUNT_USER_DELETED_ALL>,
       &ValidatePacketFunc<ACCOUNT_USER_DELETED_BY_ACCOUNT>,
       &ValidatePacketFunc<ACCOUNT_USER_GAME>,
       &ValidatePacketFunc<ACCOUNT_USER_GAME_ALL>,
       &ValidatePacketFunc<ACCOUNT_USER>,
       &ValidatePacketFunc<ACCOUNT_USER_ALL>,
       &ValidatePacketFunc<ACCOUNT_USER_BY_ACCOUNT>,
       &ValidatePacketFunc<GAME_DELETED>,
       &ValidatePacketFunc<GAME_DELETED_ALL>,
       &ValidatePacketFunc<GAME>,
       &ValidatePacketFunc<GAME_ALL>,
       &ValidatePacketFunc<PURCHASE_DELETED>,
       &ValidatePacketFunc<PURCHASE_DELETED_ALL>,
       &ValidatePacketFunc<PURCHASE>,
       &ValidatePacketFunc<PURCHASE_ALL>,
       &ValidatePacketFunc<SERVER>,
       &ValidatePacketFunc<SERVER_ALL>,
       &ValidatePacketFunc<SERVER_STATISTICS>,
       &ValidatePacketFunc<SERVER_STATISTICS_ALL>,
       &ValidatePacketFunc<ACHIEVEMENT>,
       &ValidatePacketFunc<ACHIEVEMENT_ALL>,
       &ValidatePacketFunc<ACHIEVEMENT_BY_USER>,
       &ValidatePacketFunc<ACTIVITY>,
       &ValidatePacketFunc<ACTIVITY_ALL>,
       &ValidatePacketFunc<ACTIVITY_BY_USER>,
       &ValidatePacketFunc<ITEM_DELETED>,
       &ValidatePacketFunc<ITEM_DELETED_ALL>,
       &ValidatePacketFunc<ITEM>,
       &ValidatePacketFunc<ITEM_ALL>,
       &ValidatePacketFunc<ITEM_BY_USER>,
       &ValidatePacketFunc<ITEM_STAT>,
       &ValidatePacketFunc<ITEM_STAT_ALL>,
       &ValidatePacketFunc<ITEM_STAT_BY_USER>,
       &ValidatePacketFunc<MAIL_DELETED>,
       &ValidatePacketFunc<MAIL_DELETED_ALL>,
       &ValidatePacketFunc<MAIL>,
       &ValidatePacketFunc<MAIL_ALL>,
       &ValidatePacketFunc<MAIL_BY_USER>,
       &ValidatePacketFunc<MISSION_DELETED>,
       &ValidatePacketFunc<MISSION_DELETED_ALL>,
       &ValidatePacketFunc<MISSION>,
       &ValidatePacketFunc<MISSION_ALL>,
       &ValidatePacketFunc<MISSION_BY_USER>,
       &ValidatePacketFunc<QUEST_DELETED>,
       &ValidatePacketFunc<QUEST_DELETED_ALL>,
       &ValidatePacketFunc<QUEST>,
       &ValidatePacketFunc<QUEST_ALL>,
       &ValidatePacketFunc<QUEST_BY_USER>,
       &ValidatePacketFunc<USER_POS>,
       &ValidatePacketFunc<USER_POS_ALL>,
       &ValidatePacketFunc<USER>,
       &ValidatePacketFunc<USER_ALL>,
       &ValidatePacketFunc<USER_BY_ACCOUNT>,
       &ValidatePacketFunc<HANDSHAKE_TO_PEER>,
       &ValidatePacketFunc<HANDSHAKE_TO_HOST>,
       &ValidatePacketFunc<APP_DATA>,
       &ValidatePacketFunc<USER_DATA>,
       &ValidatePacketFunc<PACKET_DEV_DATA>,
       &ValidatePacketFunc<SYNC_TIME>,
       &ValidatePacketFunc<REQ_GLOBAL_NOW>,
       &ValidatePacketFunc<ACK_GLOBAL_NOW>,
       &ValidatePacketFunc<REQ_APP_JOIN>,
       &ValidatePacketFunc<ACK_APP_JOIN>,
       &ValidatePacketFunc<APP_ADD>,
       &ValidatePacketFunc<APP_REMOVE>,
       &ValidatePacketFunc<REQ_SHELL_COMMAND>,
       &ValidatePacketFunc<ACK_SHELL_COMMAND>,
       &ValidatePacketFunc<SHELL_NOTIFY>,
       &ValidatePacketFunc<STRING_ELEM>,
       &ValidatePacketFunc<BIN_ELEM>,
       &ValidatePacketFunc<USER_CACHE_DIFF>,
       &ValidatePacketFunc<WORLD_USER_COMMAND>,
       &ValidatePacketFunc<WORLD_USER_STAT_ALL>,
       &ValidatePacketFunc<WORLD_USER_STAT_DIFF>
   };

   if ((PacketTypes::COMMON_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::COMMON_PACKET_END <= rp.GetPacketType()))
   {
       return true;
   }

   return ValidatePacketFuncArray[rp.GetPacketType() - PacketTypes::COMMON_PACKET_START - 1](rp);
}

template <typename _Owner, typename... _Args>
HandleResult DispatchPacketCOMMON(uint64_t socket, _Owner* owner, NetworkPacket& rp, _Args&&... args)
{
   static constexpr HandleResult(*DispatchPacketFuncArray[PacketTypes::COMMON_PACKET_END - PacketTypes::COMMON_PACKET_START - 1])(uint64_t, _Owner*, NetworkPacket&, _Args&&...) =
   {
       nullptr,
       &DispatchPacketFunc<_Owner, INSERT_LOG_ACHIEVEMENT, _Args&&...>,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       &DispatchPacketFunc<_Owner, HANDSHAKE_TO_PEER, _Args&&...>,
       &DispatchPacketFunc<_Owner, HANDSHAKE_TO_HOST, _Args&&...>,
       nullptr,
       nullptr,
       nullptr,
       &DispatchPacketFunc<_Owner, SYNC_TIME, _Args&&...>,
       &DispatchPacketFunc<_Owner, REQ_GLOBAL_NOW, _Args&&...>,
       &DispatchPacketFunc<_Owner, ACK_GLOBAL_NOW, _Args&&...>,
       &DispatchPacketFunc<_Owner, REQ_APP_JOIN, _Args&&...>,
       &DispatchPacketFunc<_Owner, ACK_APP_JOIN, _Args&&...>,
       &DispatchPacketFunc<_Owner, APP_ADD, _Args&&...>,
       &DispatchPacketFunc<_Owner, APP_REMOVE, _Args&&...>,
       &DispatchPacketFunc<_Owner, REQ_SHELL_COMMAND, _Args&&...>,
       &DispatchPacketFunc<_Owner, ACK_SHELL_COMMAND, _Args&&...>,
       &DispatchPacketFunc<_Owner, SHELL_NOTIFY, _Args&&...>,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr,
       nullptr
   };

   if ((PacketTypes::COMMON_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::COMMON_PACKET_END <= rp.GetPacketType()))
   {
       return HandleResult::NOT_EXISTS;
   }

   if (auto func = DispatchPacketFuncArray[rp.GetPacketType() - PacketTypes::COMMON_PACKET_START - 1])
       return func(socket, owner, rp, std::forward<_Args>(args)...);
   else
       return HandleResult::NOT_EXISTS;
}

