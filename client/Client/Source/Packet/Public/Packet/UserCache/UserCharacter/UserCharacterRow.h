// ============================================================================
// myungdal tech-demo project
// 
// Author: 안명달 (Myungdal Ahn)
// Email: mooondal@gmail.com
// GitHub: https://github.com/myungdal/tech_demo.git
// ============================================================================

#pragma once

#include "Packet/BasePacketAutoGenerated/PACKET_COMMON.h"


struct CharacterDoc;
class UserItemRow;

class PACKET_API UserCharacterRow
{
private:
	PacketKeep<ITEM> mData;
	const CharacterDoc* mDoc = nullptr;

	UserItemRowPtr mUserItemRow = nullptr;

public:
	explicit UserCharacterRow(
		PacketKeep<ITEM>&& data,
		UserItemRowPtr userItemRow
	);

public:
	const ITEM& Data() const { return **mData; }
	const CharacterDoc* GetDoc();

public:
	bool IsEmpty() const { return (nullptr == mDoc); }
};
