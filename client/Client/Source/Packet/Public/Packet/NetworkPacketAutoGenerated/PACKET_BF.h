// ============================================================================
// myungdal tech-demo project
// 
// Author: 안명달 (Myungdal Ahn)
// Email: mooondal@gmail.com
// GitHub: https://github.com/myungdal/tech_demo.git
// ============================================================================

#pragma once

#include "Packet/BasePacketAutoGenerated/PACKET_COMMON.h"
#include "Packet/NetworkPacket/NetworkPacket.h"
#include "Packet/NetworkPacket/NetworkPacketWriter.h"
#include "Packet/BasePacket/PacketWriter.h"
#include "Packet/PacketDataList/PacketDataList.h"
#include "Packet/PacketFuncs/PacketValidator.h"
#include "Packet/PacketFuncs/PacketDispatcher.h"

#pragma pack(push, 1)

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class BF_ACK_BC_BYPASS;

class BF_ACK_BC_BYPASS_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::BF_ACK_BC_BYPASS;

public:
   // (4)
   explicit BF_ACK_BC_BYPASS_WRITER(PacketTraitAck trait, uint8_t* buf, const NetworkPacket& rp, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::BF_ACK_BC_BYPASS, buf)
   {
       SetReadPacket(rp);
       GetHeader().SetPacketResult(packetResult);
   }

   // (5)
   explicit BF_ACK_BC_BYPASS_WRITER(PacketTraitAck trait, uint8_t* buf, const NetworkPacketWriter& wp, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::BF_ACK_BC_BYPASS, buf)
   {
       SetWritePacket(wp);
       GetHeader().SetPacketResult(packetResult);
   }

   // (6)
   explicit BF_ACK_BC_BYPASS_WRITER(PacketTraitAck trait, uint8_t* buf, const PacketHeader& header, Result packetResult)
       :
       NetworkPacketWriter(trait, PacketTypes::BF_ACK_BC_BYPASS, buf)
   {
       SetHeader(header);
       GetHeader().SetPacketResult(packetResult);
   }

   // (8)
   explicit BF_ACK_BC_BYPASS_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       const uint8_t* nestedPacket,
       PacketSize nestedPacketSize
   )
   {
       PacketSize& nestedPacketOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(nestedPacketSize);


       nestedPacketOffset = GetPacketSize();
       WriteBinary(nestedPacket, nestedPacketSize);

   }

   BF_ACK_BC_BYPASS& GetPacket() const noexcept { return *reinterpret_cast<BF_ACK_BC_BYPASS*>(GetPacketBufPtr()); }
   BF_ACK_BC_BYPASS* GetPacketPtr() const noexcept { return reinterpret_cast<BF_ACK_BC_BYPASS*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class BF_ACK_BC_BYPASS : public NetworkPacket
{
   public:
      using Writer = BF_ACK_BC_BYPASS_WRITER;


private:
   PacketSize m_nestedPacket_offset = 0;
   PacketSize m_nestedPacket_size = 0;

public:
   BF_ACK_BC_BYPASS()
   {
       memset(this, 0, sizeof(BF_ACK_BC_BYPASS));
       mPacketSize = static_cast<PacketSize>(sizeof(BF_ACK_BC_BYPASS));
   }
   BF_ACK_BC_BYPASS(const BF_ACK_BC_BYPASS&) = delete;
   BF_ACK_BC_BYPASS(BF_ACK_BC_BYPASS &&) = delete;
   BF_ACK_BC_BYPASS& operator = (const BF_ACK_BC_BYPASS&) = delete;
   BF_ACK_BC_BYPASS& operator = (BF_ACK_BC_BYPASS &&) = delete;

   const uint8_t* Get_nestedPacket() const noexcept { return ((uint8_t*)this + m_nestedPacket_offset); }
   PacketSize Get_nestedPacket_size() const noexcept { return m_nestedPacket_size; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(BF_ACK_BC_BYPASS) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(BF_ACK_BC_BYPASS) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(BF_ACK_BC_BYPASS); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(BF_ACK_BC_BYPASS))
           {
               size += sizeof(BF_ACK_BC_BYPASS);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           if (GetPacketSize() < m_nestedPacket_offset)
               return false;

           thisSize += m_nestedPacket_size;
           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, BF_ACK_BC_BYPASS>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}nestedPacket: BIN[{}]", tTabStr, t->Get_nestedPacket_size());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

#pragma pack(pop)

inline bool ValidatePacketBF(const Packet& rp)
{
   static constexpr bool(*ValidatePacketFuncArray[PacketTypes::BF_PACKET_END - PacketTypes::BF_PACKET_START - 1])(const Packet&) =
   {
       &ValidatePacketFunc<BF_ACK_BC_BYPASS>
   };

   if ((PacketTypes::BF_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::BF_PACKET_END <= rp.GetPacketType()))
   {
       return true;
   }

   return ValidatePacketFuncArray[rp.GetPacketType() - PacketTypes::BF_PACKET_START - 1](rp);
}

template <typename _Owner, typename... _Args>
HandleResult DispatchPacketBF(uint64_t socket, _Owner* owner, NetworkPacket& rp, _Args&&... args)
{
   HandleResult handleResult = DispatchPacketCOMMON(socket, owner, rp, std::forward<_Args>(args)...);
   if (handleResult != HandleResult::NOT_EXISTS)
       return handleResult;
   
   static constexpr HandleResult(*DispatchPacketFuncArray[PacketTypes::BF_PACKET_END - PacketTypes::BF_PACKET_START - 1])(uint64_t, _Owner*, NetworkPacket&, _Args&&...) =
   {
       &DispatchPacketFunc<_Owner, BF_ACK_BC_BYPASS, _Args&&...>
   };

   if ((PacketTypes::BF_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::BF_PACKET_END <= rp.GetPacketType()))
   {
       return HandleResult::NOT_EXISTS;
   }

   if (auto func = DispatchPacketFuncArray[rp.GetPacketType() - PacketTypes::BF_PACKET_START - 1])
       return func(socket, owner, rp, std::forward<_Args>(args)...);
   else
       return HandleResult::NOT_EXISTS;
}

