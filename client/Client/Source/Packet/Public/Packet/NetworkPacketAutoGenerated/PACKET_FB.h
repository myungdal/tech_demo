// ============================================================================
// myungdal tech-demo project
// 
// Author: 안명달 (Myungdal Ahn)
// Email: mooondal@gmail.com
// GitHub: https://github.com/myungdal/tech_demo.git
// ============================================================================

#pragma once

#include "Packet/BasePacketAutoGenerated/PACKET_COMMON.h"
#include "Packet/NetworkPacket/NetworkPacket.h"
#include "Packet/NetworkPacket/NetworkPacketWriter.h"
#include "Packet/BasePacket/PacketWriter.h"
#include "Packet/PacketDataList/PacketDataList.h"
#include "Packet/PacketFuncs/PacketValidator.h"
#include "Packet/PacketFuncs/PacketDispatcher.h"

#pragma pack(push, 1)

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class FB_REQ_CB_BYPASS;

class FB_REQ_CB_BYPASS_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::FB_REQ_CB_BYPASS;

public:
   // (1)
   explicit FB_REQ_CB_BYPASS_WRITER(PacketTraitReq trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::FB_REQ_CB_BYPASS, buf)
   {
   }

   // (2)
   explicit FB_REQ_CB_BYPASS_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacket& rp)
       :
       NetworkPacketWriter(trait, PacketTypes::FB_REQ_CB_BYPASS, buf)
   {
       SetReadPacket(rp);
   }

   // (3)
   explicit FB_REQ_CB_BYPASS_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacketWriter& wp)
       :
       NetworkPacketWriter(trait, PacketTypes::FB_REQ_CB_BYPASS, buf)
   {
       SetWritePacket(wp);
   }

   // (8)
   explicit FB_REQ_CB_BYPASS_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       const uint8_t* nestedPacket,
       PacketSize nestedPacketSize
   )
   {
       PacketSize& nestedPacketOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(nestedPacketSize);


       nestedPacketOffset = GetPacketSize();
       WriteBinary(nestedPacket, nestedPacketSize);

   }

   FB_REQ_CB_BYPASS& GetPacket() const noexcept { return *reinterpret_cast<FB_REQ_CB_BYPASS*>(GetPacketBufPtr()); }
   FB_REQ_CB_BYPASS* GetPacketPtr() const noexcept { return reinterpret_cast<FB_REQ_CB_BYPASS*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class FB_REQ_CB_BYPASS : public NetworkPacket
{
   public:
      using Writer = FB_REQ_CB_BYPASS_WRITER;


private:
   PacketSize m_nestedPacket_offset = 0;
   PacketSize m_nestedPacket_size = 0;

public:
   FB_REQ_CB_BYPASS()
   {
       memset(this, 0, sizeof(FB_REQ_CB_BYPASS));
       mPacketSize = static_cast<PacketSize>(sizeof(FB_REQ_CB_BYPASS));
   }
   FB_REQ_CB_BYPASS(const FB_REQ_CB_BYPASS&) = delete;
   FB_REQ_CB_BYPASS(FB_REQ_CB_BYPASS &&) = delete;
   FB_REQ_CB_BYPASS& operator = (const FB_REQ_CB_BYPASS&) = delete;
   FB_REQ_CB_BYPASS& operator = (FB_REQ_CB_BYPASS &&) = delete;

   const uint8_t* Get_nestedPacket() const noexcept { return ((uint8_t*)this + m_nestedPacket_offset); }
   PacketSize Get_nestedPacket_size() const noexcept { return m_nestedPacket_size; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(FB_REQ_CB_BYPASS) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(FB_REQ_CB_BYPASS) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(FB_REQ_CB_BYPASS); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(FB_REQ_CB_BYPASS))
           {
               size += sizeof(FB_REQ_CB_BYPASS);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           if (GetPacketSize() < m_nestedPacket_offset)
               return false;

           thisSize += m_nestedPacket_size;
           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, FB_REQ_CB_BYPASS>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}nestedPacket: BIN[{}]", tTabStr, t->Get_nestedPacket_size());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

#pragma pack(pop)

inline bool ValidatePacketFB(const Packet& rp)
{
   static constexpr bool(*ValidatePacketFuncArray[PacketTypes::FB_PACKET_END - PacketTypes::FB_PACKET_START - 1])(const Packet&) =
   {
       &ValidatePacketFunc<FB_REQ_CB_BYPASS>
   };

   if ((PacketTypes::FB_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::FB_PACKET_END <= rp.GetPacketType()))
   {
       return true;
   }

   return ValidatePacketFuncArray[rp.GetPacketType() - PacketTypes::FB_PACKET_START - 1](rp);
}

template <typename _Owner, typename... _Args>
HandleResult DispatchPacketFB(uint64_t socket, _Owner* owner, NetworkPacket& rp, _Args&&... args)
{
   HandleResult handleResult = DispatchPacketCOMMON(socket, owner, rp, std::forward<_Args>(args)...);
   if (handleResult != HandleResult::NOT_EXISTS)
       return handleResult;
   
   static constexpr HandleResult(*DispatchPacketFuncArray[PacketTypes::FB_PACKET_END - PacketTypes::FB_PACKET_START - 1])(uint64_t, _Owner*, NetworkPacket&, _Args&&...) =
   {
       &DispatchPacketFunc<_Owner, FB_REQ_CB_BYPASS, _Args&&...>
   };

   if ((PacketTypes::FB_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::FB_PACKET_END <= rp.GetPacketType()))
   {
       return HandleResult::NOT_EXISTS;
   }

   if (auto func = DispatchPacketFuncArray[rp.GetPacketType() - PacketTypes::FB_PACKET_START - 1])
       return func(socket, owner, rp, std::forward<_Args>(args)...);
   else
       return HandleResult::NOT_EXISTS;
}

