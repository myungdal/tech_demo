// ============================================================================
// myungdal tech-demo project
// 
// Author: 안명달 (Myungdal Ahn)
// Email: mooondal@gmail.com
// GitHub: https://github.com/myungdal/tech_demo.git
// ============================================================================

#pragma once

#include "Packet/BasePacketAutoGenerated/PACKET_COMMON.h"
#include "Packet/NetworkPacket/NetworkPacket.h"
#include "Packet/NetworkPacket/NetworkPacketWriter.h"
#include "Packet/BasePacket/PacketWriter.h"
#include "Packet/PacketDataList/PacketDataList.h"
#include "Packet/PacketFuncs/PacketValidator.h"
#include "Packet/PacketFuncs/PacketDispatcher.h"

#pragma pack(push, 1)

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class CF_REQ_PACKET_LIST;

class CF_REQ_PACKET_LIST_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::CF_REQ_PACKET_LIST;

public:
   // (1)
   explicit CF_REQ_PACKET_LIST_WRITER(PacketTraitReq trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_PACKET_LIST, buf)
   {

   }

   // (2)
   explicit CF_REQ_PACKET_LIST_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacket& rp)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_PACKET_LIST, buf)
   {
       SetReadPacket(rp);

   }

   // (3)
   explicit CF_REQ_PACKET_LIST_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacketWriter& wp)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_PACKET_LIST, buf)
   {
       SetWritePacket(wp);

   }

   // (8)
   explicit CF_REQ_PACKET_LIST_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   CF_REQ_PACKET_LIST& GetPacket() const noexcept { return *reinterpret_cast<CF_REQ_PACKET_LIST*>(GetPacketBufPtr()); }
   CF_REQ_PACKET_LIST* GetPacketPtr() const noexcept { return reinterpret_cast<CF_REQ_PACKET_LIST*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class CF_REQ_PACKET_LIST : public NetworkPacket
{
   public:
      using Writer = CF_REQ_PACKET_LIST_WRITER;


private:
public:
   CF_REQ_PACKET_LIST()
   {
       memset(this, 0, sizeof(CF_REQ_PACKET_LIST));
       mPacketSize = static_cast<PacketSize>(sizeof(CF_REQ_PACKET_LIST));
   }
   CF_REQ_PACKET_LIST(const CF_REQ_PACKET_LIST&) = delete;
   CF_REQ_PACKET_LIST(CF_REQ_PACKET_LIST &&) = delete;
   CF_REQ_PACKET_LIST& operator = (const CF_REQ_PACKET_LIST&) = delete;
   CF_REQ_PACKET_LIST& operator = (CF_REQ_PACKET_LIST &&) = delete;


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(CF_REQ_PACKET_LIST) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(CF_REQ_PACKET_LIST) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(CF_REQ_PACKET_LIST); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(CF_REQ_PACKET_LIST))
           {
               size += sizeof(CF_REQ_PACKET_LIST);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, CF_REQ_PACKET_LIST>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class CF_REQ_USER_LOGIN;

class CF_REQ_USER_LOGIN_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::CF_REQ_USER_LOGIN;

public:
   // (1)
   explicit CF_REQ_USER_LOGIN_WRITER(PacketTraitReq trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_USER_LOGIN, buf)
   {
   }

   // (2)
   explicit CF_REQ_USER_LOGIN_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacket& rp)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_USER_LOGIN, buf)
   {
       SetReadPacket(rp);
   }

   // (3)
   explicit CF_REQ_USER_LOGIN_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacketWriter& wp)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_USER_LOGIN, buf)
   {
       SetWritePacket(wp);
   }

   // (8)
   explicit CF_REQ_USER_LOGIN_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       AuthTicket authTicket
   )
   {
       WriteValue(authTicket);


   }

   CF_REQ_USER_LOGIN& GetPacket() const noexcept { return *reinterpret_cast<CF_REQ_USER_LOGIN*>(GetPacketBufPtr()); }
   CF_REQ_USER_LOGIN* GetPacketPtr() const noexcept { return reinterpret_cast<CF_REQ_USER_LOGIN*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class CF_REQ_USER_LOGIN : public NetworkPacket
{
   public:
      using Writer = CF_REQ_USER_LOGIN_WRITER;


private:
   AuthTicket m_authTicket;

public:
   CF_REQ_USER_LOGIN()
   {
       memset(this, 0, sizeof(CF_REQ_USER_LOGIN));
       mPacketSize = static_cast<PacketSize>(sizeof(CF_REQ_USER_LOGIN));
   }
   CF_REQ_USER_LOGIN(const CF_REQ_USER_LOGIN&) = delete;
   CF_REQ_USER_LOGIN(CF_REQ_USER_LOGIN &&) = delete;
   CF_REQ_USER_LOGIN& operator = (const CF_REQ_USER_LOGIN&) = delete;
   CF_REQ_USER_LOGIN& operator = (CF_REQ_USER_LOGIN &&) = delete;

   AuthTicket Get_authTicket() const noexcept { return m_authTicket; }
   AuthTicket& Ref_authTicket() noexcept { return m_authTicket; }
   const AuthTicket& ConstRef_authTicket() const noexcept { return m_authTicket; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(CF_REQ_USER_LOGIN) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(CF_REQ_USER_LOGIN) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(CF_REQ_USER_LOGIN); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(CF_REQ_USER_LOGIN))
           {
               size += sizeof(CF_REQ_USER_LOGIN);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(AuthTicket);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, CF_REQ_USER_LOGIN>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}authTicket: {}", tTabStr, t->Get_authTicket());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class CF_REQ_GAME_CHANNEL_USER_ENTER;

class CF_REQ_GAME_CHANNEL_USER_ENTER_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::CF_REQ_GAME_CHANNEL_USER_ENTER;

public:
   // (1)
   explicit CF_REQ_GAME_CHANNEL_USER_ENTER_WRITER(PacketTraitReq trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_GAME_CHANNEL_USER_ENTER, buf)
   {

   }

   // (2)
   explicit CF_REQ_GAME_CHANNEL_USER_ENTER_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacket& rp)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_GAME_CHANNEL_USER_ENTER, buf)
   {
       SetReadPacket(rp);

   }

   // (3)
   explicit CF_REQ_GAME_CHANNEL_USER_ENTER_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacketWriter& wp)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_GAME_CHANNEL_USER_ENTER, buf)
   {
       SetWritePacket(wp);

   }

   // (8)
   explicit CF_REQ_GAME_CHANNEL_USER_ENTER_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   CF_REQ_GAME_CHANNEL_USER_ENTER& GetPacket() const noexcept { return *reinterpret_cast<CF_REQ_GAME_CHANNEL_USER_ENTER*>(GetPacketBufPtr()); }
   CF_REQ_GAME_CHANNEL_USER_ENTER* GetPacketPtr() const noexcept { return reinterpret_cast<CF_REQ_GAME_CHANNEL_USER_ENTER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class CF_REQ_GAME_CHANNEL_USER_ENTER : public NetworkPacket
{
   public:
      using Writer = CF_REQ_GAME_CHANNEL_USER_ENTER_WRITER;


private:
public:
   CF_REQ_GAME_CHANNEL_USER_ENTER()
   {
       memset(this, 0, sizeof(CF_REQ_GAME_CHANNEL_USER_ENTER));
       mPacketSize = static_cast<PacketSize>(sizeof(CF_REQ_GAME_CHANNEL_USER_ENTER));
   }
   CF_REQ_GAME_CHANNEL_USER_ENTER(const CF_REQ_GAME_CHANNEL_USER_ENTER&) = delete;
   CF_REQ_GAME_CHANNEL_USER_ENTER(CF_REQ_GAME_CHANNEL_USER_ENTER &&) = delete;
   CF_REQ_GAME_CHANNEL_USER_ENTER& operator = (const CF_REQ_GAME_CHANNEL_USER_ENTER&) = delete;
   CF_REQ_GAME_CHANNEL_USER_ENTER& operator = (CF_REQ_GAME_CHANNEL_USER_ENTER &&) = delete;


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(CF_REQ_GAME_CHANNEL_USER_ENTER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(CF_REQ_GAME_CHANNEL_USER_ENTER) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(CF_REQ_GAME_CHANNEL_USER_ENTER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(CF_REQ_GAME_CHANNEL_USER_ENTER))
           {
               size += sizeof(CF_REQ_GAME_CHANNEL_USER_ENTER);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, CF_REQ_GAME_CHANNEL_USER_ENTER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class CF_REQ_CHAT;

class CF_REQ_CHAT_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::CF_REQ_CHAT;

public:
   // (1)
   explicit CF_REQ_CHAT_WRITER(PacketTraitReq trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_CHAT, buf)
   {
   }

   // (2)
   explicit CF_REQ_CHAT_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacket& rp)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_CHAT, buf)
   {
       SetReadPacket(rp);
   }

   // (3)
   explicit CF_REQ_CHAT_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacketWriter& wp)
       :
       NetworkPacketWriter(trait, PacketTypes::CF_REQ_CHAT, buf)
   {
       SetWritePacket(wp);
   }

   // (8)
   explicit CF_REQ_CHAT_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       ChatType chatType,
       RoomId roomId,
       const wchar_t* msg
   )
   {
       WriteValue(chatType);

       WriteValue(roomId);

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

   }

   CF_REQ_CHAT& GetPacket() const noexcept { return *reinterpret_cast<CF_REQ_CHAT*>(GetPacketBufPtr()); }
   CF_REQ_CHAT* GetPacketPtr() const noexcept { return reinterpret_cast<CF_REQ_CHAT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class CF_REQ_CHAT : public NetworkPacket
{
   public:
      using Writer = CF_REQ_CHAT_WRITER;


private:
   ChatType m_chatType; // 채팅 요청

   RoomId m_roomId;

   PacketSize m_msg_offset = 0;
   PacketSize m_msg_length = 0;

public:
   CF_REQ_CHAT()
   {
       memset(this, 0, sizeof(CF_REQ_CHAT));
       mPacketSize = static_cast<PacketSize>(sizeof(CF_REQ_CHAT));
   }
   CF_REQ_CHAT(const CF_REQ_CHAT&) = delete;
   CF_REQ_CHAT(CF_REQ_CHAT &&) = delete;
   CF_REQ_CHAT& operator = (const CF_REQ_CHAT&) = delete;
   CF_REQ_CHAT& operator = (CF_REQ_CHAT &&) = delete;

   ChatType Get_chatType() const noexcept { return m_chatType; }
   ChatType& Ref_chatType() noexcept { return m_chatType; }
   const ChatType& ConstRef_chatType() const noexcept { return m_chatType; }

   RoomId Get_roomId() const noexcept { return m_roomId; }
   RoomId& Ref_roomId() noexcept { return m_roomId; }
   const RoomId& ConstRef_roomId() const noexcept { return m_roomId; }

   const wchar_t* Get_msg() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_msg_offset) + m_msg_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_msg_offset);
   }

   wchar_t* Ref_msg()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_msg_offset) + m_msg_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_msg_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(CF_REQ_CHAT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(CF_REQ_CHAT) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(CF_REQ_CHAT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(CF_REQ_CHAT))
           {
               size += sizeof(CF_REQ_CHAT);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(ChatType);
       }

       {
           thisSize += sizeof(RoomId);
       }

       {
           if (GetPacketSize() < m_msg_offset)
               return false;

           if (0 < m_msg_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_msg_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, CF_REQ_CHAT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}chatType: {}", tTabStr, t->Get_chatType());
       result += std::format(L"\n{}roomId: {}", tTabStr, t->Get_roomId());
       result += std::format(L"\n{}msg: {}", tTabStr, t->Get_msg());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

#pragma pack(pop)

inline bool ValidatePacketCF(const Packet& rp)
{
   static constexpr bool(*ValidatePacketFuncArray[PacketTypes::CF_PACKET_END - PacketTypes::CF_PACKET_START - 1])(const Packet&) =
   {
       &ValidatePacketFunc<CF_REQ_PACKET_LIST>,
       &ValidatePacketFunc<CF_REQ_USER_LOGIN>,
       &ValidatePacketFunc<CF_REQ_GAME_CHANNEL_USER_ENTER>,
       &ValidatePacketFunc<CF_REQ_CHAT>
   };

   if ((PacketTypes::CF_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::CF_PACKET_END <= rp.GetPacketType()))
   {
       return true;
   }

   return ValidatePacketFuncArray[rp.GetPacketType() - PacketTypes::CF_PACKET_START - 1](rp);
}

template <typename _Owner, typename... _Args>
HandleResult DispatchPacketCF(uint64_t socket, _Owner* owner, NetworkPacket& rp, _Args&&... args)
{
   HandleResult handleResult = DispatchPacketCOMMON(socket, owner, rp, std::forward<_Args>(args)...);
   if (handleResult != HandleResult::NOT_EXISTS)
       return handleResult;
   
   static constexpr HandleResult(*DispatchPacketFuncArray[PacketTypes::CF_PACKET_END - PacketTypes::CF_PACKET_START - 1])(uint64_t, _Owner*, NetworkPacket&, _Args&&...) =
   {
       &DispatchPacketFunc<_Owner, CF_REQ_PACKET_LIST, _Args&&...>,
       &DispatchPacketFunc<_Owner, CF_REQ_USER_LOGIN, _Args&&...>,
       &DispatchPacketFunc<_Owner, CF_REQ_GAME_CHANNEL_USER_ENTER, _Args&&...>,
       &DispatchPacketFunc<_Owner, CF_REQ_CHAT, _Args&&...>
   };

   if ((PacketTypes::CF_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::CF_PACKET_END <= rp.GetPacketType()))
   {
       return HandleResult::NOT_EXISTS;
   }

   if (auto func = DispatchPacketFuncArray[rp.GetPacketType() - PacketTypes::CF_PACKET_START - 1])
       return func(socket, owner, rp, std::forward<_Args>(args)...);
   else
       return HandleResult::NOT_EXISTS;
}

