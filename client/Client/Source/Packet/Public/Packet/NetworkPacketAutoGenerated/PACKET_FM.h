// ============================================================================
// myungdal tech-demo project
// 
// Author: 안명달 (Myungdal Ahn)
// Email: mooondal@gmail.com
// GitHub: https://github.com/myungdal/tech_demo.git
// ============================================================================

#pragma once

#include "Packet/BasePacketAutoGenerated/PACKET_COMMON.h"
#include "Packet/NetworkPacket/NetworkPacket.h"
#include "Packet/NetworkPacket/NetworkPacketWriter.h"
#include "Packet/BasePacket/PacketWriter.h"
#include "Packet/PacketDataList/PacketDataList.h"
#include "Packet/PacketFuncs/PacketValidator.h"
#include "Packet/PacketFuncs/PacketDispatcher.h"

#pragma pack(push, 1)

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class FM;

class FM_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::FM;

public:
   // (7)
   explicit FM_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::FM, buf)
   {

   }

   // (8)
   explicit FM_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   FM& GetPacket() const noexcept { return *reinterpret_cast<FM*>(GetPacketBufPtr()); }
   FM* GetPacketPtr() const noexcept { return reinterpret_cast<FM*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class FM : public NetworkPacket
{
   public:
      using Writer = FM_WRITER;


private:
public:
   FM()
   {
       memset(this, 0, sizeof(FM));
       mPacketSize = static_cast<PacketSize>(sizeof(FM));
   }
   FM(const FM&) = delete;
   FM(FM &&) = delete;
   FM& operator = (const FM&) = delete;
   FM& operator = (FM &&) = delete;


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(FM) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(FM) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(FM); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(FM))
           {
               size += sizeof(FM);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, FM>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class FM_REQ_CM_BYPASS;

class FM_REQ_CM_BYPASS_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::FM_REQ_CM_BYPASS;

public:
   // (1)
   explicit FM_REQ_CM_BYPASS_WRITER(PacketTraitReq trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::FM_REQ_CM_BYPASS, buf)
   {
   }

   // (2)
   explicit FM_REQ_CM_BYPASS_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacket& rp)
       :
       NetworkPacketWriter(trait, PacketTypes::FM_REQ_CM_BYPASS, buf)
   {
       SetReadPacket(rp);
   }

   // (3)
   explicit FM_REQ_CM_BYPASS_WRITER(PacketTraitReq trait, uint8_t* buf, const NetworkPacketWriter& wp)
       :
       NetworkPacketWriter(trait, PacketTypes::FM_REQ_CM_BYPASS, buf)
   {
       SetWritePacket(wp);
   }

   // (8)
   explicit FM_REQ_CM_BYPASS_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       const uint8_t* nestedPacket,
       PacketSize nestedPacketSize
   )
   {
       PacketSize& nestedPacketOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));
       WriteValue(nestedPacketSize);


       nestedPacketOffset = GetPacketSize();
       WriteBinary(nestedPacket, nestedPacketSize);

   }

   FM_REQ_CM_BYPASS& GetPacket() const noexcept { return *reinterpret_cast<FM_REQ_CM_BYPASS*>(GetPacketBufPtr()); }
   FM_REQ_CM_BYPASS* GetPacketPtr() const noexcept { return reinterpret_cast<FM_REQ_CM_BYPASS*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class FM_REQ_CM_BYPASS : public NetworkPacket
{
   public:
      using Writer = FM_REQ_CM_BYPASS_WRITER;


private:
   PacketSize m_nestedPacket_offset = 0;
   PacketSize m_nestedPacket_size = 0;

public:
   FM_REQ_CM_BYPASS()
   {
       memset(this, 0, sizeof(FM_REQ_CM_BYPASS));
       mPacketSize = static_cast<PacketSize>(sizeof(FM_REQ_CM_BYPASS));
   }
   FM_REQ_CM_BYPASS(const FM_REQ_CM_BYPASS&) = delete;
   FM_REQ_CM_BYPASS(FM_REQ_CM_BYPASS &&) = delete;
   FM_REQ_CM_BYPASS& operator = (const FM_REQ_CM_BYPASS&) = delete;
   FM_REQ_CM_BYPASS& operator = (FM_REQ_CM_BYPASS &&) = delete;

   const uint8_t* Get_nestedPacket() const noexcept { return ((uint8_t*)this + m_nestedPacket_offset); }
   PacketSize Get_nestedPacket_size() const noexcept { return m_nestedPacket_size; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(FM_REQ_CM_BYPASS) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(FM_REQ_CM_BYPASS) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(FM_REQ_CM_BYPASS); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(FM_REQ_CM_BYPASS))
           {
               size += sizeof(FM_REQ_CM_BYPASS);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           if (GetPacketSize() < m_nestedPacket_offset)
               return false;

           thisSize += m_nestedPacket_size;
           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, FM_REQ_CM_BYPASS>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}nestedPacket: BIN[{}]", tTabStr, t->Get_nestedPacket_size());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class FM_ROOM_USER_ENTER;

class FM_ROOM_USER_ENTER_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::FM_ROOM_USER_ENTER;

public:
   // (7)
   explicit FM_ROOM_USER_ENTER_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::FM_ROOM_USER_ENTER, buf)
   {
   }

   // (9)
   explicit FM_ROOM_USER_ENTER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       RoomId roomId,
       RoomType roomType,
       const USER& enterUser
   )
       :
       NetworkPacketWriter(trait, PacketTypes::FM_ROOM_USER_ENTER, buf)
   {
       WriteValue(roomId);

       WriteValue(roomType);

       PacketSize& enterUserOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       enterUserOffset = GetPacketSize();
       WriteBinary((uint8_t*)&enterUser, enterUser.GetPacketSize());

   }

   // (9)
   explicit FM_ROOM_USER_ENTER_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       RoomId roomId,
       RoomType roomType,
       const USER::Writer& enterUser
   )
       :
       NetworkPacketWriter(trait, PacketTypes::FM_ROOM_USER_ENTER, buf)
   {
       WriteValue(roomId);

       WriteValue(roomType);

       PacketSize& enterUserOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       enterUserOffset = GetPacketSize();
       WriteBinary(enterUser.GetPacketBufPtr(), enterUser.GetPacketSize());

   }

   // (8)
   explicit FM_ROOM_USER_ENTER_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       RoomId roomId,
       RoomType roomType,
       const USER& enterUser
   )
   {
       WriteValue(roomId);

       WriteValue(roomType);

       PacketSize& enterUserOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       enterUserOffset = GetPacketSize();
       WriteBinary((uint8_t*)&enterUser, enterUser.GetPacketSize());

   }

   void SetValues(
       RoomId roomId,
       RoomType roomType,
       const USER::Writer& enterUser
   )
   {
       WriteValue(roomId);

       WriteValue(roomType);

       PacketSize& enterUserOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0));

       enterUserOffset = GetPacketSize();
       WriteBinary(enterUser.GetPacketBufPtr(), enterUser.GetPacketSize());

   }

   FM_ROOM_USER_ENTER& GetPacket() const noexcept { return *reinterpret_cast<FM_ROOM_USER_ENTER*>(GetPacketBufPtr()); }
   FM_ROOM_USER_ENTER* GetPacketPtr() const noexcept { return reinterpret_cast<FM_ROOM_USER_ENTER*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class FM_ROOM_USER_ENTER : public NetworkPacket
{
   public:
      using Writer = FM_ROOM_USER_ENTER_WRITER;


private:
   RoomId m_roomId;

   RoomType m_roomType;

   PacketSize m_enterUser_offset = 0;

public:
   FM_ROOM_USER_ENTER()
   {
       memset(this, 0, sizeof(FM_ROOM_USER_ENTER));
       mPacketSize = static_cast<PacketSize>(sizeof(FM_ROOM_USER_ENTER));
   }
   FM_ROOM_USER_ENTER(const FM_ROOM_USER_ENTER&) = delete;
   FM_ROOM_USER_ENTER(FM_ROOM_USER_ENTER &&) = delete;
   FM_ROOM_USER_ENTER& operator = (const FM_ROOM_USER_ENTER&) = delete;
   FM_ROOM_USER_ENTER& operator = (FM_ROOM_USER_ENTER &&) = delete;

   RoomId Get_roomId() const noexcept { return m_roomId; }
   RoomId& Ref_roomId() noexcept { return m_roomId; }
   const RoomId& ConstRef_roomId() const noexcept { return m_roomId; }

   RoomType Get_roomType() const noexcept { return m_roomType; }
   RoomType& Ref_roomType() noexcept { return m_roomType; }
   const RoomType& ConstRef_roomType() const noexcept { return m_roomType; }

   const USER& Get_enterUser() const noexcept { return *(USER*)((uint8_t*)this + m_enterUser_offset); }
   USER* Get_enterUser_ptr() const noexcept { return (USER*)((uint8_t*)this + m_enterUser_offset); }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(FM_ROOM_USER_ENTER) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(FM_ROOM_USER_ENTER) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(FM_ROOM_USER_ENTER); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(FM_ROOM_USER_ENTER))
           {
               size += sizeof(FM_ROOM_USER_ENTER);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(RoomId);
       }

       {
           thisSize += sizeof(RoomType);
       }

       {
           if (GetPacketSize() < m_enterUser_offset)
               return false;

           const USER* _packet = reinterpret_cast<const USER*>(GetPacketBufPtr() + m_enterUser_offset);
           if (!_packet->Validate(thisSize, ++depth))
               return false;

           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, FM_ROOM_USER_ENTER>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}roomId: {}", tTabStr, t->Get_roomId());
       result += std::format(L"\n{}roomType: {}", tTabStr, t->Get_roomType());
       result += std::format(L"\n{}enterUser: {}", tTabStr, t->Get_enterUser_ptr());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class FM_ROOM_USER_LEAVE;

class FM_ROOM_USER_LEAVE_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::FM_ROOM_USER_LEAVE;

public:
   // (7)
   explicit FM_ROOM_USER_LEAVE_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::FM_ROOM_USER_LEAVE, buf)
   {
   }

   // (9)
   explicit FM_ROOM_USER_LEAVE_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       RoomId roomId,
       UserId leaveUserId
   )
       :
       NetworkPacketWriter(trait, PacketTypes::FM_ROOM_USER_LEAVE, buf)
   {
       WriteValue(roomId);

       WriteValue(leaveUserId);


   }

   // (8)
   explicit FM_ROOM_USER_LEAVE_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       RoomId roomId,
       UserId leaveUserId
   )
   {
       WriteValue(roomId);

       WriteValue(leaveUserId);


   }

   FM_ROOM_USER_LEAVE& GetPacket() const noexcept { return *reinterpret_cast<FM_ROOM_USER_LEAVE*>(GetPacketBufPtr()); }
   FM_ROOM_USER_LEAVE* GetPacketPtr() const noexcept { return reinterpret_cast<FM_ROOM_USER_LEAVE*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class FM_ROOM_USER_LEAVE : public NetworkPacket
{
   public:
      using Writer = FM_ROOM_USER_LEAVE_WRITER;


private:
   RoomId m_roomId;

   UserId m_leaveUserId;

public:
   FM_ROOM_USER_LEAVE()
   {
       memset(this, 0, sizeof(FM_ROOM_USER_LEAVE));
       mPacketSize = static_cast<PacketSize>(sizeof(FM_ROOM_USER_LEAVE));
   }
   FM_ROOM_USER_LEAVE(const FM_ROOM_USER_LEAVE&) = delete;
   FM_ROOM_USER_LEAVE(FM_ROOM_USER_LEAVE &&) = delete;
   FM_ROOM_USER_LEAVE& operator = (const FM_ROOM_USER_LEAVE&) = delete;
   FM_ROOM_USER_LEAVE& operator = (FM_ROOM_USER_LEAVE &&) = delete;

   RoomId Get_roomId() const noexcept { return m_roomId; }
   RoomId& Ref_roomId() noexcept { return m_roomId; }
   const RoomId& ConstRef_roomId() const noexcept { return m_roomId; }

   UserId Get_leaveUserId() const noexcept { return m_leaveUserId; }
   UserId& Ref_leaveUserId() noexcept { return m_leaveUserId; }
   const UserId& ConstRef_leaveUserId() const noexcept { return m_leaveUserId; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(FM_ROOM_USER_LEAVE) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(FM_ROOM_USER_LEAVE) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(FM_ROOM_USER_LEAVE); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(FM_ROOM_USER_LEAVE))
           {
               size += sizeof(FM_ROOM_USER_LEAVE);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(RoomId);
       }

       {
           thisSize += sizeof(UserId);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, FM_ROOM_USER_LEAVE>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}roomId: {}", tTabStr, t->Get_roomId());
       result += std::format(L"\n{}leaveUserId: {}", tTabStr, t->Get_leaveUserId());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class FM_CHAT;

class FM_CHAT_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::FM_CHAT;

public:
   // (7)
   explicit FM_CHAT_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::FM_CHAT, buf)
   {
   }

   // (9)
   explicit FM_CHAT_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       ChatType chatType,
       RoomId roomId,
       const wchar_t* userName,
       const wchar_t* guildName,
       const wchar_t* msg
   )
       :
       NetworkPacketWriter(trait, PacketTypes::FM_CHAT, buf)
   {
       WriteValue(chatType);

       WriteValue(roomId);

       PacketSize& userNameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& userNameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& guildNameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& guildNameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       userNameOffset = GetPacketSize();
       userNameLength = (PacketSize)wcsnlen_s(userName, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == userNameLength) WriteString(L"", 0);
       else WriteString(userName, userNameLength); 

       guildNameOffset = GetPacketSize();
       guildNameLength = (PacketSize)wcsnlen_s(guildName, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == guildNameLength) WriteString(L"", 0);
       else WriteString(guildName, guildNameLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

   }

   // (8)
   explicit FM_CHAT_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       ChatType chatType,
       RoomId roomId,
       const wchar_t* userName,
       const wchar_t* guildName,
       const wchar_t* msg
   )
   {
       WriteValue(chatType);

       WriteValue(roomId);

       PacketSize& userNameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& userNameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& guildNameOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& guildNameLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 

       PacketSize& msgOffset = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 
       PacketSize& msgLength = *(PacketSize*)GetPacketBufWritingPtr();
       WriteValue(PacketSize(0)); 


       userNameOffset = GetPacketSize();
       userNameLength = (PacketSize)wcsnlen_s(userName, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == userNameLength) WriteString(L"", 0);
       else WriteString(userName, userNameLength); 

       guildNameOffset = GetPacketSize();
       guildNameLength = (PacketSize)wcsnlen_s(guildName, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == guildNameLength) WriteString(L"", 0);
       else WriteString(guildName, guildNameLength); 

       msgOffset = GetPacketSize();
       msgLength = (PacketSize)wcsnlen_s(msg, MAX_STRING_LENGTH);
       if (MAX_STRING_LENGTH == msgLength) WriteString(L"", 0);
       else WriteString(msg, msgLength); 

   }

   FM_CHAT& GetPacket() const noexcept { return *reinterpret_cast<FM_CHAT*>(GetPacketBufPtr()); }
   FM_CHAT* GetPacketPtr() const noexcept { return reinterpret_cast<FM_CHAT*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class FM_CHAT : public NetworkPacket
{
   public:
      using Writer = FM_CHAT_WRITER;


private:
   ChatType m_chatType; // 채팅 전파 시작

   RoomId m_roomId;

   PacketSize m_userName_offset = 0;
   PacketSize m_userName_length = 0;

   PacketSize m_guildName_offset = 0;
   PacketSize m_guildName_length = 0;

   PacketSize m_msg_offset = 0;
   PacketSize m_msg_length = 0;

public:
   FM_CHAT()
   {
       memset(this, 0, sizeof(FM_CHAT));
       mPacketSize = static_cast<PacketSize>(sizeof(FM_CHAT));
   }
   FM_CHAT(const FM_CHAT&) = delete;
   FM_CHAT(FM_CHAT &&) = delete;
   FM_CHAT& operator = (const FM_CHAT&) = delete;
   FM_CHAT& operator = (FM_CHAT &&) = delete;

   ChatType Get_chatType() const noexcept { return m_chatType; }
   ChatType& Ref_chatType() noexcept { return m_chatType; }
   const ChatType& ConstRef_chatType() const noexcept { return m_chatType; }

   RoomId Get_roomId() const noexcept { return m_roomId; }
   RoomId& Ref_roomId() noexcept { return m_roomId; }
   const RoomId& ConstRef_roomId() const noexcept { return m_roomId; }

   const wchar_t* Get_userName() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_userName_offset) + m_userName_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_userName_offset);
   }

   wchar_t* Ref_userName()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_userName_offset) + m_userName_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_userName_offset);
   }

   const wchar_t* Get_guildName() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_guildName_offset) + m_guildName_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_guildName_offset);
   }

   wchar_t* Ref_guildName()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_guildName_offset) + m_guildName_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_guildName_offset);
   }

   const wchar_t* Get_msg() const
   {
       if (0 != *((const wchar_t*)((uint8_t*)this + m_msg_offset) + m_msg_length))
           return L"";

       return (const wchar_t*)((uint8_t*)this + m_msg_offset);
   }

   wchar_t* Ref_msg()
   {
       if (0 != *((wchar_t*)((uint8_t*)this + m_msg_offset) + m_msg_length))
           return nullptr;

       return (wchar_t*)((uint8_t*)this + m_msg_offset);
   }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(FM_CHAT) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(FM_CHAT) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(FM_CHAT); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(FM_CHAT))
           {
               size += sizeof(FM_CHAT);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(ChatType);
       }

       {
           thisSize += sizeof(RoomId);
       }

       {
           if (GetPacketSize() < m_userName_offset)
               return false;

           if (0 < m_userName_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_userName_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_guildName_offset)
               return false;

           if (0 < m_guildName_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_guildName_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       {
           if (GetPacketSize() < m_msg_offset)
               return false;

           if (0 < m_msg_offset)
               thisSize += sizeof(wchar_t) * (size_t(m_msg_length) + 1);

           thisSize += sizeof(PacketSize);
           thisSize += sizeof(PacketSize);

           if (GetPacketSize() < thisSize)
               return false;
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, FM_CHAT>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}chatType: {}", tTabStr, t->Get_chatType());
       result += std::format(L"\n{}roomId: {}", tTabStr, t->Get_roomId());
       result += std::format(L"\n{}userName: {}", tTabStr, t->Get_userName());
       result += std::format(L"\n{}guildName: {}", tTabStr, t->Get_guildName());
       result += std::format(L"\n{}msg: {}", tTabStr, t->Get_msg());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

#pragma pack(pop)

inline bool ValidatePacketFM(const Packet& rp)
{
   static constexpr bool(*ValidatePacketFuncArray[PacketTypes::FM_PACKET_END - PacketTypes::FM_PACKET_START - 1])(const Packet&) =
   {
       &ValidatePacketFunc<FM>,
       &ValidatePacketFunc<FM_REQ_CM_BYPASS>,
       &ValidatePacketFunc<FM_ROOM_USER_ENTER>,
       &ValidatePacketFunc<FM_ROOM_USER_LEAVE>,
       &ValidatePacketFunc<FM_CHAT>
   };

   if ((PacketTypes::FM_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::FM_PACKET_END <= rp.GetPacketType()))
   {
       return true;
   }

   return ValidatePacketFuncArray[rp.GetPacketType() - PacketTypes::FM_PACKET_START - 1](rp);
}

template <typename _Owner, typename... _Args>
HandleResult DispatchPacketFM(uint64_t socket, _Owner* owner, NetworkPacket& rp, _Args&&... args)
{
   HandleResult handleResult = DispatchPacketCOMMON(socket, owner, rp, std::forward<_Args>(args)...);
   if (handleResult != HandleResult::NOT_EXISTS)
       return handleResult;
   
   static constexpr HandleResult(*DispatchPacketFuncArray[PacketTypes::FM_PACKET_END - PacketTypes::FM_PACKET_START - 1])(uint64_t, _Owner*, NetworkPacket&, _Args&&...) =
   {
       &DispatchPacketFunc<_Owner, FM, _Args&&...>,
       &DispatchPacketFunc<_Owner, FM_REQ_CM_BYPASS, _Args&&...>,
       &DispatchPacketFunc<_Owner, FM_ROOM_USER_ENTER, _Args&&...>,
       &DispatchPacketFunc<_Owner, FM_ROOM_USER_LEAVE, _Args&&...>,
       &DispatchPacketFunc<_Owner, FM_CHAT, _Args&&...>
   };

   if ((PacketTypes::FM_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::FM_PACKET_END <= rp.GetPacketType()))
   {
       return HandleResult::NOT_EXISTS;
   }

   if (auto func = DispatchPacketFuncArray[rp.GetPacketType() - PacketTypes::FM_PACKET_START - 1])
       return func(socket, owner, rp, std::forward<_Args>(args)...);
   else
       return HandleResult::NOT_EXISTS;
}

