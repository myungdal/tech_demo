// ============================================================================
// myungdal tech-demo project
// 
// Author: 안명달 (Myungdal Ahn)
// Email: mooondal@gmail.com
// GitHub: https://github.com/myungdal/tech_demo.git
// ============================================================================

#pragma once

#include "Packet/BasePacketAutoGenerated/PACKET_COMMON.h"
#include "Packet/NetworkPacket/NetworkPacket.h"
#include "Packet/NetworkPacket/NetworkPacketWriter.h"
#include "Packet/BasePacket/PacketWriter.h"
#include "Packet/PacketDataList/PacketDataList.h"
#include "Packet/PacketFuncs/PacketValidator.h"
#include "Packet/PacketFuncs/PacketDispatcher.h"

#pragma pack(push, 1)

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class BC;

class BC_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::BC;

public:
   // (7)
   explicit BC_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::BC, buf)
   {

   }

   // (8)
   explicit BC_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   BC& GetPacket() const noexcept { return *reinterpret_cast<BC*>(GetPacketBufPtr()); }
   BC* GetPacketPtr() const noexcept { return reinterpret_cast<BC*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class BC : public NetworkPacket
{
   public:
      using Writer = BC_WRITER;


private:
public:
   BC()
   {
       memset(this, 0, sizeof(BC));
       mPacketSize = static_cast<PacketSize>(sizeof(BC));
   }
   BC(const BC&) = delete;
   BC(BC &&) = delete;
   BC& operator = (const BC&) = delete;
   BC& operator = (BC &&) = delete;


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(BC) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(BC) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(BC); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(BC))
           {
               size += sizeof(BC);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, BC>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class BC_ERROR;

class BC_ERROR_WRITER : public NetworkPacketWriter
{
public:
   static constexpr PacketType PACKET_TYPE = PacketTypes::BC_ERROR;

public:
   // (7)
   explicit BC_ERROR_WRITER(PacketTraitNotify trait, uint8_t* buf)
       :
       NetworkPacketWriter(trait, PacketTypes::BC_ERROR, buf)
   {
   }

   // (9)
   explicit BC_ERROR_WRITER(
       PacketTraitParam trait, uint8_t* buf,
       PacketType packetType
   )
       :
       NetworkPacketWriter(trait, PacketTypes::BC_ERROR, buf)
   {
       WriteValue(packetType);


   }

   // (8)
   explicit BC_ERROR_WRITER(uint8_t* buf)
       :
       NetworkPacketWriter(buf) 
   {
   }

   void SetValues(
       PacketType packetType
   )
   {
       WriteValue(packetType);


   }

   BC_ERROR& GetPacket() const noexcept { return *reinterpret_cast<BC_ERROR*>(GetPacketBufPtr()); }
   BC_ERROR* GetPacketPtr() const noexcept { return reinterpret_cast<BC_ERROR*>(GetPacketBufPtr()); }

};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class BC_ERROR : public NetworkPacket
{
   public:
      using Writer = BC_ERROR_WRITER;


private:
   PacketType m_packetType;

public:
   BC_ERROR()
   {
       memset(this, 0, sizeof(BC_ERROR));
       mPacketSize = static_cast<PacketSize>(sizeof(BC_ERROR));
   }
   BC_ERROR(const BC_ERROR&) = delete;
   BC_ERROR(BC_ERROR &&) = delete;
   BC_ERROR& operator = (const BC_ERROR&) = delete;
   BC_ERROR& operator = (BC_ERROR &&) = delete;

   PacketType Get_packetType() const noexcept { return m_packetType; }
   PacketType& Ref_packetType() noexcept { return m_packetType; }
   const PacketType& ConstRef_packetType() const noexcept { return m_packetType; }


   bool Validate(IN OUT size_t& size, int32_t depth = 0) const
   {
       if (0 == GetPacketSize())
           return true;

       if (sizeof(BC_ERROR) > GetPacketSize())
           return false;

       if (MAX_PACKET_BUFFER_SIZE < GetPacketSize())
           return false;

       if ((depth != 0) && (sizeof(BC_ERROR) == GetPacketSize()))
       {
           size_t i = sizeof(NetworkPacket);
           for (; i < sizeof(BC_ERROR); ++i)
               if (0 != GetPacketBufPtr()[i]) break;

           if (i == sizeof(BC_ERROR))
           {
               size += sizeof(BC_ERROR);
               return true;
           }
       }

       size_t thisSize = sizeof(NetworkPacket);

       {
           thisSize += sizeof(PacketType);
       }

       if (GetPacketSize() != thisSize)
           return false;

       size += thisSize;

       return true;
   }

};

template<typename T, typename _CharType>
    requires std::is_same_v<std::remove_cv_t<std::remove_pointer_t<T>>, BC_ERROR>
struct std::formatter<T, _CharType>
{
    constexpr auto parse(auto& ctx)
    {
        return ctx.end();
    }

    auto format(MAYBE_UNUSED T t, auto& ctx) const
    {
        std::wstring result;
       result += std::format(L"\n{}{}", tTabStr, t->ConstGetHeader());
       tTabStr += std::wstring(L"\t");
       result += std::format(L"\n{}packetType: {}", tTabStr, t->Get_packetType());
       tTabStr.pop_back();

       return std::ranges::copy(result, ctx.out()).out;
   }
};

#pragma pack(pop)

inline bool ValidatePacketBC(const Packet& rp)
{
   static constexpr bool(*ValidatePacketFuncArray[PacketTypes::BC_PACKET_END - PacketTypes::BC_PACKET_START - 1])(const Packet&) =
   {
       &ValidatePacketFunc<BC>,
       &ValidatePacketFunc<BC_ERROR>
   };

   if ((PacketTypes::BC_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::BC_PACKET_END <= rp.GetPacketType()))
   {
       return true;
   }

   return ValidatePacketFuncArray[rp.GetPacketType() - PacketTypes::BC_PACKET_START - 1](rp);
}

template <typename _Owner, typename... _Args>
HandleResult DispatchPacketBC(uint64_t socket, _Owner* owner, NetworkPacket& rp, _Args&&... args)
{
   HandleResult handleResult = DispatchPacketCOMMON(socket, owner, rp, std::forward<_Args>(args)...);
   if (handleResult != HandleResult::NOT_EXISTS)
       return handleResult;
   
   static constexpr HandleResult(*DispatchPacketFuncArray[PacketTypes::BC_PACKET_END - PacketTypes::BC_PACKET_START - 1])(uint64_t, _Owner*, NetworkPacket&, _Args&&...) =
   {
       &DispatchPacketFunc<_Owner, BC, _Args&&...>,
       &DispatchPacketFunc<_Owner, BC_ERROR, _Args&&...>
   };

   if ((PacketTypes::BC_PACKET_START >= rp.GetPacketType()) ||
       (PacketTypes::BC_PACKET_END <= rp.GetPacketType()))
   {
       return HandleResult::NOT_EXISTS;
   }

   if (auto func = DispatchPacketFuncArray[rp.GetPacketType() - PacketTypes::BC_PACKET_START - 1])
       return func(socket, owner, rp, std::forward<_Args>(args)...);
   else
       return HandleResult::NOT_EXISTS;
}

