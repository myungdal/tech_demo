# ComfyUI 로컬 이미지 생성 가이드

> **AI 아트팀 공용** - 캐릭터/배경/이펙트 파트 모두 활용 가능

---

## 📍 설치 위치

```
C:\dev\nearest3\tools\ComfyUI\
```

---

## 🚀 실행 방법

```batch
# 더블클릭으로 실행
C:\dev\nearest3\tools\ComfyUI\run_comfyui.bat

# 또는 명령줄
cd C:\dev\nearest3\tools\ComfyUI
venv\Scripts\activate
python main.py --listen 0.0.0.0 --port 8188 --enable-manager
```

**접속**: http://localhost:8188

---

## 📦 설치된 모델 (2026-01-15 기준)

| 모델 | 경로 | 용량 | 용도 |
|------|------|-----:|------|
| SDXL Base 1.0 | `models/checkpoints/sd_xl_base_1.0.safetensors` | 6.5GB | 기본 이미지 생성 |
| IP-Adapter SDXL | `models/ipadapter/ip-adapter_sdxl.safetensors` | 671MB | 캐릭터 일관성 유지 |
| CLIP Vision | `models/clip_vision/CLIP-ViT-bigG-14-laion2B-39B-b160k.safetensors` | 3.5GB | IP-Adapter 이미지 인코딩 |
| ControlNet OpenPose | `models/controlnet/controlnet-openpose-sdxl.safetensors` | 2.4GB | 포즈 제어 |

**총 용량**: ~13GB

---

## 🔌 설치된 커스텀 노드

| 노드 | 경로 | 용도 |
|------|------|------|
| ComfyUI-Manager | `custom_nodes/ComfyUI-Manager/` | 노드/모델 관리 |
| IP-Adapter Plus | `custom_nodes/ComfyUI_IPAdapter_plus/` | IP-Adapter 노드 |
| ControlNet Aux | `custom_nodes/comfyui_controlnet_aux/` | OpenPose 전처리 |

---

## 🎯 주요 기능

### 1. 캐릭터 일관성 유지 (IP-Adapter)

```
입력: 캐릭터 썸네일 이미지
출력: 동일 캐릭터의 다양한 포즈/표정
```

- IP-Adapter가 입력 이미지의 스타일/특징을 학습
- 새로운 포즈에도 캐릭터 일관성 유지
- weight 0.6~0.8 권장

### 2. 포즈 제어 (ControlNet OpenPose)

```
입력: 포즈 레퍼런스 이미지 (또는 OpenPose 전처리)
출력: 지정된 포즈의 캐릭터
```

- IDLE, RUN, ATTACK 등 애니메이션 포즈 지정
- 스프라이트 시트 일괄 생성에 활용

### 3. 스프라이트 시트 생성 워크플로우

```
┌─────────────────────────────────────────────────────────────────┐
│  INPUT: 캐릭터 썸네일 (ex: warrior.png)                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  STEP 1: IP-Adapter로 캐릭터 특징 추출                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  STEP 2: ControlNet (OpenPose)로 포즈 지정                      │
│  - IDLE 6프레임, RUN 6프레임, ATTACK 7프레임...                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  STEP 3: Batch Generation → 후처리 → 스프라이트 시트 조합       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 💻 시스템 요구사항

| 항목 | 현재 환경 |
|------|----------|
| GPU | RTX 4060 Ti 8GB |
| VRAM | 8GB (SDXL 사용 가능) |
| Python | 3.12.3 (venv) |
| PyTorch | 2.6.0 + CUDA 12.4 |

### VRAM 사용량 참고

| 모델 조합 | 예상 VRAM |
|----------|:--------:|
| SDXL only | ~6GB |
| SDXL + IP-Adapter | ~7GB |
| SDXL + IP-Adapter + ControlNet | ~8GB (빡빡함) |

---

## 📝 API 사용법

ComfyUI는 REST API를 제공합니다.

### 워크플로우 실행

```python
import requests
import json

# 워크플로우 JSON 로드
with open("workflow.json", "r") as f:
    workflow = json.load(f)

# API 호출
response = requests.post(
    "http://localhost:8188/prompt",
    json={"prompt": workflow}
)

print(response.json())
```

### 결과 이미지 위치

```
tools/ComfyUI/output/
```

---

## 🔧 추가 설치 (필요 시)

### Pixel Art LoRA

ComfyUI Manager에서 검색하여 설치:
1. ComfyUI 실행
2. Manager → Install Models
3. "pixel art" 검색
4. 원하는 LoRA 설치

### 추가 ControlNet 모델

```python
# Canny (외곽선 제어)
from huggingface_hub import hf_hub_download
hf_hub_download(
    repo_id='xinsir/controlnet-canny-sdxl-1.0',
    filename='diffusion_pytorch_model.safetensors',
    local_dir='tools/ComfyUI/models/controlnet'
)
```

---

## 📚 참고 리소스

| 리소스 | URL |
|--------|-----|
| ComfyUI 공식 | https://github.com/comfyanonymous/ComfyUI |
| IP-Adapter 가이드 | https://learn.runcomfy.com/create-consistent-characters-with-ipadapter |
| 스프라이트 시트 생성 | https://apatero.com/blog/generate-clean-spritesheets-comfyui-guide-2025 |
| 픽셀아트 워크플로우 | https://inzaniak.github.io/blog/articles/the-pixel-art-comfyui-workflow-guide.html |

---

## ⚠️ 주의사항

1. **첫 실행 시 로딩 오래 걸림** - 모델 로드에 시간 소요
2. **VRAM 부족 시** - `--lowvram` 옵션 추가
3. **모델 경로 인식 안 될 때** - ComfyUI 재시작

---

**최종 수정**: 2026-01-15
**설치 담당**: AI 배경파트
