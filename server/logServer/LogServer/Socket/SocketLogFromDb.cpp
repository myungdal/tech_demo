// ============================================================================
// myungdal tech-demo project
// 
// Author: 안명달 (Myungdal Ahn)
// Email: mooondal@gmail.com
// GitHub: https://github.com/myungdal/tech_demo.git
// ============================================================================

import LogServer;
import ServerEnginePacketAutoGenerated;


bool SocketLogFromDb::OnDispatchPacket(NetworkPacket& rp)
{
	DispatchPacket(rp);
	return true;
}

void SocketLogFromDb::DispatchPacket(NetworkPacket& rp)
{
	if (LogPacketWorkerPtr worker = gLogPacketHandler->GetLogPacketWorker(rp))
		PacketUtil::DispatchPacketOnWorker(worker, this, &SocketLogFromDb::DispatchPacket_async, rp, worker);
	else if (tThreadId == INVALID_THREAD_ID)
		PacketUtil::DispatchPacketOnAnyThread(this, &SocketLogFromDb::DispatchPacket_async, rp, worker);
	else
		DispatchPacketToHandler(rp, worker);
}

void SocketLogFromDb::DispatchPacket_async(MAYBE_UNUSED SocketPtrType& ptr, PacketTemp tp, LogPacketWorkerPtr worker)
{
	DispatchPacketToHandler(**tp, worker);
}

void SocketLogFromDb::DispatchPacketToHandler(NetworkPacket& rp, LogPacketWorkerPtr worker)
{
	auto DispatchPacketToHandler = [this](auto handler, NetworkPacket& rp, LogPacketWorkerPtr worker)
		{
			if (DispatchPacketDL(mSocket, handler, rp, worker) != HandleResult::NOT_EXISTS) return true;
			return false;
		};

	if (DispatchPacketToHandler(gLogPacketHandler.get(), rp, worker)) return;

	_DEBUG_LOG(RED, L"{}", GetPacketTypeString(rp.GetPacketType()));
}

